# tellme — Status

**Last updated**: 2026-09-17 (session 8: **round 040 `040-spinner-liveness-and-turn-timer` — `/axb-tasks` DONE** (`tasks.md` T001–T016 written on the implementation branch `040-implement-spinner-liveness-and-turn-timer`; PR open for review); plan + truth half already merged into `dev` ([#84](https://github.com/gosharplite/tellme/pull/84), `146210d`); **`/axb-implement` NOT started** (operator gate)). Round 040 folds **two spinner workstreams** into one package (both touch `internal/ui/spinner.go` + `presenting-the-progress-spinner.feature`): **(a) [#82](https://github.com/gosharplite/tellme/issues/82) — liveness while a `[Tool Output]` block streams** — the round-034 whole-block pause (FR-012/G8, ADR 0005 D7) is **narrowed**: the indicator resumes after an **idle gap** and is cleared before the next output line (the invariant is **mutual exclusion + join**: admit under the block-writer mutex; the clear is the goroutine-joined `deactivate()`; lock order block-mutex → spinner-mutex); **(b) [#83](https://github.com/gosharplite/tellme/issues/83) — a dual elapsed timer** — the line becomes `({total}s {call}s)` (both unlabelled): the **total** since prompt capture (turn-scoped; never reset) **plus** the **current model call's** elapsed, **reset at each AI-endpoint call** (amends round-019 D4). Pipeline through `/axb-dsl-refine` (specify ✅ · spec-by-example ✅ · technical-research ✅ · system-analysis ✅ · dsl-refine ✅) + the **architect review loop (APPROVE WITH REQUIRED FOLDS → TD-9/TD-10/TD-11 + N-1..N-3 folded → review loop CLOSED)**; **merged into `dev` (`146210d`) as the plan + truth half**; **`/axb-tasks` DONE** (`tasks.md` T001–T016 on `040-implement-spinner-liveness-and-turn-timer`, + the PR #85 review folds); **`/axb-implement` NOT run** (operator gate). Truth: `techstack.md` (**Turn progress spinner** row) + `presenting-the-progress-spinner.feature` (the streaming Rule **replaced** + a dual-timer Rule) + `chat/dsl.md` (streaming row modified **in place** + `## Given (round 040)` + `## Then (round 040)` + a note); `/axb-api-plan` + `/axb-data-plan` **NOOP**; `/axb-ui-plan` skipped. Governance: **ADR 0009** (`docs/decisions/0009-spinner-dual-timer-and-streaming-liveness.md`, **supersedes ADR 0005 D7 only** — the rest of 0005 stands; amends round-019 D4). Operator-locked **QB1** (per-AI-call reset); **QA1–QA3 + QB2–QB4** review-confirmed (PR #84 fold review) — QB4 records the **unlabelled** format as a third divergence. Open issues: **#82** · **#83** · **#69** · **#60** · **#13** (they close on **delivery**, not on the plan-half merge). Topology audit **PASSED** (44 features · 16 root + 327 module rows · 1674 steps). **Propagation: PENDING** (round 040 not delivered — the plan half merges to `dev` only).
**Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation this phase)
**Active branch**: `040-implement-spinner-liveness-and-turn-timer` (off `dev`; `/axb-tasks` done — `tasks.md` written; `/axb-implement` held for operator review)
**Daily log**: [`docs/session-summary/2026/09/17/session-summary.md`](docs/session-summary/2026/09/17/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–039).

> **Split note (Rule 12)**: the **round-039** detail section was relocated **verbatim** into [`2026-09-17.md`](docs/archives/status/2026-09-17.md) when round 040 became current (joining rounds 035–038), keeping `STATUS.md` to a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |

Per-round detail lives in the archives (001–002 in [`2026-09-11.md`](docs/archives/status/2026-09-11.md); 003–012 in [`2026-09-13.md`](docs/archives/status/2026-09-13.md); 013–019 in [`2026-09-14.md`](docs/archives/status/2026-09-14.md); 020–026 in [`2026-09-15.md`](docs/archives/status/2026-09-15.md); 027–034 in [`2026-09-16.md`](docs/archives/status/2026-09-16.md); 035–039 in [`2026-09-17.md`](docs/archives/status/2026-09-17.md)); **039 is the most recent delivered round; 040 is in flight.**

## Current round — 040 `040-spinner-liveness-and-turn-timer` (PLAN + TRUTH HALF MERGED `146210d`; TASKS WRITTEN — IMPLEMENTATION PENDING)

**040-spinner-liveness-and-turn-timer** — two folded spinner workstreams on the round-019/025 **progress spinner** (`internal/ui/spinner.go`, `stderr`-bound). **(a) #82 — liveness during a streaming `[Tool Output]` block:** today the spinner is **paused for the whole block** (round-034 FR-012/G8, ADR 0005 D7), so a quiet/slow command shows no animation; round 040 keeps the block's `stderr` ownership but **resumes the indicator after an idle gap** (no new output line for the threshold; the seam `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS`, ms, `0` = admit immediately; a **real** idle gap in E2E) and **synchronously clears it before the next output line** (the invariant is **mutual exclusion + join**, not "single-writer") — the block literals, the bound/stop semantics, and the non-TTY/`-r` gates are unchanged. **(b) #83 — a dual elapsed timer:** the elapsed segment becomes `({total}s {call}s)` — the **total** since prompt capture (turn-scoped; **never reset**, round-019 D4 preserved) **plus** the **current model call's** elapsed, **reset at each AI-endpoint call** (stamped in `OnInferenceStart`, fired exactly once per call — round-027 AI-call semantics; an **amendment** to round-019 D4); it appears in the model-wait **and** tool-execution labels (the resource segment is unchanged). Both are **recorded divergences** from the reference (its single figure restarts **per phase**; it does not re-activate a live spinner over streamed output; its figure is **unlabelled** — tellme's two figures are unlabelled too, ADR 0009 (QB4)). Pipeline through `/axb-dsl-refine`: **specify ✅** (`spec.md` + checklist + truth-delta) · **spec-by-example ✅** (`keeping-the-progress-visible.feature` + `timing-the-current-model-call.feature`) · **technical-research ✅** (`research.md` D1–D9) · **system-analysis ✅** (`plan.md`; 1 CLI end → `/axb-dsl-refine`) · **dsl-refine ✅** (`presenting-the-progress-spinner.feature` + `chat/dsl.md`); **`/axb-tasks` DONE** (`tasks.md` T001–T016 + the PR #85 folds); **`/axb-implement` ⏸ held** for operator review. Truth: `techstack.md` (**Turn progress spinner** row MODIFY) + `presenting-the-progress-spinner.feature` (`Rule: The spinner is paused while a command's output streams` **replaced** by `Rule: A quiet command's output still shows the progress spinner`; **new** `Rule: The spinner shows the total time and the current model call's time`; the two tool-phase Examples gain the dual-timer Then) + `chat/dsl.md` (the streaming row modified **in place** → `the run shows the progress spinner again while the command stays quiet`; `## Given (round 040)` + `## Then (round 040)` + a note); `/axb-api-plan` + `/axb-data-plan` **NOOP**; `/axb-ui-plan` skipped. Witness plan: unit pins for the two-figure arithmetic (injected clock) + the idle-gap resume/clear + the row-aware clear for the longer line; E2E for the two-figure **shape** and the in-block liveness frame (the per-call reset and the no-interleave/no-residue facets are unit pins — a fast scripted turn cannot tick whole seconds; `time.Sleep` is forbidden). Governance: **ADR 0009** (**supersedes ADR 0005 D7 only** — D1–D6/D8 stand, so 0005's body is **not** edited and its overall `Status` stays `Accepted`; **amends** round-019 D4). Topology audit **PASSED** (44 features · 6 modules · 16 root + 327 module rows · 1674 steps). **Merged:** plan + truth half via PR [#84](https://github.com/gosharplite/tellme/pull/84) → `dev` (`146210d`, by `thptcnec`, 2026-09-17T09:02:31Z; head `092a89c`; **16 files, +745/−30, 13 commits, zero product code**). **Implementation half PENDING** — **`/axb-tasks` DONE**: `tasks.md` (T001–T016) written on the branch `040-implement-spinner-liveness-and-turn-timer` (off `dev`); Foundational T001–T002 · Phase 3 T003–T008 (T003 `[BDD-REMOVE]` the dead stepdef (**keeping** its `toolOutputBlockIndexes`/`hasSpinnerStatusBetween` helpers for T004); T004–T005 `[BDD-RED]` the 4 sentences; T006–T007 `[UNIT]` dual-timer arithmetic + the race/anti-vacuity/row-aware-clear stress; T008 the review gate + **`Strict: true`** in the same change) · Phase 4 T009–T013 (WS-A coordinator + `admitResume()`; WS-B `FormatSpinnerLine(… total, call, …)` + `callEpoch`; `[CODE-REMOVE]` the retired whole-block pause) · T014–T016 regression/falsifiability/close; Pre-Delivery Orphan Sweep **0**. **`/axb-tasks` review loop CLOSED — architect CERTIFICATION: implementation-ready** (five fold reviews: B1–B4/TD-1…TD-5/N-1…N-3 → R-1…R-11/N-4…N-13 → R-12/R-13/N-14). **PR open for operator review**; **`/axb-implement` NOT run**. `dev` is **15** commits ahead of `main` (`git rev-list --count origin/main..origin/dev`; measure, don't derive — no local `main`/`dev` refs).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` | Stable / released line |
| `dev` | round 039 delivered (`d48ebbc`) + **round 040 plan + truth half merged (`146210d`)** | Integration line (round work lands here before `main`) |
| `039-terminal-safe-lines-and-turn-spacing` | **merged into `dev`** (frozen) | Round 039's working branch — merged via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`). |
| `040-spinner-liveness-and-turn-timer` | **merged into `dev`** (plan + truth half; PR [#84](https://github.com/gosharplite/tellme/pull/84) → `146210d`) | Round 040's working branch — off `dev`; plan + truth half only (**zero product code**). The **implementation half** continues on a fresh branch off `dev`. |
| `040-implement-spinner-liveness-and-turn-timer` | **open** (`/axb-tasks` done — `tasks.md`) | Round 040's **implementation** branch — off `dev`; carries `tasks.md` (T001–T016); `/axb-implement` held for operator review. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–039 — **DONE (no-ff)**; round 040 — **PENDING** (the plan half merged to `dev`; propagation waits on **delivery**).
> Read live heads with `git rev-parse --short origin/main origin/dev` (this clone has no local `main`/`dev` refs — only `origin/*`).

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–039** | — | Provider-registry completeness → … → terminal-safe `[Tool …]` lines + turn-output blank-line grouping. | ✅ **Delivered** (see the archives) |
| **040** | [#82](https://github.com/gosharplite/tellme/issues/82) · [#83](https://github.com/gosharplite/tellme/issues/83) | spinner liveness while a command's `[Tool Output]` streams ([#82](https://github.com/gosharplite/tellme/issues/82)) + a dual elapsed timer ([#83](https://github.com/gosharplite/tellme/issues/83)). | 🚧 **In flight** — plan + truth half **merged** (`146210d`); **tasks written**; implementation pending |
| **future slices (candidates)** | [#69](https://github.com/gosharplite/tellme/issues/69) · [#60](https://github.com/gosharplite/tellme/issues/60) · [#13](https://github.com/gosharplite/tellme/issues/13) | the **composition root + single-ownership** refactor ([#69](https://github.com/gosharplite/tellme/issues/69)); the **dogfooding track** ([#60](https://github.com/gosharplite/tellme/issues/60)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Round-040 in-flight items** — (a) **QA1–QA3 + QB2/QB3 are proposed**, pending the operator's review gate (idle-gap mechanism + threshold seam; scope; the format/phases); **QB1 (per-AI-call reset) is locked**. (b) **WS-A interleaving safety is the highest-risk area**: the implementation must pin that a resumed frame cannot interleave an output line nor be stranded — a unit stress (concurrent line writes + a fast ticker) + the E2E no-residue/no-interleave assertions; `deactivate()` is goroutine-joined and the block writer's mutex serialises resume/clear/line. (c) the idle threshold is a **fixed small value** (assumed 3 s) with a **hermetic env seam**; a too-small value risks clear/resume flicker under slow-but-continuous output. (d) the longer two-figure line is a small extra soft-wrap risk, bounded by the round-025 rune-based row count.
- **Round-040 governance** — **ADR 0009** (`docs/decisions/0009-spinner-dual-timer-and-streaming-liveness.md`) records the dual-timer policy (per-AI-call reset; the round-019-D4 amendment) and the idle-gap liveness; it **supersedes ADR 0005 D7 only** (0005 stays otherwise immutable; its body is not edited) and carries the two recorded reference divergences. *(Review-gate option: a one-line `D7` forward pointer inside ADR 0005 — flagged in `research.md` D8.)*
- **Round-039 forward items (delivered)** — (a) the loop's **control flow** depends on a `ui` presentation predicate (`ui.ToolReasonRenders`) and the blank-reason predicate is **extended** (≈3×/call), not consolidated → **[#69](https://github.com/gosharplite/tellme/issues/69)**; (b) the **missing layer-discipline gate** in `make verify` (no `verify-architecture` analogue) → **[#69](https://github.com/gosharplite/tellme/issues/69)**; (c) sanitizing argument **keys** is a small extension over #80's literal "values" (ADR 0008 residuals); (d) **#80 CLOSED** (delivered by round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81)).
- **Round-039 governance** — **ADR 0008** (`docs/decisions/0008-terminal-safe-lines-and-blank-line-grouping.md`) records the generalized terminal-safe-line policy, the `[Tool Output]`-scoped neutral close, and the two recorded reference divergences; it **supersedes ADR 0007**. *(Detail now in [`2026-09-17.md`](docs/archives/status/2026-09-17.md).)*
- **Round-038 forward items** — (a) the sanitizer is deliberately conservative + **window-bounded** (CSI 128 / OSC 1024) — **ADR 0007**/`research.md`; (b) the `[Tool Output]` block shows command output as **plain text**; (c) the broader terminal-safe line policy → **CLOSED** (round 039).
- **Round-037 forward items** — (a) the **empty-`Ctrl+S` divergence** → **[#76](https://github.com/gosharplite/tellme/issues/76)** **CLOSED** (round 038); (b) the "a refresh resets the selection" policy hardcoded inside `suggester.set` → **[#69](https://github.com/gosharplite/tellme/issues/69)**.
- **Round-036 forward items** — (a) the blank-reason suppression predicate has **three sites**, one **provably dead on the production path** → **single ownership** on [#69](https://github.com/gosharplite/tellme/issues/69); (b) the **permanent E2E narrowing** (the `\n`/`\r` reason-collision class has no E2E carrier) → [#69](https://github.com/gosharplite/tellme/issues/69) **and** [#74](https://github.com/gosharplite/tellme/issues/74); (c) `oneLine` relocated into `toolcall.go` (orphan `toollog.go` deleted).
- **Round-035 forward items** — the observer port hook overload + the spinner-yield-policy ownership (three homes / four call sites) → [#69](https://github.com/gosharplite/tellme/issues/69).
- **Round-034 forward items** — the failed-turn **display-only `Ready` overstatement** (G2) + the **numbering skew**; `BindToolOutput` ctor injection → [#69](https://github.com/gosharplite/tellme/issues/69#issuecomment-5698460385); `LoopObserver` segregation (would **supersede ADR 0005 G1**); the round-022 row→feature audit blind spot → [#60](https://github.com/gosharplite/tellme/issues/60#issuecomment-5697192786).
- **Round-033/032 forward items** + **older rounds 018–031** — live per-round in the archives.
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items.
- **Issue tracker (2026-09-17, session 7)** — [#82](https://github.com/gosharplite/tellme/issues/82) **OPEN (new this session)** and [#83](https://github.com/gosharplite/tellme/issues/83) **OPEN (new this session)** — round 040's two anchors (both close only on delivery); [#80](https://github.com/gosharplite/tellme/issues/80) **CLOSED (completed)** by round 039 (PR [#81](https://github.com/gosharplite/tellme/pull/81) merged `d48ebbc`); [#69](https://github.com/gosharplite/tellme/issues/69) **OPEN** (single-ownership refactor; body carries the round-039 items); [#60](https://github.com/gosharplite/tellme/issues/60) open (dogfooding); [#13](https://github.com/gosharplite/tellme/issues/13) open (coverage tooling).

## Environment notes

- **Round-040 branches (this session)** — the **plan** branch `040-spinner-liveness-and-turn-timer` **merged** into `dev` (PR [#84](https://github.com/gosharplite/tellme/pull/84) → `146210d`); the **implementation** branch `040-implement-spinner-liveness-and-turn-timer` (off `dev`) is **pushed** with `tasks.md` (**PR [#85](https://github.com/gosharplite/tellme/pull/85)** open → `dev`). The **E2E-harness `Strict: true` change + the 4 stepdefs land together in the implement half** (T008); `/axb-tasks` is **DONE**, `/axb-implement` is **held** for operator review.
- **PR #84 review folds (architect, APPROVE WITH REQUIRED FOLDS) + fold reviews (FOLDS ACCEPTED; TD-9/TD-10/TD-11; review loop CLOSED)** — folded in-round: **TD-1** `Strict: true` is pinned as an **implementation-half** task directive (**not** landed here) so `dev` never goes red (`make test` = `go test ./...`, which includes `tests/e2e`) · **TD-2** truth states **mutual exclusion + join** (not "single-writer") · **TD-3** the block critical section does not span a frame write · **TD-4** the watcher poll period is pinned (~200 ms) · **TD-5** the idle seam is named/pinned (`TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS`, ms, `0` = admit immediately) and the Given is renamed to a world state · **TD-6** the second figure is renamed **"the current model call's elapsed"** (turn vs model-call terms) · **TD-7** the spinner feature header is refreshed · **TD-8** this recipe + the commit counts · **RF-1** the acceptance-rule→carrier mapping + the task-list directives (incl. the dead-stepdef `[BDD-REMOVE]`) · **RF-2** ADR 0005's **index** row annotated (no body edit) · **RF-3** the no-label resume is a defined no-op · **RF-4** the second-epoch field stays internal (no sixth ctor seam; field later named **`callEpoch`** by the N-1 nit) · **RF-5** #69's body updated · **QB3/QB4** the 3+-digit row-aware-clear re-witness + "unlabelled by design" recorded. **Consequence of TD-1 (corrected per the fold review):** the plan half leaves the harness unchanged, so `make verify` **and** `make test` are **green** on this branch. The `Strict: true` flag + the 4 stepdefs land together in `/axb-implement` (witnessed by the FAIL-then-PASS transition); `dev` is never red. **TD-9 (fold review)** — swept the pre-fold vocabulary from `plan.md`, `truth-delta.md`, `checklists/requirements.md`, `STATUS.md`, and the daily log (`single-writer` → mutual-exclusion+join; "current turn's" → "current model call's"; D1–D8 → D1–D9; `timing-the-current-turn.feature` → `timing-the-current-model-call.feature`; "two divergences" → three). **TD-10 (fold review)** — the resume-only admission path is named (`admitResume()`; `activate()` keeps its synchronous first frame) and the resumed frame renders **immediately on start**; the E2E timing budget is pinned (the child's quiet stretch exceeds `N + 2·P` with margin, e.g. a 2 s child `sleep` at `N=50 ms`) in SC-002 + the `dsl.md` Given row; the `techstack.md` round-019 sentence gains the round-040 carve-out.
- **/axb-tasks review fold (PR [#85](https://github.com/gosharplite/tellme/pull/85), architect APPROVE WITH REQUIRED FOLDS)** — plan-side fold in `tasks.md` (no truth/code): **B1** the block-mutex seam is named (`ToolOutputWriter` sole lock/state owner; **one lock owner, three entry points** (`WriteWith`/`EndWith`/`withLock`) — the full set per R-4/R-8/R-11; never reach into `w.mu` — non-reentrant) · **B2** T003 retires **only** the dead stepdef; the `toolOutputBlockIndexes`/`hasSpinnerStatusBetween` helpers are **kept** for T004 · **B3** the new idle seam registered in `scenario_context.go`'s `envUnset` · **B4** the in-code superseded-citation sweep owned by T009/T010 · **TD-1** T014's gate = `go test -count=1 ./...` + `make verify` · **TD-2** the 3 pre-existing pins are **adapted** (named in T011) · **TD-3** the no-label + gated-off no-op sub-cases (T007) · **TD-4** T013 reworded · **TD-5** `spec.md` SC-003 reworded to shape-only + the unit pin · **N-1..N-3** · the stalled-writer residual risk as T007(f). **Fold review #2 (`f37a175`; FOLDS ACCEPTED — R-1/R-1b/R-2/R-3 + N-4/N-5):** R-1 corrected T004's positive-polarity bound (anchor on the **closing separator** via `closingSeparatorIndex` → `hasSpinnerStatusBetween(lines, head, closingIdx+1)`; the resumed frame shares the reset+separator `\n`-line and carries no marker) · R-1b `End` = stop watcher → **clear** → separator → resume · R-2 the coordinator seam is the hook-parameterized `WriteWith(p, beforeLine)` (line-splitting stays in the writer) · R-3 T007(g) the `\r`-only accepted-limitation pin · TD-2 self-correction (`spinner_width_test.go` **expected unchanged**) · TD-5 accepted as an accuracy fix (PM-owned, operator ratification). **Fold review #3 (`f9e1ac9`; FOLDS ACCEPTED — R-4..R-7 + N-6..N-9):** R-4 the watcher's **admit** gets a real lock-scoped entry point (`withLock(fn)` bookkeeping path, alongside the `WriteWith(p, beforeLine)` line path; R-2 scoped to the line path) · R-5 T007(g) settles the `\r`-only case as **required** (FR-001 is line-scoped; byte-arrival stamping rejected) · R-6 the back-pressure residual sharpened + T007(f) self-limiting · R-7 the B2 disposition sweep (the helpers are **kept**) across `plan.md`/`STATUS.md`/the daily log · N-6 changed-file set includes `spec.md` · N-7 stray blank removed · N-8 `dev` **15** commits ahead (measured) · N-9 T014 records why the existing long-quiet scenarios stay green (`timeout: 1` < N). **Fold review #4 (`771e057`; FOLDS ACCEPTED — R-8/R-9 + N-10/N-11):** R-8 the `End` path gets a lock-scoped hook `EndWith(beforeSeparator)` (+ stop-AND-join the watcher before the clear), new T007(h) (End-while-Write-in-flight) · R-9 the idle clock moves into the writer (`IdleSince`) · N-10 T002 reworded "one lock owner, three entry points" · N-11 this B1 annotation. **Fold review #5 (`188d9e6`; FOLDS ACCEPTED — R-10/R-11 + N-12/N-13):** R-10 deleted the stale `lastLine`-stamp clause in T009 (clock is the writer's) · R-11 the idle query gets the locked path `withLock(fn func(idle))` (+ the `Begin`-seeded `lastLine` + T007(b') zero-output case) · N-12 this annotation restored · N-13 recorded why `Begin` needs no hook. Seam model **complete** — line path · watcher admit · `End` · idle query. **Fold review #6 (`8b71463`; FOLDS ACCEPTED — R-12/R-13 + N-14; CERTIFICATION: implementation-ready, confirmed):** R-12 T002 (ii) carries the `withLock(fn func(idle))` signature · R-13 T007's `(b′)`/`(g)`/`(h)` re-merged inside the checkbox · R-14 the per-call-reset witness re-attributed to the **unit** layer (T015(b) + `spec.md` SC-005) · B-1 the daily-log fold notes reordered oldest→newest · B-2 the ledger entry · N-15 Core Inputs primed with "one lock owner, three entry points" · **N-16** (reviewer's own mis-phrasing) T015(a)'s witness-set parenthetical corrected. **Review loop CLOSED at `c9f6ba7`** (certification CONFIRMED).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the merged round-039 head `ca14276`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Host (this session)**: **Linux** (`…/beta-niffler/ait-tellme`); the **darwin** path is the cross-compile weak spot here (round 020).
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
