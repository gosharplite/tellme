# tellme — Status

**Last updated**: 2026-09-18 (session 17 — **round 043 `043-hermetic-make-go-env` DELIVERED / FROZEN** — PR [#99](https://github.com/gosharplite/tellme/pull/99) merged into `dev` `a2fbafc` (head `d007b75`; 20 commits · 12 files · +1064/−5; **FINAL CERTIFICATION — MERGE-READY** at `d007b75` after five review rounds / eight verified folds); anchor [#96](https://github.com/gosharplite/tellme/issues/96) — a **hermetic `make` Go-toolchain invocation environment**; **tooling/truth only, zero product code**; **ADR 0012**. `#96` **CLOSED (completed)** at this closeout; the installed binary refreshed from the merged head. **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation)).
**Active branch**: `dev` (round 043 delivered/frozen; the next round `044-*` opens off `dev`)
**Daily log**: [`docs/session-summary/2026/09/18/session-summary.md`](docs/session-summary/2026/09/18/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 041 detail + its PR #88 review-fold history; round 042 detail).

> **Split note (Rule 12)**: on this closeout the **round-042 delivered-round detail** was relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 043 is now the most recently delivered round), keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`, plan+truth) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, merge; head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` (single PR; the plan branch `043-hermetic-make-go-env` was deleted after PRs [#97](https://github.com/gosharplite/tellme/pull/97)/[#98](https://github.com/gosharplite/tellme/pull/98) were closed unmerged) | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, merge; head `d007b75`) |

Per-round detail lives in the archives; **043 is the most recently delivered round; no round is in flight** (the next round `044-*` opens off `dev`).

## Delivered round — 043 `043-hermetic-make-go-env` (DELIVERED / FROZEN — PR #99 merged into `dev` `a2fbafc`; frozen head `d007b75`)

**043-hermetic-make-go-env** — a **build/quality-pipeline** round (anchor [#96](https://github.com/gosharplite/tellme/issues/96), the *outer* half of round-042's **F-2**): a single top-of-`Makefile` **hermetic invocation boundary**, **tooling/truth only, zero product code**.

- **The change (criterion-derived — ADR 0012 D2).** One `export`/`unexport` block above the `$(shell command -v …)` probes: `export GOENV := off` (load-bearing — the env-file fallback defeats an unset/empty `GOFLAGS`, round-042 F-2) · `export GOWORK := off` · `unexport GOFLAGS GO111MODULE GOEXPERIMENT GOTOOLCHAIN GOFIPS140 GODEBUG` + the ambient target triple `GOOS GOARCH GOARM GOARM64 GOAMD64 GO386 GOMIPS GOMIPS64 GOPPC64 GORISCV64 GOWASM`. **Preserve:** the warm-cache set (`PATH`/`HOME`/`GOPATH`/`GOMODCACHE`/`GOCACHE`) + the network/checksum set (`GOPROXY`/`GOSUMDB`/`GOPRIVATE`/`GONOSUMDB`/`GOINSECURE`). **`CGO_ENABLED`** preserved from the caller (the only cgo pin remains `verify-cross-compile`'s inline one, recipe-local).
- **The criterion.** Neutralise the ambient **build context** (what / which toolchain builds) · preserve the **plumbing** (only what lets the build happen).
- **Verified by.** Four **red→green** witnesses (`GOENV=<file: GOFLAGS=-mod=vendor>` · `GO111MODULE=off` · a stray `GOWORK` · `GOTOOLCHAIN=go1.99.9`; each **2 → 0**; reverting the block ⇒ red) + a **make-level neutralisation assertion** (the recipe env shows none of the neutralise-set names; non-vacuous against a block-stripped `Makefile`) + **aggregate** `GOENV=<file> make verify` ⇒ exit 0 + **positive controls** (`verify-cross-compile` 4/4; `tidy`/`fmt` no-diff) + `make test` exit 0 (nested `go build`).
- **Pipeline.** **specify ✅** (clarify Q1–Q8 locked one at a time) · **spec-by-example NOOP** · **technical-research ✅** (`research.md` D1–D12; `techstack.md` Build & Tooling MODIFY; **ADR 0012** ADD + index) · **system-analysis ✅** (0 interfaces; api/data/dsl-refine **NOOP**) · **tasks ✅** (T001–T007; orphan sweep 0) · **implement ✅**.
- **Review chain (PR #99).** **Five review rounds** (consolidating the earlier #97/#98 reviews) → **FINAL CERTIFICATION — MERGE-READY at `d007b75`, review loop CLOSED**: **B-1…B-4 · TD-1 · R-1…R-3** (plan+truth) → **N-1…N-3** → **ⓐ–ⓒ** → **N-4/ⓑ2** → **N-5** → **N-6**. Each fold removed a claim the code could not support: the incident-derived 8-name list became a **criterion** (D2) + a **scope by input class** (D5) + a **coverage-not-equality** ownership invariant (D6) + an **exact, closed residual** (R4 — the 14 names `childEnv` does not re-set) + bounded residuals **R1/R3/R5/R6** (R6 = make-level inputs `-e`/`MAKEFILES`). (Fold ledger: `specs/plans/043-hermetic-make-go-env/tasks.md`.)
- **Truth / governance.** `specs/truth/techstack.md` (Build & Tooling: **Hermetic toolchain invocation** row + a **Task runner** note) · **ADR 0012** (`docs/decisions/0012-hermetic-make-go-env.md` + index row) — now **immutable** except its `Status` line and the index. `specs/truth/features/**` unchanged (topology audit unchanged).
- **Verification at the merged head.** `make verify` **OK** (incl. `verify-architecture`; golangci-lint 0 issues; govulncheck 0 reachable; cross-compile 4/4) · `go test -count=1 ./...` green (22 pkgs, 0 FAIL) · topology audit **PASSED** (44 features · 6 modules · 16 root + 327 module rows · 1674 steps) · `gofmt` clean · `go vet ./...` clean · `go.mod`/`go.sum` unchanged · diff-level secret scan clean · **no `internal/**`/`cmd/**` changed**.
- **Process note.** PRs [#97](https://github.com/gosharplite/tellme/pull/97)/[#98](https://github.com/gosharplite/tellme/pull/98) (plan / tasks+implementation halves) were **closed unmerged** at the operator's direction and collapsed into the single PR **#99**; the plan branch `043-hermetic-make-go-env` was deleted. Their commits — incl. the folded review — are ancestors of #99 (via the merge `1b2e2f5`), so no content was lost.

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (`4ce056c`) | Stable / released line |
| `dev` | round 043 **delivered** (`a2fbafc`, PR #99) | Integration line (round work lands here before `main`) |
| `043-implement-hermetic-make-go-env` | **merged into `dev`** (PR [#99](https://github.com/gosharplite/tellme/pull/99) → `a2fbafc`; frozen head `d007b75`); **branch deleted (local + remote)** after merge | Round 043's single branch — carried spec → research+ADR 0012 → plan → tasks → the `Makefile` block + the five review folds. |
| `043-hermetic-make-go-env` | **deleted** (remote + local) | Round 043's plan branch — its commits (incl. the folded review) are ancestors of #99; PRs #97/#98 (stacked) were closed unmerged by the operator. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–040 — **DONE (no-ff)**; round 041 — **DONE (no-ff, `692a4e6`)**; the session-12/13 tracker-refresh + closeout docs — **DONE (no-ff, `150627d`)**; round 042 — **DONE (no-ff)**; **round 043 — DONE (no-ff, `4ce056c`; `main^{tree}` == `dev^{tree}`)**.
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–041** | — | Provider-registry completeness → … → resolver test load tolerance. | ✅ **Delivered** (see the archives) |
| **042** | [#93](https://github.com/gosharplite/tellme/issues/93) | **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)** — layer-discipline gate (`verify-architecture`) + violation baseline (**8**); tooling/truth only; **ADR 0011**. | ✅ **Delivered** — PR [#95](https://github.com/gosharplite/tellme/pull/95) merged into `dev` (`f4b53f6`) |
| **043** | [#96](https://github.com/gosharplite/tellme/issues/96) | A **hermetic `make` Go-toolchain invocation environment** — one top-of-`Makefile` boundary (criterion-derived: neutralise the build context, preserve the plumbing); **tooling/truth only**; **ADR 0012**. | ✅ **Delivered** — PR [#99](https://github.com/gosharplite/tellme/pull/99) merged into `dev` (`a2fbafc`) |
| **044 candidate** | [#92](https://github.com/gosharplite/tellme/issues/92) | **R2 — composition-root extraction** (the 7 `cli → infrastructure` imports → 0; inject a `Dependencies` struct so `internal/cli` imports only `internal/domain/*` + stdlib). | ⏳ **Next** (opens off `dev`) |
| **future slices** | [#92](https://github.com/gosharplite/tellme/issues/92) (R3/R4 + ride-alongs) · [#91](https://github.com/gosharplite/tellme/issues/91) · [#13](https://github.com/gosharplite/tellme/issues/13) | R3 yield-policy owner + observer hook split · R4 blank-reason owner + presentation predicate · ride-alongs (suggestion-selection policy; `BindToolOutput` ctor injection); the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91) — replaced #60); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 17)** — **[#96](https://github.com/gosharplite/tellme/issues/96) CLOSED (completed)** — delivered by round 043 (PR [#99](https://github.com/gosharplite/tellme/pull/99) merged `a2fbafc`); the issue's own reproduction (`GOENV=<file> … make verify-architecture`) is **green** at the merged head. **Open tracker: [#92](https://github.com/gosharplite/tellme/issues/92)** (composition-root + presentation-policy single ownership — R1 delivered, R2–R4 + ride-alongs remain) · **[#91](https://github.com/gosharplite/tellme/issues/91)** (self-development umbrella) · **[#13](https://github.com/gosharplite/tellme/issues/13)** (coverage tooling) — plus PR #16 Obs 1.
- **Round-043 forward items (delivered)** — (a) **#96 CLOSED**; (b) **R4** — extend `tools/arch`'s `childEnv` with the D2 names when that (frozen round-042) guard is next touched; (c) **R6** — make-level inputs (`-e` mode / a `MAKEFILES`-injected `override export`) resurrect the two `export`ed names, recorded not closed (closing costs the R3 hatch); (d) **R1/R3/R5** recorded residuals (bare `go` outside `make`; the hatch per variable class; `$(shell …)`/parse-time); (e) PRs #97/#98 closed unmerged — a **process** item, no code impact.
- **Round-042 forward items (delivered)** — (a) **#96** → delivered by round 043; (b) the baseline's remaining entries are removed by **R2** (the 7) and **R3/R4** (the 8th); (c) **recorded friction for R3/R4** — with the merged graph the rule governs an `internal/agent` **test** importing `internal/ui` (reds as RULE-A — desirable: it forces the port seam); (d) the `modelith-layers` analogue is not adopted (no modelith toolchain); (e) custom build-tag-gated imports are out of scope.
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**.
- **PM follow-ups** — none open.

## Environment notes

- **Round-043 (session 17)** — branch `043-implement-hermetic-make-go-env` (PR [#99](https://github.com/gosharplite/tellme/pull/99) → `dev` `a2fbafc`; frozen head `d007b75`; the branch was deleted after merge, as was the plan branch `043-hermetic-make-go-env`). Host: **linux/amd64** (Go **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`) · `go test -count=1 ./...` green (22 packages, 0 FAIL) · topology audit **PASSED** (44 · 6 · 16+327 · 1674) · `go.mod`/`go.sum` unchanged · diff-level secret scan clean. `make verify` runs the Go toolchain **hermetically** (ADR 0012) — an ambient/persisted Go env no longer reddens a target.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the delivered round-043 merged head `a2fbafc`; `--version` → `dev`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
