# tellme — Status

**Last updated**: 2026-09-17 (session 5: **round 038 `038-tool-output-sanitize-and-empty-submit` — DELIVERED / FROZEN**). Implementation PR [#79](https://github.com/gosharplite/tellme/pull/79) **merged** into `dev` (`a0b0653`, by `thptcnec`); frozen round head `3da9ed0` (7 commits). Round 038 fixes [#78](https://github.com/gosharplite/tellme/issues/78) (the `[Tool Output]` terminal-state leak) and [#76](https://github.com/gosharplite/tellme/issues/76) (an empty `-i` submit quit the prompt), folded into one round at the operator's direction: the streamed `[Tool Output]` **content** lines are **sanitized** at the `internal/ui` seam (7-bit ANSI escapes + stray C0/DEL removed; TAB kept; bytes ≥ 0x80 untouched; ASCII-gated + per-kind windowed, so it never *introduces* invalid UTF-8) and the block **always closes neutral** (`\x1b[0m` before the closing separator on every path); and an empty/whitespace-only `-i` submit is a **no-op** (reference parity). Full AIxBDD pipeline (specify → spec-by-example → research → system-analysis → dsl-refine → tasks → implement) then a **four-round review/fold chain to FINAL CERTIFICATION** (B1 · B2 → TD-3 · N-4 · N-5 → N-6 → N-7). Truth: `techstack.md` (the `execute_command` + `-i` rows) + `watching-the-tool-loop.feature` + `using-the-interactive-prompt.feature` (new Rules) + `chat/dsl.md` (+2 Given/1 When/2 Then + note); `/axb-api-plan` + `/axb-data-plan` **NOOP**; `/axb-ui-plan` skipped. Governance: **ADR 0007** (`docs/decisions/0007-terminal-control-sanitization.md`). New issue **[#80](https://github.com/gosharplite/tellme/issues/80)** filed (the terminal-safe line policy for the sibling `[Tool …]` formatters). Open issues: **#80** · **#69** · **#60** · **#13**. **Propagation: `dev → main` DONE (no-ff).**
**Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation this phase)
**Active branch**: `dev` (round 038 delivered/frozen; the next round starts a fresh `039-*` off `dev`)
**Daily log**: [`docs/session-summary/2026/09/17/session-summary.md`](docs/session-summary/2026/09/17/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–037).

> **Split note (Rule 12)**: the **round-037** detail section was relocated **verbatim** into [`2026-09-17.md`](docs/archives/status/2026-09-17.md) when round 038 became current (joining rounds 035–037), keeping `STATUS.md` to a single current-round section. `STATUS.md` is already lean (one current-round section) — no further split needed this closeout.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–038 | `001-*` … `038-tool-output-sanitize-and-empty-submit` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`) |

Per-round detail lives in the archives (001–002 in [`2026-09-11.md`](docs/archives/status/2026-09-11.md); 003–012 in [`2026-09-13.md`](docs/archives/status/2026-09-13.md); 013–019 in [`2026-09-14.md`](docs/archives/status/2026-09-14.md); 020–026 in [`2026-09-15.md`](docs/archives/status/2026-09-15.md); 027–034 in [`2026-09-16.md`](docs/archives/status/2026-09-16.md); 035–037 in [`2026-09-17.md`](docs/archives/status/2026-09-17.md)); **038 is the most recent delivered round.**

## Current round — 038 `038-tool-output-sanitize-and-empty-submit` (DELIVERED / FROZEN)

**038-tool-output-sanitize-and-empty-submit** — two folded operator issues on the `stderr`-bound interactive surface. **(a) #78 — the live `[Tool Output]` block leaked terminal state:** the streamed **content** lines are now **sanitized** at the `internal/ui` seam (every **7-bit** ANSI escape sequence — CSI/SGR, OSC, other ESC-introduced — and every stray C0/DEL byte removed; TAB kept; interior CR dropped; bytes ≥ 0x80 untouched; the ESC consumption is ASCII-gated and the scan window bounded per kind — CSI 128 / OSC 1024 — so a long **terminated** sequence is still removed in full and the sanitizer never *introduces* invalid UTF-8), so a colouring/killing command can no longer tint or alter the terminal; the block **always closes neutral** (a default-state restore, `\x1b[0m`, before the closing separator on every close path — normal/start-failure/trim/timeout). Presentation-only (the tool **result** keeps its raw bytes); **recorded divergence** (the reference sanitizes nothing). **(b) #76 — an empty submit quit the `-i` prompt:** `Ctrl+S`/`Alt+Enter` on an empty (or whitespace-only) editor is now a **no-op** (the prompt stays open; reference parity); only a non-empty submit submits, and `Esc`/`Ctrl+C` still aborts. Truth: `techstack.md` (the `execute_command` row + the `-i` row) + `watching-the-tool-loop.feature` (new Rule) + `using-the-interactive-prompt.feature` (new Rule) + `chat/dsl.md` (2 Given + 1 When + 2 Then + round-038 note); `/axb-api-plan` + `/axb-data-plan` NOOP; `/axb-ui-plan` skipped. Witnesses: hostile-fixture unit pins (`tooloutput_sanitize_test.go`, `model_submit_test.go`) + 3 new E2E Examples. Governance: **ADR 0007** (the round's presentation invariant + recorded divergence). `make verify` **OK**; E2E **218/218** (was 215). Merged via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); frozen head `3da9ed0` (7 commits).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` | Stable / released line |
| `dev` | round 038 delivered (`a0b0653`) | Integration line (round work lands here before `main`) |
| `037-interactive-prompt-no-selection` | **merged into `dev`** (frozen) | Round 037's working branch — merged via PR [#77](https://github.com/gosharplite/tellme/pull/77) (`3aacf32`). |
| `038-tool-output-sanitize-and-empty-submit` | **merged into `dev`** (frozen) | Round 038's working branch — merged via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`). |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–038 — **DONE (no-ff)**.
> Read live heads with `git rev-parse --short main dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–038** | — | Provider-registry completeness → … → tool-output sanitization + empty-submit no-op. | ✅ **Delivered** (see the archives) |
| **future slices (candidates)** | [#80](https://github.com/gosharplite/tellme/issues/80) · [#69](https://github.com/gosharplite/tellme/issues/69) · [#60](https://github.com/gosharplite/tellme/issues/60) · [#13](https://github.com/gosharplite/tellme/issues/13) | the **terminal-safe line policy** ([#80](https://github.com/gosharplite/tellme/issues/80)); the **composition root + single-ownership** refactor ([#69](https://github.com/gosharplite/tellme/issues/69)); the **dogfooding track** ([#60](https://github.com/gosharplite/tellme/issues/60)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Round-038 forward items (delivered)** — (a) the sanitizer is deliberately conservative and **window-bounded** (CSI 128 / OSC 1024; a beyond-window/unterminated sequence prints the rest of the line, bounded by the block's byte budget) — recorded in **ADR 0007** (+ `research.md`); (b) the `[Tool Output]` block shows command output as **plain text** (no "preserve safe styling"); (c) the broader **terminal-safe line policy** (sanitize the sibling `[Tool Reason]`/`[Tool Result]`/`[Tool Action]` formatters too) → live issue **[#80](https://github.com/gosharplite/tellme/issues/80)**; (d) **#78** + **#76** **CLOSED** this closeout (delivered by PR [#79](https://github.com/gosharplite/tellme/pull/79)).
- **Round-038 governance** — **ADR 0007** (`docs/decisions/0007-terminal-control-sanitization.md`) records the new stderr presentation invariant (the `[Tool Output]` content is control-free; the block closes **neutral**, unconditionally) and the deliberate reference divergence.
- **Round-037 forward items** — (a) the **empty-`Ctrl+S` divergence** → **[#76](https://github.com/gosharplite/tellme/issues/76)** **CLOSED** this round (delivered by round 038; reference parity); (b) `[REFACTOR]` the "a refresh resets the selection" policy is hardcoded inside `suggester.set` (the reference names it at the call site) → **[#69](https://github.com/gosharplite/tellme/issues/69)** (single-ownership theme).
- **Round-036 forward items** — (a) `[TECHNICAL DEBT]` the blank-reason suppression predicate has **three sites**, one **provably dead on the production path** → **single ownership** on [#69](https://github.com/gosharplite/tellme/issues/69); (b) the **permanent E2E narrowing** (the `\n`/`\r` reason-collision class has no E2E carrier) — recorded on [#69](https://github.com/gosharplite/tellme/issues/69) **and** [#74](https://github.com/gosharplite/tellme/issues/74); (c) `oneLine` relocated into `toolcall.go` (orphan `toollog.go` deleted).
- **Round-035 forward items** — the observer port hook overload + the spinner-yield-policy ownership (three homes / four call sites) → [#69](https://github.com/gosharplite/tellme/issues/69).
- **Round-034 forward items** — the failed-turn **display-only `Ready` overstatement** (G2) + the **numbering skew**; `BindToolOutput` ctor injection → [#69](https://github.com/gosharplite/tellme/issues/69#issuecomment-5698460385); `LoopObserver` segregation (would **supersede ADR 0005 G1**); the round-022 row→feature audit blind spot → [#60](https://github.com/gosharplite/tellme/issues/60#issuecomment-5697192786).
- **Round-033 forward items** — loader silently best-effort; recursive walk linear; large-catalog bounded by the resource contract; path-sort ordering.
- **Round-032 forward items** — local stdio MCP transport; cross-invocation tool caching; MCP-backed MEMORY/PLUR; MCP `-d` diagnostic; `mcptest/` → [#13](https://github.com/gosharplite/tellme/issues/13); stale `make help` text.
- **Older forward items** — rounds 018–031 forward items live per-round in the archives.
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items.
- **Issue tracker (2026-09-17 closeout, session 5)** — [#78](https://github.com/gosharplite/tellme/issues/78) **CLOSED (completed)** — the `[Tool Output]` terminal-state leak, delivered by round 038 (PR [#79](https://github.com/gosharplite/tellme/pull/79) merged `a0b0653`); [#76](https://github.com/gosharplite/tellme/issues/76) **CLOSED (completed)** — the empty-`Ctrl+S` divergence, delivered by round 038 (same PR); [#80](https://github.com/gosharplite/tellme/issues/80) **OPEN (new this round)** — the terminal-safe line policy for the sibling `[Tool Reason]`/`[Tool Result]`/`[Tool Action]` formatters; [#69](https://github.com/gosharplite/tellme/issues/69) open (single-ownership refactor); [#60](https://github.com/gosharplite/tellme/issues/60) open (dogfooding); [#13](https://github.com/gosharplite/tellme/issues/13) open (coverage tooling). No issues superseded this closeout.

## Environment notes

- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the merged round-038 head `3da9ed0`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Host (this session)**: **Linux** (`…/beta-niffler/ait-tellme`); the **darwin** path is the cross-compile weak spot here (round 020).
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps — **clean this closeout**.
