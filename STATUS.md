# tellme — Status

**Last updated**: 2026-09-18 (session 24 — **round 050 in flight**: `specs/plans/050-agentloop-construction-inversion/` (sub-slice 2 of [#101](https://github.com/gosharplite/tellme/issues/101) — the `AgentLoop` construction inversion, the **baseline-moving** round; **RULE-E 2 → 1 DONE in-branch**); `/axb-specify` ✅ · `/axb-clarify` ✅ (Q1 → A · Q2 → (i) · Q3 → (a) · Q4 → A) · research ✅ (**ADR 0019**) · system-analysis ✅ · tasks ✅ · implement ✅ — `make verify` **OK**, full suite green; **PR [#113](https://github.com/gosharplite/tellme/pull/113) OPEN** (review → fold `efc842b` → fold-verification → fold **`c22ce0c`**; all folds applied). Prior: session 23 closeout — round **049** `049-cli-agent-decoupling` **DELIVERED / FROZEN** — **R5.3** of [#101](https://github.com/gosharplite/tellme/issues/101) (R5 of [#92](https://github.com/gosharplite/tellme/issues/92)); anchor [#101](https://github.com/gosharplite/tellme/issues/101); the **re-cut sub-slice 1** of the `cli → agent` de-coupling: the loop's crossing **contracts** (`Result`/`ErrIncomplete`/`ToolDefs`) extracted from `internal/agent` to **`internal/domain/agent`** (referenced directly; no alias); the CLI's `→ agent` references drop **4 → 1** (`AgentLoop`); the **RULE-E baseline is UNCHANGED (2, byte-identical)** — a preparatory slice; a **new RULE-F** ratchet (the application **coupling surface**) machine-carries the identifier-count DoD; behaviour-preserving; **ADR 0018**; PR [#112](https://github.com/gosharplite/tellme/pull/112) human-merged into `dev` `d2a01b9` (by `gosharplite`). Pipeline: `/axb-specify` ✅ · `/axb-clarify` ✅ (**Q1 → B** re-cut · **Q2 → (i)** all three contracts · **Q3 → (a)** reference + delete) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (**ADR 0018** + `techstack.md` MODIFY ×2) · `/axb-system-analysis` ✅ (0 interfaces; api/data/dsl-refine NOOP) · `/axb-tasks` ✅ (T001–T011) · `/axb-implement` ✅ (all `[X]`; witnesses a/b/c reproduced+reverted) · **PR [#112](https://github.com/gosharplite/tellme/pull/112) MERGED**. **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation).
**Active branch**: `050-agentloop-construction-inversion` (round 050 in flight; **PR [#113](https://github.com/gosharplite/tellme/pull/113) OPEN**; `dev` holds round 049 delivered/frozen)
**Daily log**: [`docs/session-summary/2026/09/18/session-summary.md`](docs/session-summary/2026/09/18/session-summary.md)

## Round in flight — 050 `050-agentloop-construction-inversion` (sub-slice 2 of [#101](https://github.com/gosharplite/tellme/issues/101) · #101's R5; the **baseline-moving** round **2 → 1**)

- **Theme**: the **second (and final) sub-slice** of the re-cut `internal/cli → internal/agent` de-coupling — invert the surviving `AgentLoop` **construction/execution** (`cli.go:699`, exactly **one** `agent.AgentLoop` construction site post round 049) into an injected **domain port**, so `internal/cli` production references **zero** `internal/agent` identifiers and the RULE-E ratchet moves **2 → 1**.
- **⚠️ Two ratchet removals** (ADR 0018 §Forward, fold **F-4**): the removal reds **twice** — the RULE-E baseline line **and** the RULE-F `couplingSurface["internal/cli -> internal/agent"]` key. The `internal/cli -> internal/ui` line + its **19**-identifier surface stay **byte-identical**.
- **⚠️ R-1 cross-slice coupling** (ADR 0018 §Forward): the surviving `→ agent` site shares one ~47-line wiring block (`cli.go:699-745`) with **three** `→ ui` refs (`ui.ToolLineRenderer{}` L716 · `*ui.Spinner` L721 · `ui.NewToolOutputCoordinator` L739) → **clarify Q1 decides the split**.
- **Branch**: `050-agentloop-construction-inversion` (off `dev` `684e41e`).
- **Pipeline**: `/axb-specify` ✅ · `/axb-clarify` ✅ (**Q1 → A** port-only · **Q2 → (i)** domain `Loop` interface + `LoopSpec` + func-typed `LoopFactory` · **Q3 → (a)** CLI keeps `Lines`/observer domain-typed · **Q4 → A** CLI tests adopt a fake `agentport.Loop` — RULE-E is merged-graph) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (**ADR 0019** + `techstack.md` MODIFY ×2) · `/axb-system-analysis` ✅ (0 interfaces; api/data/dsl-refine NOOP) · `/axb-tasks` ✅ (T001–T015) · `/axb-implement` ✅ (all `[X]`).
- **Implemented (in-branch, `make verify` OK)**: `internal/domain/agent/{loop.go,loop_test.go}` (NEW — `Loop`/`LoopSpec`/`LoopFactory`) · `internal/agent/newloop.go` (NEW — `agent.NewLoop`) · `deps.Dependencies.LoopFactory` (func-typed; covered by `Validate()`) · `cmd/tellme` binds `agent.NewLoop` · `internal/cli/cli.go` builds the loop via `dp.LoopFactory(agentport.LoopSpec{…})` (**zero** `internal/agent` refs) · CLI tests reworked to an in-package `fakeLoop` · **RULE-E baseline 2 → 1** + **RULE-F `→ agent` key removed** (the two expected removals; the `→ ui` line/surface byte-identical at 19) · witnesses (a) compile-level, (b) count-level (RULE-E **new** + RULE-F **coverage**) reproduced+reverted · `go.mod`/`go.sum` unchanged.

## Delivered round — 049 `049-cli-agent-decoupling` (DELIVERED / FROZEN — PR [#112](https://github.com/gosharplite/tellme/pull/112) merged into `dev` `d2a01b9`; R5.3 of [#101](https://github.com/gosharplite/tellme/issues/101); anchor [#101](https://github.com/gosharplite/tellme/issues/101))

- **Theme**: `#92` **AC2**'s second clause — advanced by the **re-cut sub-slice 1** of the residual RULE-E edge **`internal/cli → internal/agent`**. ADR 0017 §Forward classifies that edge as *"the deepest slice"* (measured **4 crossing identifiers over 2 files** — `AgentLoop`/`AgentResult`/`ErrIncomplete`/`ToolDefs`), so it is **re-cut** (Q1 → B) into ordered sub-slices; this round extracts the loop's **crossing contracts** so sub-slice 2 (the construction inversion) is edge-sized.
- **Locked decisions (Q1–Q3)**: **Q1-B** re-cut into sub-slices → **this round = sub-slice 1** (contracts → domain; **sub-slice 2** = invert the `AgentLoop` construction/execution → the **baseline-moving** round **2 → 1**) · **Q2-(i)** extract **all three** contracts (turn-result `Result`, incomplete-turn `ErrIncomplete`, the pure `ToolDefs` projection) → **`internal/domain/agent`** · **Q3-(a)** `internal/agent` **references** the domain contracts directly (**no alias, no forwarder**; one concept → one name → one home).
- **⚠️ Baseline UNCHANGED (2, byte-identical)**: the `AgentLoop` **construction** keeps the `→ agent` edge, so `tools/arch/baseline.txt` is unchanged (0 new / 0 stale). The DoD is *"contracts domain-owned + the surviving `cli → agent` coupling is a single construction call site + gate green"* — **not** a ratchet move.
- **Artifacts**: `internal/domain/agent/result.go` (NEW — `Result` + `ErrIncomplete` + `ToolDefs`; RULE-C-pure) · `internal/domain/agent/result_test.go` (NEW — pins + the empty-registry pin) · `internal/agent/agentloop.go` (local decls **deleted**; `agentport.*`) + `{agentloop,callhooks,usage_calls}_test.go` (repointed) · `internal/cli/{call_renderer,cli}.go` (`agentport.*`; keeps `agent.AgentLoop`) · `tools/arch/arch_test.go` (NEW **RULE-F** — the application **coupling surface**: a fail-on-stale per-edge identifier allow-list + a coverage invariant; **six rules** in the guard) · **ADR 0018** (`docs/decisions/0018-cli-agent-contracts-extraction.md` + index row) · `specs/truth/techstack.md` MODIFY ×2 (Layer-discipline gate row — six rules + RULE-F; Agent tool loop row — contracts domain-owned) · `specs/plans/049-cli-agent-decoupling/**`.
- **Verification**: `make verify` **OK** (`verify-architecture`: RULE-E **0 new / 0 stale**, baseline **2 byte-identical**; RULE-A/B/C **0**; **0** cycles; **RULE-F** `→ agent == {AgentLoop}`, `→ ui == 19 ids` (0 new / 0 stale) + coverage green; **8/8** named self-tests; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~57 s godog E2E) · `gofmt`/`go vet` clean · **identifier count 4 → 1** (guard **and** grep) · falsifiability witnesses (a) compile-level / (b) flipped `Result` field / (c) **RULE-F** count-level reproduced + reverted; `go.mod`/`go.sum` unchanged.
- **Review chain (PR #112 — four rounds, all folds verified)**: review `5728513824` — **APPROVE WITH NON-BLOCKING FOLDS (no blocker; TD-1 required)** → fold `c3e8e16` (**TD-1** = the new **RULE-F** coupling-surface allow-list; R-1 cross-slice coupling at `cli.go:699-745`; R-2 `agentport` alias choice; R-3 projection siting rationale; R-4 STATUS header; N-1…N-4) → fold review `5728643445` — **FOLDS VERIFIED; TD-1 CLOSED** (new **TD-2** RULE-F coverage invariant + **F-2** name-set defence required) → fold `a348541`/`088ac7d` (**TD-2** `assertSurfaceCoversBaseline` + the `→ ui` surface tracked; **F-2** RULE-F under the order-pinned name-set; F-3 production-only scope recorded; F-4 two-removal note; N-5) → fold verification `5728754590` — **TD-2/F-2 CLOSED** (N-6…N-9) → fold `e88a1f9` (N-6 message/growth path; N-7 param accuracy; N-8 metric; N-9 STATUS; the RULE-A-closes-the-`app/**`-escape note) → final fold verification `5728843287` — **MERGE-READY, loop CLOSED** (§2 causal correction — the coverage *rule* is load-bearing, the wiring is an earliness advantage; §3 vocabulary sweep + a pure `uncoveredSurfaceEdges` + the synthetic `surface-coverage-predicate` self-test) → folded **`d2a01b9`**.
- **Merge + propagation**: PR [#112](https://github.com/gosharplite/tellme/pull/112) **human-merged** into `dev` (`d2a01b9`, by `gosharplite`); remote + local round branch deleted after merge. **Propagated `dev → main` (no-ff)** at this closeout.
- **Forward items**: **sub-slice 2** = the `AgentLoop` construction/execution inversion (the **baseline-moving** round, **2 → 1**; now provably edge-sized — a single construction call site) — **its DoD names TWO ratchet removals** (the RULE-E baseline line **and** the RULE-F key; F-4) · the R-1 **cross-slice coupling** at `cli.go:699-745` (the surviving `→ agent` site shares one ~47-line block with three `→ ui` refs) · the `Options`/`Dependencies` `Validate()` interface-seam caveat (`Kind()==reflect.Func` is blind to interface seams; ADR 0017 §Forward) · RULE-F metric = distinct **selectors**, **not** call sites (N-8); production-only scope (F-3) · the `cli → ui` edge (the last RULE-E residual; ≥ edge-sized — ADR 0017 §Forward) · **F-6/F-7/F-8**.

**Fold ledger**: round-047 (PR [#110](https://github.com/gosharplite/tellme/pull/110)) `d5ba1ae` → `4c20c98` → `a546c89` · [round-048](https://github.com/gosharplite/tellme/pull/111) (PR [#111](https://github.com/gosharplite/tellme/pull/111)) `e9c9ea2` → `36493a2` · **[round-049](https://github.com/gosharplite/tellme/pull/112) (PR [#112](https://github.com/gosharplite/tellme/pull/112))** `1e2e451` → **`c3e8e16`** (TD-1/R-1…R-4/N-1…N-4) → **`a348541`**+`088ac7d` (TD-2/F-2/F-3/F-4/N-5) → **`e88a1f9`** (N-6…N-9 + §2 positive) → **`d2a01b9`** (§2 + §3); all four reviews **verified their folds as behaviour**. *(Convention, TD-6: this line names fold heads + prior ledgers only — a ledger commit cannot name its own SHA.)*
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (rounds 041–048 detail + the 043/044/046/047 branch-model rows + the round-046/047 env notes + **the round-048 detail + its branch-model row + the round-047 env note**, relocated at the round-049 closeout).

> **Split note (Rule 12)**: on this closeout the **round-048 delivered-round detail + its branch-model row** and the **round-047 environment note** were relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 049 is now the most recently delivered round), keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`, plan+truth) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, merge; head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` (single PR) | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, merge; head `d007b75`) |
| 044 | `044-composition-root-extraction` (plan) + `044-implement-composition-root-extraction` (impl) | PR [#102](https://github.com/gosharplite/tellme/pull/102) (`4fd57fc`, plan+truth+grill fold) · PR [#104](https://github.com/gosharplite/tellme/pull/104) (`8da0b88`, merge; head `ba64508`) |
| 045 | `045-yield-policy-owner` (single PR) | PR [#106](https://github.com/gosharplite/tellme/pull/106) (`9e03a91`, merge; head `ab2c7fb`; folds `ef0c7ec`/`ab2c7fb`) |
| 046 | `046-blank-reason-owner-and-presentation-decoupling` (single PR) | PR [#109](https://github.com/gosharplite/tellme/pull/109) (`8ca4758`, merge; folds `8a39fc0`/`91e1e23`/`a96fa20`/`85fa321`) |
| 047 | `047-application-import-ceiling-gate` (single PR) | PR [#110](https://github.com/gosharplite/tellme/pull/110) (`6711e0a`, merge; folds `4c20c98`/`a546c89`; head `d39cfb1`) |
| 048 | `048-cli-tui-prompt-decoupling` (single PR) | PR [#111](https://github.com/gosharplite/tellme/pull/111) (`822e171`, merge; fold `36493a2`) |
| 049 | `049-cli-agent-decoupling` (single PR) | PR [#112](https://github.com/gosharplite/tellme/pull/112) (`d2a01b9`, merge; folds `c3e8e16`/`a348541`/`e88a1f9`; head `d2a01b9`) |

Per-round detail lives in the archives; **049 is the most recently delivered round; no round is in flight** (the next round `050-*` opens off `dev`).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (**no-ff**) at this closeout — round 049 propagation **DONE** | Stable / released line |
| `dev` | round 049 **delivered** (`d2a01b9`, PR [#112](https://github.com/gosharplite/tellme/pull/112)) | Integration line (round work lands here before `main`) |
| `050-agentloop-construction-inversion` | **in flight** — round 050 (sub-slice 2 of [#101](https://github.com/gosharplite/tellme/issues/101)): the `AgentLoop` construction/execution → the injected domain port (`agentport.Loop`/`LoopSpec`/`LoopFactory`; adapter `agent.NewLoop`); **RULE-E 2 → 1** + RULE-F `→ agent` key removed; **ADR 0019**; **PR [#113](https://github.com/gosharplite/tellme/pull/113) OPEN** | Round 050's branch (off `dev` `684e41e`) |
| `049-cli-agent-decoupling` | **merged into `dev`** (PR [#112](https://github.com/gosharplite/tellme/pull/112) → `d2a01b9`); **branch deleted (local + remote)** after merge | Round 049's single branch — the **re-cut sub-slice 1** (**R5.3** of [#101](https://github.com/gosharplite/tellme/issues/101)): the loop's crossing contracts (`Result`/`ErrIncomplete`/`ToolDefs`) → `internal/domain/agent`; CLI `→ agent` refs **4 → 1**; baseline **unchanged (2)**; new **RULE-F**; **ADR 0018**. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–047 — **DONE (no-ff)** (round 043 `4ce056c`; round 044 `ebaeebc`; round 045 `ad407cf`; round 046 — DONE; round 047 `e47dbc4`); **round 048 — DONE (no-ff)**; **round 049 — DONE (no-ff)** at this closeout. (The round-043/044/046/047 branch-model rows are archived in [`2026-09-18.md`](docs/archives/status/2026-09-18.md).)
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–041** | — | Provider-registry completeness → … → resolver test load tolerance. | ✅ **Delivered** (see the archives) |
| **042** | [#93](https://github.com/gosharplite/tellme/issues/93) | **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)** — layer-discipline gate (`verify-architecture`) + violation baseline (**8**); tooling/truth only; **ADR 0011**. | ✅ **Delivered** — PR [#95](https://github.com/gosharplite/tellme/pull/95) merged into `dev` (`f4b53f6`) |
| **043** | [#96](https://github.com/gosharplite/tellme/issues/96) | A **hermetic `make` Go-toolchain invocation environment**; tooling/truth only; **ADR 0012**. | ✅ **Delivered** — PR [#99](https://github.com/gosharplite/tellme/pull/99) merged into `dev` (`a2fbafc`) |
| **044** | [#100](https://github.com/gosharplite/tellme/issues/100) | **R2 of [#92](https://github.com/gosharplite/tellme/issues/92)** — composition-root extraction; behaviour-preserving; **ADR 0013**. | ✅ **Delivered** — PR [#104](https://github.com/gosharplite/tellme/pull/104) merged into `dev` (`8da0b88`); baseline 8 → 1 |
| **045** | [#105](https://github.com/gosharplite/tellme/issues/105) | **R3 of [#92](https://github.com/gosharplite/tellme/issues/92)** — yield-policy owner + `LoopObserver` hook split; **ADR 0014**. | ✅ **Delivered** — PR [#106](https://github.com/gosharplite/tellme/pull/106) merged into `dev` (`9e03a91`); baseline stays **1** |
| **046** | [#108](https://github.com/gosharplite/tellme/issues/108) | **R4 of [#92](https://github.com/gosharplite/tellme/issues/92)** — blank-reason single owner + loop presentation de-coupling; baseline **1 → 0**; **ADR 0015**. | ✅ **Delivered** — PR [#109](https://github.com/gosharplite/tellme/pull/109) merged into `dev` (`8ca4758`); **baseline 0** |
| **047** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.1 of [#101](https://github.com/gosharplite/tellme/issues/101)** — the **application import-ceiling gate** (**RULE-E**); the 3 residual edges baselined; tooling/truth only; **ADR 0016**. | ✅ **Delivered** — PR [#110](https://github.com/gosharplite/tellme/pull/110) merged into `dev` (`6711e0a`); RULE-A/B/C **0** + RULE-E **3** |
| **048** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.2** — the first de-coupling slice: `internal/cli → internal/ui/tui/prompt` → the injected domain `tui.Prompter` port; RULE-E **3 → 2**; **F-4 closed**; **ADR 0017**. | ✅ **Delivered** — PR [#111](https://github.com/gosharplite/tellme/pull/111) merged into `dev` (`822e171`); baseline RULE-E **2** |
| **049** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.3** — the **re-cut sub-slice 1** of the `cli → agent` de-coupling: the loop's crossing contracts → `internal/domain/agent` (no alias); CLI `→ agent` refs **4 → 1**; baseline **unchanged (2)**; new **RULE-F**; **ADR 0018**. | ✅ **Delivered** — PR [#112](https://github.com/gosharplite/tellme/pull/112) merged into `dev` (`d2a01b9`); baseline RULE-E **2** (unchanged) |
| **future slices** | [#92](https://github.com/gosharplite/tellme/issues/92) (ride-alongs) · [#101](https://github.com/gosharplite/tellme/issues/101) (**R5.x**: **sub-slice 2** = the `AgentLoop` construction inversion — baseline **2 → 1**, *two* removals; then the `cli → ui` edge; F-6/F-7/F-8) · [#91](https://github.com/gosharplite/tellme/issues/91) · [#13](https://github.com/gosharplite/tellme/issues/13) · [#103](https://github.com/gosharplite/tellme/issues/103) | **Sub-slice 2** (the deepest remaining slice — now edge-sized; the R-1 cross-slice coupling at `cli.go:699-745` must be scoped); ride-alongs (suggestion-selection policy; `BindToolOutput` ctor injection); the `cli → ui` slice (**not edge-sized** — ADR 0017 §Forward); the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)); the `-l`/`-c` + `-t` plumbing gap ([#103](https://github.com/gosharplite/tellme/issues/103)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 23 closeout)** — **[#101](https://github.com/gosharplite/tellme/issues/101) OPEN** — R5 is a **programme**: **R5.1 DELIVERED** (round 047) · **R5.2 DELIVERED** (round 048) · **R5.3 sub-slice 1 DELIVERED** (round 049, PR [#112](https://github.com/gosharplite/tellme/pull/112) merged `d2a01b9`); the **remaining `cli → agent` edge** (removed by **sub-slice 2**) + the **`cli → ui` edge** + **F-6/F-7/F-8** remain; the body carries the delivery record (updated this closeout). **[#92](https://github.com/gosharplite/tellme/issues/92) OPEN** — R1–R4 delivered; ride-alongs remain. **[#103](https://github.com/gosharplite/tellme/issues/103)** · **[#91](https://github.com/gosharplite/tellme/issues/91)** · **[#13](https://github.com/gosharplite/tellme/issues/13)** — all OPEN (accurate). **[#93](https://github.com/gosharplite/tellme/issues/93)/[#96](https://github.com/gosharplite/tellme/issues/96)/[#100](https://github.com/gosharplite/tellme/issues/100)/[#105](https://github.com/gosharplite/tellme/issues/105)/[#107](https://github.com/gosharplite/tellme/issues/107)/[#108](https://github.com/gosharplite/tellme/issues/108) CLOSED** (earlier). Round 049 delivered a slice of an already-open programme → **no closes; #101 body revised**.
- **Round-049 forward items (delivered)** — (a) **sub-slice 2** = the `AgentLoop` construction/execution inversion — the **baseline-moving** round (**2 → 1**), now provably edge-sized (a single construction call site; the `Run` result/error are already domain-owned); **its DoD names TWO ratchet removals** (the RULE-E baseline line **and** the RULE-F key; fold F-4) · (b) the **R-1 cross-slice coupling** at `cli.go:699-745`: the surviving `→ agent` site shares one ~47-line block with three `→ ui` refs, so sub-slice 2 must decide `cmd/tellme` assembly vs. the split (ADR 0018 §Forward) · (c) the `Options`/`Dependencies` `Validate()` **interface-seam caveat** (`Kind()==reflect.Func` blind to interface seams; ADR 0017 §Forward) · (d) **RULE-F** metric = distinct **selectors**, not call sites (fold N-8), and production-only scope (fold F-3) · (e) the `cli → ui` edge (last RULE-E residual; ≥ edge-sized) · (f) **F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101).
- **Round-048 forward items (delivered)** — (a) **#101** stays OPEN; (b) the **2 remaining residual edges** `cli → {agent, ui}` are the later **R5.x slices** — the **`cli → ui` slice is not edge-sized** (≈20 call sites · 3 crossing value types · 4 stateful objects · 8 formatters; ADR 0017 §Forward — likely a re-cut); (c) **F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101); (d) `Options.Validate()` is called by the composition-root smoke test, not `main` (boundary choice; `Kind()==reflect.Func` blind to interface seams); (e) the `internal/ui` adapter home pulls the Bubble Tea family transitively (ADR 0017 §Consequences — re-home candidate); (f) the nil-port class-phrase reuse is a recorded divergence.
- **Round-047 forward items (delivered)** — (a) **#101** stays OPEN; (b) third-party application-tier imports are **outside RULE-E** (a **live residual**: `internal/cli` imports `spf13/pflag` + `golang.org/x/term`; ADR 0016 D6); (c) the sanctioned set may be **re-ruled** (ADR 0016 §Forward) + the optional **per-rule row split**; (d) custom build-tag-gated imports out of scope (ADR 0011 D6).
- **Round-046 forward items (delivered)** — (a) **#108 CLOSED**; (b) the `Lines` **wire** has no unit-level carrier — a missing/false injection is caught only by the godog E2E; a `runTurn`-level assertion is the recorded follow-up; (c) `ToolReasonRenders` stays **test-facing**; (d) ADR 0015 *Consequences*: the discarded-render cost, the port-shape leak, and the **no-release-valve** policy at baseline 0.
- **Round-045/044/043/042 forward items (delivered)** — see [`2026-09-18.md`](docs/archives/status/2026-09-18.md).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**.
- **PM follow-ups** — none open.

## Environment notes

- **Round-049 (session 23)** — branch `049-cli-agent-decoupling` (single PR [#112](https://github.com/gosharplite/tellme/pull/112) → `dev` `d2a01b9`); **merged + deleted (local + remote)** after merge. Host: **darwin/arm64** this session (host/arch not load-bearing; Go toolchain **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`: RULE-E **0 new / 0 stale**, baseline **2 byte-identical**; RULE-A/B/C **0**; **0** cycles; **RULE-F** green + 8/8 named self-tests; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~57 s godog **E2E**) · `Makefile`/`go.mod`/`go.sum` unchanged · **RULE-F** ↔ grep identifier count agree (**1**); witnesses (a)/(b)/(c) reproduced + reverted. **Propagated `dev → main`** (no-ff) at this closeout.
- **Round-047 (session 21)** — env detail is in the archive ([`2026-09-18.md`](docs/archives/status/2026-09-18.md)).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here — a STATUS-level head pin goes born-stale on the next commit: the `1d36509` measured-claim precedent.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
