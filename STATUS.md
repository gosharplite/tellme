# tellme — Status

**Last updated**: 2026-09-23 (session 72 — round **084** `084-rollback-durability-witness` **IN FLIGHT** — anchor issue [#169](https://github.com/gosharplite/tellme/issues/169); the full pipeline ran to a green PR: the `fsync` durability clause becomes falsifiable via a store-level `durableFS` seam + a mechanism-seam pin, the asserted effect is calibrated, and the best-effort directory `fsync` is recorded accepted-unwitnessed (**ADR 0056**). Round 083 remains the latest delivered round.)
**Round in flight**: **084** `084-rollback-durability-witness` — branch `084-rollback-durability-witness` off `dev`; pipeline complete + **review-fold loop CLOSED** (the `architect` peer: `APPROVE WITH REQUIRED FOLDS` → F-084-1/2 folded → `FOLDS VERIFIED WITH RESIDUALS` → R-FV-084-1/2/3 folded → **`FOLDS VERIFIED — LOOP CLOSED`**); **PR [#170](https://github.com/gosharplite/tellme/pull/170) ready for a human to review/merge**.
**Active branch**: `084-rollback-durability-witness` (round 084 in flight; `dev` holds delivered round 083).
**Daily log**: [`docs/session-summary/2026/09/23/session-summary.md`](docs/session-summary/2026/09/23/session-summary.md) · prior: [`2026-09-22`](docs/session-summary/2026/09/22/session-summary.md) (round 081)

## Last delivered round - 083 `083-round-scoped-media-placement` (DELIVERED / FROZEN - PR [#168](https://github.com/gosharplite/tellme/pull/168) merged into `dev` `3436558`, **fast-forward**; **ADR 0055**)

- **Theme (anchor issue [#167](https://github.com/gosharplite/tellme/issues/167), 2026-09-23)**: a turn making **≥2 media-producing tool calls in one round** (e.g. 3 × `read_image`) was broken on the **OpenAI-compatible family** — the loop appended a media `user` message **inside** the per-call loop, so a round with N ≥ 2 media calls emitted `assistant(tool_calls), tool, user(img), tool, user(img), …`. The wire requires the `tool` results to be answered **contiguously**, so the **next** request of the turn was rejected `400` (exit 6; the round-080 failure path then persisted the completed steps). A **single** image worked; the **Gemini/Vertex** adapter buffers media and was correct by construction. No test could redden on the bug (the `GAPS.md` / `aixbdd-tmg#15` *claim-without-a-tripwire* class). The operator directed *"Open a new round, the goal is close #167."* Clarify **not escalated (0 questions)** — the issue locked the behaviour.
- **The change**: `internal/agent/agentloop.go` accumulates the round's media (`roundMedia`) at the call site and folds it **once**, **after** the per-call loop — one `user` message (media-only, call order) after **all** the round's `tool` results, so the block is contiguous. **Single-call output is byte-identical**; the adapters (`openai`/`gemini`) are **unchanged** (Gemini now receives one media message per round ⇒ one media turn of N `inlineData` parts — a recorded cardinality consequence). Placement-only: no `history.jsonl`/`history.Store`/`-b`/prompt/chrome change; the exit-code set stays **ten**. The round **clarifies ADR 0032 D7** (its body was already round-scoped; the pre-083 **implementation** read it per-call).
- **Artifacts**: `specs/plans/083-round-scoped-media-placement/**` (spec · checklist · acceptance `inspecting-several-pictures-in-one-step.feature` · research D1–D9 · plan · tasks T001–T015 + the fold ledger · truth-delta) · **ADR 0055** (`docs/decisions/0055-round-scoped-media-placement.md` + index; **clarifies ADR 0032 D7**, whose `Status` + D7 carry the back-pointer) · `specs/truth/techstack.md` ×3 MODIFY (*Agent tool loop* · *Image content on the provider wire (OpenAI-compatible)* · *… (Gemini/Vertex)*) · `specs/truth/features/cli/chat/reading-a-local-image.feature` (a new media-round Rule + 3 Examples) + `chat/dsl.md` (+ note + 5 rows + the round-063 supersession pointer) · `docs/domain-model/tellme.modelith.{yaml,md}` (the `ImageContent` description + the *Reading a local image* scenario step corrected to the round-scoped fold; re-rendered) · `internal/agent/agentloop.go` + `agentloop_media_test.go` · `internal/infrastructure/llm/gemini/client_image_test.go` (+`TestRequestBody_RoundScopedMedia_OneTurnTwoParts`) · `tests/e2e/steps/wire_tools.go` + `step_r083_media_round.go` · `GAPS.md`.
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **314 scenarios · 2354 steps**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): **W1** (the true pre-fix per-call placement) ⇒ **1** loop-tier pin red (`TestRunRoundScopedMedia_ThreeCalls_FoldedOnceAfterResults`, `8 messages want 6`) + **1** E2E Example red (`313/314`, via the shared `toolExchangeChronologyOK`); **W2** (N trailing media messages) ⇒ the unit pin red + the one-message E2E Then red (`carries 1 picture(s), want 3`).
- **Review chain (PR [#168](https://github.com/gosharplite/tellme/pull/168), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the folds were **record/witness accuracy**: **F-083-1** ADR 0032 had no back-pointer · **F-083-2** ADR 0055 misquoted D7 · **F-083-3** the W1 record overstated the red set · **F-083-4** the domain model *does* narrate the placement · **F-083-5** the D3/SC-004 cardinality had no falsifier · **F-083-6** the contiguity claim had no carrier; + TD-083-1/2/3, N-083-1/2/3) → fold `42e7d6f` → `FOLD-VERIFICATION` (**FOLDS VERIFIED** + residuals R-FV-083-1/2) → residual fold `0ccf4f3` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`**.
- **Delivered**: PR [#168](https://github.com/gosharplite/tellme/pull/168) **human-merged** into `dev` (**fast-forward**, `3436558`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-083`**. **Closes [#167](https://github.com/gosharplite/tellme/issues/167)**; the tracker returns to **0 open**.

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-23.md`](docs/archives/status/2026-09-23.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there; the **round-082 closeout** relocated the **round-081** detail there; **this closeout** relocated the **round-082 delivered-round detail** there (verbatim — never deleted).

**Delivered rounds**: rounds 001–083 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

**Delivered**: rounds 001-083 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: none - the tracker is **0 open**; a new round opens off `dev` from operator value or a live issue (Bootstrap Agent Rule 11).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-083`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-082`** at its propagation merge; **`round-081`** at its propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; counts refreshed 2026-09-23)** — the `axb-gherkin-and-dsl` topology script over `specs/truth/features/cli` reports **6 errors** (53 features · 6 feature modules · 17 root + 455 module rows · ~2350 steps): the **5 pre-existing DSL-matching errors** plus **1** of the already-documented cross-module class (a `chat`-module `a configured provider …` Given used in a `history` feature — the `history/reviewing-the-turn-log.feature:12` class, which rounds 081–083 extended). Rounds 060–083 add **no** new class. The script is **not** a `make verify` member; a carried item, not a delivery gate.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state** (139→53 lines): six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Round-083 (session 71)** — branch `083-round-scoped-media-placement` off `dev` `426a93f`: specify `faae4b1` -> acceptance + research + **ADR 0055** + truth + domain-model + implementation `87473aa` -> STATUS `ad160a0` -> PR [#168](https://github.com/gosharplite/tellme/pull/168) -> fold F-083-1…6 + TD-083-1/2/3 + N-083-1/2/3 `42e7d6f` -> residual fold R-FV-083-1/2 `0ccf4f3` -> STATUS (loop CLOSED) `3436558` -> merge `3436558`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED** + R-FV-083-1/2) -> final **FOLDS VERIFIED — LOOP CLOSED (no residuals)**. **Human-merged** into `dev` (**fast-forward**, `3436558`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-083`. Host: **darwin/arm64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (E2E **314 scenarios · 2354 steps**) - `make verify` **OK** - `make test-race` green - `go.mod`/`go.sum` unchanged.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
