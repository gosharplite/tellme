# tellme — Status

**Last updated**: 2026-09-18 (session 25 — **round 051 opened**: `/axb-specify` ✅ `specs/plans/051-cli-ui-decoupling/` — the **close-#101** round: the last RULE-E residual `internal/cli → internal/ui` (baseline **1 → 0**) + the F-6/F-7/F-8 deferrals; `/axb-clarify` ✅ (**Q1 → D** one round closes #101 · **Q2 → (i)** value types → domain peers · **Q3 → (ii)** narrow lifecycle-grouped ports · **Q4 → (A)** F-8 `OutputSink` interface, F-7 named `Discovery`, F-6 narrow seams) · research ✅ (**ADR 0020**) · system-analysis ✅ · tasks ✅ (T001–T020) · implement ✅ (**baseline 1 → 0**; `make verify` **OK**; F-6/F-7/F-8 resolved). Prior: session 24 — round **050** `050-agentloop-construction-inversion` **DELIVERED / FROZEN** — **R5.4** of [#101](https://github.com/gosharplite/tellme/issues/101) (R5 of [#92](https://github.com/gosharplite/tellme/issues/92)); anchor [#101](https://github.com/gosharplite/tellme/issues/101); the **sub-slice 2** of the `cli → agent` de-coupling: the surviving `AgentLoop` construction/execution inverted into the injected domain port (`agentport.Loop`/`LoopSpec`/`LoopFactory`; adapter `agent.NewLoop`); the CLI's `→ agent` references **1 → 0**; the **RULE-E baseline moves 2 → 1** (the `→ agent` line **and** its RULE-F `couplingSurface` key removed — **two** removals, ADR 0018 fold F-4); the `→ ui` line/surface byte-identical (**19**); behaviour-preserving; **ADR 0019**; PR [#113](https://github.com/gosharplite/tellme/pull/113) human-merged into `dev` `09d0145` (by `gosharplite`). Pipeline: `/axb-specify` ✅ · `/axb-clarify` ✅ (**Q1 → A** port-only · **Q2 → (i)** domain `Loop` interface + `LoopSpec` + func-typed `LoopFactory` · **Q3 → (a)** CLI keeps `Lines`/observer domain-typed · **Q4 → A** CLI tests adopt a fake `agentport.Loop` — RULE-E is merged-graph) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (**ADR 0019** + `techstack.md` MODIFY ×2) · `/axb-system-analysis` ✅ (0 interfaces; api/data/dsl-refine NOOP) · `/axb-tasks` ✅ (T001–T015) · `/axb-implement` ✅ (all `[X]`) · **PR [#113](https://github.com/gosharplite/tellme/pull/113) MERGED**. **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation).
**Active branch**: `051-cli-ui-decoupling` (round 051 in flight — **close [#101](https://github.com/gosharplite/tellme/issues/101)**; **PR [#114](https://github.com/gosharplite/tellme/pull/114) OPEN**; `dev` holds round 050 delivered/frozen)
**Daily log**: [`docs/session-summary/2026/09/18/session-summary.md`](docs/session-summary/2026/09/18/session-summary.md)

## Round in flight — 051 `051-cli-ui-decoupling` (the **close-[#101](https://github.com/gosharplite/tellme/issues/101)** round)

- **Programme goal (operator-declared)**: **close [#101](https://github.com/gosharplite/tellme/issues/101)** — (i) remove the **last** RULE-E residual **`internal/cli → internal/ui`** (baseline **1 → 0**; the RULE-F `couplingSurface` key removal), and (ii) resolve the three PR [#104](https://github.com/gosharplite/tellme/pull/104) deferrals **F-6** (narrow `Dependencies` seams) / **F-7** (a named `Discovery{Tools, Warnings, Closer io.Closer}`) / **F-8** (`domaintools.OutputSink` struct-of-funcs → interface).
- **The `→ ui` edge is NOT edge-sized** (ADR 0017 §Forward): ≈20 call sites · 3 crossing value types (`ui.Pricing`/`ui.UsageCounts`/`ui.ToolUsageRow`) · 4 stateful objects (`Renderer`/`Spinner`/`ToolOutputCoordinator`/`ToolLineRenderer`) · 8 pure formatters — RULE-F surface = **19 identifiers**. A **re-cut** is expected (the ADR 0017 recipe: value types → `internal/domain/**` first, then the formatter/factory inversion).
- **Branch**: `051-cli-ui-decoupling` (off `dev` `310def4`).
- **Pipeline**: `/axb-specify` ✅ · `/axb-clarify` ✅ (Q1 → D · Q2 → (i) · Q3 → (ii) · Q4 → (A)) · `/axb-spec-by-example` **NOOP** · research ✅ (**ADR 0020** + `techstack.md` MODIFY ×2) · system-analysis ✅ · tasks ✅ (T001–T020) · implement ✅ (all `[X]`; **RULE-E baseline 1 → 0** — the terminal state; two removals: the RULE-E line + the RULE-F `→ ui` key) · review `5729783315` → folds **R-51-1…R-51-5** + RF records applied.
- **Implemented (in-branch, `make verify` OK)**: `internal/domain/llm/{Pricing,ComputeCost,HitRate}` · `internal/domain/metrics/UsageCounts` · `internal/domain/history/ToolUsageRow` · `internal/domain/render/ports.go` (`Lines`/`Answer`/`Indicator`/`TurnProgress`/`ProgressFactory`) · `internal/domain/tools/outputsink.go` (**F-8**: interface) · `internal/ui/render_ports.go` (adapters) · `internal/app/deps` (`Discovery` **F-7**, `NewLines`/`NewToolLines`/`NewAnswer`/`NewProgress`) · `internal/cli` (**zero** `internal/ui` refs; **F-6** narrow seams + the `deps_boundary_test.go` audit) · `cmd/tellme` wiring · `tools/arch/{arch_test.go,baseline.txt}` (**1 → 0**).

## Delivered round — 050 `050-agentloop-construction-inversion` (DELIVERED / FROZEN — PR [#113](https://github.com/gosharplite/tellme/pull/113) merged into `dev` `09d0145`; R5.4 of [#101](https://github.com/gosharplite/tellme/issues/101); anchor [#101](https://github.com/gosharplite/tellme/issues/101))

- **Theme**: `#92` **AC2**'s second clause — the **sub-slice 2** (the **baseline-moving** slice) of the residual RULE-E edge **`internal/cli → internal/agent`**. After round 049 the surviving coupling was a **single `&agent.AgentLoop{…}` construction site** (`cli.go:699`), so it is inverted into an **injected domain port** — the port pattern (domain interface + tier-≥4 adapter + composition-root injection) reused from ADR 0017/0018.
- **Locked decisions (Q1–Q4)**: **Q1-A** **port-only** inversion — the `ui` wiring (`Lines` renderer, spinner, tool-output coordinator, composite observer) **stays in `internal/cli`** by design; the `→ ui` edge is the later slice, **not** this round. **Q2-(i)** a domain **interface** `agentport.Loop` (`Run(ctx, prompt, prior) (Result, error)`) fed by a domain **`LoopSpec`** value struct + a **func-typed** `LoopFactory`; adapter **`agent.NewLoop`** in `internal/agent` (tier 4, downward). **Q3-(a)** the CLI supplies `Lines` + the observer as **domain-typed** `LoopSpec` inputs (behaviour-preserving). **Q4-A** **RULE-E evaluates the merged (production + in-process test) import graph**, so the CLI **test** surface must also leave `internal/agent` — the fixture's `LoopFactory` returns an **in-package fake `agentport.Loop`**; the real-loop coverage stays in `internal/agent` unit tests + the godog E2E.
- **⚠️ TWO ratchet removals** (ADR 0018 §Forward, fold **F-4**): the removal reds **twice** — the **RULE-E** baseline line **and** the **RULE-F** `couplingSurface["internal/cli -> internal/agent"]` key. Both were removed deliberately in the same PR; the `internal/cli -> internal/ui` line + its **19**-identifier surface are **byte-identical**.
- **Artifacts**: `internal/domain/agent/loop.go` (NEW — `Loop`/`LoopSpec`/`LoopFactory`; RULE-C-pure) · `internal/domain/agent/loop_test.go` (NEW — the shape/compile pin) · `internal/agent/newloop.go` (NEW — `agent.NewLoop` + `var _ agentport.Loop = (*AgentLoop)(nil)`) · `internal/agent/newloop_test.go` (NEW — the **9-field sentinel parity guard**, fold TD-1) · `internal/app/deps/deps.go` (`LoopFactory` func-typed field) · `cmd/tellme/deps.go` (binds `agent.NewLoop`) · `internal/cli/cli.go` (builds the loop via `dp.LoopFactory(agentport.LoopSpec{…})`; **removes** the `internal/agent` import) + 4 CLI test files (reworked to the in-package `fakeLoop`) · `tools/arch/arch_test.go` (RULE-F `→ agent` key **removed**) + `tools/arch/baseline.txt` (**2 → 1**) · **ADR 0019** (`docs/decisions/0019-agentloop-construction-inversion.md` + index row) · `specs/truth/techstack.md` MODIFY ×2 (Layer-discipline gate row — baseline **2 → 1** + RULE-F key removal; Agent tool loop row — construction home + the witness-owner record) · `specs/plans/050-agentloop-construction-inversion/**`.
- **Verification**: `make verify` **OK** (RULE-A/B/C **0**; **RULE-E baseline 1** — `internal/cli -> internal/ui` only, 0 new / 0 stale; **0** cycles; **RULE-F** `→ ui == 19 ids` (0 new / 0 stale), **no `→ agent` key**; **8/8** named self-tests; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~56 s godog **E2E**; 24 pkgs `ok`, 0 FAIL) · `gofmt`/`go vet ./...` clean · **identifier count 1 → 0** (grep) · falsifiability witnesses (a) compile-level / (b) count-level (RULE-E **new** + RULE-F **coverage**) reproduced + reverted; the fold round added **real witness strength**: (TD-1) the 9/9 per-field rupture reds, (TD-2(i)) the restored `HasSuffix` position pin, (TD-2(ii)) `TestRunTurn_NonFinalTailPrecedesAnswer` red under both the always-defer and the no-`EmitFinalTail` ruptures · `go.mod`/`go.sum`/`Makefile` unchanged.
- **Review chain (PR #113 — review → fold → fold-verification → FINAL CERTIFICATION; all folds verified as behaviour)**: review `5729158742` — **APPROVE WITH NON-BLOCKING FOLDS (no `[ARCHITECTURAL BLOCKER]`; TD-1 a merge-gate item)** → fold **`efc842b`** (**TD-1** the 9-field `NewLoop` sentinel parity test; **TD-2(i)** restore `HasSuffix` + the fixture `Reported:false`; **TD-2(ii)** the `finals []bool` knob + `TestRunTurn_NonFinalTailPrecedesAnswer`; **TD-2(iii)** name the new witness owner; **TD-3** the `Registry`-identity / `BindToolOutput`-before-`Run` invariant; **TD-4** STATUS branch/PR consistency; **RF-1** the `LoopSpec` growth rule + "the loop **struct's** 9 fields" wording; N-1…N-4) → fold verification `5729318163` — **FOLDS VERIFIED** (mutation-tested: 9/9 TD-1 ruptures red; `Contains`→`HasSuffix` position pin genuinely restored; the two tail-wiring ruptures red) **+ follow-up F-1…F-2/N-5** → fold **`c22ce0c`** (**F-1** the wrong-package owner pointer corrected at 3 sites → the accurate layered triple; **F-2** the `spec.md` wording; **N-5** the `samePtr` pointer-only caveat) → `0406c29` (doc-only STATUS fold-chain note) → **FINAL CERTIFICATION** `5729389398` — **MERGE-READY · review loop CLOSED** (head **`0406c29`**, fold head **`c22ce0c`**).
- **Merge + propagation**: PR [#113](https://github.com/gosharplite/tellme/pull/113) **human-merged** into `dev` (`09d0145`, by `gosharplite`); remote + local round branch **deleted** after merge. **Propagated `dev → main` (no-ff)** at this closeout.
- **Forward items**: the **`cli → ui`** edge is now the **last** RULE-E residual (≥ edge-sized — ADR 0017 §Forward; likely a re-cut) — at baseline **0** after it the ratchet has **no release valve** (ADR 0011/0016) · **F-6** (narrow `Dependencies` seams) · **F-7** (a named `Discovery{Tools, Warnings, Closer io.Closer}`) · **F-8** (`domaintools.OutputSink` struct-of-funcs → interface) — all on [#101](https://github.com/gosharplite/tellme/issues/101) · the `Options`/`Dependencies` `Validate()` interface-seam caveat (`Kind()==reflect.Func` blind to interface seams; ADR 0017 §Forward — **latent** this round, the `LoopFactory` field is func-typed) · **RULE-F** metric = distinct **selectors**, not call sites (ADR 0018 N-8); production-only scope (F-3) · `cmd/tellme` single-assembly-site upheld by **review discipline** (round-044 forward item (d)).

**Fold ledger**: round-047 (PR [#110](https://github.com/gosharplite/tellme/pull/110)) `d5ba1ae` → `4c20c98` → `a546c89` · [round-048](https://github.com/gosharplite/tellme/pull/111) (PR [#111](https://github.com/gosharplite/tellme/pull/111)) `e9c9ea2` → `36493a2` · [round-049](https://github.com/gosharplite/tellme/pull/112) (PR [#112](https://github.com/gosharplite/tellme/pull/112)) `1e2e451` → `c3e8e16` → `a348541`+`088ac7d` → `e88a1f9` → `d2a01b9` · **[round-050](https://github.com/gosharplite/tellme/pull/113) (PR [#113](https://github.com/gosharplite/tellme/pull/113))** `2f63459` → **`efc842b`** (TD-1/TD-2(i)(ii)(iii)/TD-3/TD-4/RF-1/N-1…N-4) → **`c22ce0c`** (F-1/F-2/N-5) → head **`0406c29`** (doc-only STATUS) → merged **`09d0145`**; both reviews **verified their folds as behaviour**. *(Convention, TD-6: this line names fold heads + prior ledgers only — a ledger commit cannot name its own SHA.)*
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (rounds 041–049 detail + the 043/044/046/047 branch-model rows + the round-046/047 env notes + **the round-049 detail + its branch-model row + the round-049 env note**, relocated at the round-050 closeout).

> **Split note (Rule 12)**: on this closeout the **round-049 delivered-round detail + its branch-model row** and the **round-049 environment note** were relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 050 is now the most recently delivered round), keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`, plan+truth) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, merge; head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` (single PR) | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, merge; head `d007b75`) |
| 044 | `044-composition-root-extraction` (plan) + `044-implement-composition-root-extraction` (impl) | PR [#102](https://github.com/gosharplite/tellme/pull/102) (`4fd57fc`, plan+truth+grill fold) · PR [#104](https://github.com/gosharplite/tellme/pull/104) (`8da0b88`, merge; head `ba64508`) |
| 045 | `045-yield-policy-owner` (single PR) | PR [#106](https://github.com/gosharplite/tellme/pull/106) (`9e03a91`, merge; head `ab2c7fb`; folds `ef0c7ec`/`ab2c7fb`) |
| 046 | `046-blank-reason-owner-and-presentation-decoupling` (single PR) | PR [#109](https://github.com/gosharplite/tellme/pull/109) (`8ca4758`, merge; folds `8a39fc0`/`91e1e23`/`a96fa20`/`85fa321`) |
| 047 | `047-application-import-ceiling-gate` (single PR) | PR [#110](https://github.com/gosharplite/tellme/pull/110) (`6711e0a`, merge; folds `4c20c98`/`a546c89`; head `d39cfb1`) |
| 048 | `048-cli-tui-prompt-decoupling` (single PR) | PR [#111](https://github.com/gosharplite/tellme/pull/111) (`822e171`, merge; fold `36493a2`) |
| 049 | `049-cli-agent-decoupling` (single PR) | PR [#112](https://github.com/gosharplite/tellme/pull/112) (`d2a01b9`, merge; folds `c3e8e16`/`a348541`/`e88a1f9`; head `d2a01b9`) |
| 050 | `050-agentloop-construction-inversion` (single PR) | PR [#113](https://github.com/gosharplite/tellme/pull/113) (`09d0145`, merge; folds `efc842b`/`c22ce0c`; head `0406c29`) |

Per-round detail lives in the archives; **050 is the most recently delivered round; 051 is in flight** (branch `051-cli-ui-decoupling`, **PR [#114](https://github.com/gosharplite/tellme/pull/114) open**).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (**no-ff**) at this closeout — round 050 propagation **DONE** | Stable / released line |
| `dev` | round 050 **delivered** (`09d0145`, PR [#113](https://github.com/gosharplite/tellme/pull/113)) | Integration line (round work lands here before `main`) |
| `051-cli-ui-decoupling` | **in flight** — round 051 (the **close-[#101](https://github.com/gosharplite/tellme/issues/101)** round): the last RULE-E residual `internal/cli → internal/ui` → **baseline 1 → 0** + F-6/F-7/F-8; **PR [#114](https://github.com/gosharplite/tellme/pull/114) OPEN** | Round 051's branch (off `dev` `310def4`) |
| `050-agentloop-construction-inversion` | **merged into `dev`** (PR [#113](https://github.com/gosharplite/tellme/pull/113) → `09d0145`); **branch deleted (local + remote)** after merge | Round 050's single branch — the **sub-slice 2** (**R5.4** of [#101](https://github.com/gosharplite/tellme/issues/101)): the `AgentLoop` construction/execution → the injected domain port (`agentport.Loop`/`LoopSpec`/`LoopFactory`; adapter `agent.NewLoop`); CLI `→ agent` refs **1 → 0**; baseline **2 → 1** (two removals); **ADR 0019**. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–048 — **DONE (no-ff)** (round 043 `4ce056c`; round 044 `ebaeebc`; round 045 `ad407cf`; round 046 — DONE; round 047 `e47dbc4`; round 048 — DONE); **round 049 — DONE (no-ff)** · **round 050 — DONE (no-ff)** at this closeout. (The round-043/044/046/047 branch-model rows are archived in [`2026-09-18.md`](docs/archives/status/2026-09-18.md).)
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–041** | — | Provider-registry completeness → … → resolver test load tolerance. | ✅ **Delivered** (see the archives) |
| **042–049** | [#93](https://github.com/gosharplite/tellme/issues/93) · [#96](https://github.com/gosharplite/tellme/issues/96) · [#100](https://github.com/gosharplite/tellme/issues/100) · [#105](https://github.com/gosharplite/tellme/issues/105) · [#108](https://github.com/gosharplite/tellme/issues/108) · [#101](https://github.com/gosharplite/tellme/issues/101) | layer-discipline gate → hermetic make env → composition root → yield-policy owner → blank-reason owner → the R5 import-ceiling programme (R5.1 gate, R5.2 `→ ui/tui/prompt`, R5.3 sub-slice 1 contracts). | ✅ **Delivered** — see the archives / the round index |
| **050** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.4** — the **sub-slice 2** of the `cli → agent` de-coupling: the `AgentLoop` construction/execution → the injected domain port (`agentport.Loop`/`LoopSpec`/`LoopFactory`; adapter `agent.NewLoop`); CLI `→ agent` refs **1 → 0**; baseline **2 → 1** (**two** removals); **ADR 0019**. | ✅ **Delivered** — PR [#113](https://github.com/gosharplite/tellme/pull/113) merged into `dev` (`09d0145`); baseline RULE-E **1** |
| **future slices** | [#101](https://github.com/gosharplite/tellme/issues/101) (the **`cli → ui`** edge — the **last** RULE-E residual · **F-6/F-7/F-8**) · [#92](https://github.com/gosharplite/tellme/issues/92) (ride-alongs: suggestion-selection policy; `BindToolOutput` ctor injection) · [#91](https://github.com/gosharplite/tellme/issues/91) · [#13](https://github.com/gosharplite/tellme/issues/13) · [#103](https://github.com/gosharplite/tellme/issues/103) | The **`cli → ui`** slice (**not edge-sized** — ≈20 call sites, 3 crossing value types, 4 stateful objects, 8 formatters; ADR 0017 §Forward — likely a **re-cut**: value types → `internal/domain/**` first, then the renderer factory; at baseline **0** after it the ratchet has **no release valve**); the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)); the `-l`/`-c` + `-t` plumbing gap ([#103](https://github.com/gosharplite/tellme/issues/103)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 24 closeout)** — **[#101](https://github.com/gosharplite/tellme/issues/101) OPEN** — R5 is a **programme**: **R5.1 DELIVERED** (round 047) · **R5.2 DELIVERED** (round 048) · **R5.3 sub-slice 1 DELIVERED** (round 049, PR [#112](https://github.com/gosharplite/tellme/pull/112) merged `d2a01b9`) · **R5.4 sub-slice 2 DELIVERED** (round 050, PR [#113](https://github.com/gosharplite/tellme/pull/113) merged `09d0145`); the **`cli → agent` edge is GONE**; remaining: the **`cli → ui`** edge + **F-6/F-7/F-8**; the body carries the delivery record (revised this closeout). **[#92](https://github.com/gosharplite/tellme/issues/92) OPEN** — R1–R5 delivered (via #93/#96/#100/#105/#108/#101); ride-alongs remain. **[#103](https://github.com/gosharplite/tellme/issues/103)** · **[#91](https://github.com/gosharplite/tellme/issues/91)** · **[#13](https://github.com/gosharplite/tellme/issues/13)** — all OPEN (accurate). **[#93](https://github.com/gosharplite/tellme/issues/93)/[#96](https://github.com/gosharplite/tellme/issues/96)/[#100](https://github.com/gosharplite/tellme/issues/100)/[#105](https://github.com/gosharplite/tellme/issues/105)/[#107](https://github.com/gosharplite/tellme/issues/107)/[#108](https://github.com/gosharplite/tellme/issues/108) CLOSED** (earlier). Round 050 delivered a slice of an already-open programme → **no closes; #101 body revised**.
- **Round-050 forward items (delivered)** — (a) the **`cli → ui`** edge is the **last** RULE-E residual (≥ edge-sized; ADR 0017 §Forward — likely a re-cut); at baseline **0** after it the ratchet has **no release valve** (ADR 0011/0016) · (b) **F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101) · (c) the `Options`/`Dependencies` `Validate()` interface-seam caveat (ADR 0017 §Forward — **latent**; the `LoopFactory` field is func-typed) · (d) **RULE-F** metric = distinct **selectors**, not call sites (ADR 0018 N-8); production-only scope (F-3) · (e) `cmd/tellme` single-assembly-site upheld by **review discipline** only (round-044 forward item (d)).
- **Round-049 forward items (delivered)** — (a) **sub-slice 2** ✅ delivered by round 050; (b) the R-1 cross-slice coupling ✅ resolved by **Q1 → A**; (c) the `Validate()` interface-seam caveat → **latent** (func-typed field); (d) **RULE-F** metric/scope notes; (e) the `cli → ui` edge (still the last residual); (f) **F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101).
- **Earlier-round forward items (delivered)** — see [`2026-09-18.md`](docs/archives/status/2026-09-18.md).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**; round-047 items (b)–(d) (third-party application-tier imports outside RULE-E; the sanctioned set re-ruling; custom build-tag imports out of scope).
- **PM follow-ups** — none open.

## Environment notes

- **Round-050 (session 24)** — branch `050-agentloop-construction-inversion` (single PR [#113](https://github.com/gosharplite/tellme/pull/113) → `dev` `09d0145`); **merged + deleted (local + remote)** after merge. Host: **darwin/arm64** this session (host/arch not load-bearing; Go toolchain **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`: RULE-E **0 new / 0 stale**, baseline **1**; RULE-A/B/C **0**; **0** cycles; **RULE-F** `→ ui = 19` (0 new / 0 stale), no `→ agent` key, 8/8 named self-tests; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~56 s godog **E2E**; 24 pkgs `ok`) · `Makefile`/`go.mod`/`go.sum` unchanged · CLI `→ agent` refs = **0**; witnesses (a)/(b) reproduced + reverted. **Propagated `dev → main`** (no-ff) at this closeout.
- **Round-049 (session 23)** — env detail is in the archive ([`2026-09-18.md`](docs/archives/status/2026-09-18.md)).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here — a STATUS-level head pin goes born-stale on the next commit: the `1d36509` measured-claim precedent.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
