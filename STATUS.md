# tellme — Status

**Last updated**: 2026-09-23 (session 70 — round **082** `082-listing-backward-turn-indices` **review-fold loop CLOSED; PR [#166](https://github.com/gosharplite/tellme/pull/166) ready for human merge**. `architect` peer (`SESSION-BOOTSTRAP.md`-initialized once) → **APPROVE WITH REQUIRED FOLDS** (head `76474d9`) → fold `78bcde2` (F-082-1…3 + N-082-1/2) → **FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**. No Copilot review; only a human merges.)
**Round in flight**: **082** `082-listing-backward-turn-indices` — anchor issue [#165](https://github.com/gosharplite/tellme/issues/165); **PR [#166](https://github.com/gosharplite/tellme/pull/166) OPEN, review loop CLOSED** (fold head `78bcde2`; awaiting a human review/merge). Branch `082-listing-backward-turn-indices` off `dev` `7bb6c0e`.
**Active branch**: `082-listing-backward-turn-indices` (off `dev` `7bb6c0e`; round branch). `dev` holds round 081 delivered/frozen (PR [#164](https://github.com/gosharplite/tellme/pull/164) merged `f9aa96f`, **merge commit**; propagation `dev -> main` **DONE (no-ff)**, tagged `round-081`) + the **2026-09-23 hygiene commits** `e44af79`/`7bb6c0e` (propagated no-ff; docs-only — no `round-NNN` tag).
**Daily log**: [`docs/session-summary/2026/09/23/session-summary.md`](docs/session-summary/2026/09/23/session-summary.md) · prior: [`2026-09-22`](docs/session-summary/2026/09/22/session-summary.md) (round 081)

## Last delivered round - 081 `081-back-rollback-turns` (DELIVERED / FROZEN - PR [#164](https://github.com/gosharplite/tellme/pull/164) merged into `dev` `f9aa96f`, **merge commit**; **ADR 0053**)

- **Theme (anchor issue [#163](https://github.com/gosharplite/tellme/issues/163), 2026-09-22)**: tellme shipped **no way to undo a turn**; the reference's `-b`/`--back [N]` (*"go back / delete the last N turns"*, `NoOptDefVal = "1"`) was rejected (`tellme -b` → usage error, exit 2). The operator created a detailed issue and directed *"Open a new round, the goal is to close #163."* Clarify **not escalated (0 questions)** — the core behaviour is anchored by the issue; the *Open design decisions* were routed to `/axb-technical-research`.
- **The change**: **a new offline session command + a durable store capability.** `internal/domain/history.Store` gains `Rollback(n int) (removed int, err error)`; `internal/infrastructure/history/file_store.go` implements it **durably** — the survivors are copied **raw** (a new `rawNonEmptyLines` + `writeRaw`) to a same-dir temp file, `fsync`ed, then `os.Rename`d over `history.jsonl` (never truncated in place), with `removed = min(n, len)` (**clamp**), `n ≤ 0` a no-op, a missing file ⇒ 0, a decode failure ⇒ an error (never a partial write), and the **archive never touched**. `internal/cli/cli.go` registers **`-b`/`--back [N]`** (`NoOptDefVal = "1"`), **generalises** the round-054 `-l` optional-int argv pre-pass into `consumeOptionalIntArgs` (resolving **ADR 0023 §Forward RF-54-4**), adds `renderRollback` (reusing `resolveWorkspace`), runs `-b` in the offline dispatch **after `-l`** (list-then-rollback) and **before `-t`**, and rolls back in `renderTurn` (via `turnOptions.rollbackTurns`) just before the prompt-bearing turn. `-b 0`/`-b -1` and `-b` x `--new` are **usage errors** (exit 2). The confirmation is one plain `stdout` line (`Rolled back N turns. History now holds M turn(s).`, no `tellme: ` prefix); a failure reuses the environment phrase (exit 4). `history.Entry` / `history.Step` JSON shapes are **unchanged**.
- **Artifacts**: `specs/plans/081-back-rollback-turns/**` (spec · checklist · acceptance `rolling-back-the-session-history.feature` · research D1–D9 · plan · tasks T001–T015 + the fold ledger · truth-delta) · **ADR 0053** (`docs/decisions/0053-back-rollback-turns.md` + index; **resolves ADR 0023 RF-54-4**) · `specs/truth/techstack.md` *CLI flag parsing* / *Session history store* / *Session lifecycle flags* / *Prompt input* MODIFY · `specs/truth/features/cli/history/rolling-back-the-session-history.feature` (**NEW**, 7 Rules) + `history/dsl.md` (+1 Given / +5 When / +6 Then rows + the round-081 note) + the root `cli/dsl.md` offline-path scope · `docs/domain-model/tellme.modelith.{yaml,md}` — the `History` rollback action (`history-rollback-removes-complete-turns`) + `Session` offline invariant (`session-rollback-stays-offline`) (re-rendered; `modelith-check` green) · `internal/domain/history/history.go` · `internal/infrastructure/history/file_store.go` + `file_store_rollback_test.go` · `internal/cli/cli.go` + `rollback_test.go` + `dispatch_test.go` + the `fakeStore.Rollback` double · `tests/e2e/steps/step_r081_rollback.go`.
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (24 pkgs; E2E **307 scenarios · 2297 steps**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · topology audit **the same 5 pre-existing + 1** of the documented cross-module class (a `chat`-module provider Given in a `history` feature; the audit is a carried check, not a `make verify` member) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): an **in-place truncate** of `writeRaw` ⇒ `TestFileStore_Rollback_FailureLeavesPriorHistoryIntact` reddens (the durability witness, verified by both me and the reviewer); deleting the `-l`×`-b` compose branch ⇒ the `TestDispatchReportingBack` subtest reddens; a build-then-rollback regression ⇒ the E2E `the provider was asked against the trimmed history` reddens.
- **Review chain (PR [#164](https://github.com/gosharplite/tellme/pull/164), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the design was right, the folds were **evidence/record quality**: **F-081-1** the durability/atomicity claim had no discriminating witness (an in-place truncate left the suite green) · **F-081-2** the `-l`×`-b` compose was unwitnessed · **F-081-3** the decode-failure claim had no carrier · **F-081-4** SC-003's request-content evidence was absent · **F-081-5** the `techstack.md` *Prompt input* precedence row was stale; + TD-081-1/2/3, N-081-1…6) → fold `7626c62` → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; R-FV-081-1…4 nits) → residual fold `efc31d6` (doc/comment-only; product code byte-identical to the verified head).
- **Delivered**: PR [#164](https://github.com/gosharplite/tellme/pull/164) **human-merged** into `dev` (`f9aa96f`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-081`**. **Closes [#163](https://github.com/gosharplite/tellme/issues/163)**; the tracker returns to **0 open**.

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-23.md`](docs/archives/status/2026-09-23.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there. History is relocated **verbatim** — never deleted.

**Delivered rounds**: rounds 001–081 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

**Delivered**: rounds 001-081 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: the tracker is **1 open** — [#165](https://github.com/gosharplite/tellme/issues/165) (round **082**'s anchor, in flight); a new round opens off `dev` from operator value or a live issue (Bootstrap Agent Rule 11).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-081`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; counts refreshed 2026-09-22)** — the `axb-gherkin-and-dsl` topology script over `specs/truth/features/cli` reports **6 errors** (53 features · 6 feature modules · 17 root + 454 module rows · 2271 steps): the **5 pre-existing DSL-matching errors** plus **1** of the already-documented cross-module class (a `chat`-module `a configured provider …` Given used in a `history` feature — the `history/reviewing-the-turn-log.feature:12` class, which round 081 extended). Rounds 060–081 add **no** new class. The script is **not** a `make verify` member; a carried item, not a delivery gate.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state** (139→53 lines): six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Round-081 (session 68)** — branch `081-back-rollback-turns` off `dev` `0008fd0`: specify `bcf2a27` -> acceptance + research + **ADR 0053** + truth + domain-model + implementation `45f43e7` -> PR [#164](https://github.com/gosharplite/tellme/pull/164) -> fold F-081-1…5 + TD-1/2/3 + N-1…6 `7626c62` -> residual fold R-FV-081-1/2/3 `efc31d6` -> STATUS (loop CLOSED) `47a5fe4` -> merge `f9aa96f`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`f9aa96f`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-081`. Host: **darwin/arm64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E **307 scenarios · 2297 steps**) - `make verify` **OK** - `make test-race` green - topology audit **the same 5 pre-existing + 1** documented cross-module class (53 features - 454 module rows - 2271 steps) - `go.mod`/`go.sum` unchanged.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
