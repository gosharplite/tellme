# tellme — Status

**Last updated**: 2026-09-22 (session 66 — round **080** `080-failed-turn-partial-persistence` **IN FLIGHT** (anchor issue [#161](https://github.com/gosharplite/tellme/issues/161); clarify **not escalated — 0 questions**; `/axb-specify` DONE); session 65 — round **079** `079-interrupted-turn-partial-persistence` **DELIVERED / FROZEN**: PR [#160](https://github.com/gosharplite/tellme/pull/160) **human-merged** into `dev` (`30fb420`, **merge commit**); the `architect` peer's review-fold loop CLOSED (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**); propagation `dev -> main` **no-ff**, tagged **`round-079`**; **closes [#159](https://github.com/gosharplite/tellme/issues/159)**): preserve the **completed tool steps** of an operator-interrupted turn (`SIGINT`/`SIGTERM` → `context.Canceled`) by persisting ONE `history.Entry` closed with the synthetic `history.InterruptedTurnAnswer` (`[Turn interrupted by operator via Ctrl+C]`), so the stored history replays as a valid `user … assistant` sequence on **both** provider families (a partial turn ending on a `tool` result would break role alternation); a turn interrupted with **zero** completed steps writes **nothing** (today's clean abort: frozen phrase + exit 6); detection is **typed** (`errors.Is(err, context.Canceled)` — never string-matched); exit **0** + one informational `stderr` line (no `tellme: ` prefix, not in `turns.log`); `calls` = the completed rounds, **no** usage persisted; the in-flight child `SIGKILL` untouched; **ADR 0051**; anchor issue [#159](https://github.com/gosharplite/tellme/issues/159); clarify **not escalated (0 questions)**; a **recorded divergence** (the reference discards the partial turn).
**Round in flight**: **round 080** `080-failed-turn-partial-persistence` (opened off `dev` `b1c929e`; anchor issue [#161](https://github.com/gosharplite/tellme/issues/161) — **persist a failed turn's completed tool steps** (provider failure / retry exhaustion), keeping the failure surface (phrase + exit 6/7) unchanged; **DoD = close #161**; clarify **not escalated — 0 questions**; pipeline: **`/axb-specify` DONE** → next `/axb-spec-by-example` + `/axb-technical-research`; the live state below still reflects the last delivery — round 079).
**Active branch**: `080-failed-turn-partial-persistence` (round 080 work branch off `dev` `b1c929e`; the prior state was `dev` — round 079 delivered via PR [#160](https://github.com/gosharplite/tellme/pull/160) merged `30fb420`, **merge commit**; propagation `dev -> main` **DONE (no-ff)**, tagged `round-079`; round branch deleted local + remote; the closeout docs sit on top of the delivery anchor).
**Daily log**: [`docs/session-summary/2026/09/22/session-summary.md`](docs/session-summary/2026/09/22/session-summary.md) (round 079 §42 appended)

## Last delivered round - 079 `079-interrupted-turn-partial-persistence` (DELIVERED / FROZEN - PR [#160](https://github.com/gosharplite/tellme/pull/160) merged into `dev` `30fb420`, **merge commit**; **ADR 0051**)

- **Theme (anchor issue [#159](https://github.com/gosharplite/tellme/issues/159), 2026-09-22)**: a `Ctrl+C` (`SIGINT`) during a multi-round tool loop returned `context.Canceled`, which `runTurn` mapped to `the provider request failed` + **exit 6** and **skipped the append** — so the prompt and **every completed tool step** were discarded from `history.jsonl` (the `history-append-after-complete` invariant). The round keeps the work. Clarify **not escalated (0 questions)** — the core behaviour is anchored by the issue, and the issue's *Open Design Decisions* were routed by the operator to `/axb-technical-research`.
- **The change**: **typed detection + a synthetic close at the CLI turn seam.** `internal/domain/history` gains the stored constant `history.InterruptedTurnAnswer` (`[Turn interrupted by operator via Ctrl+C]`). In `internal/cli/cli.go` (`runTurn`), `errors.Is(err, context.Canceled) && len(result.Steps) > 0` ⇒ `persistInterruptedTurn` appends **one** `history.Entry` (`{prompt, answer: <synthetic>, calls: len(result.Calls), steps}`) — closing the partial turn with an assistant message so the stored history replays as a valid `user … assistant` sequence (a partial turn ending on a `tool` result would break role alternation on **both** families: Gemini/Vertex rides `functionResponse` under `role:"user"` and rejects two consecutive `user` roles) — returns **`Success` (0)** with `stdout` **empty**, and emits one informational `stderr` line `[HH:MM:SS] interrupted by operator; kept N completed tool step(s) in the session history` (chrome-styled, control-free, **no** `tellme: ` class prefix, **not** in `turns.log` — the round-078 retry-line precedent). Detection is **typed** (`errors.Is`), never string-matched, and unwraps through the adapter's `*llm.ProviderError` + the round-078 retry decorator. A **zero-step** interruption writes **nothing** (today's clean abort: frozen phrase + exit 6); a failed partial-save append degrades to the existing environment phrase (exit 4). **No** usage is persisted (a failed turn writes none today). The `history.Entry` / `history.Step` JSON shapes are **unchanged**.
- **Artifacts**: `specs/plans/079-interrupted-turn-partial-persistence/**` (spec · checklist · acceptance `preserving-an-interrupted-turns-completed-work.feature` · research D1–D10 · plan · tasks T001–T010 + the fold ledger · truth-delta) · **ADR 0051** (`docs/decisions/0051-interrupted-turn-partial-persistence.md` + index) · `specs/truth/techstack.md` *Session history store* **MODIFY** (append-after-complete qualified) + *Interrupted-turn persistence* **ADD** · `docs/domain-model/tellme.modelith.{yaml,md}` — `history-append-after-complete` + `turn-answer-stored-verbatim` **MODIFY** (the operator-interrupted exception; re-rendered; `modelith-check` green) · `specs/truth/features/cli/chat/remembering-the-conversation.feature` (2 new Rules / 2 Examples) · `chat/dsl.md` (+2 Given / +6 Then rows + the round-079 note) · `internal/domain/history/history.go` · `internal/cli/cli.go` · `internal/cli/interrupted_turn_test.go` · `tests/e2e/steps/step_r079_interrupt.go`.
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **295 scenarios · 2201 steps**) · `make check` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (52 features · 6 feature modules · 17 root + 432 module rows · 2175 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): remove the interrupted-turn branch ⇒ the E2E `A turn interrupted after a tool step keeps it` reddens (proving the **typed** detection on the real `SIGINT` path); set `Answer: ""` ⇒ the synthetic-close Then reddens; `Calls: 0` ⇒ the persisted-`calls` Then reddens; leak the class phrase ⇒ the phrase-absence Then reddens; write to `stdout` ⇒ `the run wrote nothing to standard output` reddens. The **live** interruption is produced hermetically by a scripted `execute_command` running `kill -INT $PPID; sleep 30` — a **real** `SIGINT` to the turn process (no pty, no live network).
- **Review chain (PR [#160](https://github.com/gosharplite/tellme/pull/160), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the design was right, the folds were **record / witness quality**: **F-079-1** the hermetic-seam record described a stall mechanism that does not exist (3 surfaces) · **F-079-2** the persisted-`calls` claim had no E2E carrier · **F-079-3** the kill-path result is a nil-error `Exit Code: -1`, not an `error: …` string · **F-079-4** the interrupted path leaves a dangling `╭─⠿ Turn N` frame (N-3 overclaimed); + TD-079-1/2, N-079-1/2) → fold `3e6564c` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; RES-079-FV-1…5) → residual fold `936125a` → `FOLD-VERIFICATION` (**FOLDS VERIFIED — loop CLOSED, cleared for human merge**; the post-closure cosmetic table fix `4be021c` keeps the product code byte-identical).
- **Delivered**: PR [#160](https://github.com/gosharplite/tellme/pull/160) **human-merged** into `dev` (`30fb420`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-079`**. **Closes [#159](https://github.com/gosharplite/tellme/issues/159)**; the tracker returns to **0 open**.

**Fold ledger**: round-047 (PR [#110](https://github.com/gosharplite/tellme/pull/110)) `d5ba1ae` → `4c20c98` → `a546c89` · round-048 (PR [#111](https://github.com/gosharplite/tellme/pull/111)) `e9c9ea2` → `36493a2` · round-049 (PR [#112](https://github.com/gosharplite/tellme/pull/112)) `1e2e451` → `c3e8e16` → `a348541`+`088ac7d` → `e88a1f9` → `d2a01b9` · round-050 (PR [#113](https://github.com/gosharplite/tellme/pull/113)) `2f63459` → `efc842b` → `c22ce0c` → `0406c29` → `09d0145` · **[round-051](https://github.com/gosharplite/tellme/pull/114)** `6e03c85` → **`3cae5d4`** → **`7b24a47`** → **`e7d035d`** → merged **`0d7566b`** · **[round-052](https://github.com/gosharplite/tellme/pull/117)** `76502f4` → `d1f5478` → `bfda279` → `1f1c1de` → **`56c7167`** → **`dc0fa25`** → **`9f2a688`** → merged **`f204aaa`** · **[round-053](https://github.com/gosharplite/tellme/pull/118)** `3356641` → `6b44e00` → `8c7bbd9` → `4e69377` → `6211549` → `43dc81d` → `600656c` → **`0265098`** → merged **`8c100e5`** · **[round-054](https://github.com/gosharplite/tellme/pull/119)** `1bbf0d6` → `174f10e` → **`74e0eb2`** → **`c1d248a`** → merged **`c1d248a`** (ff) · **[round-055](https://github.com/gosharplite/tellme/pull/120)** `42f942a` → **`da5f971`** → **`1561281`** → `a28285e` → merged **`0531a8f`** · **[round-056](https://github.com/gosharplite/tellme/pull/122)** `368b504` → `77a8115` → `db31d8b` → merged **`db31d8b`** (ff) · **[round-057](https://github.com/gosharplite/tellme/pull/123)** `97c1c34` → `26423af` → `6883e62` → `e41687f` → merged **`1ac41f7`** · **[round-058](https://github.com/gosharplite/tellme/pull/124)** `14dc166` → `2be386b` → `0e0844a` → merged **`0e0844a`** (ff) · **[round-059](https://github.com/gosharplite/tellme/pull/125)** `e41d864` → `968a437` → merged **`10e0fa9`** · **[round-060](https://github.com/gosharplite/tellme/pull/126)** `f22727e` → `2f59f91` → `0d63674` → `50e1ed7` → merged **`801b905`** (ff) · **[round-061](https://github.com/gosharplite/tellme/pull/128)** `6dd4701` → `4c8d290`/`81f2b17`/`9e6ede6`/`abd8548` → `ff8b161` → `5bb2583` → `c3eb45e` → `203dc56` → `c8ffa73` → `197df13` → `e67954c` → `b81b6c9` → `ef8b66d` → `faaa29f` → `d6b2786` → `872c69d` → merged **`902642a`**. · **[round-062](https://github.com/gosharplite/tellme/pull/129)** `d3b0024` → `e4d817b` → `03914f0` → `021fbed` → `7f7a297` → `b5ee70d` → `744e10d` → `0916560` → `f2a67d3` → `dadf808` → `cff2515` → merged **`cff2515`** (ff). · **[round-063](https://github.com/gosharplite/tellme/pull/130)** `25e994c` → `41cb71d` → `2888abd` → `5cda9a4` → `966ec83` → `1dd7b92` → `e8e0880` → `1b70ce8` → `4b0106d` → `c4ff73b` → merged **`6932d3b`**. · **[round-064](https://github.com/gosharplite/tellme/pull/131)** `e45b112` → `1306d07` → `5a02f72` → `3e5c6b5` → `6fc1ba9` → `a5b4957` → `c8fd54a` → `1d1be61` → `7c917b5` → `c39ad21` → `63aaa62` → merged **`63aaa62`** (ff). · **[round-065](https://github.com/gosharplite/tellme/pull/133)** `f96e8d8` → `4c2b78b` → `e7903d5` → PR open → `6ec36a8` → `35a662c` → `588a2c5` → **final fold verification `5745449629`** (loop CLOSED) → merged **`b904281`**. *(Convention, TD-6: this line names fold heads + prior ledgers only — a ledger commit cannot name its own SHA.)* · **[round-066](https://github.com/gosharplite/tellme/pull/135)** `1a9edba` → `8de2ecf` → `8fc3090` → PR open → `a9d8080` → `45239e5` → `17dc8a8` → `92d86e2` → merged **`e4410e4`** (ff). · **[round-067](https://github.com/gosharplite/tellme/pull/137)** `6baf0a6` → `8c4fa39` → `b7db897` → `537f777` → `c673ae3` → `82b4c64` (merged ff). · **[round-068](https://github.com/gosharplite/tellme/pull/138)** `0bf9dc1` → `ac8fc1d` → `7c6ca7d` → PR open → `9a8986b` → `55fab02` → `2210b4d` (merged merge-commit). · **[round-069](https://github.com/gosharplite/tellme/pull/141)** `5fcb4f0` → `62ca668` → PR open → `e46bb31` → `cd14519` → merged **`cd14519`** (ff). · **[round-070](https://github.com/gosharplite/tellme/pull/143)** `8a213eb` (specify) → `1b3484d` (research + ADR 0040 + implementation) → PR open → `8aa6174` (N1 + N2 + N4) → `da53b74` (merge commit). · **[round-071](https://github.com/gosharplite/tellme/pull/148)** `c01b3df` (specify) → `1ac1b33` (clarify fold + acceptance + research) → `18f319d` (implementation) → PR open → `85cac23` (fold A1: TD-071-1…4 + nits) → `65e7df8` (residuals RES-1…6) → `9932185` (RES-2 + RES-5a — the silent no-op fix) → `802acc0` (RES-2b) → `b87ba4c` (STATUS + summary; the merge). · **[round-072](https://github.com/gosharplite/tellme/pull/150)** `d884f38` (specify) → `abbad9c` (acceptance + research + ADR 0044) → `3a2aaed` (implementation) → PR open → `8be6ad6` (fold B1: TD-072-1…3 + nits) → `0ea433c` (residuals RES-B1/B2/B3) → `c8a671f` (final editorial) → `232cf0e` (STATUS + summary; the merge). · **[round-073](https://github.com/gosharplite/tellme/pull/151)** `9e254a3` (specify) → `69795c5` (clarify Q1→A/Q2→A/Q3→B) → `5ec4486` (acceptance) → `db9e683` (research + ADR 0045 + truth) → `a4b4070` (plan + DSL truth + tasks) → `c23f3af` (implementation) → PR open → `bd12598`/`2b5483b` (records) → `8916807` (fold: F-1/F-2/F-3 + TD-4…TD-7, RF-8/RF-9, N-1, N-3) → `31caa39` (residual homing) → `d90a79e` (the merge; PR #151). · **[round-074](https://github.com/gosharplite/tellme/pull/152)** `706e27c` (specify) → `8643ff7` (clarify Q1→A/Q2→A) → `65eee7c` (acceptance + research + ADR 0046 + truth + implementation) → PR open → `5a310b0` (STATUS) → `1185292` (fold: F-1/F-2 + TD-3/4/5, N-1…N-3, RF-2) → `75e794e` (residual homing) → `fd204f4` (the merge; PR #152). · **[round-075](https://github.com/gosharplite/tellme/pull/153)** `a61ec45` (specify) → `b2df835` (acceptance + research + ADR 0047 + truth + implementation) → PR open → `7fb0fa1` (fold F-1/F-2/F-3 + N-1; RF-075-4…7 homed) → `8126470` (residual homing) → `250221d` (the merge; PR #153). · **[round-076](https://github.com/gosharplite/tellme/pull/156)** `5454b8a` (specify) → `c56bfd0` (acceptance + research + ADR 0048 + truth + implementation) → PR open → `19a73bd` (fold F-1…F-6 + TDs/nits) → `4fb14a1` (residual fold R1/R2/R3) → `851466e` (the merge; PR #156). · **[round-077](https://github.com/gosharplite/tellme/pull/157)** `2178efa` (specify) → `999f51f` (research + ADR 0049 + implementation) → PR open → `24be271` (fold F-1…F-4 + TD-1 + nits) → `a37e838` (residual fold R-1/R-2) → `273b5a9` (the merge; PR #157). · **[round-078](https://github.com/gosharplite/tellme/pull/158)** `489c4b7` (specify) → `b098058` (acceptance) → `77b835b` (research + ADR 0050 + truth) → `d61935e` (implementation) → PR open → `9ac7e6a` (fold F-1…F-4 + TD-1…3 + RF/N) → `657ae2a` (residual fold R-1…R-3) → `c291e1d` (the merge; PR #158). · **[round-079](https://github.com/gosharplite/tellme/pull/160)** `eb27570` (specify) → `a68d06d` (research + **ADR 0051** + techstack truth + domain-model amendment) → `beb3670` (acceptance + plan + dsl-refine truth + tasks + implementation) → PR open `48a92ef` → `3e6564c` (fold F-079-1…4 + TD-1/2 + N-1/2) → `936125a` (residual fold RES-079-FV-1…5) → `4be021c` (cosmetic table fix) → `30fb420` (the merge; PR #160).
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (rounds 041–052) · [`2026-09-19.md`](docs/archives/status/2026-09-19.md) (rounds 052–061) · [`2026-09-20.md`](docs/archives/status/2026-09-20.md) (rounds 063–069 detail + their branch-model rows + env notes) · a **round-070** relocation (its delivered-round detail + branch-model row + env note) · a **round-071** relocation (its delivered-round detail + env note) · [`2026-09-21.md`](docs/archives/status/2026-09-21.md) (a **round-072** relocation + a **round-073** relocation + a **round-074** relocation: each one's delivered-round detail + env note) · [`2026-09-22.md`](docs/archives/status/2026-09-22.md) (a **round-075** relocation + a **round-076** relocation + a **round-077** relocation + a **round-078** relocation: each one's delivered-round detail + env note).

> **Split note (Rule 12)**: on **this** closeout the **round-078 delivered-round detail + its env note** were relocated **verbatim** into [`2026-09-22.md`](docs/archives/status/2026-09-22.md) (round 079 is now the current delivered round), keeping `STATUS.md` to the live state + a single delivered-round section; the **round-077 forward-item batch was compacted to a pointer row** to its `ADR §Forward` (the authority) per the curation rule.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–041 | `001-*` … `041-di-resolver-test-load-tolerance` | see the archives + the per-round PRs (035 #73 · 036 #75 · 037 #77 · 038 #79 `a0b0653` · 039 #81 `d48ebbc` · 040 #84/#85/#86 head `436bd70` · 041 #88 `ca792d7`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, head `d007b75`) |
| 044 | `044-composition-root-extraction` + `044-implement-…` | PR [#102](https://github.com/gosharplite/tellme/pull/102) (`4fd57fc`) · PR [#104](https://github.com/gosharplite/tellme/pull/104) (`8da0b88`, head `ba64508`) |
| 045 | `045-yield-policy-owner` | PR [#106](https://github.com/gosharplite/tellme/pull/106) (`9e03a91`, head `ab2c7fb`) |
| 046 | `046-blank-reason-owner-and-presentation-decoupling` | PR [#109](https://github.com/gosharplite/tellme/pull/109) (`8ca4758`) |
| 047 | `047-application-import-ceiling-gate` | PR [#110](https://github.com/gosharplite/tellme/pull/110) (`6711e0a`) |
| 048 | `048-cli-tui-prompt-decoupling` | PR [#111](https://github.com/gosharplite/tellme/pull/111) (`822e171`) |
| 049 | `049-cli-agent-decoupling` | PR [#112](https://github.com/gosharplite/tellme/pull/112) (`d2a01b9`) |
| 050 | `050-agentloop-construction-inversion` | PR [#113](https://github.com/gosharplite/tellme/pull/113) (`09d0145`, head `0406c29`) |
| 051 | `051-cli-ui-decoupling` | PR [#114](https://github.com/gosharplite/tellme/pull/114) (`0d7566b`, head `e7d035d`) |
| 052 | `052-ride-alongs-and-records` | PR [#117](https://github.com/gosharplite/tellme/pull/117) (`f204aaa`, certified fold head `9f2a688`) |
| 053 | `053-offline-session-config-and-turns-flag` | PR [#118](https://github.com/gosharplite/tellme/pull/118) (`8c100e5`, certified fold head `0265098`) |
| 054 | `054-l-default-and-chrome-colour` | PR [#119](https://github.com/gosharplite/tellme/pull/119) (`c1d248a`, ff; fold head `74e0eb2`) |
| 055 | `055-e2e-suite-throughput` | PR [#120](https://github.com/gosharplite/tellme/pull/120) (`0531a8f`, merge commit; certified fold head `1561281`) |
| 056 | `056-mcp-tool-call-reason` | PR [#122](https://github.com/gosharplite/tellme/pull/122) (`db31d8b`, **ff**; certified fold head `77a8115`) |
| 057 | `057-tool-chrome-colour-and-payload-delta` | PR [#123](https://github.com/gosharplite/tellme/pull/123) (`1ac41f7`, **merge commit**; certified fold head `e41687f`) |
| 058 | `058-grey-tool-output-content` | PR [#124](https://github.com/gosharplite/tellme/pull/124) (`0e0844a`, **ff**; certified fold head `0e0844a`) |
| 059 | `059-darwin-metrics-and-1hz-cadence` | PR [#125](https://github.com/gosharplite/tellme/pull/125) (`10e0fa9`, **merge commit**; certified fold head `968a437`) |
| 060 | `060-domain-model-and-drift-gate` | PR [#126](https://github.com/gosharplite/tellme/pull/126) (`801b905`, **ff**; certified fold head `50e1ed7`) |
| 061 | `061-mcp-schema-provider-projection` | PR [#128](https://github.com/gosharplite/tellme/pull/128) (`902642a`, **merge commit**; certified fold head `d6b2786`; closes [#127](https://github.com/gosharplite/tellme/issues/127)) |
| 062 | `062-agent-image-vision` | PR [#129](https://github.com/gosharplite/tellme/pull/129) (`cff2515`, **ff**; certified fold head `cff2515`; operator request) |
| 063 | `063-gemini-image-vision` | PR [#130](https://github.com/gosharplite/tellme/pull/130) (`6932d3b`, **merge commit**; certified fold head `c4ff73b`; operator request; lands ADR 0032 RF-062-1) |
| 064 | `064-prompt-suggestion-parity` | PR [#131](https://github.com/gosharplite/tellme/pull/131) (`63aaa62`, **ff**; certified fold head `c39ad21`; operator request; ADR 0034) |
| 065 | `065-gemini-parallel-tool-calls` | PR [#133](https://github.com/gosharplite/tellme/pull/133) (`b904281`, **merge commit**; certified fold head `588a2c5`; anchor [#132](https://github.com/gosharplite/tellme/issues/132); **ADR 0035**) |
| 066 | `066-toolcall-id-pairing` | PR [#135](https://github.com/gosharplite/tellme/pull/135) (`e4410e4`, **ff**; certified fold head `92d86e2`; anchor [#134](https://github.com/gosharplite/tellme/issues/134); **ADR 0036**; **closes #134**) |
| 067 | `067-toolcall-id-followups` | PR [#137](https://github.com/gosharplite/tellme/pull/137) (`82b4c64`, **ff**; certified fold head `c673ae3`; anchor [#136](https://github.com/gosharplite/tellme/issues/136); **ADR 0037**; **closes #136**) |
| 068 | `068-unpaired-call-diagnostic` | PR [#138](https://github.com/gosharplite/tellme/pull/138) (`d7f4ed7`, **merge commit**; certified fold head `55fab02`; **ADR 0038**; resolves ADR 0037 RF-067-1) |
| 069 | `069-toolset-spec-capability-seam` | PR [#141](https://github.com/gosharplite/tellme/pull/141) (`cd14519`, **ff**; certified fold head `cd14519`; anchor [#140](https://github.com/gosharplite/tellme/issues/140); **ADR 0039**; **closes #140**) |
| 070 | `070-media-channel-in-band` | PR [#143](https://github.com/gosharplite/tellme/pull/143) (`da53b74`, **merge commit**; certified fold head `8aa6174`; anchor [#142](https://github.com/gosharplite/tellme/issues/142); **ADR 0040**; **closes #142**; completes ADR 0032 RF-062-10 + delivers ADR 0039 RF-069-1) |
| 071 | `071-search-files-tool` | PR [#148](https://github.com/gosharplite/tellme/pull/148) (`b87ba4c`, **fast-forward**; certified fold head `802acc0`; anchor [#147](https://github.com/gosharplite/tellme/issues/147) — inventory, stays open; **ADR 0043**) |
| 072 | `072-gemini-cached-token-usage` | PR [#150](https://github.com/gosharplite/tellme/pull/150) (`6c3e5fe`, **merge commit**; certified fold head `c8a671f`; anchor [#149](https://github.com/gosharplite/tellme/issues/149); **ADR 0044**; **closes #149**) |
| 073 | `073-list-role-headers-and-rendered-body` | PR [#151](https://github.com/gosharplite/tellme/pull/151) (`d90a79e`, **merge commit**; certified fold head `8916807`; operator request, no anchor issue; **ADR 0045**) |
| 074 | `074-cli-help-and-version-shorthands` | PR [#152](https://github.com/gosharplite/tellme/pull/152) (`fd204f4`, **merge commit**; certified fold head `1185292`; operator request, no anchor issue; **ADR 0046**) |
| 075 | `075-skill-frontmatter-block-scalars` | PR [#153](https://github.com/gosharplite/tellme/pull/153) (`250221d`, **merge commit**; certified fold head `7fb0fa1`; operator request, no anchor issue; **ADR 0047**) |
| 076 | `076-recoverable-unknown-tool` | PR [#156](https://github.com/gosharplite/tellme/pull/156) (`851466e`, **merge commit**; certified fold head `19a73bd`; anchor [#154](https://github.com/gosharplite/tellme/issues/154); **ADR 0048**; **closes #154**) |
| 077 | `077-mcp-tool-name-presentation` | PR [#157](https://github.com/gosharplite/tellme/pull/157) (`273b5a9`, **merge commit**; certified fold head `24be271`; anchor [#155](https://github.com/gosharplite/tellme/issues/155); **ADR 0049**; **closes #155**) |
| 078 | `078-provider-transport-retry` | PR [#158](https://github.com/gosharplite/tellme/pull/158) (`c291e1d`, **merge commit**; certified fold head `9ac7e6a`; operator request, no anchor issue; **ADR 0050**) |
| 079 | `079-interrupted-turn-partial-persistence` | PR [#160](https://github.com/gosharplite/tellme/pull/160) (`30fb420`, **merge commit**; certified fold head `936125a`; anchor [#159](https://github.com/gosharplite/tellme/issues/159); **ADR 0051**; **closes #159**) |

Per-round detail lives in the archives (the delivered-rounds index above records **delivery**, not liveness — see `STATUS.md`'s top **Last delivered round** section for the current round).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (**no-ff**) - round 079 propagation (tagged `round-079`) · round 078 propagation (tagged `round-078`) · round 077 propagation (tagged `round-077`) · round 076 propagation (tagged `round-076`) · round 075 propagation (tagged `round-075`) · round 074 propagation (tagged `round-074`) · round 073 propagation (tagged `round-073`) · round 072 propagation (tagged `round-072`) · round 071 propagation (tagged `round-071`) · round 070 propagation (tagged `round-070`) · the session-56 **docs/quality passes** (`6dfff6c`) · the session-56 **issue-tracker reconciliation** docs (**no-ff** `37a261c`) | Stable / released line |
| `dev` | round 079 **delivered** (PR [#160](https://github.com/gosharplite/tellme/pull/160) merged `30fb420`, **merge commit**) | Integration line (round work lands here before `main`) |
> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–078 — **DONE (no-ff)**; **round 079 — DONE (no-ff)** at its closeout (tagged **`round-079`**); **the session-56 docs/quality passes — DONE (no-ff, no tag)** (`dev` @ `0d5518f` → `main` @ `6dfff6c`) **and the session-56 issue-tracker reconciliation docs — DONE (no-ff, no tag)** (`dev` @ `3322059` → `main` @ `37a261c`; docs/quality only, not round deliveries) **and the session-59 tracker-curation docs (#147 settled + closed) — DONE (no-ff, no tag)** (docs/issue-tracker only, not a round delivery). (Earlier branch-model rows are archived — rounds 043–061 in [`2026-09-18.md`](docs/archives/status/2026-09-18.md)/[`2026-09-19.md`](docs/archives/status/2026-09-19.md); rounds 063–071 in [`2026-09-20.md`](docs/archives/status/2026-09-20.md).)
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–068** | — | Provider-registry completeness → the R1–R5 import-ceiling programme → the Gemini/Vertex tool-call hardening chain (rounds 061–068). | ✅ **Delivered** (see the archives) |
| **069** | [#140](https://github.com/gosharplite/tellme/issues/140) | **The `ToolSetSpec` capability seam** — replace the registry-construction positional scalars with one named `deps.ToolSetSpec`; **ADR 0039**; delivers ADR 0032 RF-062-10's seam half + ADR 0033 RF-063-6. | ✅ **Delivered** — PR [#141](https://github.com/gosharplite/tellme/pull/141) merged into `dev` (`cd14519`, **fast-forward**); **#140 closed** |
| **070** | [#142](https://github.com/gosharplite/tellme/issues/142) | **The media channel in-band** — a media-producing tool returns its media from `Execute` (a `domain/tools` value), translated by the loop; the per-call `context` collector is deleted and `infrastructure/tools` no longer imports `domain/llm`; **ADR 0040**; **completes ADR 0032 RF-062-10** + **delivers ADR 0039 RF-069-1**. | ✅ **Delivered** — PR [#143](https://github.com/gosharplite/tellme/pull/143) merged into `dev` (`da53b74`, **merge commit**; certified fold head `8aa6174`); **#142 closed** |
| **071** | [#147](https://github.com/gosharplite/tellme/issues/147) | **`search_files`** — a ninth agent tool: a **bounded, deterministic** in-file content search (the missing half of the reader trio); **ADR 0043**; three deliberate divergences from the reference. | ✅ **Delivered** — PR [#148](https://github.com/gosharplite/tellme/pull/148) merged into `dev` (`b87ba4c`, **fast-forward**; certified fold head `802acc0`); **#147 stays OPEN** (an inventory) |
| **072** | [#149](https://github.com/gosharplite/tellme/issues/149) | **Gemini/Vertex cached-token usage** — decode `cachedContentTokenCount`/`thoughtsTokenCount` into `llm.Usage` (family-disjoint; cached capped at the prompt), ending the `H: 0` / ~10× over-charge; **ADR 0044**. | ✅ **Delivered** — PR [#150](https://github.com/gosharplite/tellme/pull/150) merged into `dev` (`6c3e5fe`, **merge commit**; certified fold head `c8a671f`); **#149 closed** |
| **073** | — | **The `-l` listing presentation parity** (operator request) — role header lines (`[USER]`/`[MODEL]`), the **model** body glamour-rendered (the **operator** body verbatim), one blank line after every message, the header accented blue/magenta only on a terminal `stdout` with `-r` off; `-r` honoured; **ADR 0045**. | ✅ **Delivered** — PR [#151](https://github.com/gosharplite/tellme/pull/151) merged into `dev` (`d90a79e`, **merge commit**; certified fold head `8916807`); operator request (no anchor issue) |
| **074** | — | **The `-h`/`--help` and `-v`/`--version` CLI shorthands** (operator request) — `-h`/`--help` prints the flag list to `stdout` and exits 0 (a successful, offline, prompt-less action, precedence before `--version`); `-v` mirrors `--version`; the unrecognized-flag refusal (phrase on `stderr`, exit 2) is unchanged; **ADR 0046**. | ✅ **Delivered** — PR [#152](https://github.com/gosharplite/tellme/pull/152) merged into `dev` (`fd204f4`, **merge commit**; certified fold head `1185292`); operator request (no anchor issue) |
| **075** | — | **Skill frontmatter block scalars** (operator request) — the skills loader resolves a YAML **block scalar** (`>`/`|` + chomping) `name`/`description`, so a skill authored with `description: >` lists by its **real** text, not the bare indicator `>` (an inline `>` stays literal; a no-body indicator skips the file — best-effort unchanged); **stdlib-only** (no YAML dependency); **ADR 0047**; a **deliberate divergence *beyond* the reference** (its line-based `parseSkill` shares the limitation). | ✅ **Delivered** — PR [#153](https://github.com/gosharplite/tellme/pull/153) merged into `dev` (`250221d`, **merge commit**; certified fold head `7fb0fa1`); operator request (no anchor issue) |
| **076** | [#154](https://github.com/gosharplite/tellme/issues/154) | **Recover from an unknown tool name** — an off-list tool name becomes a **recoverable, per-turn-bounded fold-back** (`error: no tool named "…"; available tools: …`, then continue; capped by `maxUnknownToolFolds = 3`), so a naming slip self-corrects instead of aborting the turn; round-008 FR-010 **narrowed** (phrase + exit 7 for bound reached / cap exhausted / no tools registered); **ADR 0048**; family-local (zero wire diff). | ✅ **Delivered** — PR [#156](https://github.com/gosharplite/tellme/pull/156) merged into `dev` (`851466e`, **merge commit**; certified fold head `19a73bd`); **#154 closed** |
| **tool surface** | — | **settled** — the reference's 95-tool inventory is fully reconciled: **9 present** (the deliberately small surface) + **94 excluded** ([#146](https://github.com/gosharplite/tellme/issues/146) baseline 42 · Enterprise/network 25 · Go/AST 24 · Teams/`http_request`/`read_external_docs` 3); each absence is shell/`curl`-reachable, `gopls`-covered, an existing gate, or better as MCP (round 032). **No candidate tools.** | ✅ **Settled** |
| **077** | [#155](https://github.com/gosharplite/tellme/issues/155) | **Make the callable MCP wire name discoverable** — the offered declaration's description is prefixed with a tellme-authored note naming `mcp_<server>_<tool>` (the server's text relayed unchanged; ADR 0025 D1), and the empty-description fallback names the callable name (never the bare one); **ADR 0049**; description-only (schema unchanged; no prompt change); a recorded divergence *beyond* the reference. | ✅ **Delivered** — PR [#157](https://github.com/gosharplite/tellme/pull/157) merged into `dev` (`273b5a9`, **merge commit**; certified fold head `24be271`); **#155 closed** |
| **079** | [#159](https://github.com/gosharplite/tellme/issues/159) | **Preserve completed tool steps on an operator-interrupted turn** — a `Ctrl+C`-cancelled tool turn with ≥1 completed step is persisted as ONE `history.Entry` closed with the synthetic `history.InterruptedTurnAnswer`, so the stored history replays as a valid `user … assistant` sequence (role alternation on both families); a **zero-step** interruption writes nothing (today's clean abort: phrase + exit 6); typed detection; exit **0** + an informational `stderr` line; `calls` = the completed rounds, no usage persisted; the schema unchanged; **ADR 0051**; a recorded divergence (the reference discards the partial turn). | ✅ **Delivered** — PR [#160](https://github.com/gosharplite/tellme/pull/160) merged into `dev` (`30fb420`, **merge commit**; certified fold head `936125a`); **closes [#159](https://github.com/gosharplite/tellme/issues/159)** |
| **078** | — | **Bounded provider transport retry** (operator request) — a retryable provider failure (transport, or HTTP 429/5xx) is retried automatically **at most twice** (fixed **1 s → 3 s**) before failing with the frozen phrase + exit **6** (unchanged); retryability is **typed** (`llm.ProviderError.Status`/`Transport` + the domain-owned `llm.Retryable`); a non-retryable failure fails at once; one `calls`/usage/frame per retried inference; **ADR 0050**; a recorded divergence (no failover / auth-refresh / jitter). | ✅ **Delivered** — PR [#158](https://github.com/gosharplite/tellme/pull/158) merged into `dev` (`c291e1d`, **merge commit**; certified fold head `9ac7e6a`); operator request (no anchor issue) |

## Open items (non-blocking)

> **Curation rule (2026-09-20, sessions 51/52/55 — the `RF-063-10`/`RF-068-1`/`RF-066-4` lesson).** This section is an **index of disclosures, not a work queue**. A **forward item** is a decision *deferred to a trigger* (a named issue/ADR precondition) or a recorded divergence — **not** open tasking. **Never re-raise, re-litigate, or propose a forward item as a round theme unless its trigger has fired.** A fired trigger moves the item to the roadmap as a real candidate; an untriggered item sits here only as a pointer to `ADR 00NN §Forward` (the ADR is the authority, not this index). **Session-52 addenda:** (a) a **forward-item cluster must never generate a round theme** — a theme comes from operator value or a live issue, never from "this would clear the backlog"; (b) a **settled / declined decision is never reopened** (even to fire a trigger) **without explicit operator intent**. **Session-55 addendum:** when answering "what is next?" / naming candidates, list **only live issues + operator-value themes** — **never** a forward item from this index, **not even labelled a "disclosure"**; a disclosure inside a candidates list *is* surfacing it. *(The rule exists because such items were re-litigated at every bootstrap until annotated; session 52 opened a round-069 off a forward-item cluster against this very rule — retracted; session 55 surfaced the `RF-069` disclosures in a "what is next" table. The recurrence / the process, not the item, was the bug.)*
>
> **⛔ Retired 2026-09-20 (session 52) — the concurrency trigger is settled-off.** Tool calls execute **sequentially** (the operator re-affirmed the settled decision; truth *Tool-call concurrency — NOT PLANNED*, [#47](https://github.com/gosharplite/tellme/issues/47)). The four forward items it gated are **retired** in their ADR §Forward — **`RF-068-1`** (ADR 0038; the diagnostic stays **defensive-only**, no E2E carrier) · **`RF-067-4`** (ADR 0037) · **`RF-066-4`** · **`RF-066-6`** (ADR 0036). **Do not re-raise them.**

- **Round-079 forward items (RF-079-\* — non-blocking, `ADR 0051 §Forward`)** — **RF-079-1** the synthetic answer is stored as the turn's `answer` (a `-l` reader shows it as the "answer"; no marker beyond the text) · **RF-079-2** a step whose tool was killed mid-execution is counted (its `result` is the nil-error `Exit Code: -1`) · **RF-079-3** no usage is persisted for the interrupted turn · **RF-079-4** the exit-0 vs zero-step-exit-6 asymmetry · **RF-079-5** `SIGTERM` shares the "via Ctrl+C" wording · **RF-079-A** no scenario chains the **product-written** interrupted entry into a resume (the resume leg arranges it by hand) · **RF-079-B** the aborted call's chrome frame (`╭─⠿ Turn N` + estimated payload, no tail) reaches `stderr` **and** `turns.log` · **RF-079-C** superseded (the persisted-`calls` E2E carrier landed at fold F-079-2) · **RF-079-7** no `history.archive.jsonl` interaction beyond the existing semantics · **RF-079-8** the informational line is `stderr`-only, not in `turns.log` · **RF-079-9** the ~150 ms window between `ind.Stop()` and the append is not exercised.
- **Round-078 forward items (RF-078-\* — non-blocking, `ADR 0050 §Forward`)** — **RF-078-1** the delays (1 s, 3 s) and the retry count (2) are constants (a config knob is out of scope) · **RF-078-2** no jitter (a deliberate divergence from the reference's jittered backoff) · **RF-078-3** the retry covers transport/status only (a successful-but-unusable body stays terminal) · **RF-078-4** added worst-case latency is 1 s + 3 s (+ up to 3 × the unchanged 300 s client timeout) · **RF-078-5** the retry line's spinner yield/restore + its no-class-prefix shape (resolved at implementation; unit- + E2E-pinned) · **RF-078-6** the MCP path is unchanged · **RF-078-7** the offline readers are untouched · **RF-078-8** a byte-identity pin for the re-sent request (resolved — `the retry re-sent the same request`) · **RF-078-9** the retry policy is split three ways (predicate in `domain/llm`, schedule/line in `cli`) — deliberate, review RF-078-4 · **RF-078-10** the `DropFirst` hijack fallback is defensive / unreachable on httptest (review N-078-1).
- **Earlier rounds (≤077) — compacted to pointers (the curation rule).** Their forward items are recorded **in their own `ADR §Forward`** (the authority, not this index): **ADR 0049** (RF-077-1…6) · **ADR 0048** (RF-076-1…5) · **0047** (RF-075-1…7) · **0046** (RF-074-1…7) · **0045** (RF-073-1…10) · **0044** (RF-072-1…3) · **0043** (RF-071-1…5) · **0040** (RF-070-1…3) · **0039** (RF-069-2…4; RF-069-1 delivered, RF-069-5 closed) · **0038** (RF-068-1 retired; RF-068-2…7) · **0037** (RF-067-1…7) · **0036** (RF-066-1…10) · **0035** (RF-065-1…5) · **0034** (RF-064-1…7) · **0033** (RF-063-1…10) · **0032** (RF-062-1…14) · **0031** (RF-061-1…12) · **0030** (RF-060-1…5 / R-060-1…3) · **0029** (RF-059-x) · **0028** (RF-058-x) · **0027** (RF-057-x) · **0025** (RF-056-x) · **0024** (RF-055-x) · **0023** (RF-054-x) · **0022** (RF-053-x) · **0021** (RF-52-x) · **0020** (RF-51-x). Prior live-state snapshots: [`2026-09-20.md`](docs/archives/status/2026-09-20.md) + [`2026-09-19.md`](docs/archives/status/2026-09-19.md).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#145**; round-047 items (b)–(d); the pre-existing Gherkin/DSL topology-audit errors (see the env note).
- **PM follow-ups** — **none open.** The `R-056-c / R-058-c / R-059-c / R-062-c / RF-063-10` comment-only-meta-Rule class is **RETIRED (2026-09-20)** (convention in **ADR 0033 §Forward**; do **not** re-open or re-raise it).
- **Issue tracker (2026-09-22, session 65)** — **[#159](https://github.com/gosharplite/tellme/issues/159)** **CLOSED (completed)** — the completed tool steps of an operator-interrupted turn are now persisted with a synthetic closing answer (fixed by **ADR 0051 / round 079**, PR [#160](https://github.com/gosharplite/tellme/pull/160)). Tracker after: **0 open** (`gh issue list --state open` = empty).
- **Issue tracker (2026-09-22, session 64)** — round 078 had **no anchor issue** (an operator request); the tracker remains **0 open** (`gh issue list --state open` = empty) — nothing to close or revise.
- **Issue tracker (2026-09-22, session 63)** — **[#155](https://github.com/gosharplite/tellme/issues/155)** **CLOSED (completed)** — the callable MCP wire name is now discoverable in the offered declaration (fixed by **ADR 0049 / round 077**, PR [#157](https://github.com/gosharplite/tellme/pull/157)). Tracker after: **0 open** (`gh issue list --state open` = empty).
- **Issue tracker (2026-09-22, session 62)** — **[#154](https://github.com/gosharplite/tellme/issues/154)** **CLOSED (completed)** — an unknown tool name is now a recoverable fold-back (fixed by **ADR 0048 / round 076**, PR [#156](https://github.com/gosharplite/tellme/pull/156)) · **[#155](https://github.com/gosharplite/tellme/issues/155)** **stays OPEN** (MCP name presentation — research-gated, a separate issue). Tracker after: **1 open** ([#155](https://github.com/gosharplite/tellme/issues/155)).
- **Issue tracker (2026-09-21, session 61)** — round 075 had **no anchor issue** (an operator request); the tracker remains **0 open** (`gh issue list --state open` = empty) — nothing to close or revise.
- **Issue tracker (2026-09-21, session 60, cont.)** — round 074 had **no anchor issue** (an operator request); the tracker remains **0 open** — nothing to close or revise.
- **Issue tracker (2026-09-21, session 60)** — **0 open issues** (`gh issue list --state open` = empty). Round 073 had **no anchor issue** (an operator request), so there was **nothing to close or revise**; the tracker already matches the delivered state.
- **Issue tracker (2026-09-20, session 58, cont.)** — **[#149](https://github.com/gosharplite/tellme/issues/149)** **CLOSED (completed)** — the Gemini cached-token bug, fixed by **ADR 0044 / round 072** (PR [#150](https://github.com/gosharplite/tellme/pull/150)) · **[#147](https://github.com/gosharplite/tellme/issues/147)** **CLOSED `completed`** (2026-09-20) — the `tell-me-go` **not-excluded inventory** fully reconciled: **9 present + 0 candidates**; **94 excluded**; the whole reference tool inventory is settled · **[#146](https://github.com/gosharplite/tellme/issues/146)** CLOSED `completed` (the `tell-me-go` **excluded-tools** inventory — 42 baseline tools, settled) · **[#145](https://github.com/gosharplite/tellme/issues/145)** CLOSED `completed` (the **self-development tracker** — goal **ACHIEVED**: `tellme` drives `tellme`; `tell-me-go` is the reference only) · **[#91](https://github.com/gosharplite/tellme/issues/91)** CLOSED `not_planned` (superseded by [#145](https://github.com/gosharplite/tellme/issues/145)).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-079`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-078`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **Round-079 (session 65)** — branch `079-interrupted-turn-partial-persistence` off `dev` `4f9c96d`: specify `eb27570` -> research + **ADR 0051** + truth `a68d06d` -> acceptance + plan + dsl-refine truth + tasks + implementation `beb3670` -> PR [#160](https://github.com/gosharplite/tellme/pull/160) -> fold F-079-1…4 + TD-1/2 + N-1/2 `3e6564c` -> residual fold RES-079-FV-1…5 `936125a` -> cosmetic table fix `4be021c` -> merge `30fb420`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; RES-079-FV-1…5) -> residual fold -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — loop CLOSED, cleared for human merge**). **Human-merged** into `dev` (`30fb420`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-079`. Host: **linux/amd64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E **295 scenarios**) - `make check` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (52 features - 432 module rows - 2175 steps) - `go.mod`/`go.sum` unchanged.
- **Round-078 (session 64)** — branch `078-provider-transport-retry` off `dev` `dd9b94b`: specify `489c4b7` -> acceptance `b098058` -> research + **ADR 0050** + truth `77b835b` -> implementation `d61935e` -> PR [#158](https://github.com/gosharplite/tellme/pull/158) -> fold F-1…F-4 + TD-1…3 + RF/N `9ac7e6a` -> residual fold R-1…R-3 `657ae2a` -> merge `c291e1d`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; R-1…R-3) -> residual fold -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`c291e1d`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-078`. Host: **darwin/arm64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E **293 scenarios**) - `make check` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (52 features - 424 module rows - 2154 steps) - `go.mod`/`go.sum` unchanged.
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34)** — the `axb-gherkin-and-dsl` topology script over `specs/truth/features/cli` reports the **same 5 pre-existing DSL-matching errors** (52 features · 6 feature modules · 17 root + 432 module rows · 2175 steps); rounds 060–079 add **none**. The script is **not** a `make verify` member; a carried item, not a delivery gate.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
