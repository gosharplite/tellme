# tellme — Status

**Last updated**: 2026-09-19 (round **053** `053-offline-session-config-and-turns-flag` **IN FLIGHT / PR [#118](https://github.com/gosharplite/tellme/pull/118)** — closes [#103](https://github.com/gosharplite/tellme/issues/103): the offline session commands (`-l`, prompt-less `--new`, new `-t`) now honour the **`-c` config's `MODE`** (mode-only read; stays **offline**; an explicit unreadable `-c` **fails**), and `tellme` writes its own per-session **`turns.log`** (the rendered turn chrome) that `-t` prints; **ADR 0022**). Prior: session 26 — round **052** `052-ride-alongs-and-records` **DELIVERED / FROZEN** — **closes [#115](https://github.com/gosharplite/tellme/issues/115) + [#116](https://github.com/gosharplite/tellme/issues/116)**: the `execute_command` tool's **construction-time** `[Tool Output]` sink + the suggester's **caller-owned** selection cursor + the three `#92` **records relocated to ADR 0021**; **ADR 0021**; PR [#117](https://github.com/gosharplite/tellme/pull/117) merged into `dev` `f204aaa`. **Session mode**: `butler`.
**Active branch**: `053-offline-session-config-and-turns-flag` (round **053** — closes [#103](https://github.com/gosharplite/tellme/issues/103); implemented, **PR pending** — sits directly after this line's round-052 delivered entry)
**Daily log**: [`docs/session-summary/2026/09/19/session-summary.md`](docs/session-summary/2026/09/19/session-summary.md)

## Round 053 — `053-offline-session-config-and-turns-flag` (IN FLIGHT — closes [#103](https://github.com/gosharplite/tellme/issues/103))

- **Theme**: the offline session commands (`-l`, prompt-less `--new`) **ignore `-c`** (they resolve the mode from `TELL_ME_MODE` → the *default* config → `butler`, never the `-c` path) ⇒ `tellme -l 1 -c architect.yaml` silently reads the **butler** session, breaking the documented `tmg-chat-ingroup` / `tmg-grill-round` retrieve; plus **no `-t`** (turns-log) flag.
- **US1** — mode resolution is now `TELL_ME_MODE` → else the **`-c` config's `MODE`** → else the default config → else `butler` (mode-only read; stays **offline**); one widened seam `resolveWorkspace(homeDir, configPath)`. An **explicit** `-c` that cannot be read **fails** (Q2 → A); an absent default stays tolerant (round-007).
- **US2** — a new `-t`/`--turns` offline command (order `-d` → `-l` → `-t` → `--tool-usage`) prints `output/<mode>/turns.log` and exits; `tellme` now **writes its own `turns.log`** (Q1 → C1) — the rendered turn chrome (turn rule/header, payload status, metrics), teed on the prompt path via the new `history.TurnsLogStore` port + infra adapter (best-effort); `--new` archives it.
- **Clarify**: **Q1 → (C1)** (tellme writes its own `turns.log`, its rendered chrome — not `tokens.log`, not a `history.jsonl` projection, not the reference's bytes) · **Q2 → (A)** (an explicit unreadable `-c` fails).
- **Pipeline**: `/axb-specify` ✅ · `/axb-clarify` ✅ (Q1/Q2) · `/axb-spec-by-example` ✅ · `/axb-technical-research` ✅ (**ADR 0022** + `techstack.md`) · `/axb-system-analysis` ✅ (1 CLI interface; api NOOP) · `/axb-dsl-refine` ✅ (history Examples/rows) · `/axb-tasks` ✅ (T001–T015) · `/axb-implement` ✅ (all `[X]`).
- **Verification**: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (arch gate 0 new/0 stale; lint 0; vulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` **green** (incl. the godog E2E, **Strict**) · witnesses (a)/(b)/(c) reproduced + reverted · `go.mod`/`go.sum` unchanged.
- **Artifacts**: `internal/domain/history/turns_log.go` · `internal/infrastructure/history/turns_log_store.go` (+ test) · `internal/app/deps/deps.go` · `cmd/tellme/deps.go` · `internal/cli/{cli,call_renderer,testdeps_test,history_mode_test}.go` · `tests/e2e/steps/step_r053_history.go` · CLI truth (`history/inspecting-the-session-history.feature`, `history/reviewing-the-turn-log.feature`, `history/dsl.md`) · `specs/truth/data/data-model.dbml` (`turns_log_line`) · **ADR 0022** + index · `specs/plans/053-offline-session-config-and-turns-flag/**`.

## Last delivered round — 052 `052-ride-alongs-and-records` (DELIVERED / FROZEN — PR [#117](https://github.com/gosharplite/tellme/pull/117) merged into `dev` `f204aaa`; closes [#115](https://github.com/gosharplite/tellme/issues/115) + [#116](https://github.com/gosharplite/tellme/issues/116))

- **Theme**: the two **ride-alongs** of the [#92](https://github.com/gosharplite/tellme/issues/92) ledger + the three **records**' durable disposition — the last two residual issues of the #92 lineage, closed together (one round, Q1).
- **#115 R-2** (`execute_command`'s construction-time sink): `NewCommandTool(sink domaintools.OutputSink)`; the tool holds the sink directly; the `toolOutputBox` pointer indirection, `infratools.BindToolOutput`, and `deps.Dependencies.BindToolOutput` are **deleted**; the agent registry is built through **one** sink-aware builder (`assembleAgentTools`; `agentTools()` stays parameterless + read-free, passing a nil sink), and the prompt path builds `prog` **before** the registry so `dp.NewToolRegistry(prog.ToolOutput)` injects at construction. `NewToolRegistry` widens to `func(sink) Registry`.
- **#115 R-1** (suggester selection policy owner): `suggester.set(items []string, cursor int)` — the reset is a **caller** decision (`noChoice`), no longer a `set` side effect; rendering byte-unchanged.
- **#116 (records → ADR 0021 §Records)**: the permanent E2E narrowing (round 036 D4) · the coordinator's one-concurrent-block assumption (round 040) · the `End`-while-write-stalled accepted residual — **relocated** into ADR 0021 (non-frozen, indexed); the frozen `036-*`/`040-*` packages are untouched; #116 closes **completed**.
- **Pipeline**: `/axb-specify` ✅ · `/axb-clarify` ✅ (Q1 one round · Q2 → (i) relocate+close) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (ADR 0021 + `techstack.md` MODIFY ×3 + task-runner NOOP) · `/axb-system-analysis` ✅ (0 interfaces; api/data/dsl-refine NOOP) · `/axb-tasks` ✅ (T001–T016) · `/axb-implement` ✅ (all `[X]`).
- **Verification**: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (RULE-A/B/C **0**; **RULE-E baseline 0**, 0 new / 0 stale; RULE-F consistent; 0 cycles; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` **green** (24 pkgs; incl. the godog E2E) · witnesses (a)/(b)/(c) reproduced + reverted · `go.mod`/`go.sum` unchanged · no new Gherkin/DSL row.
- **Artifacts**: `internal/infrastructure/tools/command.go` · `internal/infrastructure/tools/command_sink_test.go` (NEW) · `internal/app/deps/deps.go` · `cmd/tellme/deps.go` · `internal/cli/cli.go` · `internal/ui/tui/prompt/{suggester,model,suggester_set_test}.go` · fixtures (`internal/cli/{cli,testdeps}_test.go`, `cmd/tellme/deps_test.go`, `tests/e2e/steps/tool_usage.go`) · **ADR 0021** + index · `specs/truth/techstack.md` ×3 · `specs/plans/052-ride-alongs-and-records/**`.

**Fold ledger**: round-047 (PR [#110](https://github.com/gosharplite/tellme/pull/110)) `d5ba1ae` → `4c20c98` → `a546c89` · round-048 (PR [#111](https://github.com/gosharplite/tellme/pull/111)) `e9c9ea2` → `36493a2` · round-049 (PR [#112](https://github.com/gosharplite/tellme/pull/112)) `1e2e451` → `c3e8e16` → `a348541`+`088ac7d` → `e88a1f9` → `d2a01b9` · round-050 (PR [#113](https://github.com/gosharplite/tellme/pull/113)) `2f63459` → `efc842b` → `c22ce0c` → `0406c29` → `09d0145` · **[round-051](https://github.com/gosharplite/tellme/pull/114) (PR [#114](https://github.com/gosharplite/tellme/pull/114))** `6e03c85` → **`3cae5d4`** (R-51-1…R-51-5) → **`7b24a47`** (F-1/F-2/N-1/N-2) → **`e7d035d`** (F-3/N-2′) → merged **`0d7566b`** · **[round-052](https://github.com/gosharplite/tellme/pull/117) (PR [#117](https://github.com/gosharplite/tellme/pull/117))** `76502f4` → `d1f5478` → `bfda279` → `1f1c1de` → **`56c7167`** (F-52-1/F-52-2 + RF-52-1/2/3 + nits) → **`dc0fa25`** (F-52-3 + N-52-1) → **`9f2a688`** (the fold-verification ledger record — **the certified round-052 fold head**) → merged **`f204aaa`**. *(Convention, TD-6: this line names fold heads + prior ledgers only — a ledger commit cannot name its own SHA.)*
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (rounds 041–**051** detail + the 043/044/046/047/050 branch-model rows + the round-046/047/049/050 env notes; the round-050 items relocated at the round-051 closeout, the **round-051 detail relocated at the round-052 closeout**).

> **Split note (Rule 12)**: on **this** closeout the **round-051 delivered-round detail** was relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 052 is now the current delivered round), keeping `STATUS.md` to the live state + a single delivered-round section. (The round-050 detail + its branch-model row + its env note were relocated at the round-051 closeout; earlier, the round-046 detail + row, the round-042 detail, and the round-041 detail + its PR #88 review-fold history.)

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–041 | `001-*` … `041-di-resolver-test-load-tolerance` | see the archives + the per-round PRs (035 #73 · 036 #75 · 037 #77 · 038 #79 `a0b0653` · 039 #81 `d48ebbc` · 040 #84/#85/#86 head `436bd70` · 041 #88 `ca792d7`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, head `d007b75`) |
| 044 | `044-composition-root-extraction` + `044-implement-…` | PR [#102](https://github.com/gosharplite/tellme/pull/102) (`4fd57fc`) · PR [#104](https://github.com/gosharplite/tellme/pull/104) (`8da0b88`, head `ba64508`) |
| 045 | `045-yield-policy-owner` | PR [#106](https://github.com/gosharplite/tellme/pull/106) (`9e03a91`, head `ab2c7fb`) |
| 046 | `046-blank-reason-owner-and-presentation-decoupling` | PR [#109](https://github.com/gosharplite/tellme/pull/109) (`8ca4758`) |
| 047 | `047-application-import-ceiling-gate` | PR [#110](https://github.com/gosharplite/tellme/pull/110) (`6711e0a`) |
| 048 | `048-cli-tui-prompt-decoupling` | PR [#111](https://github.com/gosharplite/tellme/pull/111) (`822e171`) |
| 049 | `049-cli-agent-decoupling` | PR [#112](https://github.com/gosharplite/tellme/pull/112) (`d2a01b9`) |
| 050 | `050-agentloop-construction-inversion` | PR [#113](https://github.com/gosharplite/tellme/pull/113) (`09d0145`, head `0406c29`) |
| 051 | `051-cli-ui-decoupling` | PR [#114](https://github.com/gosharplite/tellme/pull/114) (`0d7566b`, head `e7d035d`) |
| 052 | `052-ride-alongs-and-records` | PR [#117](https://github.com/gosharplite/tellme/pull/117) (`f204aaa`, certified fold head `9f2a688`) |
| 053 | `053-offline-session-config-and-turns-flag` | in flight — closes [#103](https://github.com/gosharplite/tellme/issues/103) |

Per-round detail lives in the archives (the delivered-rounds index above records **delivery**, not liveness — see `STATUS.md`'s top **Last delivered round** section for the current round).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (**no-ff**) at this closeout — round 052 propagation **DONE** | Stable / released line |
| `dev` | round 052 **delivered** (`f204aaa`, PR [#117](https://github.com/gosharplite/tellme/pull/117)) | Integration line (round work lands here before `main`) |
| `051-cli-ui-decoupling` | **merged into `dev`** (PR [#114](https://github.com/gosharplite/tellme/pull/114) → `0d7566b`); **branch deleted (local + remote)** after merge | Round 051's single branch — the **terminal R5 slice**: the last RULE-E residual `internal/cli → internal/ui` → **baseline 1 → 0** + F-6/F-7/F-8; **ADR 0020**. |
| `052-ride-alongs-and-records` | **merged into `dev`** (PR [#117](https://github.com/gosharplite/tellme/pull/117) → `f204aaa`); **branch deleted (local + remote)** after merge | Round 052's single branch — closes [#115](https://github.com/gosharplite/tellme/issues/115) + [#116](https://github.com/gosharplite/tellme/issues/116): the command tool's construction-time `[Tool Output]` sink + the suggester's caller-owned selection cursor + the three `#92` records → **ADR 0021**. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–051 — **DONE (no-ff)**; **round 052 — DONE (no-ff)** at this closeout. (The round-043/044/046/047/050 branch-model rows are archived in [`2026-09-18.md`](docs/archives/status/2026-09-18.md).)
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–050** | — | Provider-registry completeness → … → the R5 import-ceiling programme (R5.1 gate · R5.2 `→ ui/tui/prompt` · R5.3 contracts · R5.4 `→ agent`). | ✅ **Delivered** (see the archives) |
| **051** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.5 (terminal)** — the last RULE-E residual `internal/cli → internal/ui` → **baseline 1 → 0** + **F-6/F-7/F-8**; **closes #101**; **ADR 0020**. | ✅ **Delivered** — PR [#114](https://github.com/gosharplite/tellme/pull/114) merged into `dev` (`0d7566b`); **baseline 0 (terminal)** |
| **052** | [#115](https://github.com/gosharplite/tellme/issues/115) + [#116](https://github.com/gosharplite/tellme/issues/116) | The two `#92` **ride-alongs** (the command tool's construction-time `[Tool Output]` sink; the suggester's single-owned selection policy) + the three `#92` **records**' durable relocation (**ADR 0021**). | ✅ **Delivered** — PR [#117](https://github.com/gosharplite/tellme/pull/117) merged into `dev` (`f204aaa`); **#115 + #116 closed** |
| **future slices** | [#91](https://github.com/gosharplite/tellme/issues/91) · [#13](https://github.com/gosharplite/tellme/issues/13) · [#103](https://github.com/gosharplite/tellme/issues/103) | the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91)) · **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)) · the `-l`/`-c` + `-t` plumbing gap ([#103](https://github.com/gosharplite/tellme/issues/103)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 26 closeout)** — **[#115](https://github.com/gosharplite/tellme/issues/115) CLOSED** (the two ride-alongs, delivered by round 052 / PR [#117](https://github.com/gosharplite/tellme/pull/117) merged `f204aaa`). **[#116](https://github.com/gosharplite/tellme/issues/116) CLOSED** (its three records relocated to **ADR 0021 §Records** — its own closure path; the ADR-0021 relocation comment is on the issue). **[#103](https://github.com/gosharplite/tellme/issues/103)** · **[#91](https://github.com/gosharplite/tellme/issues/91)** · **[#13](https://github.com/gosharplite/tellme/issues/13)** — all OPEN (accurate). [#92](https://github.com/gosharplite/tellme/issues/92)/[#101](https://github.com/gosharplite/tellme/issues/101)/[#93](https://github.com/gosharplite/tellme/issues/93)/[#96](https://github.com/gosharplite/tellme/issues/96)/[#100](https://github.com/gosharplite/tellme/issues/100)/[#105](https://github.com/gosharplite/tellme/issues/105)/[#107](https://github.com/gosharplite/tellme/issues/107)/[#108](https://github.com/gosharplite/tellme/issues/108) CLOSED (earlier).
- **Round-052 forward items (RF-52-x, non-blocking — all in ADR 0021)** — **RF-52-1**: `BindSkillsCatalog` is now the **only** pointer-based post-construction seam, and its `ok`-guarded assertion would **silently no-op** if `listSkills` became a value type — a future round converting it MUST migrate the bind in the same change · **RF-52-2**: the records' ADR home is **write-once** — a changed record is superseded by a new ADR citing ADR 0021 · **RF-52-3**: the cursor's **upper** bound is the pinned half (deliberately at the `suggester` tier) · **RF-52-4**: prefer a named per-path seam over a growing positional list if `NewToolRegistry` gains a second per-turn dependency · **RF-52-5**: Record 1's E2E narrowing (a `\n`+`\r`-covering count formulation would allow a carrier) · **RF-52-6**: Record 2's re-scope gated on a concurrent-tools round ([#47](https://github.com/gosharplite/tellme/issues/47) `not_planned`).
- **Round-051 forward items (RF-51-x, non-blocking)** — **RF-51-2**: the `render.Lines` port family keeps a **presentation-shape leak** (the domain names the spelling; the bytes stay owned+pinned by `internal/ui`+E2E) — adjudicated in **ADR 0020 §Decision** with two forward options (i: move the pure formatters + `sanitizeControl` into `domain/render` and delete the interface; ii: per-formatter func-typed deps seams) · **RF-51-1** (the fix-5 gate-before-provider-read discipline dropped — `NewProgress` builds the provider unconditionally; harmless, recorded) · **RF-51-3** (`dp.NewLines()` ~4×/run) · **RF-51-4** (`internal/domain/render`/`metrics` package-side pins) · **RF-51-5** (the coverage clause is load-bearing on the guarded `-update-baseline` path) · **RF-51-6** (positional parameter lists as the new bag). All in ADR 0020.
- **Earlier-round forward items (delivered)** — see [`2026-09-18.md`](docs/archives/status/2026-09-18.md).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**; round-047 items (b)–(d) (third-party application-tier imports outside RULE-E; the sanctioned set re-ruling; custom build-tag imports out of scope).
- **PM follow-ups** — none open.

## Environment notes

- **Round-052 (session 26)** — branch `052-ride-alongs-and-records` (single PR [#117](https://github.com/gosharplite/tellme/pull/117) → `dev` `f204aaa`; certified fold head `9f2a688`); **merged + deleted (local + remote)** after merge. Host: **darwin/arm64** this session (host/arch not load-bearing; Go toolchain **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (RULE-A/B/C **0**; **RULE-E baseline 0**, 0 new / 0 stale; **0** cycles; RULE-F coverage green with an **empty** table; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the godog **E2E**; 24 pkgs `ok`) · `go.mod`/`go.sum` unchanged · no new Gherkin/DSL row; witnesses (a) ctor / (b) cursor-reset / (c) ratchet / (d) E2E-wiring reproduced + reverted. **Review chain**: review `5730498811` (APPROVE WITH REQUIRED FOLDS) → fold `56c7167` (F-52-1/F-52-2 + RF-52-1/2/3 + nits) → fold-verification `5730602144` (F-52-3 + N-52-1) → fold `dc0fa25` → ledger `9f2a688` → **FINAL CERTIFICATION** `5730678971` — **MERGE-READY, review loop CLOSED**. **Propagated `dev → main`** (no-ff) at this closeout.
- **Round-051/050/049 (sessions 25/24/23)** — env detail is in the archive ([`2026-09-18.md`](docs/archives/status/2026-09-18.md)).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here — a STATUS-level head pin goes born-stale on the next commit: the `1d36509` measured-claim precedent.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
