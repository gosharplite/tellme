# tellme — Status

**Last updated**: 2026-09-18 (session 22 closeout — round **048** `048-cli-tui-prompt-decoupling` **DELIVERED / FROZEN** — **R5.2** of [#101](https://github.com/gosharplite/tellme/issues/101) (R5 of [#92](https://github.com/gosharplite/tellme/issues/92)); anchor [#101](https://github.com/gosharplite/tellme/issues/101); the **first de-coupling slice**: the residual edge `internal/cli → internal/ui/tui/prompt` is inverted into an injected **domain port** (`internal/domain/tui.Prompter`, satisfied by an `internal/ui` adapter, wired at `cmd/tellme`); the **RULE-E** baseline ratchets **3 → 2**; **review-deferral F-4 CLOSED**; behaviour-preserving; **ADR 0017**; PR [#111](https://github.com/gosharplite/tellme/pull/111) human-merged into `dev` `822e171` (by `thptcnec`). Pipeline: `/axb-specify` ✅ · `/axb-clarify` ✅ (**Q1 → B** the TUI-prompt edge · **Q2 → A** the port lives in `internal/domain/**` · **Q3 → A** F-4 folded in) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (**ADR 0017** + `techstack.md` MODIFY ×3) · `/axb-system-analysis` ✅ (0 interfaces; api/data/dsl-refine NOOP) · `/axb-tasks` ✅ (T001–T012) · `/axb-implement` ✅ (all `[X]`; witnesses a/b/c reproduced+reverted) · **PR [#111](https://github.com/gosharplite/tellme/pull/111) MERGED**. **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation).)
**Active branch**: `049-cli-agent-decoupling` (round 049 **implementation delivered** — all `[X]`; **PR [#112](https://github.com/gosharplite/tellme/pull/112) OPEN** awaiting human merge)

## Round in flight — 049 `049-cli-agent-decoupling` (IMPLEMENTATION DELIVERED — all tasks `[X]`; **PR [#112](https://github.com/gosharplite/tellme/pull/112) OPEN**)

- **Theme**: **R5.3** de-coupling of [#101](https://github.com/gosharplite/tellme/issues/101) (R5 of [#92](https://github.com/gosharplite/tellme/issues/92)) — the residual RULE-E edge **`internal/cli → internal/agent`**. **Re-cut** per ADR 0017 §Forward (the edge is *"the deepest slice"*, **not** edge-sized: 4 crossing identifiers over 2 files).
- **Clarify round 1 (CLOSED)**: **Q1 → B** re-cut into ordered sub-slices → this round = **sub-slice 1** (extract the loop's crossing *contracts* to `internal/domain/**`); **sub-slice 2** = invert the `AgentLoop` construction/execution (the round that drops the edge). **Q2 → (i)** extract **all three** contracts (turn-result + incomplete-turn error + the `ToolDefs` projection) → `internal/domain/agent`; CLI production `→ agent` references drop **4 → 1** (`AgentLoop`). **Q3 → (a)** `internal/agent` **references** the domain contracts directly (**no alias**).
- **Baseline note (as designed)**: this round does **NOT** move the RULE-E baseline — `tools/arch/baseline.txt` is **byte-identical (2)**; the baseline-moving round is sub-slice 2 (**2 → 1**).
- **Delivered (implementation)**: `internal/domain/agent/result.go` (NEW — `Result` + `ErrIncomplete` + `ToolDefs`; RULE-C-pure) · `internal/domain/agent/result_test.go` (NEW — pins) · `internal/agent/agentloop.go` (local decls deleted; `agentport.Result`/`ErrIncomplete`/`ToolDefs`) · internal/agent `{agentloop,callhooks,usage_calls}_test.go` (repointed) · `internal/cli/call_renderer.go` (import dropped; `agentport.*`) · `internal/cli/cli.go` (`*agentport.ErrIncomplete`; keeps `agent.AgentLoop`).
- **Verification**: `make verify` **OK** (RULE-E **0 new / 0 stale**, baseline **2 byte-identical**; RULE-A/B/C 0; 0 cycles; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the godog E2E) · `gofmt`/`go vet` clean · **identifier count = 1** (`agent.AgentLoop`) · falsifiability witnesses (a)/(b)/(c) reproduced + reverted · `go.mod`/`go.sum` unchanged.
- **Artifacts**: `specs/plans/049-cli-agent-decoupling/**` (`spec.md` · `checklists/requirements.md` · `research.md` D1–D10 · `plan.md` · `tasks.md` T001–T011 all `[X]` · `truth-delta.md`) · **ADR 0018** (`docs/decisions/0018-cli-agent-contracts-extraction.md` + index row) · `specs/truth/techstack.md` MODIFY ×2.
- **Pipeline position**: `/axb-specify` ✅ · `/axb-clarify` ✅ (Q1→B / Q2→i / Q3→a) · `/axb-spec-by-example` **NOOP** · `/axb-technical-research` ✅ (D1–D10 + ADR 0018 + `techstack.md` MODIFY ×2 + NOOP ×3) · `/axb-system-analysis` ✅ (0 interfaces; no waves; api/data/dsl-refine NOOP; `plan.md`) · `/axb-tasks` ✅ (T001–T011; orphan sweep 0) · `/axb-implement` ✅ (all `[X]`; verify green; witnesses reproduced+reverted) · **PR [#112](https://github.com/gosharplite/tellme/pull/112) OPEN** (branched `049-cli-agent-decoupling`; awaiting human merge → then propagate `dev → main` at closeout).

## Delivered round — 048 `048-cli-tui-prompt-decoupling` (DELIVERED / FROZEN — PR [#111](https://github.com/gosharplite/tellme/pull/111) merged into `dev` `822e171`; R5.2 of [#101](https://github.com/gosharplite/tellme/issues/101); anchor [#101](https://github.com/gosharplite/tellme/issues/101))

- **Theme**: `#92` **AC2**'s second clause — *"`internal/cli` depends only on `internal/domain/*`, stdlib, and application utilities"* — advanced by the **first de-coupling slice**: remove the RULE-E residual edge **`internal/cli → internal/ui/tui/prompt`** (ADR 0016) by inverting the CLI's call into the interactive TUI prompt into an **injected domain port**. Baseline **3 → 2**; behaviour-preserving; **ADR 0017**; **F-4 closed**.
- **Locked decisions (Q1–Q3)**: **Q1-B** slice = the TUI prompt edge (the smallest, already behind a seam) · **Q2-A** the port lives in `internal/domain/**` (`internal/domain/tui`: `Source` + `Prompter`, stdlib-only, RULE-C-pure) · **Q3-A** F-4 folded in (delete `cli.Options.RunTUIPrompt` + the unexported `tuiPromptRunner` func type).
- **Artifacts**: `internal/domain/tui/prompter.go` (NEW port) · `internal/ui/tuiprompt.go` + `tuiprompt_test.go` (NEW adapter + pin) · `internal/cli/cli.go` (CHANGED — `Options{Deps; Prompter}`; port routed; nil-default removed; `Options.Validate()`) · `cmd/tellme/deps.go` (wires `ui.TUIPrompter{}`) · `internal/cli/{testdeps,tui_dispatch,tui_submit_chrome,tui_debounce}_test.go` (fake port + debounce-sentinel pin) · `cmd/tellme/deps_test.go` (composition-root wiring assertion) · `tools/arch/baseline.txt` (3 → 2) · **ADR 0017** + index (0013 D2 narrowed) · `specs/truth/techstack.md` MODIFY ×3 · `specs/plans/048-cli-tui-prompt-decoupling/**`.
- **Verification**: `make verify` **OK** (`verify-architecture`: RULE-E **0 new / 0 stale**, baseline **2**; RULE-A/B/C **0**; 0 cycles; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~61 s godog E2E) · `gofmt`/`go vet` clean · **3 falsifiability witnesses** (new edge · stale baseline line · nil-port loud failure) + a **review mutation campaign** (M1 debounce hard-code · M2 dropped `Prompter` · M3 mirror-fake · M4/M4b source-drift) reproduced then reverted; `go.mod`/`go.sum` unchanged.
- **Review**: PR #111 architectural review `5727635484` — **APPROVE WITH REQUIRED FOLDS (no blocker)** → folded **`36493a2`** (F-1 stale `cli.go` comment · F-2 test doc names the port · F-3 ADR 0013 **D2** narrowed + index annotation · F-4 `Options.Validate()` + composition-root assertion + ADR §Forward `Kind()==reflect.Func` blindness · F-5 package state reconciled · TD-1 debounce sentinel + `TestTUIDebounceDurationResolution` · TD-2 class-phrase-reuse divergence recorded · REFACTOR-1/2 ADR §Consequences costs · N-1 conversion pin · §5 sizing caution · §6 durable-home) → fold review `5727735491` — **ALL TEN ITEMS VERIFIED AS BEHAVIOUR (mutation campaign), review loop CLOSED, cleared for human merge**.
- **Merge + propagation**: PR [#111](https://github.com/gosharplite/tellme/pull/111) **human-merged** into `dev` (`822e171`, by `thptcnec`); remote + local round branch deleted after merge. **Propagated `dev → main` (no-ff)** at this closeout.
- **Forward items**: the **2 remaining residual edges** (`cli → agent`, `cli → ui`) are the later **R5.x de-coupling slices** ([#101](https://github.com/gosharplite/tellme/issues/101)) — the `cli → ui` slice is **not edge-sized** (≈20 call sites · 3 crossing value types `ui.Pricing`/`ui.UsageCounts`/`ui.ToolUsageRow` · 4 stateful objects · 8 pure formatters → likely a re-cut: value types → `internal/domain/**` first; ADR 0017 §Forward) · **F-6/F-7/F-8** ([#101](https://github.com/gosharplite/tellme/issues/101)); **F-4 CLOSED** · `Options.Validate()`'s predicate note (`Kind()==reflect.Func` blind to interface seams; ADR 0017 §Forward) · the `internal/ui` adapter home pulls the Bubble Tea family transitively (ADR 0017 §Consequences — re-home candidate).


**Fold ledger**: round-047 review (PR [#110](https://github.com/gosharplite/tellme/pull/110)) `d5ba1ae` → `4c20c98` → `a546c89` (F-1…F-3 · TD-1…TD-5 · N-1…N-3 · N-1′/N-2′ + durable-home) · **[round-048](https://github.com/gosharplite/tellme/pull/111) review PR [#111](https://github.com/gosharplite/tellme/pull/111)** `e9c9ea2` → **`36493a2`** (F-1…F-5 · TD-1/TD-2 · REFACTOR-1/2 · N-1 + §5/§6); fold review `5727735491` **verified all ten items (mutation campaign), loop CLOSED**. *(Convention, TD-6: this line names fold heads + prior ledgers only — a ledger commit cannot name its own SHA; the ledger SHAs `8684941`/`d39cfb1` are carried in the PR comments.)*
**Daily log**: [`docs/session-summary/2026/09/18/session-summary.md`](docs/session-summary/2026/09/18/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (rounds 041–046 detail + the 043/044/046 branch-model rows + **the round-047 detail + its branch-model row + the round-046 env note**, relocated at the round-048 closeout).

> **Split note (Rule 12)**: on this closeout the **round-047 delivered-round detail + its branch-model row** and the **round-046 environment note** were relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 048 is now the most recently delivered round), keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`, plan+truth) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, merge; head `61414fc`) |
| 043 | `043-implement-hermetic-make-go-env` (single PR) | PR [#99](https://github.com/gosharplite/tellme/pull/99) (`a2fbafc`, merge; head `d007b75`) |
| 044 | `044-composition-root-extraction` (plan) + `044-implement-composition-root-extraction` (impl) | PR [#102](https://github.com/gosharplite/tellme/pull/102) (`4fd57fc`, plan+truth+grill fold) · PR [#104](https://github.com/gosharplite/tellme/pull/104) (`8da0b88`, merge; head `ba64508`) |
| 045 | `045-yield-policy-owner` (single PR) | PR [#106](https://github.com/gosharplite/tellme/pull/106) (`9e03a91`, merge; head `ab2c7fb`; folds `ef0c7ec`/`ab2c7fb`) |
| 046 | `046-blank-reason-owner-and-presentation-decoupling` (single PR) | PR [#109](https://github.com/gosharplite/tellme/pull/109) (`8ca4758`, merge; folds `8a39fc0`/`91e1e23`/`a96fa20`/`85fa321`) |
| 047 | `047-application-import-ceiling-gate` (single PR) | PR [#110](https://github.com/gosharplite/tellme/pull/110) (`6711e0a`, merge; folds `4c20c98`/`a546c89`; head `d39cfb1`) |
| 048 | `048-cli-tui-prompt-decoupling` (single PR) | PR [#111](https://github.com/gosharplite/tellme/pull/111) (`822e171`, merge; fold `36493a2`; head `36493a2`) |

Per-round detail lives in the archives; **048 is the most recently delivered round; no round is in flight** (the next round `049-*` opens off `dev`).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` (**no-ff**) at this closeout — round 048 propagation **DONE** | Stable / released line |
| `dev` | round 048 **delivered** (`822e171`, PR [#111](https://github.com/gosharplite/tellme/pull/111)) | Integration line (round work lands here before `main`) |
| `048-cli-tui-prompt-decoupling` | **merged into `dev`** (PR [#111](https://github.com/gosharplite/tellme/pull/111) → `822e171`); **branch deleted (local + remote)** after merge | Round 048's single branch — the **first de-coupling slice** (**R5.2** of [#101](https://github.com/gosharplite/tellme/issues/101)): `internal/cli → internal/ui/tui/prompt` inverted into the injected **domain** `internal/domain/tui.Prompter` port (+ an `internal/ui` adapter + `cmd/tellme` wiring); RULE-E baseline **3 → 2**; **F-4 closed**; **ADR 0017**. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–046 — **DONE (no-ff)** (round 043 `4ce056c`; round 044 `ebaeebc`; round 045 `ad407cf`; **round 046 — DONE**; **round 047 — DONE (no-ff, `e47dbc4`)**); **round 048 — DONE (no-ff)** at this closeout. (The round-043/044 + round-046 branch-model rows are archived in [`2026-09-18.md`](docs/archives/status/2026-09-18.md).)
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–041** | — | Provider-registry completeness → … → resolver test load tolerance. | ✅ **Delivered** (see the archives) |
| **042** | [#93](https://github.com/gosharplite/tellme/issues/93) | **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)** — layer-discipline gate (`verify-architecture`) + violation baseline (**8**); tooling/truth only; **ADR 0011**. | ✅ **Delivered** — PR [#95](https://github.com/gosharplite/tellme/pull/95) merged into `dev` (`f4b53f6`) |
| **043** | [#96](https://github.com/gosharplite/tellme/issues/96) | A **hermetic `make` Go-toolchain invocation environment** — one top-of-`Makefile` boundary (criterion-derived: neutralise the build context, preserve the plumbing); **tooling/truth only**; **ADR 0012**. | ✅ **Delivered** — PR [#99](https://github.com/gosharplite/tellme/pull/99) merged into `dev` (`a2fbafc`) |
| **044** | [#100](https://github.com/gosharplite/tellme/issues/100) | **R2 of [#92](https://github.com/gosharplite/tellme/issues/92)** — composition-root extraction (composition root → `cmd/tellme`; domain-typed `internal/app/deps.Dependencies` + `cli.Options`; 7 `cli → infrastructure` imports → 0; MCP discovery → `internal/infrastructure/mcp`); **behaviour-preserving**; **ADR 0013**. | ✅ **Delivered** — PR [#104](https://github.com/gosharplite/tellme/pull/104) merged into `dev` (`8da0b88`); baseline 8 → 1 |
| **045** | [#105](https://github.com/gosharplite/tellme/issues/105) | **R3 of [#92](https://github.com/gosharplite/tellme/issues/92)** — yield-policy owner (`internal/ui` `YieldController`) + `LoopObserver` hook split (`YieldIndicator`/`RestoreIndicator`); behaviour-preserving; **ADR 0014**. | ✅ **Delivered** — PR [#106](https://github.com/gosharplite/tellme/pull/106) merged into `dev` (`9e03a91`); baseline stays **1** |
| **046** | [#108](https://github.com/gosharplite/tellme/issues/108) | **R4 of [#92](https://github.com/gosharplite/tellme/issues/92)** — blank-reason predicate single owner + the loop's presentation de-coupling (an injected `agentport.ToolLineRenderer` port; the dead tail guard deleted); removes the last baseline entry (`internal/agent → internal/ui`), **1 → 0**; behaviour-preserving; **ADR 0015**. | ✅ **Delivered** — PR [#109](https://github.com/gosharplite/tellme/pull/109) merged into `dev` (`8ca4758`); **baseline 0** |
| **047** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.1 of [#101](https://github.com/gosharplite/tellme/issues/101)** — the **application import-ceiling gate** (**RULE-E**): an application tier (`internal/app/**`, `internal/cli`) may import only `internal/domain/**` + stdlib + `internal/config`/`internal/home` + `internal/app/**`; the sanctioned set is normative in the guard's tier table, **default-deny** + **fail-on-stale allow-list**; the **3** residual edges (`cli → {agent,ui,ui/tui/prompt}`) are baselined; tooling/truth only; **ADR 0016**. | ✅ **Delivered** — PR [#110](https://github.com/gosharplite/tellme/pull/110) merged into `dev` (`6711e0a`); baseline RULE-A/B/C **0** + RULE-E **3** |
| **048** | [#101](https://github.com/gosharplite/tellme/issues/101) | **R5.2 of [#101](https://github.com/gosharplite/tellme/issues/101)** — the **first de-coupling slice**: invert the residual edge `internal/cli → internal/ui/tui/prompt` into an injected **domain** port (`internal/domain/tui.Prompter`, satisfied by an `internal/ui` adapter, wired at `cmd/tellme`); RULE-E baseline **3 → 2**; **F-4 closed**; behaviour-preserving; **ADR 0017**. | ✅ **Delivered** — PR [#111](https://github.com/gosharplite/tellme/pull/111) merged into `dev` (`822e171`); baseline RULE-E **2** |
| **future slices** | [#92](https://github.com/gosharplite/tellme/issues/92) (ride-alongs) · [#101](https://github.com/gosharplite/tellme/issues/101) (**R5.x** de-coupling: the 2 remaining edges `cli → {agent,ui}` + F-6/F-7/F-8) · [#91](https://github.com/gosharplite/tellme/issues/91) · [#13](https://github.com/gosharplite/tellme/issues/13) · [#103](https://github.com/gosharplite/tellme/issues/103) | Ride-alongs (suggestion-selection policy; `BindToolOutput` ctor injection); the remaining **R5.x** de-coupling slices (the `cli → ui` slice is **not edge-sized** — ADR 0017 §Forward); the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91) — replaced #60); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)); the `-l`/`-c` + `-t` plumbing gap ([#103](https://github.com/gosharplite/tellme/issues/103)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 22 closeout)** — **[#101](https://github.com/gosharplite/tellme/issues/101) OPEN** — R5 is a **programme**: **R5.1 (the RULE-E gate) DELIVERED** by round 047 and **R5.2 (the `cli → ui/tui/prompt` edge) DELIVERED** by round 048 (PR [#111](https://github.com/gosharplite/tellme/pull/111) merged `822e171`; baseline **3 → 2**; **F-4 closed**; body carries the delivery record); the **remaining 2 edges** (`cli → {agent,ui}`) + **F-6/F-7/F-8** remain. **[#92](https://github.com/gosharplite/tellme/issues/92) OPEN** — R1–R4 delivered; ride-alongs remain. **[#103](https://github.com/gosharplite/tellme/issues/103)** · **[#91](https://github.com/gosharplite/tellme/issues/91)** · **[#13](https://github.com/gosharplite/tellme/issues/13)** — all OPEN (accurate). **[#93](https://github.com/gosharplite/tellme/issues/93)/[#96](https://github.com/gosharplite/tellme/issues/96)/[#100](https://github.com/gosharplite/tellme/issues/100)/[#105](https://github.com/gosharplite/tellme/issues/105)/[#107](https://github.com/gosharplite/tellme/issues/107)/[#108](https://github.com/gosharplite/tellme/issues/108) CLOSED** (earlier). No closes/revisions needed this closeout (round 048 delivered a slice of an already-open programme).
- **Round-048 forward items (delivered)** — (a) **#101** stays OPEN (R5.2 of a programme; its body carries the delivery record + F-4 CLOSED); (b) the **2 remaining residual edges** `cli → {agent, ui}` are the later **R5.x de-coupling slices**; the **`cli → ui` slice is not edge-sized** (≈20 call sites · 3 crossing value types · 4 stateful objects · 8 formatters; ADR 0017 §Forward — likely a re-cut); (c) **F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101); (d) `Options.Validate()` is called by the composition-root smoke test, not `main` (boundary choice recorded; its `Kind()==reflect.Func` predicate is blind to interface seams — ADR 0017 §Forward); (e) the `internal/ui` adapter home pulls the Bubble Tea family transitively (ADR 0017 §Consequences — leaf `internal/ui/tuiprompt/` re-home candidate); (f) the nil-port class-phrase reuse is a recorded divergence (ADR 0017; in-code marker).
- **Round-047 forward items (delivered)** — (a) **#101** stays OPEN; (b) third-party application-tier imports are **outside RULE-E** (a **live residual**: `internal/cli` imports `spf13/pflag` + `golang.org/x/term`; ADR 0016 D6); (c) the sanctioned set may be **re-ruled** (ADR 0016 §Forward) + the optional **per-rule row split** (ADR 0016 §Forward item); (d) custom build-tag-gated imports out of scope (ADR 0011 D6).
- **Round-046 forward items (delivered)** — (a) **#108 CLOSED**; (b) the `Lines` **wire** has no unit-level carrier — a missing/false injection is caught only by the godog E2E; a `runTurn`-level assertion is the recorded follow-up; (c) `ToolReasonRenders` stays **test-facing**; (d) ADR 0015 *Consequences*: the discarded-render cost, the port-shape leak, and the **no-release-valve** policy at baseline 0.
- **Round-045 forward items (delivered)** — (a) **#105 CLOSED**; (b) the `YieldController` is the single yield entry point — a future yield primitive outside the owner would re-scatter the policy (ADR 0014 Edge Case); (c) the `AdmitResume` unexport + the two `#92` records (one-concurrent-block; `End`-while-write-stalled) stay parked (→ #92/#101).
- **Round-044 forward items (delivered)** — (a) **#100 CLOSED**; (b) the baseline's last entry (`internal/agent -> internal/ui`) is **R4**'s to remove (1 → 0) — R3 left it, correctly; (c) **F-4/F-6/F-7/F-8** → [#101](https://github.com/gosharplite/tellme/issues/101) (recorded on its body); (d) `cmd/tellme` is exempt from the tier table — the "single assembly site" property is upheld by review discipline, not a machine check (a candidate RULE-E → #101/#92); (e) `internal/cli`'s legitimate downward imports (`agent`/`ui`/`config`/`home`/`app`) remain (→ #101).
- **Round-043/042 forward items (delivered)** — see [`2026-09-18.md`](docs/archives/status/2026-09-18.md).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**.
- **PM follow-ups** — none open.

## Environment notes

- **Round-048 (session 22)** — branch `048-cli-tui-prompt-decoupling` (single PR [#111](https://github.com/gosharplite/tellme/pull/111) → `dev` `822e171`); **merged + deleted (local + remote)**. Host: **linux/amd64** (Go **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`: RULE-E **0 new / 0 stale**, baseline **2**; RULE-A/B/C **0**; 0 cycles; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~61 s godog **E2E**) · `Makefile`/`go.mod`/`go.sum` unchanged · 3 falsifiability witnesses + a review mutation campaign (M1–M4b) reproduced + reverted. **Propagated `dev → main`** (no-ff) at this closeout.
- **Round-047 (session 21)** — branch `047-application-import-ceiling-gate` (single PR [#110](https://github.com/gosharplite/tellme/pull/110) → `dev` `6711e0a`); **merged + deleted (local + remote)**. Host: **linux/amd64** (Go **1.26.6**). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`: RULE-A/B/C **0** + RULE-E **3** baselined, 0 new / 0 stale / 0 cycles; lint 0; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green (incl. the ~63 s godog **E2E**) · `Makefile`/`go.mod`/`go.sum` unchanged · 4 falsifiability witnesses + a mutation campaign reproduced + reverted. **Propagated `dev → main`** (no-ff, `e47dbc4`). Round-046 env detail is in the archive.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here — a STATUS-level head pin goes born-stale on the next commit: the `1d36509` measured-claim precedent.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
