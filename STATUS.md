# tellme — Status

**Last updated**: 2026-09-17 (session 6: **round 039 `039-terminal-safe-lines-and-turn-spacing` — DELIVERED / FROZEN**). Implementation PR [#81](https://github.com/gosharplite/tellme/pull/81) **merged** into `dev` (`d48ebbc`, by `thptcnec`); frozen round head `ca14276` (10 commits). Round 039 folds **two workstreams** into one package (the round-038 #78+#76 precedent): **(a)** closes [#80](https://github.com/gosharplite/tellme/issues/80) — the terminal-control sanitization becomes a **single-owned** `internal/ui` policy (`sanitize.go`) applied by **every** `[Tool …]` formatter (`[Tool Reason]`/`[Tool Result]`/`[Tool Action]` keys **and** values, like `[Tool Output]`); the order is fold+trim → sanitize → rune cap (caps bound the *visible* output); the block's neutral-close restore stays `[Tool Output]`-scoped; an escape-only reason emits no row; **(b)** the operator's **blank-line grouping** request — one blank before **each** call's begin block (per call), one blank before the grouped tail `[Tool Reason]` block (none inside), one blank before the post-status group **on a tool-using turn only**. Full AIxBDD pipeline (specify → spec-by-example → technical-research → system-analysis → dsl-refine → tasks → implement) then a **four-round review/fold chain to FINAL CERTIFICATION** (B1 · TD-1..TD-4 · RF-1..RF-3 · the rename/hardening). Truth: `techstack.md` (the **Agent tool loop** row + the `execute_command` sibling note closed) + `watching-the-tool-loop.feature` (two new Rules) + `presenting-the-post-turn-status.feature` (a tool-less negative Example) + `chat/dsl.md` (+1 Given · +8 Then · a round-039 note); `/axb-api-plan` + `/axb-data-plan` **NOOP**; `/axb-ui-plan` skipped. Governance: **ADR 0008** (`docs/decisions/0008-terminal-safe-lines-and-blank-line-grouping.md`, **supersedes ADR 0007**). Open issues: **#69** · **#60** · **#13**. **Propagation: `dev → main` DONE (no-ff).**
**Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation this phase)
**Active branch**: `dev` (round 039 delivered/frozen; the next round starts a fresh `040-*` off `dev`)
**Daily log**: [`docs/session-summary/2026/09/17/session-summary.md`](docs/session-summary/2026/09/17/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–038).

> **Split note (Rule 12)**: the **round-038** detail section was relocated **verbatim** into [`2026-09-17.md`](docs/archives/status/2026-09-17.md) when round 039 became current (joining rounds 035–037), keeping `STATUS.md` to a single current-round section. `STATUS.md` remains lean (one current-round section) — no further split needed this closeout.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |

Per-round detail lives in the archives (001–002 in [`2026-09-11.md`](docs/archives/status/2026-09-11.md); 003–012 in [`2026-09-13.md`](docs/archives/status/2026-09-13.md); 013–019 in [`2026-09-14.md`](docs/archives/status/2026-09-14.md); 020–026 in [`2026-09-15.md`](docs/archives/status/2026-09-15.md); 027–034 in [`2026-09-16.md`](docs/archives/status/2026-09-16.md); 035–038 in [`2026-09-17.md`](docs/archives/status/2026-09-17.md)); **039 is the most recent delivered round.**

## Current round — 039 `039-terminal-safe-lines-and-turn-spacing` (DELIVERED / FROZEN)

**039-terminal-safe-lines-and-turn-spacing** — two folded workstreams on the `stderr`-bound diagnostic surface. **(a) #80 — generalize the terminal-safe-line policy:** `sanitizeControl` + its helpers (`isControlRune`, `escSequenceLen`, `csiLen`, `oscLen`, `genericEscLen`, the per-kind windows CSI 128 / OSC 1024) **moved verbatim** into a dedicated single-owned `internal/ui/sanitize.go` and are now applied by **every** `[Tool …]` formatter — `FormatToolReason`, `FormatToolResult`, and `FormatToolAction` (argument **keys and values**) are control-free like `[Tool Output]` (byte-identical, round-038 pins intact). Order = **fold + trim → sanitize → rune cap**, so the caps (`{189, 200, 200}`) bound the **visible** output; the block's neutral-close restore stays `[Tool Output]`-scoped; an escape-only reason emits **no** row (`ui.ToolReasonRenders` reads the sanitized value, derived from `toolReasonText`); the `[Tool Action]` key path now folds and is **sorted after sanitizing** (a hostile key cannot render out of ascending order). **(b) Blank-line grouping (operator request):** one blank before **each** call's begin block (the `[Tool Reason]` line, else the `[Tool Action]` line — per call); one blank before the grouped tail `[Tool Reason]` block (no blanks inside it); one blank before the post-status group (measured `Payload:` + metrics + `Ready`) — **on a turn that rendered a tool round only** (a tool-less turn is unchanged; gated by a `renderedToolRound` marker set on a non-final `OnCallEnd`). Both are **recorded divergences** from the reference. Truth: `techstack.md` (Agent tool loop row MODIFY + the `execute_command` sibling-scope note closed) + `watching-the-tool-loop.feature` (2 new Rules) + `presenting-the-post-turn-status.feature` (1 tool-less Example) + `chat/dsl.md` (+1 Given · +8 Then · note); `/axb-api-plan` + `/axb-data-plan` NOOP; `/axb-ui-plan` skipped. Witnesses: hostile-fixture unit pins (`toolcall_sanitize_test.go`, `agentloop_spacing_test.go`, `call_renderer_reason_test.go`) + 8 new E2E Examples. Governance: **ADR 0008** (supersedes **ADR 0007**). `make verify` **OK**; E2E **226/226** (was 218). Merged via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`); frozen head `ca14276` (10 commits).

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` | Stable / released line |
| `dev` | round 039 delivered (`d48ebbc`) | Integration line (round work lands here before `main`) |
| `038-tool-output-sanitize-and-empty-submit` | **merged into `dev`** (frozen) | Round 038's working branch — merged via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`). |
| `039-terminal-safe-lines-and-turn-spacing` | **merged into `dev`** (frozen) | Round 039's working branch — merged via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`). |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–039 — **DONE (no-ff)**.
> Read live heads with `git rev-parse --short main dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–039** | — | Provider-registry completeness → … → terminal-safe `[Tool …]` lines + turn-output blank-line grouping. | ✅ **Delivered** (see the archives) |
| **future slices (candidates)** | [#69](https://github.com/gosharplite/tellme/issues/69) · [#60](https://github.com/gosharplite/tellme/issues/60) · [#13](https://github.com/gosharplite/tellme/issues/13) | the **composition root + single-ownership** refactor ([#69](https://github.com/gosharplite/tellme/issues/69)); the **dogfooding track** ([#60](https://github.com/gosharplite/tellme/issues/60)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Round-039 forward items (delivered)** — (a) the loop's **control flow** now depends on a `ui` presentation predicate (`ui.ToolReasonRenders`) and the blank-reason predicate is **extended** (≈3×/call), not consolidated → **[#69](https://github.com/gosharplite/tellme/issues/69)**; (b) the **missing layer-discipline gate** in `make verify` (no `verify-architecture` analogue) → **[#69](https://github.com/gosharplite/tellme/issues/69)**; (c) sanitizing argument **keys** is a small extension over #80's literal "values" (recorded in ADR 0008 residuals); (d) **#80 CLOSED** this closeout (delivered by PR [#81](https://github.com/gosharplite/tellme/pull/81)).
- **Round-039 governance** — **ADR 0008** (`docs/decisions/0008-terminal-safe-lines-and-blank-line-grouping.md`) records the generalized terminal-safe-line policy (single-owned; all four `[Tool …]` formatters; sanitize-before-cap), the `[Tool Output]`-scoped neutral close, and the two recorded reference divergences (the reference sanitizes nothing and writes the lines densely); it **supersedes ADR 0007** (whose scope boundary this round closes; 0007's `Status` flips to `Superseded by 0008`).
- **Round-038 forward items** — (a) the sanitizer is deliberately conservative + **window-bounded** (CSI 128 / OSC 1024; a beyond-window/unterminated sequence prints the rest of the line, bounded by the block's byte budget) — recorded in **ADR 0007**/`research.md`; (b) the `[Tool Output]` block shows command output as **plain text**; (c) the broader terminal-safe line policy → **CLOSED** (delivered by round 039 via [#80](https://github.com/gosharplite/tellme/issues/80)).
- **Round-037 forward items** — (a) the **empty-`Ctrl+S` divergence** → **[#76](https://github.com/gosharplite/tellme/issues/76)** **CLOSED** (delivered by round 038); (b) the "a refresh resets the selection" policy hardcoded inside `suggester.set` (the reference names it at the call site) → **[#69](https://github.com/gosharplite/tellme/issues/69)**.
- **Round-036 forward items** — (a) the blank-reason suppression predicate has **three sites**, one **provably dead on the production path** → **single ownership** on [#69](https://github.com/gosharplite/tellme/issues/69); (b) the **permanent E2E narrowing** (the `\n`/`\r` reason-collision class has no E2E carrier) — recorded on [#69](https://github.com/gosharplite/tellme/issues/69) **and** [#74](https://github.com/gosharplite/tellme/issues/74); (c) `oneLine` relocated into `toolcall.go` (orphan `toollog.go` deleted).
- **Round-035 forward items** — the observer port hook overload + the spinner-yield-policy ownership (three homes / four call sites) → [#69](https://github.com/gosharplite/tellme/issues/69).
- **Round-034 forward items** — the failed-turn **display-only `Ready` overstatement** (G2) + the **numbering skew**; `BindToolOutput` ctor injection → [#69](https://github.com/gosharplite/tellme/issues/69#issuecomment-5698460385); `LoopObserver` segregation (would **supersede ADR 0005 G1**); the round-022 row→feature audit blind spot → [#60](https://github.com/gosharplite/tellme/issues/60#issuecomment-5697192786).
- **Round-033/032 forward items** + **older rounds 018–031** — live per-round in the archives.
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items.
- **Issue tracker (2026-09-17 closeout, session 6)** — [#80](https://github.com/gosharplite/tellme/issues/80) **CLOSED (completed)** — the terminal-safe line policy for the sibling `[Tool …]` formatters, delivered by round 039 (PR [#81](https://github.com/gosharplite/tellme/pull/81) merged `d48ebbc`); [#69](https://github.com/gosharplite/tellme/issues/69) **OPEN** — body carries the round-039 items (loop-control-flow-on-`ui`-predicate, ≈3×/call predicate, missing layer-discipline gate); [#60](https://github.com/gosharplite/tellme/issues/60) open (dogfooding); [#13](https://github.com/gosharplite/tellme/issues/13) open (coverage tooling). No issues superseded this closeout.

## Environment notes

- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the merged round-039 head `ca14276`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Host (this session)**: **Linux** (`…/beta-niffler/ait-tellme`); the **darwin** path is the cross-compile weak spot here (round 020).
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps — **clean this closeout**.
