# tellme — Status

**Last updated**: 2026-09-26 (session 82 — **no round**; a **docs-only reference addition** on `dev` `5884082` — [`docs/model-specs.md`](docs/model-specs.md) + the `SESSION-BOOTSTRAP.md` **§7** / **Agent Rule 12** pointer. Earlier the same day: session 81 docs-only lint hygiene (`a49134c`). Last delivered round: **093** `093-interactive-prompt-seam-determinism` — PR [#193](https://github.com/gosharplite/tellme/pull/193) merged into `dev` `e7ec4a9`; propagation `dev -> main` **DONE (no-ff)**, tagged `round-093`.)
**Round in flight**: none — the next round opens off `dev` via `/axb-specify` (a theme from operator value or a live issue; Bootstrap Agent Rule 11).
**Active branch**: `dev` (== `origin/dev`). This session's docs-only commits (`5884082` + the closeout commit) are **propagated** `dev -> main` (**no-ff**, **untagged** — docs-only, not a round; ADR 0026 tags rounds only); the earlier 2026-09-26 docs-only commits are likewise **propagated**.
**Daily log**: [`docs/session-summary/2026/09/26/session-summary.md`](docs/session-summary/2026/09/26/session-summary.md) (sessions 81–82 — docs-only) · prior: [`2026-09-25`](docs/session-summary/2026/09/25/session-summary.md) (rounds 087–093)

## Last delivered round - 093 `093-interactive-prompt-seam-determinism` (DELIVERED / FROZEN - PR [#193](https://github.com/gosharplite/tellme/pull/193) merged into `dev` `e7ec4a9`, **merge commit**)

- **Theme (anchor issue [#191](https://github.com/gosharplite/tellme/issues/191), 2026-09-25; ADR 0063)**: make the **`-i` interactive-prompt E2E seam** **deterministic and faithful** — **test-seam only**, **no product change**. The #191 flake (round-082 **O-082-1** / round-091 **RES-091-FV-4**) had **two** root causes, both in the scripted-key seam: (A) the round-023 output-synchronized handshake gated the terminal key on the generic editor border `┌`, satisfied by any *partial* frame → the key coalesced with the rest of the composition (the flake); (B) the seam scripted Enter as a raw LF, which bubbletea decodes as `KeyCtrlJ` (the bubbles `textarea` binds **CR**/`enter` only) → the line break was silently dropped and the typed lines **joined** (`line oneline two`), masked by a **non-discriminating** substring assertion.
- **The change (test-seam only; records)**: `tests/e2e/harness/cmd_helper.go` (**`paintGate(compose, fallback)`** — the composition's last visible line; `runExecSynced` derives the gate; `┌` is only the compose-less fallback) · `tests/e2e/steps/tui_keys.go` (**`tuiKeyEnter = "\r"`** + `typeText` `\n → CR`) · `step_r038.go` (routes through `typeText`) · `step_t011_chat_then_keeps_typed_text.go` (**`joinedRow`** — each line on its own editor row) · three mechanism unit pins (`cmd_helper_test.go`, `tui_keys_test.go`, `step_t011_..._test.go`).
- **Truth/records**: **ADR 0063** (+ `docs/decisions/README.md` index) · `specs/truth/techstack.md` (*Interactive TUI prompt harness* row) · `specs/truth/features/cli/chat/presenting-the-interactive-prompt.feature` (header note + carrier comment) · `chat/dsl.md` (the `keeps the typed text` row's separate-rows clause + the CR note) · `docs/domain-model/**` **NOT** modelled (ADR 0041 escape hatch) · **no** Accepted-ADR body edited · no `.feature` step sentence changed (the sentence's **semantics** are strengthened) · `internal/**`/`cmd/**` and `go.mod`/`go.sum` **unchanged**.
- **The witness**: `make check` **OK** · E2E **330 scenarios · 2487 steps** (unchanged) · `make test-race` **no data races** · topology audit (advisory) **PASSED** (53 features · 21 root + 467 module rows · 2461 steps) · `modelith-check` no drift · product diff **empty**. **W-A** (gut `paintGate` → constant) ⇒ the `paintGate` pin red **and** the flake re-opens (measured ~62 % at 16-way concurrency, **authoring host** — the magnitude is host/load-specific) · **W-A′** (revert the **call site** `marker := paintGate(…)` → `marker := fallbackMarker`) ⇒ the pin stays **green**, only the (probabilistic) repetition witness reds — the two mutations are **not equivalent** · **W-B** (revert the Enter byte to LF) ⇒ the multi-line Example reds (joined row) **and** the `typeText` pin reds · **W-C** (drop the separate-rows clause) ⇒ the joined state passes (the `joinedRow` pin reds) — all reproduced then reverted. Fixed seam **0 red** (20/20 serial; 32/32 at 16-way).
- **Review chain (PR [#193](https://github.com/gosharplite/tellme/pull/193), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; all folds **record / carrier accuracy**: **F-093-1** the `paintGate` pin covers the gate **rule**, not its **call-site wiring** · **F-093-2** the determinism magnitudes are host/load-specific · **F-093-3** the STATUS Candidates self-contradiction; + **TD-093-1** · **N-093-1**/**N-093-2**) → fold `bc76745` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — **RES-093-FV-1**, the ADR 0063 index-row figure) → residual fold `d0f2c2d` → final verification (**`FOLDS VERIFIED — LOOP CLOSED`**, no residuals) → loop record + nit `d3b2064`.
- **Delivered**: PR [#193](https://github.com/gosharplite/tellme/pull/193) **human-merged** into `dev` (**merge commit** `e7ec4a9`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-093`**. **Closes [#191](https://github.com/gosharplite/tellme/issues/191)**.

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-25.md`](docs/archives/status/2026-09-25.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there; each closeout relocates the previous round's detail; **this closeout** relocated the **round-092 delivered-round detail + its env note** there (verbatim — never deleted).

**Delivered rounds**: rounds 001–093 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15; recorded by [ADR 0061](docs/decisions/0061-operator-declared-direction.md))** — no security · no Windows · **bash-first** · POSIX-only · a deliberately small tool surface (ADR 0061 is the direction's **authoritative home**; `README.md` summarises it).

**Delivered**: rounds 001-093 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: none pending; a new round opens off `dev` from operator value or a **live issue** (Bootstrap Agent Rule 11) — the tracker is **0 open** (round 093 closed [#191](https://github.com/gosharplite/tellme/issues/191)).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-093`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-092`** at its propagation merge; **`round-091`** at its propagation merge; **`round-090`** at its propagation merge; **`round-089`** at its propagation merge; **`round-088`** at its propagation merge; **`round-087`** at its propagation merge; **`round-086`** at its propagation merge; **`round-085`** at its propagation merge; **`round-084`** at its propagation merge; **`round-083`** at its propagation merge; **`round-082`** at its propagation merge; **`round-081`** at its propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; reconciled 2026-09-24 by round 085; anchor [#173](https://github.com/gosharplite/tellme/issues/173))** — the `axb-gherkin-and-dsl` topology script (`audit_feature_dsl_topology.py`) over `specs/truth/features/cli` reports **0 errors / 0 warnings** (53 features · 6 feature modules · **21** root + **467** module rows · 2461 steps; round 088 added the `## Given/Then (round 088)` rows). Round 085 reconciled the **11** errors (the pre-round-085 env-note figure "6" was stale — the live count was **11**): **5 cross-module scope** (rows promoted to the interface root) · **5 unparsed round-083 rows** (restored the missing table headings/headers) · **1 missing composite row** (added). The script is **not** a `make verify` member; an **advisory** check (`topology-audit-not-a-gate`) — reported, not a delivery gate. **Fix-forward**: keep each round's DSL rows inside a headed table (`## Given/Then (round NNN)` + the `DSL 句型` header) and keep cross-module rows at the interface root (the audit's parser only reads headed tables).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state**: six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Domain-model lint hygiene (2026-09-26, session 81; docs-only, `a49134c`)** — the round-086 `chrome-colour-terminal-gated` invariant backticked the bare placeholder `M` (in `[TOOLS] - M (N calls)`), which `modelith-lint` flagged as an undefined term; written **plain** (matching the sibling `N` convention) in `docs/domain-model/tellme.modelith.yaml` + the re-rendered `.md`. `modelith-lint` is now **0 errors / 0 warnings** (all three models); `modelith-check` + `modelith-drift` green; no modelled behaviour change; **`make verify` OK**. Landed directly on `dev` (docs-only hygiene, the 2026-09-23 precedent — not a round).
- **Round-093 (session 80, 2026-09-25)** — branch `093-interactive-prompt-seam-determinism` off `dev` `4723d3b`: specify + plan package `327db98` -> implementation `9b48841` -> PR [#193](https://github.com/gosharplite/tellme/pull/193) -> fold F-093-1/2/3 + TD-093-1 + N-093-1/2 `bc76745` -> residual fold RES-093-FV-1 `d0f2c2d` -> loop-closed record + nit `d3b2064` -> merge (merge commit) `e7ec4a9`. **Test-seam only** — the `-i` E2E seam made **deterministic + faithful**: (1) the round-023 handshake's paint gate is **content-aware** (`paintGate` = the composition's last visible line; `┌` is only the compose-less fallback); (2) a scripted line break is the terminal **Enter byte (CR)** (`tuiKeyEnter`/`typeText`), not LF (bubbletea reads LF as `ctrl+j`, which the textarea does not bind to insert-newline); (3) the `keeps the typed text` assertion is **discriminating** (`joinedRow` — each line on its own editor row). **ADR 0063**; `techstack.md` harness row; the `chat` feature note + `chat/dsl.md` row; `docs/domain-model/**` **not** modelled; **product diff empty** (no `internal/**`/`cmd/**`); **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold -> **`FOLDS VERIFIED — LOOP CLOSED`** (via WITH RESIDUALS). Host: **linux/amd64** (Go 1.26.6). **PR [#193](https://github.com/gosharplite/tellme/pull/193) human-merged** into `dev` (**merge commit**, `e7ec4a9`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-093`**; **closes [#191](https://github.com/gosharplite/tellme/issues/191)**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **330 scenarios · 2487 steps**) · `make test-race` no data races.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
- **Vendor API specs reference (2026-09-26, session 82; docs-only, `5884082`)** — [`docs/model-specs.md`](docs/model-specs.md) added: a **descriptive** (not truth; `specs/truth/**` wins) guide to fetching the **live** DeepSeek and Gemini/Vertex API specs and reproducing a call — the Gemini/Vertex **discovery documents**, DeepSeek's **HTML docs** (+ the OpenAI Chat Completions spec for its deltas), presence-only credential checks (**never print a secret**), and a reconcile-an-adapter recipe. `SESSION-BOOTSTRAP.md` gains **§7** (on-demand reference) + **Agent Rule 12** so a bootstrapped model knows it exists and, on a provider issue, checks the live spec (`test egress → fetch the live spec → reproduce the call`). Docs-only; **no** product / `specs/truth/**` / `docs/domain-model/**` change; the repo's offline/hermetic **test** posture is unchanged.
