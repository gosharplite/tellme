# tellme — Status

**Last updated**: 2026-09-24 (session 73 — round **085** `085-dsl-topology-reconciliation` **PR OPEN**: anchors [#173](https://github.com/gosharplite/tellme/issues/173) (the topology audit — reconciles **11 → 0** errors) + [#174](https://github.com/gosharplite/tellme/issues/174) (the round-032 R8d `make help` tidy-up). **Truth-only + one Makefile string; no product code.** Three unhomed surfaces were also opened as tracker records at the session start: [#171](https://github.com/gosharplite/tellme/issues/171) (ADR §Forward register) · [#172](https://github.com/gosharplite/tellme/issues/172) (`Not Introduced Yet` inventory) — plus #173/#174.)
**Round in flight**: **085** `085-dsl-topology-reconciliation` — branch `085-dsl-topology-reconciliation` off `dev` `2502133`; the pipeline is complete and the **PR is open** (awaiting a human review/merge). **DoD = close [#173](https://github.com/gosharplite/tellme/issues/173) + [#174](https://github.com/gosharplite/tellme/issues/174)**.
**Active branch**: `085-dsl-topology-reconciliation` (the round branch; merges to `dev` via the round PR — human-merged).
**Daily log**: [`docs/session-summary/2026/09/24/session-summary.md`](docs/session-summary/2026/09/24/session-summary.md) (round 085) · prior: [`2026-09-23`](docs/session-summary/2026/09/23/session-summary.md) (rounds 082–084)

## Last delivered round - 084 `084-rollback-durability-witness` (DELIVERED / FROZEN - PR [#170](https://github.com/gosharplite/tellme/pull/170) merged into `dev` `05b23e2`, **merge commit**; **ADR 0056**)

- **Theme (anchor issue [#169](https://github.com/gosharplite/tellme/issues/169), 2026-09-23)**: round 081 (ADR 0053) asserted a **durable/atomic** rollback (`temp file + fsync + atomic rename`) on **six live surfaces**, but only the temp+rename half was falsifiable — removing the file `fsync` (or `syncDir`) left the whole suite green (the `docs/archives/notes/2026-09-23-gaps-claim-without-a-tripwire.md` / `aixbdd-tmg#15` *claim-without-a-tripwire* class; the obligation is now upstream **ADR 0006**). The operator directed *"Open a new aixbdd round, the goal is to close #169."* Clarify **not escalated (0 questions)** — the goal is locked by the issue + ADR 0006.
- **The change**: `internal/infrastructure/history/file_store.go` gains an **unexported `durableFS` seam** (`Sync`/`Rename`/`SyncDir`, `os`-defaulted, nil-guarded); `writeRaw` routes its durability primitives through it. **No observable behaviour change** — production is the same `os` calls; the public `history.Store` surface is unchanged; `-b`/`--back` semantics, shapes, exit codes, and phrases are untouched (the exit-code set stays **ten**).
- **The witness (the missing tripwire)**: `file_store_sync_test.go` — a recording fake + `TestFileStore_Rollback_SyncsTempFileBeforeRename` (asserts the **order relation** `sync`-before-`rename` **and** that a dir-`fsync` event is invoked; **reddens** when the file `fsync` is removed, when the sync follows the rename, or when the dir-`fsync` call is dropped) + three more pins (sync-error aborts, dir-sync best-effort, survivor bytes).
- **Calibration & records**: the asserted effect is calibrated to the witnessed mechanism (drops the un-calibrated *crash mid-rollback* over-claim); the best-effort directory `fsync` is recorded **accepted-unwitnessed** (ADR 0056 §Forward RF-084-1…5); the misfiled behaviour guarantee leaves `techstack.md` (Rule 6) and the `history/dsl.md` prologue (truth-prose boundary); `docs/domain-model/**` invariant `history-rollback-removes-complete-turns` calibrated + re-rendered; **ADR 0056** (index + ADR 0053 `Status`/D3/Consequences pointer) and the archived notes file (`docs/archives/notes/2026-09-23-gaps-claim-without-a-tripwire.md`) §2/§7 updated; a **Claim→Witness ledger** in `tasks.md`.
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `make check` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile) · `go test -count=1 ./...` **green** (E2E **314 scenarios · 2354 steps — unchanged**) · `make test-race` green · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): drop-`SyncDir` ⇒ red · remove `Sync` ⇒ red · sync-after-rename ⇒ red · seam-bypass (`f.Sync()` direct) ⇒ red — each attributed to the pin's intended assertion.
- **Review chain (PR [#170](https://github.com/gosharplite/tellme/pull/170), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the folds were **falsifier/record accuracy**: **F-084-1** the ledger's CLM-084-3 falsifier was non-discriminating (a re-marshal mutant leaves `T005` green; the red is the pre-existing hand-filled pin) · **F-084-2** two issue-named surfaces still asserted the pre-calibration over-claim (ADR 0053 D3 + Consequences; the index row 0053); + TD-084-1, N-084-1/2/3) → fold `5b5accc` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — R-FV-084-1/2/3) → residual fold `8cf8b9c` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`** (+ the optional N-084-4 folded `a103ed2`).
- **Delivered**: PR [#170](https://github.com/gosharplite/tellme/pull/170) **human-merged** into `dev` (**merge commit**, `05b23e2`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-084`**. **Closes [#169](https://github.com/gosharplite/tellme/issues/169)**; the tracker is **0 open**.

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-23.md`](docs/archives/status/2026-09-23.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there; the round-082/083 closeouts relocated the **round-081/082** detail; **this closeout** relocated the **round-083 delivered-round detail + its env note** there (verbatim — never deleted).

**Delivered rounds**: rounds 001–084 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

**Delivered**: rounds 001-084 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: none - the tracker is **0 open**; a new round opens off `dev` from operator value or a live issue (Bootstrap Agent Rule 11).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-084`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-083`** at its propagation merge; **`round-082`** at its propagation merge; **`round-081`** at its propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; reconciled 2026-09-24 by round 085; anchor [#173](https://github.com/gosharplite/tellme/issues/173))** — the `axb-gherkin-and-dsl` topology script (`audit_feature_dsl_topology.py`) over `specs/truth/features/cli` now reports **0 errors / 0 warnings** (53 features · 6 feature modules · **21** root + **457** module rows · 2328 steps). Round 085 reconciled the **11** errors (the pre-round-085 env-note figure "6" was stale — the live count was **11**; re-classified at round open): **5 cross-module scope** (rows promoted to the interface root) · **5 unparsed round-083 rows** (restored the missing table headings/headers) · **1 missing composite row** (added). The script is **not** a `make verify` member; a **carried check**, not a delivery gate. **Fix-forward**: keep each round's DSL rows inside a headed table (`## Given/Then (round NNN)` + the `DSL 句型` header) and keep cross-module rows at the interface root (the audit's parser only reads headed tables).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state** (139→53 lines): six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Round-085 (session 73, 2026-09-24)** — branch `085-dsl-topology-reconciliation` off `dev` `2502133`: specify + plan package `ad7b0c0` -> implementation (DSL reconciliation + the R8d help text) `4532111` -> STATUS/day log. **Truth-only + one Makefile string**: `specs/truth/features/cli/{dsl.md,chat/dsl.md,configuration/dsl.md}` (promote 4 cross-module rows to the root; restore the round-083 table structure; add the composite row) + `specs/truth/techstack.md` (R8d bullet retired) + `Makefile` (help line + target comment) + ADR 0012 §Forward annotation; **no product code, no `.feature` change, no `go.mod`/`go.sum` change**. **Witness**: the topology audit **11 -> 0** errors (0 warnings); `make verify` **OK**; `go test -count=1 ./...` **green** (2328 steps unchanged); `gofmt`/`goimports` clean. Host: **linux/amd64** (Go 1.26.6). **PR open** (human merges).
- **Round-084 (session 72)** — branch `084-rollback-durability-witness` off `dev` `030e142`: specify `2d09e33` -> acceptance `ddf50cc` -> research + **ADR 0056** + truth calibration `3103c1a` -> plan + dsl-refine `e2eb86c` -> implementation (the `durableFS` seam + the mechanism-seam witness pins) `c12ad85` -> STATUS/day log `c0e5047` -> PR [#170](https://github.com/gosharplite/tellme/pull/170) -> fold F-084-1/2 + TD-084-1 + N-084-1/2/3 `5b5accc` -> residual folds R-FV-084-1/2/3 `8cf8b9c` -> N-084-4 `a103ed2` -> STATUS/day log (loop CLOSED) `b796067` -> merge `05b23e2`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**) -> residual fold -> final **FOLDS VERIFIED — LOOP CLOSED (no residuals)**. **Human-merged** into `dev` (**merge commit**, `05b23e2`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-084`. Host: **darwin/arm64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `make check` **OK** - `go test -count=1 ./...` green (E2E **314 scenarios · 2354 steps**) - `make test-race` green - `go.mod`/`go.sum` unchanged.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
