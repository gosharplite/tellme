# tellme — Status

**Last updated**: 2026-09-18 (session 15 / day close — **round 042 `042-layer-discipline-gate` DELIVERED / FROZEN** — **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)**, anchor [#93](https://github.com/gosharplite/tellme/issues/93): a **layer-discipline gate** + a committed violation **baseline**, **tooling/truth only, zero product code**; **ADR 0011**. Delivered in two PRs — **plan half** [#94](https://github.com/gosharplite/tellme/pull/94) (`dev` `a82237a`) + **implementation half** [#95](https://github.com/gosharplite/tellme/pull/95) (`dev` `f4b53f6`; head `61414fc`; the review printed **FINAL CERTIFICATION — MERGE-READY** after four review rounds / nine verified folds). `#93` **CLOSED (completed)** on this merge; **#96** filed (outer-env hermeticity residual). **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation)).
**Active branch**: `dev` (round 042 delivered/frozen; the next round `043-*` opens off `dev`)
**Daily log**: [`docs/session-summary/2026/09/18/session-summary.md`](docs/session-summary/2026/09/18/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history) · [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 041 detail + its PR #88 review-fold history).

> **Split note (Rule 12)**: on this closeout the **round-041 detail section + its PR #88 review-fold history** were relocated **verbatim** into [`2026-09-18.md`](docs/archives/status/2026-09-18.md) (round 042 is now the most recently delivered round), keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`) |
| 042 | `042-layer-discipline-gate-plan` + `042-implement-layer-discipline-gate` | PR [#94](https://github.com/gosharplite/tellme/pull/94) (`a82237a`, plan+truth) · PR [#95](https://github.com/gosharplite/tellme/pull/95) (`f4b53f6`, merge; head `61414fc`) |

Per-round detail lives in the archives; **042 is the most recent delivered round; no round is in flight** (the next round `043-*` opens off `dev`).

## Delivered round — 042 `042-layer-discipline-gate` (DELIVERED / FROZEN — PR #95 merged into `dev` `f4b53f6`; frozen head `61414fc`)

**042-layer-discipline-gate** — **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)** (the gate-first split), anchor [#93](https://github.com/gosharplite/tellme/issues/93): a **build/quality-pipeline** round — a `make verify` member **`verify-architecture`** + a committed **baseline**, **tooling/truth only, zero product code**.

- **The rule (two-part predicate — ADR 0011 D2).** **(A)** no **upward** import (imported tier > importer tier) · **(B)** the application tiers (`internal/app/**`, `internal/cli`) MUST NOT import `internal/infrastructure/**` · **(C)** `internal/domain/**` purity · **(D)** default-deny for an unranked `internal/**` package. Ranking: `domain` 0 · `config`/`home` 1 · `app` 2 · `infrastructure` 3 · `agent`(+subtree) 4 · `ui`/`tui` 5 · `cli` 6; `cmd`/`tests`/`tools` exempt. **Why two parts:** a one-part "no upward import" rule cannot reproduce the 7 `cli → infrastructure` entries (they are **downward** imports RULE-B forbids); the superseded one-part draft is recorded in ADR 0011 D2. The gate's **tier table is the normative machine-readable ranking source** (ADR 0011 D7).
- **The baseline (8, generated, not transcribed).** The 7 `internal/cli → internal/infrastructure/{di, history, llm, mcp, skills, telemetry, tools}` edges (RULE-B — **R2**'s) + `internal/agent → internal/ui` (the **only** RULE-A violation — **R3/R4**'s). A **fail-on-stale ratchet**: a new violation **fails**; a stale entry **fails**; absent/unreadable/unparsable **fails**; an emptied baseline fails **iff** violations exist. Count reaches **0 across R2–R4**.
- **Mechanism.** `tools/arch/` (`//go:build arch` guard + untagged `doc.go`); enumeration **anchored to the module root** (CWD-first, trimpath-immune; `runtime.Caller` fallback) via stdlib `go list`; **union over `CROSS_TARGETS`** (`linux/amd64 linux/arm64 darwin/amd64 darwin/arm64`); **filtered child env** (explicit non-empty neutralisation — `GOFLAGS=-mod=readonly`/`GO111MODULE=on`/`GOWORK=off` + `GOOS`/`GOARCH`/`CGO_ENABLED` per target — preserving `PATH`/`HOME`/`GOPATH`/`GOMODCACHE`/`GOCACHE`); **test imports governed** (`.Imports`+`.TestImports`+`.XTestImports`); **acyclicity** via a stdlib SCC over the **production-only** union (Go permits test-only cycles); **baseline generation affordance** `make verify-architecture-update`.
- **Wiring.** `Makefile` `verify-architecture` = `go vet -tags=arch ./tools/arch` + `golangci-lint run --build-tags=arch ./tools/arch/...` + **`go test -count=1`**` -tags=arch -run TestVerifyRealArchitecture ./tools/arch` (the `-count=1` disables the Go test cache, which cannot see the whole-module `go list` the verdict depends on). Member of `verify`; `.PHONY` + `help`.
- **Pipeline.** **specify ✅** · **clarify ✅** (Q1 broad → two-part predicate; Q2 scope resolved by measurement; Q3 fail-on-stale) · **spec-by-example NOOP** · **technical-research ✅** (`research.md` D1–D12; `techstack.md` Build & Tooling MODIFY; **ADR 0011** ADD + index) · **system-analysis ✅** (0 interfaces; api/data/dsl-refine **NOOP**) · **dsl-refine NOOP** · **tasks ✅** (T001–T007; orphan sweep 0) · **implement ✅** (witnesses reproduced then reverted).
- **Review chain (PR #95).** Four architect reviews → **FINAL CERTIFICATION — MERGE-READY, review loop CLOSED**; every fold verified as **behaviour**: F-1 zero-violation endpoint reachable; F-2 child env truly *neutralised*; F-3 `moduleRoot` trimpath-immune; F-4 `internal/agent` subtree; test-import coverage implemented; SCC over the production-only union; `-count=1` documented in `doc.go`/baseline header/**truth row**/`research.md` D2; tagged lint in the target. (Fold ledger in `specs/plans/042-layer-discipline-gate/tasks.md` §*Fold review #1…#4*.)
- **Truth / governance.** `specs/truth/techstack.md` (Build & Tooling: new **Layer-discipline gate** row + **Task runner** `verify` aggregate) · **ADR 0011** `docs/decisions/0011-layer-discipline-gate.md` (+ index row) — now **immutable** except its `Status` line and the index. `specs/truth/features/**` unchanged (topology audit unchanged).
- **Verification at the merged head.** `make verify` **OK** (incl. `verify-architecture`; golangci-lint 0 issues; govulncheck clean; cross-compile 4/4) · `go test -count=1 ./...` green · topology audit **PASSED** (44 features · 6 modules · 16 root + 327 module rows · 1674 steps) · `gofmt` clean · `go.mod`/`go.sum` unchanged · diff-level secret scan clean · **no `internal/**`/`cmd/**` changed**.

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` | Stable / released line |
| `dev` | round 042 **delivered** (`f4b53f6`, PR #95) | Integration line (round work lands here before `main`) |
| `042-layer-discipline-gate-plan` | **merged into `dev`** (frozen) | Round 042's plan + truth branch (PR [#94](https://github.com/gosharplite/tellme/pull/94) → `a82237a`). |
| `042-implement-layer-discipline-gate` | **merged into `dev`** (PR [#95](https://github.com/gosharplite/tellme/pull/95) → `f4b53f6`; frozen head `61414fc`) | Round 042's implementation branch — off `dev`; carried the `tools/arch/` gate + baseline + `Makefile` wiring + the four review folds. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–040 — **DONE (no-ff)**; round 041 — **DONE (no-ff, `692a4e6`)**; the session-12/13 tracker-refresh + closeout docs — **DONE (no-ff, `150627d`)**; round 042 — **DONE (no-ff)** (see the step-7 record in the 2026-09-18 summary).
> Read live heads with `git rev-parse --short origin/main origin/dev`.

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–041** | — | Provider-registry completeness → … → resolver test load tolerance. | ✅ **Delivered** (see the archives) |
| **042** | [#93](https://github.com/gosharplite/tellme/issues/93) | **R1 of [#92](https://github.com/gosharplite/tellme/issues/92)** — layer-discipline gate (`verify-architecture`) + violation baseline (**8**); tooling/truth only; **ADR 0011**. | ✅ **Delivered** — PR [#95](https://github.com/gosharplite/tellme/pull/95) merged into `dev` (`f4b53f6`) |
| **043 candidate** | [#92](https://github.com/gosharplite/tellme/issues/92) | **R2 — composition-root extraction** (the 7 `cli → infrastructure` imports → 0; inject a `Dependencies` struct so `internal/cli` imports only `internal/domain/*` + stdlib). | ⏳ **Next** (opens off `dev`) |
| **future slices** | [#92](https://github.com/gosharplite/tellme/issues/92) (R3/R4 + ride-alongs) · [#91](https://github.com/gosharplite/tellme/issues/91) · [#96](https://github.com/gosharplite/tellme/issues/96) · [#13](https://github.com/gosharplite/tellme/issues/13) | R3 yield-policy owner + observer hook split · R4 blank-reason owner + presentation predicate · ride-alongs (suggestion-selection policy; `BindToolOutput` ctor injection); the **self-development umbrella** ([#91](https://github.com/gosharplite/tellme/issues/91) — replaced #60); **Makefile-gate env hermeticity** ([#96](https://github.com/gosharplite/tellme/issues/96)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Issue tracker (2026-09-18, session 15)** — **[#93](https://github.com/gosharplite/tellme/issues/93) CLOSED (completed)** (delivered by round 042, PR [#95](https://github.com/gosharplite/tellme/pull/95) merged `f4b53f6`); **[#96](https://github.com/gosharplite/tellme/issues/96) OPEN (new)** — *Makefile gates are not hermetic against a persisted/ambient Go env (`go env -w` / `GOENV` / `GOFLAGS`)* (the **outer** half of round-042 F-2; same family as PR #46 TD1). **Open tracker: [#92](https://github.com/gosharplite/tellme/issues/92)** (composition-root + presentation-policy single ownership — the gate-first split; **R1 delivered**, R2–R4 + ride-alongs remain) · **[#91](https://github.com/gosharplite/tellme/issues/91)** (self-development umbrella) · **[#96](https://github.com/gosharplite/tellme/issues/96)** · **[#13](https://github.com/gosharplite/tellme/issues/13)** (coverage tooling) — plus PR #16 Obs 1.
- **Round-042 forward items (delivered)** — (a) **#96** (outer-env hermeticity: the parent `go test`/`go vet` still inherit a persisted/ambient Go env — repo-wide, every gate); (b) the baseline's remaining entries are removed by **R2** (the 7) and **R3/R4** (the 8th); (c) **recorded friction for R3/R4** — with the merged graph the rule now governs an `internal/agent` **test** importing `internal/ui` (reds as RULE-A — desirable: it forces the port seam); (d) the `modelith-layers` analogue is not adopted (no modelith toolchain); (e) custom build-tag-gated imports are out of scope (recorded residual; the only custom-tagged file is the guard itself, in exempt `tools/**`).
- **Round-041 forward items (delivered)** — the **sibling wall-clock-assertion class** (Q6) → its own round; **#89/#90 CLOSED (not planned)**; the 2 s ceiling's accepted residual (ADR 0010 Consequences). *(Detail in [`2026-09-18.md`](docs/archives/status/2026-09-18.md).)*
- **Round-040 forward items (delivered)** — the **one-concurrent-block** limit → **#92**; the `End`-while-write-stalled accepted residual; the fixed 3 s idle default; the two-figure soft-wrap residual; the `toolOutputHeaderMarker` content-keying. *(Detail in [`2026-09-17.md`](docs/archives/status/2026-09-17.md).)*
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items; the round-022 row→feature audit blind spot → **#91**.
- **PM follow-ups** — none open.

## Environment notes

- **Round-042 (session 15)** — branches `042-layer-discipline-gate-plan` (PR [#94](https://github.com/gosharplite/tellme/pull/94) → `a82237a`) + `042-implement-layer-discipline-gate` (PR [#95](https://github.com/gosharplite/tellme/pull/95) → `dev` `f4b53f6`; frozen head `61414fc`). Host: **darwin/arm64** (Go 1.26). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `make verify` **OK** (incl. `verify-architecture`: `go vet -tags=arch` + tagged lint 0 issues + the `-count=1` gate — 8 baselined · 0 new · 0 stale · 0 cycles) · `go test -count=1 ./...` green · topology audit **PASSED** (44 · 6 · 16+327 · 1674) · `go.mod`/`go.sum` unchanged · diff-level secret scan clean.
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the delivered round-042 head `f4b53f6`; `--version` → `dev`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
