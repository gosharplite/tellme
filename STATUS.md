# tellme — Status

**Last updated**: 2026-09-25 (session 75 — round **087** `087-mcp-tool-cache` **OPEN** off `dev` `2162729`; anchor issue [#180](https://github.com/gosharplite/tellme/issues/180); full AIxBDD pipeline → implementation → **PR [#181](https://github.com/gosharplite/tellme/pull/181) OPEN** (awaiting a human review/merge; **no Copilot review**). Round 086 remains DELIVERED / FROZEN.)
**Round in flight**: **087 `087-mcp-tool-cache`** — the prompt prelude's remote-MCP tool discovery caches a server's tool list across invocations (a warm fresh entry dials **nothing**); PR [#181](https://github.com/gosharplite/tellme/pull/181) open.
**Active branch**: `087-mcp-tool-cache` off `dev` `2162729` (PR [#181](https://github.com/gosharplite/tellme/pull/181), head `d6def99`; **merged by a human only**).
**Daily log**: [`docs/session-summary/2026/09/25/session-summary.md`](docs/session-summary/2026/09/25/session-summary.md) (round 087) · prior: [`2026-09-24`](docs/session-summary/2026/09/24/session-summary.md) (rounds 085 + 086)

## Last delivered round - 086 `086-tools-listing-line` (DELIVERED / FROZEN - PR [#179](https://github.com/gosharplite/tellme/pull/179) merged into `dev` `77c21c5`, **merge commit**; **ADR 0057**)

- **Theme (operator request, 2026-09-24)**: the `-l`/`--list` listing gains a per-turn **tool-activity line** `[TOOLS] - M (N calls)` — `M` the turn's backward turn index (round 082), `N = len(history.Entry.Steps)` (the **tool** calls, **not** `Entry.Calls`) — printed **between** the turn's `[USER]` block and its `[MODEL]` header as its own block (one blank line each side), **yellow** (whole label) on a terminal `stdout` with `-r` off, and printed for **every** turn incl. `(0 calls)`. It is a **rider** (`-l N` still selects the last N **messages**), surfaces **only the count** (never tool content), and rides the turn's `[MODEL]` message (a partial listing keeps it). Supersedes the round-073 *"tool activity must not be surfaced at all"* reading in substance (the content rule stands).
- **The change**: `internal/domain/render/ports.go` (`ListingMessage.ToolCount` — the `TurnIndex` precedent) · `internal/ui/listing.go` (emit the line before the `[MODEL]` header; `toolLine`) · `internal/ui/colour.go` (the yellow reuse note) · `internal/cli/cli.go` (`listingMessages` sets `ToolCount: len(e.Steps)`).
- **Truth**: `specs/truth/features/cli/history/dsl.md` (a `## Then (round 086)` block + the prologue note; the `tellme lists only the operator's messages` row reframed to `tellme lists the tools' activity but not their contents`; the `the listing carries no accents` row now covers yellow) · `inspecting-the-session-history.feature` (3 new Rules / 7 Examples; the tool-activity Rule reframed) · `specs/truth/techstack.md` (*Output rendering* + *Session lifecycle flags* reconciled in place — the two stale "tool activity omitted" clauses now read *count surfaced, content not*). **Records**: **ADR 0057** (+ index row; amends ADR 0045) · `docs/domain-model/tellme.modelith.{yaml,md}` (the listing colour invariant).
- **The witness**: `make check` **OK** · `go test -count=1 ./...` **green** (E2E **320 scenarios · 2394 steps**) · `make test-race` **no data races** · `gofmt`/`goimports` clean · `modelith-check` no drift · ADR index consistent · topology audit **PASSED** (53 features · 6 modules · 21 root + 459 module rows · 2368 steps) · `go.mod`/`go.sum` **unchanged**. Four mutation witnesses reproduced + reverted: reporting `Calls` ⇒ the CLI pin + E2E red; un-gating the yellow ⇒ the unit pins + the E2E no-accents Then red; emitting after the model body ⇒ the E2E tool-activity Then red; emitting the line for the `[USER]` message instead of the `[MODEL]` one (a literal guard swap) ⇒ **4** E2E scenarios red + the unit byte pins.
- **Review chain (PR [#179](https://github.com/gosharplite/tellme/pull/179), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-086-1** the `techstack.md` *Session lifecycle flags* cell left self-contradictory · **F-086-2** FR-005 (`-r`) had **no discriminating carrier** for the tool line · TD-086-1 · N-086-1 · N-086-2) → fold `88ed8cf` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — RES-086-1) → fold `bf440f7` → `FOLD-VERIFICATION` (**WITH RESIDUALS** — RES-086-2: the fold commit was **local-only**, the repo had drifted to a detached HEAD) → re-push `bf440f7` → `FOLD-VERIFICATION` (**WITH RESIDUALS** — RES-086-3: day-summary figure drift) → fold `0a4e7c7` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`**.
- **Delivered**: PR [#179](https://github.com/gosharplite/tellme/pull/179) **human-merged** into `dev` (**merge commit**, `77c21c5`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-086`**. No anchor issue (an operator request); the tracker is **0 open**.

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-24.md`](docs/archives/status/2026-09-24.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there; the round-082/083/084/085 closeouts relocated the **round-081/082/083/084** detail; **this closeout** relocated the **round-085 delivered-round detail + its env note** there (verbatim — never deleted).

**Delivered rounds**: rounds 001–086 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

**Delivered**: rounds 001-086 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: none — the tracker is **0 open**; a new round opens off `dev` from operator value or a live issue (Bootstrap Agent Rule 11).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-086`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-085`** at its propagation merge; **`round-084`** at its propagation merge; **`round-083`** at its propagation merge; **`round-082`** at its propagation merge; **`round-081`** at its propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; reconciled 2026-09-24 by round 085; anchor [#173](https://github.com/gosharplite/tellme/issues/173))** — the `axb-gherkin-and-dsl` topology script (`audit_feature_dsl_topology.py`) over `specs/truth/features/cli` now reports **0 errors / 0 warnings** (53 features · 6 feature modules · **21** root + **459** module rows · 2368 steps; round 086 added the `## Then (round 086)` rows). Round 085 reconciled the **11** errors (the pre-round-085 env-note figure "6" was stale — the live count was **11**; re-classified at round open): **5 cross-module scope** (rows promoted to the interface root) · **5 unparsed round-083 rows** (restored the missing table headings/headers) · **1 missing composite row** (added). The script is **not** a `make verify` member; an **advisory** check (`topology-audit-not-a-gate`) — reported, not a delivery gate. **Fix-forward**: keep each round's DSL rows inside a headed table (`## Given/Then (round NNN)` + the `DSL 句型` header) and keep cross-module rows at the interface root (the audit's parser only reads headed tables).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state** (139→53 lines): six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Round-086 (session 74, 2026-09-24)** — branch `086-tools-listing-line` off `dev` `6f0004f`: specify + plan package + implementation (the `[TOOLS]` line + truth + records + witnesses) `e086690` -> PR [#179](https://github.com/gosharplite/tellme/pull/179) + name `f6755f6` -> fold F-086-1/2 + TD-086-1 + N-086-1/2 `88ed8cf` -> RES-086-1 `bf440f7` -> RES-086-2 (re-push) -> RES-086-3 `0a4e7c7` -> loop CLOSED `9df620a` -> merge `77c21c5`. **Presentation-only** on the offline `-l` path: `internal/domain/render/ports.go` (`ListingMessage.ToolCount`) + `internal/ui/listing.go` + `internal/ui/colour.go` + `internal/cli/cli.go`; truth `history/{dsl.md,inspecting-the-session-history.feature}` + `techstack.md` (2 rows reconciled) + **ADR 0057** (+ index; amends ADR 0045) + `docs/domain-model/tellme.modelith.{yaml,md}`; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> 4 fold-verification passes -> final **FOLDS VERIFIED — LOOP CLOSED (no residuals)**. Host: **linux/amd64** (Go 1.26.6). **PR [#179](https://github.com/gosharplite/tellme/pull/179) human-merged** into `dev` (**merge commit**, `77c21c5`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-086`**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **320 scenarios · 2394 steps**) · `make test-race` no data races.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
