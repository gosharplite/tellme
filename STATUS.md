# tellme — Status

**Last updated**: 2026-09-25 (session 76 — round **089** `089-context-control-position` **IN FLIGHT**: PR [#185](https://github.com/gosharplite/tellme/pull/185) open, awaiting human review/merge; reconcile the stale `-b`/`--retry` clause in `techstack.md` + record the context-control position (**ADR 0060**); anchor [#184](https://github.com/gosharplite/tellme/issues/184); the tracker is **1 open**.)
**Round in flight**: round **089** `089-context-control-position` — **truth/record-only**: reconcile the `techstack.md` clause that still excluded the *delivered* `-b` (keeping `--retry` a genuine non-introduction) + record the context-control position (**ADR 0060**: no `summarize_history` port; operator-manual `-l`/`-b`); anchor [#184](https://github.com/gosharplite/tellme/issues/184); **PR [#185](https://github.com/gosharplite/tellme/pull/185) open**.
**Active branch**: `089-context-control-position` (off `dev` `53753a0`; round 088 delivered/frozen on `dev` — PR [#183](https://github.com/gosharplite/tellme/pull/183) merged `033f364`, propagation `dev -> main` **DONE (no-ff)**, tagged `round-088`).
**Daily log**: [`docs/session-summary/2026/09/25/session-summary.md`](docs/session-summary/2026/09/25/session-summary.md) (rounds 087–089) · prior: [`2026-09-24`](docs/session-summary/2026/09/24/session-summary.md) (rounds 085 + 086)

## Last delivered round - 088 `088-mcp-cache-header-routing-and-mode-location` (DELIVERED / FROZEN - PR [#183](https://github.com/gosharplite/tellme/pull/183) merged into `dev` `033f364`, **fast-forward**; **ADR 0059**)

- **Theme (anchor issue [#182](https://github.com/gosharplite/tellme/issues/182), 2026-09-25)**: repair the two defects round 087 shipped with the cross-invocation MCP tool cache. **(A, severe)** a **warm-cached MCP tool could not call any server that routes an argument as an HTTP header** — the MCP Go SDK resolves a tool's `x-mcp-header` (SEP-2243) annotations from its **`tools/list` cache**, and round 087's lazy client connected **without** issuing `tools/list`, so the SDK never emitted `Mcp-Param-*` and the whole **GitHub MCP server** 400'd (`missing Mcp-Param-owner header`); **(B)** the cache was written to the `$TELL_ME_HOME` **root** instead of the per-mode workspace. Both shipped because of a **witness gap** (the hermetic fake MCP server did not require a header-routed argument).
- **The change**: `internal/infrastructure/mcp/lazyclient.go` — `warmToolsList` issues **one `tools/list`** (memoized, best-effort, bounded by the call timeout) after `connect` and before delegating, so the SDK has the declarations and emits `Mcp-Param-*`; a cache hit that runs **no** tool still dials nothing. `internal/infrastructure/mcp/toolcache.go` + `internal/app/deps/deps.go` + `internal/cli/cli.go` + `cmd/tellme/deps.go` — the cache moves to **`$TELL_ME_HOME/output/<mode>/mcp-toolcache.json`** (the `MCPDiscoverer` seam takes `res.Workspace`, not `res.Home`); `--new` still preserves it. `internal/infrastructure/mcp/mcptest/server.go` — `Options.HeaderRouted` advertises a tool whose named argument carries `x-mcp-header`, so the SDK **server** requires the header (reproducing the real failure hermetically); `lazyclient_warm_test.go` — the warm-once / best-effort / no-warm-when-unused / workspace-path pins.
- **Truth**: `specs/truth/techstack.md` (*MCP tool discovery* + *MCP tool cache* rows) · `specs/truth/data/data-model.dbml` (the cache entry's per-mode location) · `specs/truth/features/cli/chat/using-tools-from-a-remote-mcp-server.feature` (+2 Rules / 2 Examples) + `chat/dsl.md` (+ `## Given/Then (round 088)` rows). **Records**: **ADR 0059** (+ index row; **supersedes ADR 0058 D2**, qualifies its D4) · `docs/domain-model/**` **NOT** modelled (an internal optimization; ADR 0041 same-PR rule does not engage — `plan.md` §5).
- **The witness**: `make verify` **OK** · `go test -count=1 ./...` **green** (E2E **330 scenarios · 2487 steps**) · `make test-race` **no data races** · `gofmt`/`goimports` clean · `modelith-check` no drift · ADR index consistent · topology audit **PASSED** (53 features · 6 modules · 21 root + 467 module rows · 2461 steps) · `go.mod`/`go.sum` **unchanged**. Falsifiability witnesses reproduced + reverted: remove the `tools/list` warm-up ⇒ the unit pin `lists=0 calls=1` **and** the header-routed E2E Example red (`did not record a call to "create_issue"`); root the cache one level up (`filepath.Dir(workspace)` = `<home>/output`) ⇒ **6** E2E scenarios red at `the cache must live in the session workspace … no such file`; an eager **assembly-time** warm ⇒ **2** zero-contact Examples red (the mutation that violates FR-002); a no-servers write ⇒ `TestDiscoverCached_NoServersIsInert` reports `saves != 0`.
- **Review chain (PR [#183](https://github.com/gosharplite/tellme/pull/183), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-088-1** `techstack.md` self-contradiction (*shared across modes*) + a stale path · **F-088-2** the round-087 `chat/dsl.md` rows documented the old path · **F-088-3** EC-003's carrier over-claimed (no write half) + a stale `EC-004` label · **F-088-4** the rejected alternative mis-stated (`connect` is itself lazy) · **F-088-5** superseded home-root claims still present-tense · TD-088-1 · N-088-1…4) → fold `81d83f6` → `FOLD-VERIFICATION` (**`FOLDS VERIFIED — LOOP CLOSED`**, nit residual RES-088-FV-1) → `b9d63f3`.
- **Delivered**: PR [#183](https://github.com/gosharplite/tellme/pull/183) **human-merged** into `dev` (**fast-forward**, `033f364`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-088`**. **Closes [#182](https://github.com/gosharplite/tellme/issues/182)** (at that closeout the tracker was **0 open**; round **089** later opened [#184](https://github.com/gosharplite/tellme/issues/184)).

**Archive**: [`docs/archives/status/`](docs/archives/status/) — the relocated status history (oldest → newest; this closeout's file is [`2026-09-25.md`](docs/archives/status/2026-09-25.md)).

> **Split note (Rule 12)**: `STATUS.md` holds the **live state only**; relocated history/reference lives in [`docs/archives/status/`](docs/archives/status/). The **2026-09-23** hygiene pass moved the **Fold ledger**, the **delivered-rounds index**, the **branch-model propagation**, the **roadmap slices**, the **issue-tracker rows**, and the **open-items index** there; each closeout relocates the previous round's detail; **this closeout** relocated the **round-087 delivered-round detail + its env note** there (verbatim — never deleted).

**Delivered rounds**: rounds 001–088 are delivered; history lives in [`specs/plans/`](specs/plans/) (per-round package + fold ledger), [`docs/archives/status/`](docs/archives/status/), and the round PRs (`round-NNN` tags on `main`).

## Branch model

| Branch | Role |
| --- | --- |
| `main` | Stable / released line - each round's `dev` is propagated here (**no-ff**) at its closeout and tagged `round-NNN` (**ADR 0026**). |
| `dev` | Integration line - round work lands here (via the round PR) before `main`. |
| `NNN-*` | The round's working branch, off `dev`; deleted after merge. |

> **Convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the round PR; propagation is the no-ff merge `dev -> main`, tagged `round-NNN`. Live heads: `git rev-parse --short origin/main origin/dev`. Propagation history: `git tag -l 'round-*'` + the archives.

## Roadmap

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

**Delivered**: rounds 001-088 - per-round detail in the [archives](docs/archives/status/) + `specs/plans/`; delivery rounds/PRs in the archive index. **Candidates**: **[#184](https://github.com/gosharplite/tellme/issues/184)** (the round-089 anchor) — the tracker is **1 open**; a new round opens off `dev` from operator value or a live issue (Bootstrap Agent Rule 11).

## Environment notes

- **Quality gates (ADR 0042, 2026-09-20)** — **`make check`** (= `make verify` + `make test`) is the **whole local gate**; **`make check-full`** (= `check` + `test-race`) is the pre-push form. `make verify` (fast, hermetic static/convention aggregate) = `verify-no-test-sleep` · `verify-no-network` · **`verify-fmt`** (`gofmt -l` **+ `goimports -l`** — formatting *and* import grouping; `goimports` is a PATH prereq) · **`verify-adr-index`** (ADRs indexed once) · `vet` · `verify-cross-compile` · `verify-mcp-sdk-confinement` · `verify-architecture` · `modelith-check` · `lint` · `vulncheck`. **`make verify` does NOT run the E2E contract** — that is `go test -count=1 ./...` / `make test` (which uses the cache); `make check` closes that gap. **`make test-race`** (race detector, package-by-package; `RACE_PKGS` override; measured ~72 s for `./...` — the per-package loop's build overhead is the AI-safe cost; a single `go test -race ./...` is ~29 s) is the pre-push check, **not** a `verify` member. Advisory: `make modelith-drift` (ADR 0041).

- **Domain model is load-bearing (ADR 0041, 2026-09-20)** — `docs/domain-model/**` is descriptive-but-maintained: a round that changes **modelled behaviour** updates the model **in the same PR** (closeout Rule 18 / Step-3 item 11). `make modelith-check` is the zero-tolerance YAML↔MD gate (`make verify` member); the **advisory** `make modelith-drift` (`scripts/modelith-drift.sh`; never fails; **not** a `verify` member) surfaces a modeled entity whose concept has left the code. The reference's **name-diff** advisory gates stay unadopted (measured ~79 % false-positive). *Semantic* model rot has no mechanical carrier — the same-PR rule + review is the guard. Authority: `docs/domain-model/README.md` → *Drift guard*.

- **Round-close tags (operator, 2026-09-19; ADR 0026)** — each delivered round is marked by an **annotated `round-NNN`** git tag on its **`dev → main` propagation merge commit**, created by the operator-approved closeout **Step 7** after `main^{tree} == dev^{tree}`. **Immutable**; **not** a version/release (`--version` stays `dev`; no scheme yet). **`round-088`** is tagged at **this** closeout's `dev → main` propagation merge; **`round-087`** at its propagation merge; **`round-086`** at its propagation merge; **`round-085`** at its propagation merge; **`round-084`** at its propagation merge; **`round-083`** at its propagation merge; **`round-082`** at its propagation merge; **`round-081`** at its propagation merge; **`round-080`** at its propagation merge; **`round-079`** at its propagation merge; **`round-077`** at its propagation merge (`5c50b81`); **`round-076`** at its propagation merge (`7595269`); **`round-075`** at its propagation merge (`b394f4f`); **`round-074`** at its propagation merge (`498eaf6`); **`round-073`** at its propagation merge (`d7647f8`); **`round-072`** at its propagation merge (`232cf0e`); **`round-071`** at its propagation merge (`f70ce73`); **`round-070`** at its propagation merge; **`round-069`** at its propagation merge; **`round-068`** at `d7f4ed7`; **`round-067`** at `3610fa6`; **`round-066`** at `03773c8`; **`round-065`** at `3b9bf8c`; **`round-064`** at `8914fee`; **`round-063`** at `f5d5850`; **`round-061`** at `dd9b43b`; **`round-060`** at `b3dcdc9`; **`round-059`** at `67b3c0b`; **`round-058`** at `2fe09fa`; **`round-057`** at `baed89b`; **`round-056`** at `e1502cc`. Rounds 001–055 are deliberately **untagged** (forward-only).
- **PR review & merge convention (operator, 2026-09-19)** — tellme PRs are **human-reviewed only**: **no Copilot review** (`request_copilot_review` is not used on this repo), and **only a human merges** — an agent never merges a PR and never pushes to `dev`/`main` **except the closeout propagation**; it stops at "PR open". The round branch is deleted after merge; propagation `dev → main` is the no-ff merge at closeout.
- **Topology audit (2026-09-19, session 34; reconciled 2026-09-24 by round 085; anchor [#173](https://github.com/gosharplite/tellme/issues/173))** — the `axb-gherkin-and-dsl` topology script (`audit_feature_dsl_topology.py`) over `specs/truth/features/cli` reports **0 errors / 0 warnings** (53 features · 6 feature modules · **21** root + **467** module rows · 2461 steps; round 088 added the `## Given/Then (round 088)` rows). Round 085 reconciled the **11** errors (the pre-round-085 env-note figure "6" was stale — the live count was **11**): **5 cross-module scope** (rows promoted to the interface root) · **5 unparsed round-083 rows** (restored the missing table headings/headers) · **1 missing composite row** (added). The script is **not** a `make verify` member; an **advisory** check (`topology-audit-not-a-gate`) — reported, not a delivery gate. **Fix-forward**: keep each round's DSL rows inside a headed table (`## Given/Then (round NNN)` + the `DSL 句型` header) and keep cross-module rows at the interface root (the audit's parser only reads headed tables).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **STATUS hygiene (2026-09-23, session 69; docs-only)** — `STATUS.md` was reduced to the **live state**: six accreted history/reference blocks (Fold ledger · delivered-rounds index · branch-model propagation · roadmap slices · issue-tracker rows · open-items index) were relocated **verbatim** into [`docs/archives/status/2026-09-23.md`](docs/archives/status/2026-09-23.md) (Rule 12 — never delete history). `SESSION-CLOSEOUT.md` + `SESSION-BOOTSTRAP.md` updated to match: **no open-items index** on `STATUS.md` (a disclosure is homed in its `ADR 00NN §Forward` or a live issue — closeout Rule 17 / Bootstrap Agent Rule 11); the header **Archive** line points at the archive **folder**; the roadmap keeps only a delivered/candidates line.
- **Round-088 (session 75, 2026-09-25)** — branch `088-mcp-cache-header-routing-and-mode-location` off `dev` `13d5e08`: full pipeline + plan package + implementation `e452b33` -> docs `10a7950` -> fold F-088-1…5 + TD-088-1 + N-088-1…4 `81d83f6` -> RES-088-FV-1 `b9d63f3` -> day-log `033f364` -> merge (fast-forward) `033f364`. **Repairs round 087's cache**: (A) the lazy client now warms `tools/list` on the first call so a warm-cached tool again routes `x-mcp-header` args as `Mcp-Param-*` (the GitHub MCP server works again); (B) the cache moves from the `$TELL_ME_HOME` root to the per-mode workspace `output/<mode>/mcp-toolcache.json`; the fake MCP server gains a **header-routed** tool so the regression reddens. Code: `internal/infrastructure/mcp/{lazyclient,toolcache}.go` + the `MCPDiscoverer` seam (`res.Workspace`) + `cmd/tellme` + `mcptest`; truth `techstack.md` + `data-model.dbml` + the `chat` feature + `dsl.md`; **ADR 0059** (+ index; supersedes ADR 0058 D2, qualifies its D4); `docs/domain-model/**` **not** modelled; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold -> **`FOLDS VERIFIED — LOOP CLOSED`** (nit residual). Host: **linux/amd64** (Go 1.26.6). **PR [#183](https://github.com/gosharplite/tellme/pull/183) human-merged** into `dev` (**fast-forward**, `033f364`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-088`**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **330 scenarios · 2487 steps**) · `make test-race` no data races.
- **Host / toolchain**: Go 1.26.6; `golangci-lint` / `govulncheck` **/ `goimports`** in `$GOPATH/bin` (`staticcheck` runs **inside** the `lint` aggregator; the standalone `staticcheck` binary/`make staticcheck` target was retired 2026-09-20 as redundant; `goimports` is required by `verify-fmt` — ADR 0042 D6); the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` and is **refreshed during closeout** via `go install ./cmd/tellme` from the then-current `dev` head (so `go version -m` stamps that `vcs.revision` with `vcs.modified=false`; `--version` → `dev`). *(Deliberately **not** sha-pinned here.)* Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
