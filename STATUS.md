# tellme — Status

**Last updated**: 2026-09-17 (session 11 / day close: **round 041 `041-di-resolver-test-load-tolerance` — DELIVERED / FROZEN** — a **test-harness determinism** round resolving issue [#87](https://github.com/gosharplite/tellme/issues/87), **test-only + documentation, zero product code** (`internal/infrastructure/di/mcp_factory.go` untouched). Merged via PR [#88](https://github.com/gosharplite/tellme/pull/88) → `dev` (`ca792d7`; head `bbd7ead`; 13 files, +845/−28, 12 commits) by `thptcnec` 2026-09-17T13:34:19Z, after a **four-round review chain closed (CERTIFIED FINAL)**. **Propagation `dev → main` DONE (no-ff, `692a4e6`)** — round 041 is delivered on both lines. Truth: `specs/truth/techstack.md` (Testing & Verification: **Host test harness** + **Pure-helper unit tests** rows) + **ADR 0010** (`docs/decisions/0010-test-deadline-decoupling.md`). **#87 CLOSED (completed)**; the review's two forward items ([#89](https://github.com/gosharplite/tellme/issues/89) bound headroom · [#90](https://github.com/gosharplite/tellme/issues/90) narrative ownership) were **CLOSED (not planned)** on operator triage — disproportionate to their value (the #89 wiring pin is a ride-along for a future round that touches `mcp_discovery.go`/`di`). Validated at the merged head: `go test -count=1 ./...` green (the `di` package **7.001s** under whole-suite contention — the very gate the flake used to redden) · `make verify` OK · topology audit **PASSED** (44 features · 6 modules · 16 root + 327 module rows · 1674 steps). **Active branch `dev`**; the next round `042-*` opens off `dev`. Open issues: **#69** · **#60** · **#13** (· PR #16 Obs 1). **Session mode**: `butler` (working directly with the user — no `pm`/`rd` delegation this phase)
**Active branch**: `dev` (round 041 delivered/frozen; the next round `042-*` opens off `dev`)
**Daily log**: [`docs/session-summary/2026/09/17/session-summary.md`](docs/session-summary/2026/09/17/session-summary.md)
**Archive**: [`2026-09-11.md`](docs/archives/status/2026-09-11.md) (rounds 001–002) · [`2026-09-13.md`](docs/archives/status/2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](docs/archives/status/2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](docs/archives/status/2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](docs/archives/status/2026-09-16.md) (rounds 027–034) · [`2026-09-17.md`](docs/archives/status/2026-09-17.md) (rounds 035–040 + the round-040 review-fold history).

> **Split note (Rule 12)**: the **round-040** detail section was relocated **verbatim** into [`2026-09-17.md`](docs/archives/status/2026-09-17.md) when round 041 opened; on this closeout the accumulated **round-040 review-fold history** (PR #84/#85/#86 folds) was relocated there too, keeping `STATUS.md` to the live state + a single current-round section.

## Delivered rounds (index)

| Round | Branch | Delivered via |
| --- | --- | --- |
| 001–039 | `001-*` … `039-terminal-safe-lines-and-turn-spacing` | see the archives + the per-round PRs; round 035 via PR [#73](https://github.com/gosharplite/tellme/pull/73); round 036 via PR [#75](https://github.com/gosharplite/tellme/pull/75); round 037 via PR [#77](https://github.com/gosharplite/tellme/pull/77); round 038 via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); round 039 via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`) |
| 040 | `040-spinner-liveness-and-turn-timer` (+ `040-implement-…` / `040-implement-v2-…`) | PR [#84](https://github.com/gosharplite/tellme/pull/84) (`146210d`, plan+truth) · PR [#85](https://github.com/gosharplite/tellme/pull/85) (`07c88ea`, `/axb-tasks`) · PR [#86](https://github.com/gosharplite/tellme/pull/86) (`87af8c8`, implementation; head `436bd70`) |
| 041 | `041-di-resolver-test-load-tolerance` | PR [#88](https://github.com/gosharplite/tellme/pull/88) (`ca792d7`, merge; head `bbd7ead`; plan+truth+tasks+implementation in one PR) |

Per-round detail lives in the archives (001–002 in [`2026-09-11.md`](docs/archives/status/2026-09-11.md); 003–012 in [`2026-09-13.md`](docs/archives/status/2026-09-13.md); 013–019 in [`2026-09-14.md`](docs/archives/status/2026-09-14.md); 020–026 in [`2026-09-15.md`](docs/archives/status/2026-09-15.md); 027–034 in [`2026-09-16.md`](docs/archives/status/2026-09-16.md); 035–040 + the round-040 review-fold history in [`2026-09-17.md`](docs/archives/status/2026-09-17.md)); **041 is the most recent delivered round; no round is in flight.**

## Current round — 041 `041-di-resolver-test-load-tolerance` (DELIVERED / FROZEN — PR #88 merged into `dev` `ca792d7`; frozen head `bbd7ead`)

**041-di-resolver-test-load-tolerance** — a **test-harness determinism** round (issue [#87](https://github.com/gosharplite/tellme/issues/87)), **test-only + documentation, zero product code**. **Root cause (measured, not hypothesised):** the positive trimming test **hardcoded its own `2 * time.Second`** wall-clock budget — **not** the caller's bound (production's fast-fail bound is **`mcpDiscoveryBound = 3 * time.Second`**; the 2 s literal mirrors the sibling `ghWaitDelay = 2 s`, both introduced in round-032 `0a3ad9c`). Under whole-suite contention the instant shim's fork+exec exceeded that budget ⇒ the resolver's own SIGKILL ⇒ `signal: killed` at exactly `2.00s` (measured ≈**14–17×** host-speed factor: 0.14 s standalone vs ≈1.9–2.4 s loaded). **The fix (D1–D4):** **D1** the trimming test takes a generous **test-local** deadline (`generousResolverBound = 30s`); **D2** `writeFakeGh` gives the shim a **dominant** PATH (retiring the round-032 **N1** in-shim PATH restoration; the hanging shim simplifies to `exec sleep 3`); **D3** `TestNewGhTokenResolver_Bounded` stays the **sole** boundedness carrier (200 ms bound + an **exit-code** non-vacuity pin — the child must be **signal-killed** by the deadline, `ee.ExitCode() == -1` — + a `boundedCeiling = 2s` that stays strictly below the ≈3 s unbounded case); **D4** `TestNewGhTokenResolver_MissingGh` = recorded non-change. Pipeline (all in **one PR** — the plan half was never separately PR'd): **specify ✅** · **technical-research ✅** (`research.md` D1–D8; `techstack.md` Testing rows MODIFY; **ADR 0010** ADD + index) · **system-analysis ✅** (`plan.md`; **0 interfaces** — `/axb-api-plan` + `/axb-data-plan` + `/axb-dsl-refine` **NOOP**; `/axb-ui-plan` + `/axb-spec-by-example` skipped) · **tasks ✅** (T001–T006; Pre-Delivery Orphan Sweep **0**) · **implement ✅** (witnesses (a)/(b)/(c) reproduced then reverted). Witness/acceptance: `go test -count=20 -timeout 30m ./...` **exit 0** (16m15s) + `go test -count=20 ./internal/infrastructure/di/` green **under contention**; `make verify` OK; topology audit PASSED & unchanged. **Whole round = 12 commits; everything after `8f6ed73` is docs/ledger-only** (the product half was correct from the first implementation commit).
- **Architect review (PR #88, reviewed `c88ee46` → fold `b5197a0` + ledger `48b5bbc`) — APPROVE WITH REQUIRED FOLDS:** **R-1** the premise mis-identified the hardcoded budget as *the production constant* (production is `mcpDiscoveryBound = 3 s`) → the rule reworded to *"a test must not hardcode a tight wall-clock budget that is not its subject"* + the provenance lesson + the ADR index row · **R-2** the exit-code fold swept across 8 remaining `elapsed >= bound` sites · **R-3** the ceiling residual recorded (validated in the `-count=20 <pkg>`-under-e2e config; whole-suite package parallelism not covered; the ceiling adds no discriminating power) · **R-4** forward item filed as **[#89](https://github.com/gosharplite/tellme/issues/89)** · **R-5** nits (STATUS head block regenerated as one unit).
- **Fold re-review (PR #88, reviewed `3cb8ec8` → fold: *"docs(041): fold PR #88 re-review — R-6 (#87 body), R-7/R-8 (ADR 0010), R-9 convention"*) — CERTIFIED:** **R-6** issue [#87](https://github.com/gosharplite/tellme/issues/87)'s **body** (the durable surface) rewritten to the **measured** cause · **R-7** ADR 0010 **D2** forward pointer to its own recorded Residual · **R-8** the provenance lesson re-attributed (the phrase **entered via #87's root-cause comment**, ratified unverified — *"a reviewer's claim is not evidence either"*) · **R-9** the ledger-row convention.
- **Fold #3 (PR #88, reviewed `ba935af` → fold: *"docs(041): fold PR #88 re-review #2 — R-10 (PR title/body), R-11/R-12/R-13 (session-11 lessons)"*) — the third durable surface corrected:** the **PR #88 title + body** carried the same disproved premise → corrected on the PR itself; R-11/R-12/R-13 recorded as session-11 process lessons (§19-a…§19-d).
- **Fold #4 (PR #88, reviewed `9e1cc50` → fold: *"docs(041): fold PR #88 re-review #3 — F-1…F-4 (ledger rows, edit-stable refs) + §19-e"*) — F-1…F-4 + §19-e:** the entangled ledger row split **one-row-per-review** (F-1); the row self-reference made **edit-stable** (the fold's **commit subject**, not the row's mutable text — F-2); §19-a sharpened (F-3); §19-c gains the accidental-publication case (F-4); **§19-e** the *one-owning-artifact* narrative-ownership recommendation → **[#90](https://github.com/gosharplite/tellme/issues/90)**.
- **Final approval (PR #88, reviewed `bbd7ead`) — ✅ ARCHITECTURAL APPROVAL, MERGE-READY; review loop CLOSED.** Reviewer's independent check: `go test -count=1 ./...` → **exit 0** at the head (the closeout gate the flake used to redden) · `mergeable_state: clean`.

## Branch model

| Branch | State | Role |
| --- | --- | --- |
| `main` | merged up from `dev` | Stable / released line |
| `dev` | round 041 **delivered** (`ca792d7`, PR #88) | Integration line (round work lands here before `main`) |
| `040-spinner-liveness-and-turn-timer` | **merged into `dev`** (frozen) | Round 040's plan + truth branch (PR [#84](https://github.com/gosharplite/tellme/pull/84) → `146210d`). |
| `040-implement-spinner-liveness-and-turn-timer` | **merged into `dev`** (frozen) | Round 040's `/axb-tasks` branch (PR [#85](https://github.com/gosharplite/tellme/pull/85) → `07c88ea`). |
| `040-implement-v2-spinner-liveness-and-turn-timer` | **merged into `dev`** (frozen; head `436bd70`) | Round 040's implementation branch (PR [#86](https://github.com/gosharplite/tellme/pull/86) → `87af8c8`). |
| `041-di-resolver-test-load-tolerance` | **merged into `dev`** (PR [#88](https://github.com/gosharplite/tellme/pull/88) → `ca792d7`; frozen head `bbd7ead`) | Round 041's single working branch — off `dev`; carried the plan half + the retuned harness (`mcp_factory_test.go`) + the four review folds. |

> **Branch convention**: each round works on its own `NNN-*` branch off `dev`; only a human merges the PR. Propagation is the no-ff merge `dev → main`.
> **Propagation history**: rounds 026–040 — **DONE (no-ff)**; round 041 — **DONE (no-ff, `692a4e6`)**.
> Read live heads with `git rev-parse --short origin/main origin/dev` (this clone has no local `main`/`dev` refs — only `origin/*`).

## Roadmap — next slices

> **Direction (2026-09-15)** — no security · no Windows · **bash-first** · a deliberately small tool surface (see [`README.md`](README.md#-design-intent--direction-operator-declared)).

| Slice | Issue | Scope | Status |
| --- | --- | --- | --- |
| **003–040** | — | Provider-registry completeness → … → spinner liveness + dual timer. | ✅ **Delivered** (see the archives) |
| **041** | [#87](https://github.com/gosharplite/tellme/issues/87) | **resolver test load tolerance** — decouple the `di` resolver unit tests from a tight hardcoded wall-clock budget that is not their subject (test-only; **ADR 0010**). | ✅ **Delivered** — PR [#88](https://github.com/gosharplite/tellme/pull/88) merged into `dev` (`ca792d7`) |
| **future slices (candidates)** | [#69](https://github.com/gosharplite/tellme/issues/69) · [#60](https://github.com/gosharplite/tellme/issues/60) · [#13](https://github.com/gosharplite/tellme/issues/13) | the **composition root + single-ownership** refactor ([#69](https://github.com/gosharplite/tellme/issues/69)); the **dogfooding track** ([#60](https://github.com/gosharplite/tellme/issues/60)); **coverage tooling** ([#13](https://github.com/gosharplite/tellme/issues/13)). | ⏳ **Candidates** |

## Open items (non-blocking)

- **Round-041 forward items (delivered)** — (a) the **sibling wall-clock-assertion class** elsewhere in the suite (round-041 **Q6**) → its own round; (b) **#89 CLOSED (not planned)** — the `mcpDiscoveryBound` measurement was speculative (fail-open; ms-scale real `gh`); its one cheap residue (a ~10-line CLI-layer **wiring pin**: `mcp_discovery.go:46` → `di.NewGhTokenResolver(mcpDiscoveryBound)`) is a **ride-along** for the next round touching `mcp_discovery.go`/`di`; (c) **#90 CLOSED (not planned)** — ADR 0010 already owns the D1–D4 rules; if ever aggregated, it is a single doc edit, no round; (d) the 2 s ceiling's **accepted residual** (whole-suite package parallelism not covered by its margin) is recorded in ADR 0010's Consequences.
- **Round-040 forward items (delivered)** — (a) the **one-concurrent-block** limit → **[#69](https://github.com/gosharplite/tellme/issues/69)** (`issuecomment-5713770526`); (b) the `End`-while-write-stalled accepted residual (documented in `coordinator.go`); (c) the fixed 3 s idle default with a hermetic seam; (d) the two-figure soft-wrap residual; (e) the `toolOutputHeaderMarker` content-keying (fail-loud, no change). *(Detail in [`2026-09-17.md`](docs/archives/status/2026-09-17.md).)*
- **Round-039 forward items** — loop control flow on a `ui` presentation predicate + the ≈3×/call predicate + the missing layer-discipline gate → **[#69](https://github.com/gosharplite/tellme/issues/69)**. **Round-038/037/036/035/034 forward items** — live per-round in the archives (ADR 0008/0006, the `#69` single-ownership items, the round-022 row→feature audit blind spot → **[#60](https://github.com/gosharplite/tellme/issues/60)**).
- **Carried forward items** — PR #16 **Obs 1** stdout TTY probe **OPEN**; round-006 **Obs 3** renderer lifecycle deferred; sequential tool execution / **no pruning** / **no `flock`**; round-011 forward items.
- **PM follow-ups** — none open. (The round-040 PM wording ratifications, SC-003/SC-005, remain recorded in the archive.)
- **Issue tracker (2026-09-17, session 11)** — **[#87](https://github.com/gosharplite/tellme/issues/87) CLOSED (completed)** (delivered by round 041, PR [#88](https://github.com/gosharplite/tellme/pull/88) merged `ca792d7`; its corrected body is the closure record); **[#89](https://github.com/gosharplite/tellme/issues/89) + [#90](https://github.com/gosharplite/tellme/issues/90) CLOSED (not planned)** on operator triage — the round-041 review's forward items were disproportionate to their value (see the forward-items bullet; a review response should not mint issues — the sharper lesson is *an unbounded review loop is itself the pathology*). **Open: [#69](https://github.com/gosharplite/tellme/issues/69)** (single-ownership refactor + the round-040 one-concurrent-block item), **[#60](https://github.com/gosharplite/tellme/issues/60)** (dogfooding), **[#13](https://github.com/gosharplite/tellme/issues/13)** (coverage tooling).

## Environment notes

- **Round-041 (session 11)** — branch `041-di-resolver-test-load-tolerance` (off `dev`), **PR [#88](https://github.com/gosharplite/tellme/pull/88)** merged into `dev` (`ca792d7`; frozen head `bbd7ead`). Host: **darwin/arm64** (8 CPU, Go 1.26.6) — the host on which the `di` flake reproduces; the **linux** path is the cross-compile weak spot here (round 020). Gates at delivery: `gofmt` clean · `go vet ./...` clean · `go test -count=1 ./...` green (the `di` package **7.001s** under whole-suite contention) · `make verify` OK · topology audit PASSED (44 · 6 · 16+327 · 1674) · `go.mod`/`go.sum` unchanged · diff-level secret scan clean.
- **Round-041 delivery + propagation** — PR #88 merged (`ca792d7`) and **propagated `dev → main` (no-ff, `692a4e6`)**; `dev` was 13 commits ahead of `main` before propagation. Next session starts on **`dev`** — open round **`042-*`** via `/axb-specify` (candidates: [#69](https://github.com/gosharplite/tellme/issues/69) — the biggest single-ownership payload; [#60](https://github.com/gosharplite/tellme/issues/60); [#13](https://github.com/gosharplite/tellme/issues/13); [#89](https://github.com/gosharplite/tellme/issues/89); [#90](https://github.com/gosharplite/tellme/issues/90)).
- **Dev tooling — `tellme.sh` (external)**: the Niffler-style manager driving the `tellme` binary lives at `~/tmp/dualnets/seed/notebooks/{beta-niffler,mbp-johndoe-niffler}/tellme.sh`; invoke via `source tellme.sh` or the `tm` alias. Its banner is round-agnostic (current-state pointer = this `STATUS.md`).
- **Host / toolchain**: Go 1.26; `golangci-lint` / `staticcheck` / `govulncheck` in `$GOPATH/bin`; the `tellme` binary is installed at `$(go env GOPATH)/bin/tellme` (**refreshed** from the delivered round-041 head; `--version` → `dev`). Sandbox: `unshare -n` unavailable → the offline-path guard uses the unprivileged canary + hostile-env differential.
- **MCP client dependency (round 032)**: `github.com/modelcontextprotocol/go-sdk` **v1.7.0** (vendored), confined to `internal/infrastructure/mcp/**` behind the `tools.MCPClient` port (`verify-mcp-sdk-confinement`); the hermetic fake lives in `internal/infrastructure/mcp/mcptest/`.
- **Persistent path authorizations**: read+write for `…/beta-niffler/`, `…/mbp-johndoe-niffler/tellme.sh`, `~/.bashrc`; read for `$TELL_ME_HOME`; read for `…/tell-me-go` and `…/aixbdd-tmg` (bootstrap reference trees).
- **Secret scanning**: `mcp_github_run_secret_scanning` unavailable for this repo; closeout scans are diff-level pattern greps.
