# chat module DSL

Module-specific rows for the `chat` module — the single-prompt reasoning turn and its output presentation.
Cross-module rows live in [`../dsl.md`](../dsl.md). Merged with the interface root, every step in this
module's feature must match exactly one row.

> **Parameter escapes (round 005, grill Q6):** a quoted parameter on a single Gherkin line supports the
> escapes `\n`, `\t`, `\\`, and `\xHH` (hex byte); the step definitions decode them. This lets a
> newline-terminated or control-byte-bearing answer be written without a docstring, so the output
> contract is falsifiable.

> **Output rows (round 006):** under reference parity the **default** path renders the answer
> (`rendering-the-answer.feature` — the `is the rendered answer` row); the **`-r`** path is the byte-fidelity
> contract (`piping-the-answer-out.feature` — the `is exactly` row), and the `carries no terminal decoration`
> row is the FR-007 intent carrier for tellme's **own** chrome (the redirected stream adds no
> presentation control codes introduced by tellme; any control bytes the **answer itself** carries are
> passed through and are **not** flagged). The decoration row relies on the scenario's scripted answer
> (the `a configured provider … answers with …` Given) and fails loudly if it is absent.

> Round 004 (`004-first-reasoning-turn`): one `tellme "<prompt>"` performs **exactly one** non-streaming
> provider request and prints the answer. The provider transport is OpenAI-compatible; the request
> endpoint is the resolved provider `URL`. The E2E suite arranges all provider behaviour on an
> in-process **fake provider**, so "answers with", "error status", "unreachable", and "no usable
> answer" are all scripted on that fake (and a reachable fake doubles as the recording sink for the
> offline-path witness in the root DSL).

> **Ordering rows (round 010):** the ordering Thens (`… before the answer` / `… after the answer`) are asserted against the **merged** capture — the round-010 harness witness (`stdout` and `stderr` into one ordered buffer, the `2>&1` equivalent). They pin the *position* of the diagnostic lines relative to the answer bytes — the facet round 009 pinned only as presence. The answer itself stays on `stdout`.

> **Round 011 (`011-persona-and-payload-estimate`):** the persona rows arrange the configured `PERSON` (`the runtime home holds a configuration whose persona is …` / `… with no persona`) and assert the **leading `system` message** on the recorded request (`the request carried the persona …` / `the request carried no persona`). The estimate rows pin the **wire-faithful** pre-flight estimate (`the estimated payload exceeds the conversation messages alone` / `… is larger than the previous run's` / `… matches the previous run's` — persona + tool declarations + messages; responsive; deterministic). The existing `the request carried no earlier exchange` row is reworded so the conversation may be preceded by the leading persona `system` message (the default fixture script sets one).

> **Round 012 (`012-interactive-multiline-prompt`):** the interactive multi-line reader is a POSIX-terminal capability. The **positive** read — print the hint `[Reading multi-line input. Press Ctrl+C to cancel, or Ctrl+D to send]` to `stderr`, read the prompt from a terminal to EOF (`Ctrl+D`), bounded 1 MiB, one reasoning turn — is driven **end-to-end** through the `TELL_ME_FORCE_STDIN_TTY` diagnostic seam (`the operator is working at an interactive terminal`, review RF1) as well as at the unit layer (`internal/cli`); the empty/cancel contract (no request, exit `0`) stays a unit pin. Both a **piped** run and a run with the **null device** on standard input (`the operator starts tellme with the null device on standard input` — a character device that is NOT a terminal) print **no** reading announcement and take the boot/turn path (review B1/RF2). The `no reading announcement is reported` row asserts against the single-sourced product hint (`cli.MultiLineHint`), so a product-side text change cannot make the guard vacuous (review TD1). **POSIX-only, no Windows variant.** **Amendment A8:** a prompt-less `--new` on a terminal now archives the session **first**, then engages the reader (pinned by the `the operator starts a fresh session with "--new" and pipes "{content}"` row); a non-terminal prompt-less `--new` keeps its round-007 archive-and-exit behaviour (no announcement).

> **Round 013 (`013-vertex-gemini-provider`):** the CLI now also drives a **Vertex AI Gemini** provider (`TYPE: "gemini"`, an `aiplatform.googleapis.com` URL). A `gemini` provider's `API_KEY` ending in `.json` is a **service-account credential file**: tellme exchanges it (a JWT-RS256 assertion at the credential's `token_uri`) for an access token, sends `Authorization: Bearer`, and caches the token in memory for the run. A missing/unusable key fails with the frozen `the provider request failed` phrase (exit 6) and **no** silent bearer fallback; a family tellme cannot drive (e.g. `anthropic`) keeps its existing refusal. The `Gemini provider` Givens arrange a resolvable default config whose `TYPE` is `gemini` and whose `URL` is a **Vertex-shaped** endpoint pointing at the fake (`<fake>/v1/projects/<project>/locations/<location>/publishers/google/models`), with a service-account key file the fake's token endpoint backs; the fake serves the Vertex `:generateContent` shape and the token exchange. The frozen class-phrase vocabulary is **unchanged**.

> **Round 014 (`014-session-replay-fidelity`):** a resumed conversation that used a tool on a Vertex/Gemini provider replays the earlier tool step **with its persisted provider token** (`the request replayed the earlier tool step "{tool}" carrying the provider token "{token}"`), so the resume completes instead of failing at the provider; a conversation that used no tool, or used a tool on the OpenAI-compatible family, is unaffected (`the request replayed the earlier tool step "{tool}"`). The earlier tool-using turn is arranged by the `history` Given `the session history already holds a tool-using exchange carrying the provider token "{token}"`. The class-phrase vocabulary is **unchanged**.

> **Round 015 (`015-interactive-tui-prompt`):** the `-i` interactive TUI prompt offers live **suggestions** (recent prompts + workspace paths + registered tools), a **session dashboard**, and terminal keybindings; it is **opt-in** (`-i`/`USE_TUI_PROMPT` AND a terminal stdin), and a terminal invocation without it keeps the round-012 plain reader (coexist). Driven end-to-end through the `TELL_ME_FORCE_STDIN_TTY` seam with a **scripted key sequence**; the suggestion/dashboard Thens assert **presence** in the captured output (not exact ANSI bytes), and the shown/not-shown Thens assert a single-sourced announcement (`cli.TUIHint`). An `-i` submission is recorded in the **shared** prompt log (`$TELL_ME_HOME/output/global_prompts.jsonl`, the `output/` root) in the identical `{"timestamp","prompt"}` shape; a one-shot or piped run writes nothing. The class-phrase vocabulary is **unchanged**.

> **Round 016 (`016-interactive-prompt-visual-parity`):** the `-i` prompt now matches `tell-me-go`'s surface — a **bordered** multi-line editor (`lipgloss.NormalBorder`) above a **styled** suggestion list, with the keybinding hints in the placeholder and **no** session metrics header (the round-015 dashboard header is **removed**, superseding its `reports the active provider` / `reports the session's token usage and turn count` rows). The suggestion refresh is **debounced** and **cancelable**, and entries spanning more than three lines are **dropped**; `Tab`/`Shift+Tab` **insert** the selection (replacing the last token for a single-token suggestion on a multi-word line). The chrome Thens assert **presence/absence** in the captured output (not exact ANSI bytes); the **resize/reflow** mechanics (a `WindowSizeMsg`-driven editor width), the **debounce timing** of the refresh, and the **last-token replacement** (a multi-word line + a single-token suggestion) are **unit** pins (no pty; see `features/acceptance/composing-with-settled-suggestions.feature` and `…/fitting-the-terminal-window.feature`). The class-phrase vocabulary is **unchanged**.

## Given

| DSL 句型 | Gherkin 參數 | Data Table 參數 | 預設參數 | StepDef 實作語意 |
| --- | --- | --- | --- | --- |
| `a configured provider "{provider}" whose endpoint answers with "{answer}"` | `provider`: string; the selected provider key. `answer`: string; the answer text the endpoint returns. | 不支援 | `config`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` with `SELECTED_PROVIDER` = `{provider}`; endpoint URL = the fake provider. | `怎麼做`: write a resolvable default configuration whose `SELECTED_PROVIDER` is `{provider}` and whose `PROVIDERS.{provider}.URL` points at the fake provider; script the fake to answer `{answer}`. `權威狀態落地`: the configuration is resolvable and the fake is ready to answer. `回寫`: the configuration file + the fake's scripted response. |
| `configured providers "{provider_a}" and "{provider_b}" whose endpoints answer` | `provider_a`, `provider_b`: string; two provider keys. | 不支援 | `config`: default config with both providers in the registry; each endpoint = the fake. | `怎麼做`: write a resolvable default configuration with two providers (`{provider_a}`, `{provider_b}`), both pointing at the fake (each answering a canned text). `權威狀態落地`: the registry holds both providers and both fakes are ready. `回寫`: the configuration file. |
| `a configured provider "{provider}" whose endpoint answers with an error status` | `provider`: string; the selected provider key. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at the fake; script the fake to return an error status (e.g. `500`). `權威狀態落地`: the fake is scripted to answer with a non-2xx status. `回寫`: the configuration file + the fake's scripted response. |
| `a configured provider "{provider}" whose endpoint is unreachable` | `provider`: string; the selected provider key. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = a closed/unroutable address. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at a closed port / unroutable address, so the connection fails. `權威狀態落地`: the endpoint refuses the connection. `回寫`: the configuration file. |
| `a configured provider "{provider}" whose endpoint answers with no usable answer` | `provider`: string; the selected provider key. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at the fake; script the fake to return a body that carries no usable answer (e.g. an empty `choices`). `權威狀態落地`: the fake is scripted to answer with an uninterpretable body. `回寫`: the configuration file + the fake's scripted response. |
| `the rendered width is "{width}"` | `width`: string; the rendered column width. | 不支援 | `來源`: arranged via the environment override `TELL_ME_WRAP_WIDTH`. | `怎麼做`: set `TELL_ME_WRAP_WIDTH` to `{width}` in the subprocess environment. `權威狀態落地`: the effective rendered width resolves to `{width}`. `回寫`: none. |
| `the working directory contains a file "{name}" whose text is "{content}"` | `name`: string; the file name relative to the run's working directory. `content`: string; the file's text. | 不支援 | `工作目錄`: the subprocess working directory the run is launched in. | `怎麼做`: create a file named `{name}` with text `{content}` in the subprocess working directory. `權威狀態落地`: the file exists on disk and is readable. `回寫`: the file. |
| `a configured provider "{provider}" whose endpoint asks tellme to read "{path}" and then answers with "{answer}"` | `provider`: string; the selected provider key. `path`: string; the file the fake asks tellme to read. `answer`: string; the final answer text. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. `行為`: the fake returns a `read_files` tool-call response for `{path}` on the first request, then answers `{answer}`. | `怎麼做`: write the config; script the fake to return a tool-call response (requesting `read_files` on `{path}`) once, then a final answer `{answer}` on the next request. `權威狀態落地`: the fake is scripted for a two-step exchange (tool call → answer). `回寫`: the configuration file + the fake's scripted responses. |
| `a configured provider "{provider}" whose endpoint always asks tellme to read "{path}"` | `provider`: string; the selected provider key. `path`: string; the file the fake asks tellme to read. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. `行為`: every request returns a `read_files` tool-call response for `{path}`. | `怎麼做`: write the config; script the fake to return the same `read_files` tool-call response on every request (never a final answer), so the loop hits its bound. `權威狀態落地`: the fake never emits a final answer. `回寫`: the configuration file + the fake's scripted response. |
| `a configured provider "{provider}" whose endpoint asks for a tool that is not available` | `provider`: string; the selected provider key. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. `行為`: the fake returns a tool call naming a tool tellme does not provide. | `怎麼做`: write the config; script the fake to return a tool-call response naming a tool that is not declared. `權威狀態落地`: the fake requests an unknown tool. `回寫`: the configuration file + the fake's scripted response. |
| `the tool-loop limit is "{limit}"` | `limit`: string; the tool-iteration bound. | 不支援 | `來源`: arranged via the environment override `MAX_TOOL_LOOP`. | `怎麼做`: set `MAX_TOOL_LOOP` to `{limit}` in the subprocess environment. `權威狀態落地`: the effective loop bound resolves to `{limit}`. `回寫`: none. |
| `a configured provider "{provider}" whose endpoint asks tellme to summarise the conversation and then answers with "{answer}"` | `provider`: string; the selected provider key. `answer`: string; the final answer text. | 不支援 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. `行為`: the fake returns a summarise tool-call response, then answers `{answer}`. | `怎麼做`: write the config; script the fake to return a tool-call response requesting the summarise tool once, then a final answer `{answer}`. `權威狀態落地`: the fake is scripted for a two-step exchange. `回寫`: the configuration file + the fake's scripted responses. |
| `a configured provider "{provider}" whose endpoint answers with "{answer}" and reports its usage` | `provider`: string; the selected provider key. `answer`: string; the answer text the endpoint returns. | 不支援 | `config`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` with `SELECTED_PROVIDER` = `{provider}`; endpoint URL = the fake provider. `行為`: the fake answers `{answer}` and attaches a `usage` block with a scripted prompt-token count. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at the fake; script the fake to answer `{answer}` with a JSON `usage` object (`{"prompt_tokens": N, "completion_tokens": M, "total_tokens": N+M}`). `權威狀態落地`: the fake reports a usage block. `回寫`: the configuration file + the fake's scripted response. |
| `a configured provider "{provider}" whose endpoint answers with "{answer}" and reports no usage` | `provider`: string; the selected provider key. `answer`: string; the answer text the endpoint returns. | 不支援 | `config`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` with `SELECTED_PROVIDER` = `{provider}`; endpoint URL = the fake provider. `行為`: the fake answers `{answer}` and attaches **no** `usage` block. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at the fake; script the fake to answer `{answer}` with no `usage` object. `權威狀態落地`: the fake reports no usage. `回寫`: the configuration file + the fake's scripted response. |
| `the payload budget is "{budget}"` | `budget`: string; the payload budget value. | 不支援 | `來源`: arranged via the environment override `MAX_HISTORY_TOKENS`. | `怎麼做`: set `MAX_HISTORY_TOKENS` to `{budget}` in the subprocess environment. `權威狀態落地`: the effective payload budget resolves to `{budget}`. `回寫`: none. |
| `the runtime home holds a configuration whose persona is "{persona}"` | `persona`: string; the persona instruction (may be empty). | 不支援 | `來源`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` written by the provider Given (its default persona is `e2e persona`). | `怎麼做`: set the default configuration's `PERSON` attribute to `{persona}` (an empty string clears it); the default config must already exist (arranged by the provider Given). `權威狀態落地`: the default config's `PERSON` is `{persona}`. `回寫`: the configuration file. |
| `the runtime home holds a configuration with no persona` | 無 | 不支援 | `來源`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` written by the provider Given. | `怎麼做`: clear the default configuration's `PERSON` attribute (set it to the empty string); the default config must already exist. `權威狀態落地`: the default config carries no persona. `回寫`: the configuration file. |
| `a previous run with the persona "{persona}" and the prompt "{prompt}" reported an estimated payload status` | `persona`: string; the persona the previous run used. | 不支援 | `安排`: against a throwaway **copy** of the runtime home, set `PERSON` to `{persona}`, run `tellme "{prompt}"` once, parse the pre-flight `~<n>` estimate, and store it on the scenario; the real home (history, config) is left untouched, and the fake's recorded request count is restored. | `怎麼做`: set the persona on a copy of the default config, run tellme once against the fake, parse and store the estimated payload size `n`. `權威狀態落地`: a prior estimated payload size is recorded; the real home is unchanged. `回寫`: the stored prior estimate. |
| `the operator is working at an interactive terminal` | 無 | 不支援 | `來源`: arranged via the diagnostic environment seam `TELL_ME_FORCE_STDIN_TTY`. | `怎麼做`: set `TELL_ME_FORCE_STDIN_TTY` to `1` in the subprocess environment so tellme's terminal probe reports the stream as a terminal (the round-012 review RF1 seam). `權威狀態落地`: the effective terminal probe reports stdin as a terminal. `回寫`: none. |
| `a configured Gemini provider "{provider}" whose endpoint answers with "{answer}"` | `provider`: string; the selected provider key. `answer`: string; the answer text the endpoint returns. | 不支援 | `config`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` with `SELECTED_PROVIDER` = `{provider}`; the entry `TYPE` is `gemini`, its `URL` is a **Vertex-shaped** endpoint at the fake (`<fake>/v1/projects/e2e/locations/global/publishers/google/models`), and its `API_KEY` is a service-account `.json` key file the fake's token endpoint backs. | `怎麼做`: write a resolvable default configuration whose `SELECTED_PROVIDER` is `{provider}` and whose `PROVIDERS.{provider}` is a `gemini` entry — a Vertex-shaped `URL` pointing at the fake, `API_KEY` a `.json` service-account key file (plus any `MAX_TOKENS`/`THINKING_BUDGET`/`THINKING_LEVEL`); write the service-account key file (its `token_uri` is the fake token endpoint); script the fake to answer `{answer}` in the Vertex `:generateContent` shape and to serve the JWT-bearer token exchange. `權威狀態落地`: the configuration resolves, the credential file exists, and the fake is ready. `回寫`: the configuration file, the key file, and the fake's scripted responses. |
| `a configured Gemini provider "{provider}" whose endpoint asks tellme to read "{path}" and then answers with "{answer}"` | `provider`: string; the selected provider key. `path`: string; the file the fake asks tellme to read. `answer`: string; the final answer text. | 不支援 | `config`: as the `a configured Gemini provider … answers with …` Given above; `行為`: the fake returns a Vertex `functionCall` requesting `read_files` on `{path}` on the first request, then a final Vertex answer `{answer}`. | `怎麼做`: as above; script the fake for a two-step Vertex exchange (a `functionCall` for `read_files` on `{path}`, then the answer `{answer}`). `權威狀態落地`: the fake is scripted for a two-step exchange. `回寫`: the configuration file, the key file, and the fake's scripted responses. |
| `a configured Gemini provider "{provider}" whose key file is missing` | `provider`: string; the selected provider key. | 不支援 | `config`: the default config with `SELECTED_PROVIDER` = `{provider}`; the entry `TYPE` is `gemini`, its `API_KEY` is a `.json` path that does not exist. | `怎麼做`: write the config selecting `{provider}` as a `gemini` entry whose `API_KEY` ends in `.json` (e.g. `"${TELL_ME_HOME}/secrets/key.json"`) and whose file is absent. `權威狀態落地`: the credential file is missing. `回寫`: the configuration file. |
| `the configured Gemini provider entry allows at most {tokens} output tokens` | `tokens`: integer; the output-token cap. | 不支援 | `來源`: the default config written by the Gemini provider Given — set its selected provider entry's `MAX_TOKENS`. | `怎麼做`: set `PROVIDERS.{selected}.MAX_TOKENS` to `{tokens}` in the default configuration. `權威狀態落地`: the entry's `MAX_TOKENS` is `{tokens}`. `回寫`: the configuration file. |
| `a configured provider "{provider}" of a family tellme cannot drive` | `provider`: string; the selected provider key. | 不支援 | `config`: the default config with `SELECTED_PROVIDER` = `{provider}`; the entry `TYPE` is `anthropic` (a family tellme cannot drive). | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose entry `TYPE` is a family the transport cannot drive (e.g. `anthropic`) with a `URL` and `API_KEY`. `權威狀態落地`: the configuration resolves; the family is not supported by the transport. `回寫`: the configuration file. |
| `the session history already holds a tool-using exchange carrying the provider token "{token}"` | `token`: string; the opaque provider token the replayed tool step must carry. | 不支援 | `工作區`: writes into `$TELL_ME_HOME/output/<mode>/history.jsonl` (the effective mode's session workspace). | `怎麼做`: create the per-mode session workspace directory if absent, then append one JSON line carrying `prompt`, `answer`, and an ordered `steps` array with one step `{tool: "read_files", arguments, result, signature: "{token}"}` (the widened, signed record from `specs/truth/data/data-model.dbml`). `權威狀態落地`: the active history holds a completed tool-using turn whose step carries `{token}`. `回寫`: the session history file. |
| `the session history already holds a tool-using exchange with no provider token` | 無 | 不支援 | `工作區`: writes into `$TELL_ME_HOME/output/<mode>/history.jsonl` (the effective mode's session workspace). | `怎麼做`: create the per-mode session workspace directory if absent, then append one JSON line carrying `prompt`, `answer`, and an ordered `steps` array with one step `{tool: "read_files", arguments, result}` (no `signature` — the provider-neutral shape). `權威狀態落地`: the active history holds a completed tool-using turn whose step carries no provider token. `回寫`: the session history file. |
| `the shared prompt log already holds "{prompt}"` | `prompt`: string; a prompt already recorded in the shared log. | 不支援 | `工作區`: writes `$TELL_ME_HOME/output/global_prompts.jsonl` (the `output/` root — not `output/<mode>/`). | `怎麼做`: create `$TELL_ME_HOME/output/` if absent, then append one JSON line `{"timestamp":"<RFC3339>","prompt":"{prompt}"}` to `global_prompts.jsonl`. `權威狀態落地`: the shared prompt log holds `{prompt}`. `回寫`: the shared prompt log file. |
| `a configured provider "{provider}" whose endpoint answers with "{answer}" and reports the token usage:` | `provider`: string; the selected provider key. `answer`: string; the answer text the endpoint returns. | 支援（表頭 `prompt`, `cached`, `completion`, `thinking`；單列）。 | `config`: the default `$TELL_ME_HOME/configs/<effective mode>.yaml` with `SELECTED_PROVIDER` = `{provider}`; endpoint URL = the fake provider. | `怎麼做`: write a resolvable default configuration selecting `{provider}` whose endpoint points at the fake; script the fake to answer `{answer}` with a JSON `usage` block carrying the cited counts — `prompt_tokens` = prompt, `completion_tokens` = completion, `total_tokens` = prompt + completion + thinking, and the details `prompt_tokens_details.cached_tokens` = cached and `completion_tokens_details.reasoning_tokens` = thinking. `權威狀態落地`: the fake reports a usage block with cached/reasoning details. `回寫`: the configuration file + the fake's scripted response. |
| `a configured provider "{provider}" whose endpoint asks tellme to read "{path}" and then answers with "{answer}" and reports the token usage:` | `provider`: string; the selected provider key. `path`: string; the file the fake asks tellme to read. `answer`: string; the final answer text. | 支援（表頭 `prompt`, `cached`, `completion`, `thinking`；單列）。 | `config`: default config with `SELECTED_PROVIDER` = `{provider}`; endpoint = the fake. `行為`: the fake returns a `read_files` tool-call response for `{path}` on the first request (also carrying the cited usage block), then answers `{answer}` with the cited usage block — so **both** provider calls report usage. | `怎麼做`: write the config; script the fake to return a tool-call response (requesting `read_files` on `{path}`) carrying the JSON `usage` block, then a final answer `{answer}` carrying the same JSON `usage` block (as the `… reports the token usage:` Given). `權威狀態落地`: the fake is scripted for a two-step exchange where **each** response (the tool-call response and the answer) reports the cited usage, so the turn accumulates **two** usage-bearing calls and `$#1 < $#2`. `回寫`: the configuration file + the fake's scripted responses. |
| `the configuration prices the active model with hit "{hit}", miss "{miss}", and completion "{comp}" per million tokens` | `hit`, `miss`, `comp`: string; the USD-per-million-token rates. | 不支援 | `來源`: the default config written by the provider Given — add a `MODELS` entry for the active provider's `MODEL`. | `怎麼做`: set the default configuration's `MODELS: { <active model>: { PRICING: { HIT: {hit}, MISS: {miss}, COMP: {comp} } } }` (the round-018 config-only pricing table). `權威狀態落地`: the active model's pricing resolves to the cited rates. `回寫`: the configuration file. |
| `the session's usage log already records a prior call` | 無 | 不支援 | `工作區`: writes `$TELL_ME_HOME/output/<mode>/tokens.log` (the effective mode's session workspace). | `怎麼做`: create the per-mode session workspace directory if absent, then append one JSON record — a prior API call with fixed counts (prompt 10, cached 6, completion 3, thinking 2) and a fixed non-zero cost — to `tokens.log`. `權威狀態落地`: the active usage log already holds one earlier call. `回寫`: the usage log file. |

## When

| DSL 句型 | Gherkin 參數 | Data Table 參數 | 預設參數 | StepDef 實作語意 |
| --- | --- | --- | --- | --- |
| `the operator starts tellme with the prompt "{prompt}"` | `prompt`: string; the operator's prompt. | 不支援 | `旗標`: no `-c`; the configuration is the default for the effective mode. | `怎麼做`: run `tellme "{prompt}"` under the current environment. `權威狀態落地`: the process has run to completion. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator starts tellme with the prompt "{prompt}" and the raw flag` | `prompt`: string; the operator's prompt. | 不支援 | `旗標`: `-r` (raw) plus the positional prompt; no `-c`; the configuration is the default for the effective mode. | `怎麼做`: run `tellme -r "{prompt}"` under the current environment. `權威狀態落地`: the process has run to completion; the raw path bypassed rendering. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator runs tellme's diagnostic with the prompt "{prompt}"` | `prompt`: string; a positional prompt supplied alongside the diagnostic flag. | 不支援 | `旗標`: the run is invoked with `-d` plus a positional prompt. | `怎麼做`: run `tellme -d "{prompt}"` under the current environment. `權威狀態落地`: `-d` dispatch precedes the prompt turn, so the diagnostic report is produced. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator pipes "{content}" into tellme` | `content`: string; the text written to standard input. | 不支援 | `旗標`: no `-c` and no positional prompt; the configuration is the default for the effective mode. `輸入`: `{content}` is written to a piped standard input, then EOF. | `怎麼做`: run `tellme` (no positional prompt) with `{content}` on a piped standard input, under the current environment. `權威狀態落地`: the process has run to completion. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator pipes "{content}" into tellme with the instruction "{instruction}"` | `content`: string; the text written to standard input. `instruction`: string; the positional instruction argument. | 不支援 | `旗標`: no `-c`; the configuration is the default for the effective mode. `輸入`: `{instruction}` is the positional argument; `{content}` is written to a piped standard input. | `怎麼做`: run `tellme "{instruction}"` with `{content}` on a piped standard input. `權威狀態落地`: the process has run to completion. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator pipes nothing into tellme` | 無 | 不支援 | `旗標`: no `-c` and no positional prompt; the standard input is an empty pipe (immediate EOF). | `怎麼做`: run `tellme` with an empty piped standard input. `權威狀態落地`: the process has run to completion. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator runs tellme's diagnostic with "{content}" piped in` | `content`: string; the text written to standard input. | 不支援 | `旗標`: the run is invoked with `-d`; `{content}` is written to a piped standard input. | `怎麼做`: run `tellme -d` with `{content}` on a piped standard input. `權威狀態落地`: `-d` dispatch precedes any prompt handling, so the diagnostic report is produced. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator uses the persona "{persona}" and starts tellme with the prompt "{prompt}"` | `persona`: string; the persona for this run. `prompt`: string; the operator's prompt. | 不支援 | `旗標`: no `-c`; the configuration is the default for the effective mode. | `怎麼做`: set the default configuration's persona to `{persona}`, then run `tellme "{prompt}"` under the current environment. `權威狀態落地`: the process has run to completion (one run with the given persona). `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator starts tellme with the null device on standard input` | 無 | 不支援 | `輸入`: the null device (`/dev/null`) is the child's standard input — a character device that is not a terminal. | `怎麼做`: run `tellme` (no positional prompt) with the null device on standard input. `權威狀態落地`: the process has run to completion. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator starts a fresh session with "--new" and pipes "{content}"` | `content`: string; the text written to standard input. | 不支援 | `旗標`: `--new` plus no positional prompt; no `-c`. `輸入`: `{content}` is written to a piped standard input, then EOF. | `怎麼做`: run `tellme --new` with `{content}` on a piped standard input. `權威狀態落地`: the process has run to completion; with a terminal stdin (the forced-terminal seam) the session is archived and the prompt read from stdin. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator opens the interactive prompt` | 無 | 不支援 | `旗標`: `-i`; `輸入`: a scripted terminal key sequence that opens the prompt and then aborts (so the process exits). `來源`: the forced-terminal seam. | `怎麼做`: run `tellme -i` with the forced-terminal seam (`TELL_ME_FORCE_STDIN_TTY=1`) and a scripted key sequence (open → abort). `權威狀態落地`: the process has run to completion with the interactive prompt engaged. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator opens the interactive prompt and types "{query}"` | `query`: string; the initial text typed into the prompt. | 不支援 | `旗標`: `-i`; `輸入`: a scripted key sequence (open → type `{query}` → abort). | `怎麼做`: run `tellme -i` with the forced-terminal seam and a scripted key sequence that types `{query}` then aborts. `權威狀態落地`: the process has run to completion; the prompt computed suggestions for `{query}`. `回寫`: captured exit code, stdout, stderr. |
| `the operator submits the prompt "{prompt}" at the interactive prompt` | `prompt`: string; the composed prompt to submit. | 不支援 | `旗標`: `-i`; `輸入`: a scripted key sequence (open → type `{prompt}` → submit). | `怎麼做`: run `tellme -i` with the forced-terminal seam and a scripted key sequence that types `{prompt}` then submits. `權威狀態落地`: the process has run to completion; the submitted text became the prompt of one reasoning turn. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator aborts the interactive prompt` | 無 | 不支援 | `旗標`: `-i`; `輸入`: a scripted key sequence (open → abort). | `怎麼做`: run `tellme -i` with the forced-terminal seam and a scripted sequence that aborts. `權威狀態落地`: the process has run to completion with no prompt sent. `回寫`: captured exit code, stdout, stderr. |
| `the operator pipes "{content}" into tellme with the interactive prompt enabled` | `content`: string; the text written to standard input. | 不支援 | `旗標`: `-i` (plus no positional prompt); `輸入`: `{content}` on a piped standard input. | `怎麼做`: run `tellme -i` with `{content}` on a piped standard input. `權威狀態落地`: the process has run to completion; the non-terminal input took the piped path. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |
| `the operator opens the interactive prompt, types "{query}", and accepts the current suggestion` | `query`: string; the initial text typed before accepting. | 不支援 | `旗標`: `-i`; `輸入`: a scripted key sequence (open → type `{query}` → `Tab` accept → abort). | `怎麼做`: run `tellme -i` with the forced-terminal seam and a scripted key sequence that types `{query}`, presses `Tab` to accept the current suggestion, then aborts. `權威狀態落地`: the process has run to completion; the accepted suggestion was inserted into the editor. `回寫`: captured exit code, stdout, stderr. |
| `the operator starts a fresh session with "--new" and the prompt "{prompt}"` | `prompt`: string; the operator's prompt. | 不支援 | `旗標`: `--new` plus the positional prompt; no `-c`; the configuration is the default for the effective mode. | `怎麼做`: run `tellme --new "{prompt}"` under the current environment. `權威狀態落地`: the process has run to completion; the active session was archived before the turn. `回寫`: captured exit code, stdout, stderr, and the fake's recorded requests. |

## Then

| DSL 句型 | Gherkin 參數 | Data Table 參數 | 預設參數 | StepDef 實作語意 |
| --- | --- | --- | --- | --- |
| `tellme sends exactly one request to the provider "{provider}"` | `provider`: string; the provider that must have received the request. | 不支援 | 無 | `必查`: `呈現結果`: the fake provider for `{provider}` recorded **exactly one** request. `權威狀態`: that request carried the prompt with the resolved model and credential for `{provider}`. `不該發生`: no request reached any other provider. |
| `tellme prints the provider's answer "{answer}"` | `answer`: string; the expected answer text. | 不支援 | 無 | `必查`: `呈現結果`: stdout contains the provider's answer `{answer}`. `不該發生`: the answer must not be swallowed, and must not be emitted only on stderr. |
| `tellme exits with the provider error code` | 無 | 不支援 | `碼值`: `6` (**pinned**, round 004 / Clarify Q3) — a non-zero code distinct from success (0) and from every other error class (usage 2, configuration 3, environment 4, diagnostic 5). | `必查`: `呈現結果`: the exit code **equals the pinned provider error code `6`**. `不該發生`: it must not collapse to the success code or to any other error class. |
| `the request carried the piped content "{content}"` | `content`: string; the expected prompt text. | 不支援 | 無 | `必查`: `呈現結果`: the fake provider recorded exactly one request whose current user prompt (the request's user-role message; a leading persona `system` message is ignored) is `{content}` (trimmed). `不該發生`: the recorded request must not carry extra or missing text. |
| `the request carried the instruction "{instruction}" followed by the piped content "{content}"` | `instruction`: string; the positional instruction. `content`: string; the piped content. | 不支援 | 無 | `必查`: `呈現結果`: the recorded request's current user prompt (a leading persona `system` message is ignored) equals `{instruction}`, then a newline, then `{content}`. `不該發生`: the piped content must not replace or precede the instruction. |
| `the captured standard output is the rendered answer, not its raw Markdown` | 無 | 不支援 | `來源`: the scripted answer from the scenario's `a configured provider … answers with …` Given. | `必查`: `呈現結果`: the captured standard output carries the answer's **rendered** form — the literal Markdown emphasis markers (`**`, `_`) that the scripted answer carries are **absent**, and the answer's words are present. `不該發生`: the literal Markdown source must not be printed verbatim. |
| `the captured standard output is wrapped so that no line is wider than {width} columns` | `width`: integer; the maximum rendered line width. | 不支援 | 無 | `必查`: `呈現結果`: the **visible text** of every line of the captured standard output — after ANSI-stripping and normalising the renderer's leading margin whitespace — is at most `{width}` **characters** wide (the acceptance answer is ASCII; M5 clarifies the metric). `不該發生`: no rendered line's visible text exceeds `{width}` characters. |
| `the captured standard output contains the answer on a single line` | 無 | 不支援 | `來源`: the scripted answer from the scenario's `a configured provider … answers with …` Given. | `必查`: `呈現結果`: the captured standard output carries the scripted answer as **one unwrapped line** — no line break inserted by tellme within the answer. `不該發生`: the raw answer must not be wrapped. |
| `the captured standard output is exactly "{answer}"` | `answer`: string; the expected answer text (decoded through the escape convention). | 不支援 | 無 | `必查`: `呈現結果`: the captured standard output equals `{answer}` — the answer bytes **verbatim** — followed by exactly one newline **appended by the CLI**. `不該發生`: no leading bytes and no system-added bytes beyond the single appended terminating newline. |
| `the captured standard output carries no terminal decoration` | 無 | 不支援 | 無 | `必查`: `呈現結果`: the captured standard output adds **no presentation control codes introduced by tellme** — the answer bytes are passed through unchanged, plus the single CLI-appended terminating newline under `-r` (any control bytes the **answer itself** carries are passed through and are **not** flagged). `不該發生`: tellme must not wrap or decorate the captured answer with its own presentation control codes. |
| `tellme completes the turn without waiting for terminal input` | 無 | 不支援 | 無 | `必查`: `呈現結果`: the run completed (an exit code was captured) **within a bounded deadline** with the prompt read from the piped standard input; the process did not block for a terminal. `不該發生`: the run must not hang awaiting interactive input (a hang fails with an explicit deadline error, not a suite timeout). |
| `tellme stored the exchange "{prompt}" and "{answer}" in the session history` | `prompt`: string; the operator prompt. `answer`: string; the provider's answer. | 不支援 | 無 | `必查`: `權威狀態`: the session history file `<runtime_home>/output/<mode>/history.jsonl` holds a line whose prompt and answer are `{prompt}` and `{answer}`. `不該發生`: a completed exchange must not be missing from the store. |
| `the request carried the earlier exchange "{prompt}" and "{answer}"` | `prompt`: string; an earlier user prompt. `answer`: string; the earlier provider answer. | 不支援 | `來源`: the arranged session history (the `the session history already holds the exchanges:` Given). | `必查`: `呈現結果` / `權威狀態`: the fake recorded exactly one request whose messages carry the earlier user message `{prompt}` and the earlier assistant message `{answer}`, in order, ahead of the current prompt. `不該發生`: the earlier exchange must not be dropped or reordered. |
| `the request carried no earlier exchange` | 無 | 不支援 | `來源`: the fake's recorded request (`MessagesAt`). | `必查`: `呈現結果`: the fake recorded exactly one request whose messages are the current user prompt, preceded only by the leading `system` (persona) message when a persona is configured (the default fixture script sets one). `不該發生`: no earlier turn may be carried when the session history holds no exchange. |
| `tellme read "{path}" using its read_files tool` | `path`: string; the file the run was asked to read. | 不支援 | `來源`: the scenario's `… whose endpoint asks tellme to read "{path}" …` Given. | `必查`: `權威狀態`: the fake recorded the model's `read_files` tool call for `{path}` and the run fed that tool's result back into the conversation. `不該發生`: the requested read must not be skipped. |
| `tellme used no tool` | 無 | 不支援 | 無 | `必查`: `呈現結果` / `權威狀態`: the run invoked no tool — the request carried no tool result and the fake recorded no tool round. `不該發生`: a prompt that needs no tool must not invoke one. |
| `the run reported the tool call "{tool}" on its diagnostic output` | `tool`: string; the tool name that must appear in the tool-loop log. | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries a tool-loop log line naming `{tool}`. `不該發生`: the tool-loop activity must not be silent, and the log must not be written to standard output. |
| `the request carried the read-tool error for "{path}"` | `path`: string; the file whose read failed. | 不支援 | `來源`: the scenario's `… asks tellme to read "{path}" …` Given (a path that does not exist). | `必查`: `權威狀態`: the fake recorded a request carrying a `read_files` tool result for `{path}` whose content is an error, fed back as a non-terminal result. `不該發生`: a recoverable tool error must be fed back, not swallowed and not made terminal. |
| `tellme stopped after {count} tool iterations` | `count`: integer; the expected number of tool-iteration rounds. | 不支援 | `來源`: the arranged `MAX_TOOL_LOOP` bound (the `the tool-loop limit is "{limit}"` Given). | `必查`: `權威狀態`: the fake recorded exactly `{count}` tool-iteration rounds. `不該發生`: the loop must not exceed the bound nor stop short of it under a perpetually tool-requesting provider. |
| `tellme exits with the tool error code` | 無 | 不支援 | `碼值`: `7` (**pinned**, round 008) — a non-zero code distinct from success (0) and from every other class (usage 2, configuration 3, environment 4, diagnostic 5, provider 6). | `必查`: `呈現結果`: the exit code **equals `7`**. `不該發生`: it must not collapse to the success code or to any other error class. |
| `tellme summarised the earlier conversation using its summarise tool` | 無 | 不支援 | `來源`: the scenario's `… asks tellme to summarise the conversation …` Given. | `必查`: `權威狀態`: the fake recorded the model's summarise tool call and the run fed the produced summary back into the conversation. `不該發生`: the summarisation must be an on-demand tool, not automatic context pruning. |
| `the earlier conversation records are unchanged` | 無 | 不支援 | `來源`: the arranged session history (the `the session history already holds the exchanges:` Given). | `必查`: `權威狀態`: the history file's previously stored lines are byte-unchanged. `不該發生`: invoking the summarise tool must not rewrite earlier records. |
| `tellme reports the estimated payload status for the turn` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries a **pre-flight** status line matching `[HH:MM:SS] Payload: ~<n>/<max> tokens - <mode> - <model>` (the estimate is `~`-prefixed). `不該發生`: the line must not be the `~`-less measured line, and must not be written to standard output. |
| `tellme reports the measured payload status for the turn` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries a **post-turn** status line matching `[HH:MM:SS] Payload: <n>/<max> tokens - <mode> - <model>` (no `~`). `不該發生`: the line must not be `~`-prefixed. |
| `tellme reports no measured payload status` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries no `~`-less payload status line. `不該發生`: a provider that reports no usage must not yield a measured line. |
| `the payload status measures against a budget of {budget} tokens` | `budget`: integer; the expected payload budget. | 不支援 | `來源`: the reported payload status line (the pre-flight or the measured line). | `必查`: `呈現結果`: the `<max>` field of the reported payload status line equals `{budget}`. `不該發生`: the budget shown must not differ from the configured value. |
| `the payload status names the active mode and model` | 無 | 不支援 | `來源`: the reported payload status line. | `必查`: `呈現結果`: the reported payload status line ends with ` - <mode> - <model>`, naming the run's effective mode and the active provider's configured `MODEL` attribute (reference parity — **not** the registry key). `不該發生`: the mode/model suffix must not be missing. |
| `the estimated payload status is reported before the answer` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). `順序`: the estimated (`~`) line precedes the answer bytes in the **merged** (`stdout`+`stderr`) view. | `必查`: `呈現結果`: in the merged capture, the pre-flight estimated payload status line appears **before** the answer bytes. `不該發生`: the estimated line must not appear after the answer. |
| `the measured payload status is reported after the answer` | 無 | 不支援 | `通道`: standard error. `順序`: the measured line follows the answer bytes in the **merged** view. | `必查`: `呈現結果`: in the merged capture, the measured payload status line appears **after** the answer bytes. `不該發生`: the measured line must not appear before the answer. |
| `the tool activity is reported before the answer` | 無 | 不支援 | `通道`: standard error. `順序`: the tool-loop log line(s) precede the answer bytes in the **merged** view. | `必查`: `呈現結果`: in the merged capture, the tool-loop log line appears **before** the answer bytes. `不該發生`: the tool-loop log must not appear after the answer. |
| `the request carried the persona "{persona}"` | `persona`: string; the expected persona instruction. | 不支援 | `來源`: the fake's recorded request (`MessagesAt`). | `必查`: `呈現結果`: the recorded request's messages begin with a `system`-role message whose content equals `{persona}`. `不該發生`: the persona must not be absent, and must not be merged into the user message. |
| `the request carried no persona` | 無 | 不支援 | `來源`: the fake's recorded request. | `必查`: `呈現結果`: the recorded request's messages carry no `system`-role message. `不該發生`: an empty persona must not produce a system message. |
| `the estimated payload exceeds the conversation messages alone` | 無 | 不支援 | `來源`: the reported pre-flight estimate + the recorded request's messages. | `必查`: `呈現結果`: the reported pre-flight estimate `~<n>` is strictly greater than the estimator applied to the recorded request's messages excluding any leading `system` message. `不該發生`: the estimate must not equal the conversation-message-only size — it must also count the persona and the offered tools. |
| `the estimated payload is larger than the previous run's` | 無 | 不支援 | `來源`: the stored previous estimate (the `a previous run …` Given) and the reported estimate of the current run. | `必查`: `呈現結果`: the current pre-flight estimate `~<n>` is strictly greater than the stored previous estimate. `不該發生`: a strictly larger wired payload (a longer persona) must not yield an equal or smaller estimate. |
| `the estimated payload matches the previous run's` | 無 | 不支援 | `來源`: the stored previous estimate and the reported estimate of the current run. | `必查`: `呈現結果`: the current pre-flight estimate `~<n>` equals the stored previous estimate. `不該發生`: identical inputs must not yield a different estimate. |
| `every request carried the persona "{persona}"` | `persona`: string; the expected persona instruction. | 不支援 | `來源`: every recorded request body (`BodyAt` / `RequestCount`). | `必查`: `呈現結果`: **every** request the fake recorded leads with a `system`-role message whose content equals `{persona}` (round-011 `FR-004` — the persona rides every request of the turn, including tool-driven completions). `不該發生`: any request must be missing the persona. |
| `no reading announcement is reported` | 無 | 不支援 | `來源`: the captured standard output and standard error of the run. | `必查`: `呈現結果`: neither the captured standard output nor standard error carries the multi-line reading hint (`[Reading multi-line input. Press Ctrl+C to cancel, or Ctrl+D to send]`). `不該發生`: the interactive reader must not engage on a non-terminal input. |
| `the reading announcement is reported on the diagnostic output` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). `來源`: the product hint literal `cli.MultiLineHint`. | `必查`: `呈現結果`: the captured standard error carries the multi-line reading hint (`[Reading multi-line input. Press Ctrl+C to cancel, or Ctrl+D to send]`). `不該發生`: the hint must not be written to standard output. |
| `the request to the provider "{provider}" carried the service-account access token` | `provider`: string; the provider that received the request. | 不支援 | `來源`: the fake's recorded request headers. | `必查`: `呈現結果`: the fake recorded exactly one request to `{provider}` carrying an `Authorization: Bearer <token>` header whose token was minted at the fake token endpoint from the configured service-account credential. `不該發生`: the request must not go out without a service-account access token, and the raw key-file path must not be sent as the token. |
| `the request to the provider "{provider}" allows at most {tokens} output tokens` | `provider`: string; the provider that received the request. `tokens`: integer; the expected output-token cap. | 不支援 | `來源`: the fake's recorded request body. | `必查`: `呈現結果`: the fake recorded exactly one request to `{provider}` whose generation config caps the output at `{tokens}` tokens. `不該發生`: the request must not omit the cap nor exceed it. |
| `the request replayed the earlier tool step "{tool}" carrying the provider token "{token}"` | `tool`: string; the tool name of the replayed step. `token`: string; the expected provider token. | 不支援 | `來源`: the fake's recorded request body (`BodyAt`). | `必查`: `呈現結果` / `權威狀態`: the fake recorded exactly one request carrying an assistant tool-call message for `{tool}` whose provider token equals `{token}` (echoed verbatim), followed by a tool-result message for that call. `不該發生`: the replayed call must not drop the token nor alter it. |
| `the request replayed the earlier tool step "{tool}"` | `tool`: string; the tool name of the replayed step. | 不支援 | `來源`: the fake's recorded request body (`BodyAt`). | `必查`: `呈現結果` / `權威狀態`: the fake recorded exactly one request carrying an assistant tool-call message for `{tool}` followed by a tool-result message for that call. `不該發生`: the earlier tool step must not be dropped on resume. |
| `the interactive prompt is shown` | 無 | 不支援 | `通道`: standard error (the announcement). `來源`: the product announcement literal `cli.TUIHint`. | `必查`: `呈現結果`: the captured standard error carries the interactive-prompt announcement (`[Interactive prompt. Type a prompt; Tab accepts a suggestion, Ctrl+S sends, Esc cancels]`). `不該發生`: the announcement must not be written to standard output. |
| `the interactive prompt is not shown` | 無 | 不支援 | `通道`: standard error + stdout. `來源`: the product announcement literal `cli.TUIHint`. | `必查`: `呈現結果`: neither the captured standard output nor standard error carries the interactive-prompt announcement. `不該發生`: the interactive prompt must not render on a non-terminal input. |
| `the interactive prompt offers the recent prompt "{prompt}"` | `prompt`: string; the expected recent-prompt suggestion. | 不支援 | `來源`: the shared prompt log (the `the shared prompt log already holds …` Given). | `必查`: `呈現結果`: the captured output (the rendered prompt) contains the recent prompt `{prompt}` as a suggestion. `不該發生`: the recorded recent prompt must not be missing from the suggestions. |
| `the interactive prompt offers the workspace entry "{entry}"` | `entry`: string; the expected workspace entry suggestion. | 不支援 | `來源`: the working directory (`the working directory contains a file …` Given). | `必查`: `呈現結果`: the captured output contains the workspace entry `{entry}` as a suggestion. `不該發生`: the matching workspace entry must not be missing from the suggestions. |
| `the interactive prompt offers the available tool "{tool}"` | `tool`: string; the expected tool-name suggestion. | 不支援 | `來源`: tellme's registered tool registry. | `必查`: `呈現結果`: the captured output contains the tool name `{tool}` as a suggestion. `不該發生`: a registered tool must not be missing from the suggestions. |
| `the interactive prompt is framed around a multi-line editor` | 無 | 不支援 | `通道`: the rendered prompt on the captured output. | `必查`: `呈現結果`: the captured output draws the editor inside a border (a horizontal border line of the editor frame is present). `不該發生`: the editor must not render without its frame. |
| `the interactive prompt lists suggestions beneath the editor` | 無 | 不支援 | `通道`: the rendered prompt on the captured output. | `必查`: `呈現結果`: the captured output carries the suggestion-list header (`Suggestions:`) below the editor frame. `不該發生`: the suggestion list must not be rendered above the editor. |
| `the interactive prompt marks one suggestion as the current choice` | 無 | 不支援 | `通道`: the rendered prompt. | `必查`: `呈現結果`: the captured output marks **exactly one** suggestion row as the current choice (the `>` cursor). `不該發生`: no suggestion row must carry the cursor, or more than one. |
| `the interactive prompt shows no session metrics header` | 無 | 不支援 | `通道`: the rendered prompt. | `必查`: `呈現結果`: the captured output carries **no** session metrics header — i.e. **no** line matching the specific dashboard pattern `provider .* \| tokens .* \| turns .*` (the round-015 header format). `不該發生`: the metrics/dashboard header line must not be rendered on the `-i` surface. |
| `the interactive prompt advertises the submit and abort keys` | 無 | 不支援 | `通道`: the rendered prompt. | `必查`: `呈現結果`: the captured output carries the editor placeholder naming the submit key (`Ctrl+S`) and the abort key (`Esc`). `不該發生`: the keybinding hint must not be missing from the prompt. |
| `the interactive prompt keeps the typed text "{text}"` | `text`: string; the text the operator typed. | 不支援 | `來源`: the scripted key sequence (the `… types "{query}" …` When). | `必查`: `呈現結果`: the captured output carries the typed text `{text}` inside the editor. `不該發生`: the typed text must not be dropped. |
| `the interactive prompt frames the editor within the terminal width` | 無 | 不支援 | `寬度`: the run's rendered width (the default when no terminal size is available). | `必查`: `呈現結果`: the captured output draws the editor frame within the terminal width (the border spans the run's width). `不該發生`: the frame must not exceed the terminal width. |
| `the interactive prompt holds the accepted suggestion "{text}"` | `text`: string; the accepted suggestion text. | 不支援 | `來源`: the scripted key sequence (the `… accepts the current suggestion` When). | `必查`: `呈現結果`: the captured output carries `{text}` inside the editor (the accepted suggestion was inserted). `不該發生`: the accepted suggestion must not be dropped from the editor. |
| `no suggestion offered to the operator spans more than three lines` | 無 | 不支援 | `通道`: the rendered suggestion list. | `必查`: `呈現結果`: every suggestion row the captured output renders spans at most three lines. `不該發生`: a suggestion with four or more lines must not be offered. |
| `the interactive prompt shows no line numbers` | 無 | 不支援 | `通道`: the rendered editor. | `必查`: `呈現結果`: the captured output's editor renders **no** line-number gutter (no leading line-number column). `不該發生`: the editor must not render line numbers. |
| `the shared prompt log records the prompt "{prompt}"` | `prompt`: string; the prompt that must be recorded. | 不支援 | `工作區`: reads `$TELL_ME_HOME/output/global_prompts.jsonl`. | `必查`: `權威狀態`: the shared prompt log holds `{prompt}`. `不該發生`: an interactive submission must not fail to record the prompt. |
| `the shared prompt log still holds only "{prompt}"` | `prompt`: string; the sole prompt the log must still hold. | 不支援 | `工作區`: reads `$TELL_ME_HOME/output/global_prompts.jsonl`. | `必查`: `權威狀態`: the shared prompt log holds exactly the arranged prompt `{prompt}` and nothing more (a non-`-i` run appended nothing). `不該發生`: a non-interactive run must not append to the shared log. |
| `tellme sends no request to the provider "{provider}"` | `provider`: string; the provider that must have received no request. | 不支援 | 無 | `必查`: `呈現結果`: the fake provider for `{provider}` recorded **zero** requests. `不該發生`: an aborted or non-interactive-empty submission must not reach any provider. |
| `the input capture is announced for the turn` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries the input-capture acknowledgement — a line matching `[HH:MM:SS] Input captured. Processing...` (the timestamp matched by pattern). `不該發生`: the acknowledgement must not be written to standard output. |
| `the input capture is announced before the turn frame` | 無 | 不支援 | `通道`: the **merged** capture (`stdout` + `stderr` into one ordered buffer). | `必查`: `呈現結果`: in the merged capture, the input-capture acknowledgement appears **before** the turn's horizontal rule. `不該發生`: the acknowledgement must not appear after the rule. |
| `the turn opens with a horizontal rule` | 無 | 不支援 | `通道`: standard error. | `必查`: `呈現結果`: the captured standard error carries a line whose visible content is the reference's **80-column** `─` rule. `不該發生`: the rule must not be written to standard output. |
| `the turn is headed "Turn {number}" for the active mode` | `number`: integer; the expected turn number. | 不支援 | `通道`: standard error. `模式`: the run's effective mode. `號數`: the count of the session's **completed turns** (the number of `history_entry` lines in the active `history.jsonl` — one line per completed turn) + 1. | `必查`: `呈現結果`: the captured standard error carries a header line matching `╭─⠿ Turn {number} - <mode>`, where `<mode>` is the run's effective mode and `{number}` equals the session's completed-turn count + 1. `不該發生`: the header must not be written to standard output, nor carry a different turn number. |
| `the turn chrome is shown before the answer` | 無 | 不支援 | `通道`: the **merged** capture. `來源`: the whole turn chrome — the input-capture acknowledgement, the 80-column `─` rule, the `╭─⠿ Turn …` header, and the pre-flight payload line. | `必查`: `呈現結果`: in the merged capture, the whole turn chrome (the input-capture acknowledgement, the `─` rule, the `╭─⠿ Turn …` header, and the pre-flight payload line) appears **before** the answer bytes. `不該發生`: no part of the turn chrome may appear after the answer. |
| `the turn frame is separated from the answer` | 無 | 不支援 | `通道`: the **merged** capture. | `必查`: `呈現結果`: in the merged capture, a blank line separates the frame's last line (the pre-flight payload line) from the answer bytes. `不該發生`: the answer must not immediately follow the payload line with no blank line. |
| `the run reports the token metrics of the request that just completed` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries a line matching `[HH:MM:SS] [<provider>] M: <n> H: <n> C: <n> Th: <n>` (the timestamp matched by pattern; `<provider>` is the active provider key). `不該發生`: the metrics line must not be written to standard output, and must not be missing when the provider reports usage. |
| `the reported metrics line shows {miss} missed, {cached} cached, {completion} completed, and {thinking} reasoning tokens` | `miss`, `cached`, `completion`, `thinking`: integer; the expected counts. | 不支援 | `來源`: the reported metrics line (the `the run reports the token metrics …` Then). | `必查`: `呈現結果`: the reported metrics line's `M`/`H`/`C`/`Th` fields equal `{miss}`/`{cached}`/`{completion}`/`{thinking}` (with `M` = prompt − cached). `不該發生`: the counts must not differ, and `Th` must be present even when zero. |
| `the run reports the cost of the request, the turn, and the session` | 無 | 不支援 | `通道`: standard error (the diagnostic stream). | `必查`: `呈現結果`: the captured standard error carries a summary line matching `╰─⠿ Ready ($<a> $<b> $<c> - M: <n> H: <n> O: <n> - <pct>%)` (three `$` costs, the session token totals, and the cache-hit rate). `不該發生`: the summary must not be written to standard output. |
| `the reported request, turn, and session costs are equal` | 無 | 不支援 | `來源`: the reported summary line. | `必查`: `呈現結果`: the summary line's three `$` costs are equal (a fresh session: request == turn == session). `不該發生`: a fresh session must not show divergent three-cost values. |
| `the reported turn cost is greater than the request cost` | 無 | 不支援 | `來源`: the reported summary line (a tool-using turn). | `必查`: `呈現結果`: the summary line's second `$` (the turn) is strictly greater than its first `$` (the last request). `不該發生`: a tool-using turn must not report a turn cost equal to its last request. |
| `the reported session summary includes the earlier call` | 無 | 不支援 | `來源`: the reported summary line + the arranged prior usage record (the `the session's usage log already records a prior call` Given). | `必查`: `呈現結果`: the summary line's session (`$` #3) and token totals include the arranged prior call (strictly greater than the current call alone). `不該發生`: a resumed session must not drop the earlier call. |
| `the run reports the session's missed, cached, and output tokens` | 無 | 不支援 | `來源`: the reported summary line. | `必查`: `呈現結果`: the summary line carries the session token totals `M: <n> H: <n> O: <n>`. `不該發生`: the session token totals must not be missing. |
| `the run reports the share of the prompt served from cache` | 無 | 不支援 | `來源`: the reported summary line. | `必查`: `呈現結果`: the summary line carries a cache-hit percentage (`<pct>%`). `不該發生`: the cache share must not be missing. |
| `the run reports a zero cost` | 無 | 不支援 | `來源`: the reported summary line (an un-priced model). | `必查`: `呈現結果`: the summary line's `$` costs are `$0.0000` (the model has no `MODELS` pricing entry). `不該發生`: an un-priced model must not report a non-zero cost. |
| `the post-turn status trails the answer` | 無 | 不支援 | `通道`: the **merged** capture (`stdout` + `stderr` into one ordered buffer). `來源`: the metrics line + the summary line. | `必查`: `呈現結果`: in the merged capture, both the metrics line and the summary line appear **after** the answer bytes. `不該發生`: the post-turn status must not precede the answer. |
