# Truth Delta: 001-cli-bootstrap-and-config

**Plan Package**: `specs/plans/001-cli-bootstrap-and-config`
**Truth Root**: `specs/truth`

> Skeleton initialized by `/axb-specify`. Each truth owner replaces its placeholder row(s) with its own ADD / MODIFY / DELETE / NOOP entries during the round. Every owner must record at least one entry; a `NOOP` entry proves the area was checked.

## /axb-technical-research

| Action | Truth Spec | Change Summary | Reason |
| --- | --- | --- | --- |
| ADD | `specs/truth/techstack.md` | Created the initial techstack truth: Go 1.26, module `github.com/gosharplite/tellme`, layout `cmd/tellme/` + `internal/…`; `spf13/pflag` for CLI flags; `gopkg.in/yaml.v3` + a hand-written resolver for config; `godog` as the CLI BDD runner; E2E black-box test strategy; `go build` `-ldflags -X` version injection; `gofmt`/`go vet`/`make` tooling. Explicitly excludes cobra, viper, golangci-lint, testify, provider SDKs, TUI, MCP, SQLite, and web frontend. | First plan package (starting project): `spec.md` fixed Go but deferred the toolchain/test stack to this phase; three AIxBDD must-ask decisions locked — system ends = one CLI end, BDD techstack = godog, test strategy = E2E (black-box CLI). |
| MODIFY | `specs/truth/techstack.md` | Corrected the techstack **representation** after the grill round ([issue #1](https://github.com/gosharplite/tellme/issues/1), verdict *proceed with changes*): (i) layout row justified by separation of concerns / E2E-observable FR mapping, **not** unit-testability; (ii) **`internal/version` dropped** — single version source is `main.version`; (iii) `godog` labelled **adopted this round; first instantiated in `/axb-dsl-refine`** via an explicit "Adopted, Not Yet Instantiated" bucket (executed = interface Gherkin; acceptance = carried); (iv) added a **no-network verification** row (`unshare -n` sandbox + build-graph capability guard) proving SC-004 as a host-harness assertion; (v) added **`staticcheck`** to the static gate; (vi) version-injection row + **`VERSION=0.0.0-harness` sentinel** test row (single target `main.version`); (vii) deferral reasons for `golangci-lint`/`govulncheck` changed from install-cost to policy-artifact; (viii) title de-round-scoped. **Deliberately NOT placed in truth:** the `-d` diagnostic exit-code contract and the resolver algorithm (behaviour, not technology). | Grill round 001 review gate: the core stack choices held, but nearly every *representation* of them overstated or mis-scoped. Recorded as `MODIFY` (not a silent edit) per the round's outcome. |

## /axb-api-plan

| Action | Truth Spec | Change Summary | Reason |
| --- | --- | --- | --- |
| PENDING | `specs/truth/contracts/**` | To be recorded by `/axb-api-plan` | CLI has a single end — no OpenAPI/HTTP surface; expected `NOOP`, to be confirmed by the owner. |

## /axb-data-plan

| Action | Truth Spec | Change Summary | Reason |
| --- | --- | --- | --- |
| NOOP | `specs/truth/data/**` | Checked; left empty. A round-001 data truth (`data-model.dbml`) was authored, then **removed within the same round** after **grill round #3** ([issue #3](https://github.com/gosharplite/tellme/issues/3), verdict *proceed with changes*): by the artifact's own input-vs-state test the config file is *read-time input* (→ the CLI contract) and the empty workspace lifecycle is interface behaviour — so nothing legitimately belongs in `data/**`. The user **ratified reversing clarify Q2 → Option 1** on 2026-09-11. | Net truth change is **zero** → recorded as `NOOP` (area checked) per `delta-covers-all-owners`. The CLI input contract + workspace lifecycle are owned by `/axb-dsl-refine` (`truth-single-owner`), and `data-model-covers-all-state` is satisfied vacuously (no persisted/modelled data this slice). |

## /axb-dsl-refine

| Action | Truth Spec | Change Summary | Reason |
| --- | --- | --- | --- |
| ADD | `specs/truth/features/cli/dsl.md` -> 8 cross-module rows | Created the CLI interface-root shared DSL (`the operator has a runnable tellme installation`, `the runtime home is "{home}"`, `a well-formed configuration "{config_path}"`, `the operator starts tellme`, `the operator starts tellme pointing at the configuration "{config_path}"`, `tellme exits successfully`, `tellme refuses to proceed`, `tellme explains on stderr that "{reason}"`). | Rows used identically by ≥2 CLI modules go to the interface root, not a module; first interface feature/dsl truth (`truth-single-owner` for `specs/truth/features/cli/**`). |
| ADD | `specs/truth/features/cli/configuration/starting-with-a-configuration.feature` -> 8 Rules | Executable CLI scenarios for configuration presence / parse / validation, default-path discovery, and effective-selected-provider integrity (incl. the env-override `TELL_ME_SELECTED_PROVIDER=ghost` false-pass case). | Carries the acceptance `starting-with-a-configuration` rules (`acceptance-coverage`); atomicized to one Act per Example. |
| ADD | `specs/truth/features/cli/configuration/dsl.md` -> 9 module rows | Module DSL for the configuration module: arrange sentences (missing / malformed / empty-registry / no-default-location / effective-mode / registry variants) + `tellme reports the configuration is ready` + `tellme exits with the configuration error code`. | Module-scoped contracts unique to configuration (`dsl-single-authority`). |
| ADD | `specs/truth/features/cli/workspace/runtime-home-and-session-workspace.feature` -> 5 Rules | Executable CLI scenarios for workspace create-on-first-run / reuse / effective-mode selection, an unusable runtime home, and a workspace path occupied by a regular file. | Carries the acceptance `runtime-home-and-session-workspace` rules. |
| ADD | `specs/truth/features/cli/workspace/dsl.md` -> 12 module rows | Module DSL for the workspace module: arrange (no workspace / existing workspace / preserved file / file conflict / mode declared / mode override / home unset) + create / report / reuse / preserved Thens + `tellme exits with the environment error code`. | Module-scoped contracts unique to workspace. |
| ADD | `specs/truth/features/cli/diagnostics/version-and-setup-diagnostic.feature` -> 3 Rules | Executable CLI scenarios for `--version`, resolved `-d` (plain + `--json`), and unresolved `-d` (plain + `--json`) with the dedicated diagnostic exit code. | Carries the acceptance `version-and-setup-diagnostic` rules, incl. the PM-landed `-d`-unresolved cases (PM-1). |
| ADD | `specs/truth/features/cli/diagnostics/dsl.md` -> 14 module rows | Module DSL for diagnostics: `--version` / `-d` / `-d --json` Whens; resolved/unresolved report Thens; `tellme performs no network access` (harness-mediated); `tellme exits with the diagnostic error code`. | Module-scoped contracts unique to diagnostics. |
| ADD | `specs/truth/features/cli/usage/unsupported-cli-usage.feature` -> 1 Rule | Executable CLI scenario for an unrecognized flag → a usage error distinct from configuration and environment failures. | Carries the acceptance `unsupported-cli-usage` rule. |
| ADD | `specs/truth/features/cli/usage/dsl.md` -> 2 module rows | Module DSL for usage: the unrecognized-flag When + `tellme exits with the usage error code`. | Module-scoped contracts unique to usage. |
| MODIFY | `specs/truth/features/cli/workspace/runtime-home-and-session-workspace.feature` -> W1/W2/W5; `…/diagnostics/dsl.md` -> the two structured-output rows; the four `…/dsl.md` error-code rows | Grill round #4 corrections (in-round, completing the ADD): (a) restored the acceptance `Given` the interface had dropped in W1/W2/W5 (`the configuration "{config_path}" declares the mode "{mode}"`), without which W1/W2 are unsatisfiable and W5 asserts the wrong exit class under the ratified resolver; (b) pinned the `--json` key schema in the two `emits … as structured output` rows; (c) reworded the four error-code rows to assert **distinctness** (FR-014) rather than a pre-committed literal. | Grill round #4 ([issue #4](https://github.com/gosharplite/tellme/issues/4), verdict *proceed with changes*): the Rule-5/6 arrange loss and the unnamed `--json` schema are contract defects only this owner can fix. The atomicity-convention and English-override matters are routed upstream (`STATUS.md` residuals), not edited locally. |
