# Tasks — round 040 (spinner liveness while a command streams + a per-model-call elapsed timer)

Plan package: `specs/plans/040-spinner-liveness-and-turn-timer`

Task markers: `[UNIT]` unit test · `[BDD-ALIGN]` realign an existing stepdef to the current truth · `[BDD-REMOVE]` retire a stepdef/assertion no longer in truth · `[BDD-RED]` new stepdef that must fail before the product change · `[BDD-GREEN]`/`[BDD-REFACTOR]` feature green/refactor · `[CODE-REMOVE]` delete dead code · `[REGRESSION]` regression + falsifiability.

## Core Inputs (must read)

- `spec.md` (FR-001..FR-011, SC-001..SC-006), `research.md` (D1–D9), `plan.md` (incl. the acceptance→carrier table + the task directives), `truth-delta.md`.
- Truth: `specs/truth/features/cli/chat/presenting-the-progress-spinner.feature` (one Rule **replaced**, one Rule **added**, two tool-phase Examples amended; header refreshed), `specs/truth/features/cli/chat/dsl.md` (the streaming row modified **in place** + `## Given (round 040)` + `## Then (round 040)`), `specs/truth/techstack.md` (**Turn progress spinner** row — MODIFY, incl. the round-019 carve-out).
- Governance: `docs/decisions/0009-spinner-dual-timer-and-streaming-liveness.md` (ADD; supersedes **ADR 0005 D7 only**; amends round-019 D4) + the ADR 0005 index annotation.
- Code seams: `internal/ui/spinner.go` (`Spinner`, `FormatSpinnerLine` — 4 params today, `activate`/`resume`/`deactivate`/`loop`/`renderLocked`/`eraseRows`/`rowsForLine`, the `newTicker`/`now` seams), `internal/ui/tooloutput.go` (**`ToolOutputWriter` — the sole owner of the block's mutex + `buf`/`open` row state; it self-locks in every method, `sync.Mutex` is non-reentrant** → see T002/T009: the coordinator must go through the writer's lock-scoped entry points (**one lock owner, three entry points**), never reach into `mu`), `internal/cli/cli.go` (`:734–:767` the spinner decl + the `ToolOutputSink` closure; `newTurnSpinner`/`spinnerGate`/`stderrColumns`), `internal/cli/composite_observer.go`, `internal/infrastructure/tools/command.go` (`runCaptured` Begin/End), `tests/e2e/steps/toolcall_log.go` (`toolOutputBlockIndexes`/`hasSpinnerStatusBetween` — the block-span predicate, reused by T004), `tests/e2e/steps/scenario_context.go` (the `envUnset` hermeticity deny-list — the new idle seam must be registered here).

## Setup

_(none — stdlib-only; no new dependency, no build/SDL change.)_

## Foundational

- [ ] **T001** — Add the round-040 E2E stepdef landing skeletons as **independent files** (Zero Shared Edits): `tests/e2e/steps/step_r040_idle_gap.go` (register the idle-gap `Given`, the quiet-command provider `Given`, and the liveness `Then`) and `tests/e2e/steps/step_r040_dual_timer.go` (register the dual-timer `Then`), each with stub bodies returning `nil`. **只做** registration + stubs; **不做** the assertions or the fixtures. `[scaffold]`
- [ ] **T002** — Add the unit-test landing files + the `internal/ui` coordinator seam **skeleton**: `internal/ui/spinner_round040_test.go` and `internal/ui/coordinator_test.go` (empty test funcs), and a new `internal/ui/coordinator.go` with the type/constructor skeleton (the `ToolOutputWriter` + the `*Spinner` + the idle seams; no behaviour). **The coordinator's seam shape is named here (B1):** `ToolOutputWriter` stays the **sole owner** of its mutex + row state — **one lock owner, three entry points** (`WriteWith`, `EndWith`, `withLock`), all under the writer's single mutex (N-10 wording; R-4 + R-8): (i) the **line path** — `func (w *ToolOutputWriter) WriteWith(p []byte, beforeLine func()) (int, error)`, with `Write(p)` = `WriteWith(p, nil)`; the coordinator passes `beforeLine = clear`; and (ii) the **coordinator-bookkeeping path** — `func (w *ToolOutputWriter) withLock(fn func(idle time.Duration))`, used for the watcher's check+admit (the parameter is the writer-owned idle clock — the R-11 shape); and (iii) the **`End`-path hook** — `func (w *ToolOutputWriter) EndWith(beforeSeparator func())`, with `End()` = `EndWith(nil)` (R-8). **R-9 (the idle clock moves into the writer):** `ToolOutputWriter` stamps `lastLine` **inside the same critical section that emits a line** (it already knows when it last emitted) and exposes the idle query **through the same lock** — the bookkeeping path is **`func (w *ToolOutputWriter) withLock(fn func(idle time.Duration))`**: the writer computes the idle duration under its own mutex and hands it to the closure, so the coordinator does `if idle >= N { admit() }` (R-11: a self-locking `IdleSince` called inside `withLock` would **self-deadlock**, and an unlocked one would race `Begin`/`Write`/`End`). **`lastLine` MUST be seeded at `Begin`** (the header emit), so a command that prints **nothing at all** still resumes after `N` (FR-001's zero-line case — the original #82 complaint). The coordinator holds **no** block state and needs **no** separate stamp critical section — removing both the ordering question and a third lock-order hazard. **N-13 (`Begin` needs no hook):** `sink.Begin()` runs **before** `cmd.Start()`, so no drain goroutine exists yet and the header write cannot race a line or a frame (why the `End`-hook / no-`Begin`-hook asymmetry is correct). **(R-2 scope: the *line* path cannot use a bare `withLock(fn)` — the line-splitting stays inside the writer over its private `buf`; a *bookkeeping* closure is fully implementable and is the only way the watcher can honour lock order `block-mutex → spinner-mutex`.)** **one lock owner**, no reentrancy hazard, no `buf` exposure. **不做** reaching into `w.mu` from `coordinator.go` (that would convert a single-owner lock into a two-owner one — the [#69](https://github.com/gosharplite/tellme/issues/69) class). **B3:** register `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS` in `tests/e2e/steps/scenario_context.go`'s `envUnset` deny-list alongside `TELL_ME_FORCE_STDERR_TTY`, so an ambient shell value cannot leak into a scenario. **只做** skeletons + the seam shape + the hermeticity registration; **不做** the resume/clear logic or the assertions.

## Phase 3 — Test alignment & implementation (Red first)

- [ ] **T003** `[BDD-REMOVE]` — retire **only** the dead stepdef `tests/e2e/steps/step_r034_t016_chat_then_no_spinner_while_streaming.go` (registration + body; 0 matching feature steps after the Rule replacement). **KEEP** the helpers `toolOutputBlockIndexes` / `hasSpinnerStatusBetween` in `tests/e2e/steps/toolcall_log.go` — T004 reuses them (B2; deleting them here would fork a **second** block-span predicate). **RED**: the suite must compile and report **no** undefined step from the retired sentence. No product code.
- [ ] **T004** `[BDD-RED]` — `tests/e2e/steps/step_r040_idle_gap.go`: implement the `Given` `the command stays quiet for longer than the spinner's idle gap` (set `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS` to a small **nonzero** value, e.g. `50`, alongside `TELL_ME_FORCE_STDERR_TTY`; the child's quiet stretch **exceeds `N + 2·P` with margin** — a **child** `sleep 2`, never a Go `time.Sleep`, so `verify-no-test-sleep` is untouched), the `Given` `a configured provider "…" whose endpoint runs a command that prints a line and then stays quiet and then answers with "…"`, and the `Then` `the run shows the progress spinner again while the command stays quiet` — asserted **inside** the `[Tool Output]` block's span, **bounded by the closing separator, not the last `[Tool Output]` line** (R-1): add a tiny `closingSeparatorIndex(stderr)` (the last index carrying `ui.ToolOutputSeparator` after the header) and call `hasSpinnerStatusBetween(lines, head, closingIdx+1)` (keep/reuse the existing helpers — B2 — and refresh their doc comments: name the round-040 liveness pin, drop the stale `(T016)`/"spinner paused during a stream"). **Why (R-1):** the resumed frame shares one `\n`-line with the reset + the closing separator and carries **no** `[Tool Output]` marker, so the old `(head, lastMarker)` bound would make the assertion **vacuously false** with the feature working (a positive-polarity reuse needs a range that provably contains the artefacted line — `chat/dsl.md:399` already specifies the span as "between its header/opening separator and its closing separator"). Plus the whole-stream `the run shows no progress spinner` for the no-residue leg. **RED** before T009.
- [ ] **T005** `[BDD-RED]` — `tests/e2e/steps/step_r040_dual_timer.go`: implement the `Then` `the progress spinner shows the time since the prompt and the current model call's time` (the elapsed segment is **two** whole-second figures inside one parentheses, `({total}s {call}s)`, the first the total since prompt capture, the second the current model call's elapsed; both unlabelled). Covers the ×4 occurrences (2 amended tool-phase Examples + the 2 new Examples). **RED** before T011.
- [ ] **T006** `[UNIT]` — `internal/ui/spinner_round040_test.go`: the **dual-timer arithmetic** under an injected `now` — a first `OnInferenceStart` then a render shows `(t t)`; a later render shows `(t+k t+k)`; a second `OnInferenceStart` shows the **total grown** but the **second figure reset** (per-call `callEpoch`; the total never decreases). **RED** before T011.
- [ ] **T007** `[UNIT]` — `internal/ui/coordinator_test.go`: the WS-A **race + anti-vacuity + no-residue** stress under an injected ticker — (a) a line arriving as the resume is admitted ⇒ the frame is cleared **before** the line (no interleave); (b) **continuous** output ⇒ **no** frame between lines (anti-vacuity — the resume cannot decay into the rejected per-line yield); (b′) **R-11** — the **zero-output** case: a command that prints **nothing at all** (no line ever) still resumes the indicator after `N` (the `Begin`-seeded `lastLine`; FR-001's zero-line case — the original #82 complaint); (c) no residue survives the block close. Also: (d) the round-025 **row-aware clear on the two-figure 3+-digit** frame `1234s 567s` (SC-006); (e) **TD-3** — a resume with **no phase label** is a **defined no-op** (RF-3) and the WS-A resume/clear is a **no-op when the spinner is gated off** (FR-011); (f) the **stalled-writer** case (residual risk below) — a `stderr` writer that blocks until **explicitly released**, then asserting the **line path completes** and no interleave occurs thereafter (the case must **release** the writer, else a literal implementation hangs); (g) **R-5** — the **`\r`-only** case is settled as **required behaviour**: a command emitting `\r`-only bytes (no newline) produces **no new output line**, so under **FR-001** the indicator **MUST** reappear — pin the **resume as expected** (the idle clock is defined on **emitted lines**, and `Write` emits only on `\n` while `End` drops the partial buffer, so a `\r`-only stream is **visually silent**; **byte-arrival stamping is rejected** — it would contradict FR-001's line-scoped trigger); (h) **R-8** — **`End` while a `Write` is in flight** (the trim/timeout shape: a drain goroutine still inside `Write` at `End`) ⇒ **no interleave and no residue** — **or** record it explicitly as an accepted race with the reason (implicit is not acceptable). **RED** before T009/T010.
- [ ] **T008** — Review gate: after T003–T007, every new step matches exactly one `DSLRow` and the suite reports **0 undefined** steps. **Add `Strict: true` to `tests/e2e/suite_test.go` in this same change** (TD-1 — `godog.Options.Strict` defaults to `false`, so undefined steps are reported-and-ignored; enabling it makes the round's new Examples fail the suite until the stepdefs land — the FAIL→PASS transition is the witness). The plan half left the harness unchanged so `dev` stays green; this task lands the flag with the stepdefs. **Converse guard (N-3):** `Strict: true` MUST NOT land **before** T004/T005 are registered, or the suite goes red for the wrong reason (a misleading FAIL→PASS witness).

## Phase 4 — Feature green / refactor

- [ ] **T009** `[BDD-GREEN]` — `presenting-the-progress-spinner.feature` Rule *A quiet command's output still shows the progress spinner* green (WS-A): extract the `internal/ui` **coordinator** (the `ToolOutputWriter` + the `*Spinner` + the idle seams) so `internal/cli` passes **one** object to the sink (the `#69` pay-down); a block-scoped **idle watcher** polls on the spinner's ~200 ms cadence and **resumes** after `N` with no new line; on each complete output line the indicator is **cleared synchronously first** (`deactivate()`), then the line is written; `End` = **stop the watcher AND join it → clear (`deactivate()`) → closing separator (inside the lock, via `EndWith(beforeSeparator)` — R-8) → resume**. **R-8:** the clear runs **inside the writer's critical section** (`w.EndWith(func(){ c.clear() })`), and the watcher is **joined** (not merely stopped — stopping the ticker is not stopping the goroutine) before the clear, so an in-flight `admitResume()` cannot race it. This matters because the sink's contract is *"after the child is **reaped**"* (not *"after the drains are joined"*): on the trim/timeout paths `abortCapture` waits for `finished` only under the bounded `commandWaitDelay` (2 s), so an `io.Copy` goroutine can still be **inside `Write`** at `End` — an out-of-mutex clear would interleave a frame with a line (the FR-002 property), the R-1b reason (never leave the separator on the live frame's row / never let the post-`End` resume erase it) and R-9 (the idle clock lives in the writer — read via the `withLock` idle closure). The resume/clear is **mutual exclusion + join** — the watcher's **admit happens under the block-writer's mutex via `w.withLock(fn func(idle time.Duration))`** — the writer evaluates idleness under its own lock and the closure admits (R-4 + R-11: the admit is bookkeeping-only and fires precisely when **no `WriteWith` is in flight**, so it cannot route through the byte path; one critical section for check+admit, no reentrancy); the per-line **clear** happens inside the **`WriteWith(p, beforeLine=clear)`** line path; the idle **clock is the writer's** (`Begin`-seeded `lastLine`, read via the `withLock(fn func(idle))` closure) — the coordinator never stamps (R-10). **Never** reach into `w.mu`; the clear is the goroutine-joined `deactivate()`; lock order block-mutex → spinner-mutex. The resume uses a **named resume-only admission path** (`admitResume()`), the **redraw goroutine draws the resumed first frame immediately on start** (once before waiting for the tick), and `activate()` keeps its **synchronous** first frame (rounds 019/025/034/035 depend on it). The idle seam `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS` (ms; `0` = admit immediately; unset/invalid = the 3 s default) resolves once at construction. **B4 in-code citation sweep (this task):** re-cite `internal/ui/tooloutput.go` (`:10–17`) and `internal/cli/cli.go` (`:748–:751`) to **ADR 0009** (D7-superseded pointer), and replace the "**single writer** / yielded once per call" clause with the **mutual-exclusion + join** + idle-gap wording. Test Scope: the Rule's Example + the unchanged `the run streamed the command's output …` Examples.
- [ ] **T010** `[BDD-REFACTOR]` — tidy the coordinator + the `internal/cli` wiring under green protection: one blank-free resume path, the **lock-order comment**, no frame write inside the block critical section, and the single-owner seam (a no-op when the spinner is gated off). **B4 (cont.):** update the `internal/ui/spinner.go` (`:20–25`, `:216–224`) and `tests/e2e/steps/toolcall_log.go` (`:28`, `:163–165`) comments — restate the elapsed comment as *"the **total** figure is turn-scoped and never reset; the second (model-call) figure resets per AI-endpoint call"* and drop the stale per-call-yield/T016 wording. Test Scope: the WS-A Rule green + the round-019/025/034/035 spinner Examples remain green (with the T011-adapted pins).
- [ ] **T011** `[BDD-GREEN]` — `presenting-the-progress-spinner.feature` Rule *The spinner shows the total time and the current model call's time* green (WS-B): `FormatSpinnerLine(frame, status string, total, call int, resource string)` renders `({total}s {call}s)`; the `Spinner` gains an internal **`callEpoch`** (initialised to `epoch`, **not** a sixth ctor param — round-019 R-2) set in `OnInferenceStart` **before** activating; `OnToolsStart`/`BeforeToolLog`/`AfterToolLog`/the WS-A resume preserve it. **TD-2 — three pre-existing pins are ADAPTED (not deleted):** `spinner_test.go` `TestFormatSpinnerLine` (the 4-arg calls + `(3s)`/`(0s)` expectations) and `TestSpinnerElapsedIsTurnScoped` (the round-019 D4 pin — the never-reset property **survives on the total figure**), and `spinner_width_test.go`'s row-count table + exact-frame pins (**verified — expected unchanged**; the table is a pure function on a literal, signature-independent, and the clear assertions keep the same row counts at 40 columns with the extra figure — TD-2 self-correction). Test Scope: the Rule's 2 Examples + the 2 amended tool-phase Examples + the three adapted pins; add the `(t t)` → `(t+k t+k)` → **second `OnInferenceStart` (total grown, second figure reset)** sub-assertion to the D4 pin.
- [ ] **T012** `[BDD-REFACTOR]` — tidy the formatter + the epoch handling under green protection (one elapsed-formatting site; no allocation regression; the resource segment unchanged). Test Scope: the WS-B Rule green + the tool-phase Examples green.
- [ ] **T013** `[CODE-REMOVE]` — remove the retired **whole-block-pause semantics**: T009 **replaces** the per-call-pause closure (`internal/cli` sink binding) with the coordinator's binding — this task deletes the now-dead **whole-block-pause branch/comment** and any branch that assumed the spinner stays hidden for the whole block (the sink **binding itself is not dead** — `BindToolOutput` still hands a sink to the command tool), keeping the block literals + the bound/stop semantics intact. Test Scope: the retired Rule is gone (no feature step requests it); the `[Tool Output]` Examples stay green.

## Regression & falsifiability

- [ ] **T014** `[REGRESSION]` — **the gate set is `go test -count=1 ./...` (unit packages AND `tests/e2e`) + `make verify`** (TD-1: `make verify` runs **no unit tests**, so it would leave the T006/T007 pins unrun). **N-9 note:** the 3 s default idle resume activates **globally**, so "existing long-quiet scenarios stay green" is safe **because** every such scenario carries `timeout: 1` (**< N**, the child is killed before the gap) and must force the spinner on to draw at all — record this bound against future drift (a scenario with a `timeout > N` would need the seam pinned). `stdout` byte-exact; every existing spinner/tool-loop Example unchanged-green (the round-019/025/034/035/039 pins, with the T011-adapted unit pins). Re-run the Gherkin/DSL topology audit (`--root specs/truth/features/cli`) and confirm PASSED.
- [ ] **T015** — Falsifiability witnesses (each reproduced then reverted): (a) remove the idle-gap resume → the WS-A Example fails (its carrier is the **only** witness where the E2E is the sole detector, alongside T007's (a)/(c) pins); (b) freeze the second figure (no per-call reset) ⇒ the **unit** pin fails (T006's second-`OnInferenceStart` sub-assertion, SC-001) — the WS-B E2E Example asserts the two-figure **shape** only, so a frozen `callEpoch` still passes it (SC-003); (c) drop the row-aware clear → a residue frame survives the 3+-digit frame → the SC-006 pin fails.
- [ ] **T016** — Update `STATUS.md` + the daily summary; open the implementation PR; close **#82** + **#83** at closeout (delivery).

## Boundary

- In scope: `internal/ui/spinner.go`, a new `internal/ui/coordinator.go`, `internal/ui/tooloutput.go` (the **three** entry points — `WriteWith`, `EndWith`, `withLock(fn func(idle))` — + the `Begin`-seeded `lastLine` clock; sole lock/state owner), `internal/cli/cli.go` (the sink/spinner wiring + the idle seam), `tests/e2e/steps/step_r040_*.go` (+ the `[BDD-REMOVE]` of the dead stepdef; **keeping** the `toolOutputBlockIndexes`/`hasSpinnerStatusBetween` helpers in `toolcall_log.go`), `tests/e2e/steps/scenario_context.go` (the `envUnset` registration), `tests/e2e/suite_test.go` (`Strict: true`), and the `internal/ui` unit tests (incl. the T011-adapted pins).
- Out of scope (unchanged): the `[Tool Output]` header/separator literals, the block bound/stop semantics, `output_file`, the tool **result** fed to the model, the call's stored arguments, `stdout`, `-r`, flags, exit codes, the class-phrase vocabulary, the provider transport, every persisted record, and the offline paths (`spec.md` FR-009/FR-010/FR-011). No new dependency (stdlib-only; POSIX-only).

## Parallel Hint

- T001/T002 are independent landing files (Zero Shared Edits). T006/T007 are independent unit files. T004/T005 are independent step files. T003 (the stepdef removal) must land with T008 (`Strict: true`) so the suite is defined; **T008's `Strict: true` must not land before T004/T005 are registered** (N-3). T009–T013 touch product files (`internal/ui`, `internal/cli`) and must serialize (T009 → T010; T011 → T012; T013 after T009).

## Pre-Delivery Orphan Coverage Sweep

- `truth-delta.md` non-NOOP rows: `techstack.md` spinner row (Read by T009/T011 + delivered there), the two feature Rules + the `chat/dsl.md` rows (Read by T004/T005 and delivered by T009–T013), ADR 0009 (governance, ADD — no task action). **Covered.**
- `research.md` D1–D9: D1/D2 (WS-B timer) → T006/T011; D3/D4/D5 (WS-A mechanism/protocol/scope) → T007/T009/T010; D6 (witnesses) → T004–T007/T014/T015; D7 (truth scope) → T009–T013; D8 (ADR lifecycle) → no task action (governance); D9 (harness `Strict`) → T008. **Covered.**
- `techstack.md` round-040 section (the dual timer + the idle-gap liveness + the carve-out) → T009/T011 + the `techstack.md` Read in Core Inputs. **Covered.**
- **Orphans: 0.**

## Round-040 tasks review fold (PR #85)

Plan-side fold (no truth, no ADR, no code) applying the architect review `APPROVE WITH REQUIRED FOLDS`:

- **B1 [blocker]** — the block-mutex seam is named in **T002** + **T009**: `ToolOutputWriter` stays the **sole** owner of its mutex/state, and the coordinator admits the resume + clears/writes **through one lock-scoped entry point** (`withLock`, or locked internals used by a thin `Write`) — never by reaching into `w.mu` (non-reentrant `sync.Mutex`). Prevents the two-owner lock/state the review flagged.
- **B2 [blocker]** — **T003** retires **only** the dead stepdef; the helpers `toolOutputBlockIndexes` / `hasSpinnerStatusBetween` are **kept** and handed to **T004** as the positive-polarity carrier (with refreshed doc comments).
- **B3 [debt]** — `tests/e2e/steps/scenario_context.go` added to Core Inputs + Boundary, and **T002** registers `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS` in its `envUnset` deny-list.
- **B4 [debt]** — the superseded-citation sweep is owned in-code by **T009** (tooloutput.go / cli.go) + **T010** (spinner.go / toolcall_log.go): re-cite to ADR 0009, replace "single writer" with mutual-exclusion + join.
- **TD-1** — **T014**'s gate set is `go test -count=1 ./...` **+** `make verify` (`make verify` runs no unit tests).
- **TD-2** — **T011** names the **three adapted pre-existing pins** (not "unchanged") and adds the second-`OnInferenceStart` reset sub-assertion.
- **TD-3** — **T007** adds the no-label-resume (RF-3) + gated-off (FR-011) no-op sub-cases.
- **TD-4** — **T013** reworded: replace the pause **semantics**; the sink binding itself is not dead.
- **TD-5** — `spec.md` **SC-003** reworded to the **shape-only** claim + the unit pin (the per-call reset is **not** E2E-observable on a fast scripted turn; `chat/dsl.md` + `research.md` D6 already say so). *(PM-owned artifact — raised to the operator for ratification.)*
- **N-1** — Core Inputs cli.go range corrected to `:734–:767`. **N-2** — the `tooloutput.go` mutex/row-state ownership note added to Core Inputs. **N-3** — the converse guard added to **T008** (and the Parallel Hint).
- **Residual risk recorded** — the back-pressure wording is sharpened: the **resume-admit** is bookkeeping only, but the **clear** runs `deactivate()` (join + erase) inside the block critical section, so a stalled `stderr` can hold the block mutex across one erase write (the in-mutex clear is still the right choice — an out-of-mutex clear could strand a frame, the round-035 defect class); carried as **T007(f)** (stalled-writer case).

## Round-040 tasks fold review #2 (PR #85, `f37a175` → fold)

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `FOLDS ACCEPTED + R-1/R-1b/R-2/R-3/N-4/N-5`:

- **R-1 [blocker]** — the positive-polarity bound is corrected: `T004` anchors on the **closing separator** (`closingSeparatorIndex(stderr)` → `hasSpinnerStatusBetween(lines, head, closingIdx+1)`), not the last `[Tool Output]` marker line (the resumed frame shares the reset+separator `\n`-line and carries no marker, so the old bound made the assertion **vacuously false**; the retired sentence was safe only because it asserted `== false`).
- **R-1b [debt]** — `T009`'s `End` ordering is now `stop watcher → clear → closing separator → resume` (else the separator lands on the live frame's row and the post-`End` resume erases it).
- **R-2 [debt]** — the coordinator's seam is the hook-parameterized locked write `WriteWith(p []byte, beforeLine func())` (with `Write(p) = WriteWith(p, nil)`), named in `T002` + `T009` — the line-splitting stays inside the writer's private `buf` (a bare `withLock(fn)` cannot enumerate lines).
- **R-3 [nit/decision]** — `T007(g)` pins the **`\r`-only** case as the accepted limitation (or asserts no false resume if byte-arrival stamping is chosen); "quiet" is defined as no new output **line**.
- **N-4** — the PR #85 fold note was relocated from the §9 (round 036) section to **§15** (session 8) in the daily log; §15's Next steps records the fold outcome.
- **N-5** — `STATUS.md`'s self-contradicting head + current-round clauses swept (both now say `/axb-tasks` **DONE**, implementation pending).
- **TD-2 self-correction** — `spinner_width_test.go` is **expected unchanged** (pure-function table + same row counts); the two genuine breaks stay in `spinner_test.go` (`TestFormatSpinnerLine`, `TestSpinnerElapsedIsTurnScoped`).
- **TD-5** — accepted by the architect as an **accuracy fix**; remains PM-owned (operator ratification).

## Round-040 tasks fold review #3 (PR #85, `f9e1ac9` → fold)

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `FOLDS ACCEPTED + R-4..R-7 + N-6..N-9`:

- **R-4 [blocker]** — the watcher's **admit** gets a real lock-scoped entry point. `ToolOutputWriter` exposes **two** entry points under its single mutex (named in `T002` + `T009`): the **line path** `WriteWith(p, beforeLine)` (per-line clear) **and** the **bookkeeping path** `withLock(fn)` (the watcher's `admitResume()` + the `lastLine` stamp). R-2's "unimplementable" claim is **scoped to the line path** (a bookkeeping closure is implementable and is the only way the watcher honours lock order `block-mutex → spinner-mutex`).
- **R-5 [debt]** — `T007(g)` settles the `\r`-only case as **required** (the resume MUST reappear: FR-001 is line-scoped, and `\r`-only output is visually silent); **byte-arrival stamping is rejected** (contradicts FR-001).
- **R-6 [debt]** — the back-pressure residual is sharpened (`deactivate()` takes the **spinner** mutex, which the redraw goroutine can hold **inside a blocking frame write** → the block mutex can be held for that write, arbitrarily long, coupling the child's drains to the spinner's stream), and `T007(f)` becomes **self-limiting** (the stalled writer must be **released**; then assert completion + no interleave).
- **R-7 [debt]** — the B2 disposition sweep (the helpers are **kept**, not removed): `plan.md` (the RF-1 directive), `STATUS.md`, and the daily log §15.
- **N-6** — the changed-file set includes **`spec.md`** (`{tasks.md, spec.md, STATUS.md, session-summary}`). **N-7** — the stray double blank in the daily log removed. **N-8** — the `dev`-ahead count corrected to **15** (measured). **N-9** — `T014` records why the existing long-quiet scenarios stay green (their `timeout: 1` < N).

## Round-040 tasks fold review #4 (PR #85, `771e057` → fold)

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `FOLDS ACCEPTED + R-8/R-9 + N-10/N-11`:

- **R-8 [debt, required]** — the `End` path gets the same seam R-4 gave the watcher: `EndWith(beforeSeparator func())` (with `End()` = `EndWith(nil)`), so the coordinator runs the **clear inside the writer's critical section**; the watcher is **stopped AND joined** before the clear (stopping the ticker is not stopping the goroutine), so an in-flight `admitResume()` cannot race it. Named in `T002` + `T009` + Boundary. **Why:** the sink's contract is *after the child is **reaped*** (not *after the drains are joined*); on the trim/timeout paths `abortCapture` waits only under the bounded `commandWaitDelay` (2 s), so a drain goroutine can still be **inside `Write`** at `End` — an out-of-mutex clear could interleave a frame with a line (the FR-002 property). New **T007(h)** pins `End`-while-`Write`-in-flight ⇒ no interleave/residue (or an explicitly recorded accepted race).
- **R-9 [refactor]** — the **idle clock moves into the writer**: `ToolOutputWriter` stamps `lastLine` inside the line-emit critical section and exposes `IdleSince(now)`; the coordinator holds no block state and needs no separate stamp critical section (removes the "data in the coordinator, lock in the writer" #69 smell and a third lock-order question).
- **N-10** — `T002`'s "one entry point" contradiction reworded to **"one lock owner, three entry points"** (`WriteWith`, `EndWith`, `withLock`).
- **N-11** — the `STATUS.md` fold-1 B1 note annotated `(→ two entry points per R-4)` to avoid reading as a contradiction with fold 3.
- **Seam model complete:** with R-4 + R-8 the three coordinator/writer interactions (line path, watcher admit, `End`) all have a lock-scoped entry point.

## Round-040 tasks fold review #5 (PR #85, `188d9e6` → fold)

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `FOLDS ACCEPTED + R-10/R-11 + N-12/N-13`:

- **R-10 [sweep]** — `T009`'s stale *"the `lastLine` stamp is a `withLock` bookkeeping write"* clause is deleted (it contradicted R-9 in the **same bullet**); the clock is the writer's, read via the `withLock(fn func(idle))` closure — the coordinator never stamps.
- **R-11 [required]** — the idle **query** gets a lock-scoped path: the bookkeeping entry point is **`withLock(fn func(idle time.Duration))`** (the writer computes the idle duration under its own lock and hands it to the closure), so check+admit are **one critical section** (a self-locking `IdleSince` inside `withLock` would self-deadlock; an unlocked one would race). `lastLine` is **seeded at `Begin`**, and **T007(b′)** pins the **zero-output** case (a command that prints nothing still resumes after `N` — FR-001's zero-line case, the original #82 complaint).
- **N-12** — `STATUS.md`'s fold-1 B1 annotation restored to *"one lock owner, three entry points"*.
- **N-13** — recorded **why `Begin` needs no hook**: `sink.Begin()` runs **before** `cmd.Start()` (no drain goroutine exists yet), so the header write cannot race a line or a frame — making the R-8 `End`-hook / no-`Begin`-hook asymmetry explicit.
- **R-4/R-8/R-11 family CLOSED:** the four coordinator↔writer interactions (line path, admit, `End`, idle query) all have a lock-scoped path under the writer's single mutex.

## Round-040 tasks fold review #6 (PR #85, `8b71463` → fold) — **CERTIFICATION: implementation-ready**

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `CERTIFICATION: implementation-ready + R-12/R-13 + N-14`:

- **R-12 [mechanical]** — `T002`'s entry point (ii) now carries the **R-11 signature** `withLock(fn func(idle time.Duration))` (it previously still read `withLock(fn func())`, the N-10 class resurfacing); the task now has **exactly one definition** of each of the three entry points.
- **R-13 [mechanical]** — `T007`'s clauses `(b′)`, `(g)`, `(h)` are **inside the T007 checkbox** in order `(a)…(b)(b′)(c)…(f)(g)(h)` (the `(g)`/`(h)` text had detached into an unmarkered bullet, which a One-Shot run could skip); the detached bullet is removed.
- **N-14 [naming]** — `T009`'s parenthetical no longer names the removed `IdleSince` shape; it now reads *"the idle clock lives in the writer — read via the `withLock` idle closure"* (the negative `IdleSince` mentions that explain why the method is rejected remain).
- **Certification:** the architect certified the task list **implementation-ready** for a One-Shot `/axb-implement` (scope, mechanism, seam model, witness set, truth/code boundary); `/axb-implement` is the operator's call.

## Round-040 tasks fold review #7 (PR #85, `9e02442` → fold) — certification CONFIRMED

Plan-side fold (no truth, no ADR, no code) applying the architect fold review `certification CONFIRMED + R-14 + B-1/B-2 + N-15`:

- **R-14 [required]** — the **per-call-reset witness** is re-attributed to the **unit** layer: `T015(b)` now reads *"freeze the second figure ⇒ the **unit** pin fails (T006's second-`OnInferenceStart` sub-assertion, SC-001) — the WS-B E2E Example asserts the shape only (SC-003)"*; `T015(a)` notes the WS-A E2E is the sole detector (with T007's (a)/(c) pins). `spec.md` **SC-005** reworded the same (PM-owned; the TD-11/SC-004 and TD-5/SC-003 precedent).
- **B-1 [bookkeeping]** — the daily-log fold notes are **reordered oldest → newest** (they had read 1 → 6 → 5 → 4 → 3 → 2), and Fold 2's label now names its own fold commit (`f9e1ac9`).
- **B-2 [bookkeeping]** — the `STATUS.md` fold ledger gains the **#6** entry.
- **N-15 [nit]** — Core Inputs' primer reads **"one lock owner, three entry points"** (it had said "one lock-scoped entry point").
- **Position:** the content certification **stands** (no scope/mechanism/seam/truth/acceptance change); the review loop is closed at this artifact.
