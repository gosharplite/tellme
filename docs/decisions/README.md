# Decisions (ADRs)

Lightweight, immutable **decision records** for this project's own governed artifacts — mirroring the
upstream `aixbdd-tmg` `decisions/` convention. An ADR here also serves as a **named home** for a
project-language declaration (see the upstream `axb-gherkin-and-dsl/STANDARDS.md` "Project Language"
clause, added by [aixbdd-tmg PR #8](https://github.com/gosharplite/aixbdd-tmg/pull/8)).

## When to write one

- A change to a project-level rule, convention, or a language / scope declaration.
- A decision that other artifacts (or future rounds) depend on and must be able to cite.
- The resolution of a question escalated to, or answered by, an upstream host rule.

Typo- and editorial-only fixes do not need an ADR.

## Naming & lifecycle

- One file per decision: `docs/decisions/NNNN-<kebab-slug>.md`, zero-padded, ascending.
- `Status` is one of `Proposed` / `Accepted` / `Superseded by NNNN` / `Rejected`.
- Immutable once `Accepted` (except the `Status` line and this index) — supersede with a new ADR
  rather than rewriting history.

## Index

| ADR | Title | Status |
| --- | --- | --- |
| [0001](0001-project-language.md) | Project language: English artifact declaration | Accepted |
| [0002](0002-first-presentation-dependency.md) | First presentation dependency: glamour | Accepted |
| [0003](0003-terminal-detection-isatty.md) | Terminal detection: a real isatty (`golang.org/x/term`) | Accepted |
| [0004](0004-user-global-prompt-log.md) | User-global interactive prompt log (`~/.tellme/global_prompts.jsonl`) | Accepted |
| [0005](0005-tool-call-log-parity.md) | Tool-call log parity: per-call frame cadence, CLI-computed per-call estimate, rune-safe rendering | Accepted (**D7 superseded by [0009](0009-spinner-dual-timer-and-streaming-liveness.md)**; **D1 amended by [0014](0014-yield-policy-owner.md)**) |
| [0006](0006-tool-reason-fold-and-cap.md) | Tool reason fold, trim, and cap: the model-authored reason joins its sibling sanitize+cap family | Accepted |
| [0007](0007-terminal-control-sanitization.md) | Terminal-control sanitization of the `[Tool Output]` block: a new stderr presentation invariant, and a deliberate reference divergence | Superseded by [0008](0008-terminal-safe-lines-and-blank-line-grouping.md) |
| [0008](0008-terminal-safe-lines-and-blank-line-grouping.md) | Terminal-safe `[Tool …]` line policy (generalized) + live turn-output blank-line grouping | Accepted |
| [0009](0009-spinner-dual-timer-and-streaming-liveness.md) | Spinner dual elapsed timer + streaming liveness (supersedes ADR 0005 **D7** — the whole-block pause — only) | Accepted |
| [0010](0010-test-deadline-decoupling.md) | Test deadlines: a test must not hardcode a tight wall-clock budget that is not its subject | Accepted |
| [0011](0011-layer-discipline-gate.md) | Layer-discipline gate: the pinned layer rule + a fail-on-stale violation baseline | Accepted (**D10**'s "no modelith toolchain" clause amended by [0030](0030-domain-model-and-modelith-toolchain.md) for the model + drift gate) |
| [0012](0012-hermetic-make-go-env.md) | A hermetic `make` Go-toolchain invocation environment (generalises ADR 0011 **D5**; round-020 TD1 is the precedent — its pin stays recipe-local) | Accepted |
| [0013](0013-composition-root-injection.md) | Composition-root extraction: an injected, domain-typed `Dependencies` seam (R2 of #92) | Accepted (**D2**'s unexported-typed, nil-defaulted `RunTUIPrompt` seam *narrowed* by [0017](0017-cli-tui-prompt-decoupling.md)) |
| [0014](0014-yield-policy-owner.md) | Yield-policy owner + `LoopObserver` hook split (R3 of #92; amends ADR 0005 **D1**) | Accepted |
| [0015](0015-loop-presentation-port.md) | Loop presentation port: the loop owns the schedule, `internal/ui` owns the tool-line rendering + the blank-reason predicate (R4 of #92) | Accepted |
| [0016](0016-application-import-ceiling.md) | Application import-ceiling gate (RULE-E): the application tiers' downward-import allow-list (R5 of #92) | Accepted |
| [0017](0017-cli-tui-prompt-decoupling.md) | De-couple `internal/cli` from the TUI prompt: an injected domain `tui.Prompter` port (R5.2 of #92; closes review-deferral F-4) | Accepted |
| [0018](0018-cli-agent-contracts-extraction.md) | De-couple `internal/cli` from the turn loop (re-cut sub-slice 1): extract the loop's crossing contracts to `internal/domain/agent` (R5.3 of #92; baseline unchanged — the edge move is sub-slice 2) | Accepted |
| [0019](0019-agentloop-construction-inversion.md) | De-couple `internal/cli` from the turn loop (sub-slice 2): invert the `AgentLoop` construction/execution into an injected domain port (R5.4 of #92; baseline **2 → 1**; the `→ ui` edge retained) | Accepted |
| [0020](0020-cli-ui-decoupling.md) | Close [#101](https://github.com/gosharplite/tellme/issues/101): de-couple `internal/cli` from `internal/ui` (RULE-E baseline **1 → 0**, the terminal state) + the F-6/F-7/F-8 seam resolutions (R5.5 of #92) | Accepted |
| [0021](0021-ride-alongs-and-records.md) | Ride-alongs: the command tool's construction-time output sink + the suggester's single-owned selection policy; the durable home of the three `#92` records (closes [#115](https://github.com/gosharplite/tellme/issues/115) + [#116](https://github.com/gosharplite/tellme/issues/116)) | Accepted |
| [0022](0022-offline-session-config-and-turns-log.md) | Offline session commands honour `-c` (+ an explicit-`-c` failure) + a per-session `turns.log` and a `-t` reader (closes [#103](https://github.com/gosharplite/tellme/issues/103)) | Accepted |
| [0023](0023-list-default-and-chrome-colour.md) | `-l` takes an optional count (bare `-l` = 1) + green chrome accents on a terminal (four elements; supersedes round-017 D3 for them) | Accepted |
| [0024](0024-e2e-suite-throughput.md) | E2E suite throughput: parallel scenarios by default (`Concurrency=4` + the `TELL_ME_E2E_CONCURRENCY` seam) + a `test-fast` subset that is never the gate | Accepted |
| [0025](0025-mcp-tool-call-reason.md) | A tellme-owned `reason` for MCP tool calls (the `{reason, MCP_PAYLOAD}` envelope; the server's schema relayed verbatim) + a universal, single-owned *no reason, no go* gate (folds [#121](https://github.com/gosharplite/tellme/issues/121)) | Accepted (**D1 qualified at the provider wire by [0031](0031-provider-supported-schema-surface.md)**) |
| [0026](0026-round-close-tags.md) | Round-close tags: an annotated `round-NNN` on the `dev → main` propagation merge (closeout Step 7, operator-approved; immutable; **not** a version — `--version` stays `dev`) | Accepted |
| [0027](0027-tool-chrome-colour-and-payload-delta.md) | Tool-chrome colour accents (grey `[Tool Output]` frame + yellow `[Tool Action]`, round-054 gate) + `argValueCap` **189 → 500** + a signed pre-flight payload increment (in-memory, session-scoped) | Accepted (**A3 superseded by [0028](0028-grey-tool-output-block.md)**) |
| [0028](0028-grey-tool-output-block.md) | Every `[Tool Output]` line grey — the header, **each streamed content line**, and both separators (extends ADR 0027; supersedes its plain-content assumption A3) | Accepted |
| [0029](0029-darwin-metrics-and-sample-cadence.md) | macOS CPU/MEM land (cgo Mach `host_statistics64` + cgo-less `getrusage` — a recorded divergence from the reference's `runtime/metrics` path, **D1a**; `golang.org/x/sys/unix`; reference-exact MEM) + a 1 Hz resource-sample cadence (the braille frame stays 200 ms) | Accepted |
| [0030](0030-domain-model-and-modelith-toolchain.md) | tellme domain model: three modelith models under `docs/domain-model/` (product · quality · environment-management) + the modelith toolchain (`make modelith-lint`/`render`/`check`) + a zero-tolerance drift gate; **amends ADR [0011](0011-layer-discipline-gate.md) D10** (its "no modelith toolchain" position is superseded for the model + drift gate; no `modelith-layers`) | Accepted |
| [0031](0031-provider-supported-schema-surface.md) | The provider-supported schema surface: a tool declaration is projected onto the closed wire type (default-deny allowlist, empirically probed; a family-agnostic vendor-extension floor at the normalizer) — **amends [ADR 0025](0025-mcp-tool-call-reason.md) D1** at the provider wire | Accepted |
| [0032](0032-agent-image-vision.md) | Agent image vision: an explicit per-provider `VISION` capability key + a content-sniffed local image + an inline `image_url` block on the OpenAI-compatible wire (the `read_image` agent tool, offered only when the provider declares vision) | Accepted (**D4/D8 narrowed; RF-062-1 superseded by [0033](0033-gemini-image-vision.md); RF-062-10 FULLY delivered — seam by [0039](0039-toolset-spec-capability-seam.md), media by [0040](0040-media-channel-in-band.md) (which supersedes its D7a)**) |
| [0033](0033-gemini-image-vision.md) | Gemini image vision: the `inlineData` proto-JSON blob on the Gemini/Vertex wire (a standalone `user` turn after the tool result; the round-062 loud refusal retired) + a **family-aware** inline ceiling enforced by `read_image` (14 MiB derived vs 32 MiB) — extends [ADR 0032](0032-agent-image-vision.md), supersedes its **RF-062-1**, narrows its D4/D8 | Accepted (**supersedes [0032](0032-agent-image-vision.md)'s RF-062-1; narrows its D4/D8; its RF-063-7 superseded by [0035](0035-gemini-parallel-tool-call-batching.md); its RF-063-6 delivered by [0039](0039-toolset-spec-capability-seam.md)**) |
| [0034](0034-prompt-suggestion-pool-depth.md) | Prompt suggestion parity: the `-i` recent-prompt **candidate pool** deepens to the newest **50** distinct prompts (the reference's `LoadTopN(ctx, 50)`) while the surfaced cap stays **10** — pool-only, per the operator's Q1 → A; keeps tellme's tool source + `~/.tellme/` log; the reference's empty-query-first-5 / session source / `WorkspacePolicy` / compaction stay unadopted forward items | Accepted |
| [0035](0035-gemini-parallel-tool-call-batching.md) | Gemini/Vertex parallel tool calls: a round's tool results are **batched into one `user` turn** (N `functionResponse` parts in call order; media turns after) so a turn with ≥2 tool calls no longer 400s — extends [ADR 0033](0033-gemini-image-vision.md), supersedes its **RF-063-7** and annotates its D2 multi-call note; closes [#132](https://github.com/gosharplite/tellme/issues/132) | Accepted (**supersedes [0033](0033-gemini-image-vision.md)'s RF-063-7; its D2 FIFO note + §Forward RF-065-1 annotated by [0036](0036-toolcall-id-pairing.md)**) |
| [0036](0036-toolcall-id-pairing.md) | Gemini/Vertex: **id-link** a round's `functionCall`/`functionResponse` parts on the wire and **pair each result to its call by `ToolCallID`** (order-independent), keeping the FIFO name match as the fallback — **delivers [ADR 0035](0035-gemini-parallel-tool-call-batching.md) §Forward RF-065-1**, closes [#134](https://github.com/gosharplite/tellme/issues/134); a hardening/parity round (no user-visible change) | Accepted (**delivers [0035](0035-gemini-parallel-tool-call-batching.md)'s RF-065-1; its §Forward RF-066-2/RF-066-7 delivered and RF-066-8 retired by [0037](0037-gemini-toolcall-id-provenance.md)**) |
| [0037](0037-gemini-toolcall-id-provenance.md) | Gemini/Vertex tool-call id provenance: **prefer the provider-issued `functionCall.id`** (else the deterministic `call_<n>`), Gemini-local by construction (the OpenAI-compatible wire is untouched); **surface a round's unpaired calls** via a single-owned accessor — **delivers [ADR 0036](0036-toolcall-id-pairing.md) §Forward RF-066-2 + RF-066-7**, **retires RF-066-8**, closes [#136](https://github.com/gosharplite/tellme/issues/136); a hardening/parity round | Accepted (**delivers [0036](0036-toolcall-id-pairing.md)'s RF-066-2/RF-066-7; retires its RF-066-8**) |
| [0038](0038-unpaired-call-diagnostic.md) | Surface a Gemini/Vertex round's **unpaired tool calls** as a plain `stderr` `[Tool Warning]` diagnostic — **delivers [ADR 0037](0037-gemini-toolcall-id-provenance.md) §Forward RF-067-1**; the account gets a family-neutral single owner (`llm.UnpairedToolCalls`) + a CLI gateway decorator; informational (Q2 → A), never in `turns.log` (Q1 → A); a defensive surface (no live producer today) | Accepted (**delivers [0037](0037-gemini-toolcall-id-provenance.md)'s RF-067-1**) |
| [0039](0039-toolset-spec-capability-seam.md) | The `ToolSetSpec` capability seam: the agent tool-registry build takes **one named `deps.ToolSetSpec`** (the `[Tool Output]` sink + the `vision` capability gate + the provider label) instead of the positional `(sink, vision bool, providerType)` scalars, so a new capability is a **field**, not a fourth positional argument — **delivers [ADR 0032](0032-agent-image-vision.md) §Forward RF-062-10** (the seam half) and **[ADR 0033](0033-gemini-image-vision.md) §Forward RF-063-6**; a pure internal-shape refactor (no config/UX change) | Accepted (**delivers [0032](0032-agent-image-vision.md)'s RF-062-10 (seam half) + [0033](0033-gemini-image-vision.md)'s RF-063-6; its §Forward RF-069-1 delivered by [0040](0040-media-channel-in-band.md)**) |
| [0040](0040-media-channel-in-band.md) | The media channel **in-band**: the media type is `tools.MediaPart` and a media-producing tool returns its media through the optional **`tools.MediaTool`** capability (`ExecuteMedia`), which the loop type-asserts — **retiring the per-call `context` collector** (ADR 0032 **D7a**) and the `infrastructure/tools → domain/llm` import; **completes [ADR 0032](0032-agent-image-vision.md) §Forward RF-062-10** and **delivers [ADR 0039](0039-toolset-spec-capability-seam.md) §Forward RF-069-1**; pure internal-shape refactor (no config/UX change) | Accepted (**completes [0032](0032-agent-image-vision.md)'s RF-062-10 (media half; supersedes its D7a) + delivers [0039](0039-toolset-spec-capability-seam.md)'s RF-069-1**) |
| [0041](0041-domain-model-drift-guard.md) | Domain-model **drift guard (advisory)**: the model is **load-bearing** — a round that changes modelled behaviour updates `docs/domain-model/**` in the same PR — and a POSIX-only, advisory `make modelith-drift` (never fails; not a `verify` member) surfaces a modeled entity whose concept left the code; the reference's **name-diff** advisory gates stay **unadopted** (measured ~79 % false-positive); **qualifies [0030](0030-domain-model-and-modelith-toolchain.md) D5/RF-060-3** | Accepted (**qualifies [0030](0030-domain-model-and-modelith-toolchain.md) D5/RF-060-3**) |
| [0042](0042-quality-gates-fmt-adrindex-race.md) | Quality gates: **`verify-fmt`** (`gofmt -l`; a non-empty result fails — the `make fmt` check's counterpart) and **`verify-adr-index`** (every `docs/decisions/` ADR indexed once) **join `make verify`**; a standalone **`make test-race`** ships (package-by-package, **not** a `verify` member — expense); the audit's other candidates (fold `vet`; coverage; topology-audit errors) stay **rejected** | Accepted |
| [0043](0043-search-files-tool.md) | The **`search_files`** agent tool — a **bounded, deterministic** in-file content search (the missing half of the reader trio): literal by default with an `is_regex` opt-in; the round-024 byte budget (every return path) + a 100-match cap + a 500-**byte** line trim; matches sorted path-then-line; binary files skipped, **no** directory ignore list, **no** `SafePath` gate — three deliberate divergences (+ three further recorded differences) from the reference; a ninth tool offered to every provider | Accepted |
| [0044](0044-gemini-cached-token-usage.md) | Gemini/Vertex usage: the adapter decodes `cachedContentTokenCount` → `Usage.CachedTokens` and `thoughtsTokenCount` → `Usage.ThinkingTokens`; the cached count was dropped so `H` was always 0 and the whole prompt was billed at the MISS rate (~10x over-charge) — closes [#149](https://github.com/gosharplite/tellme/issues/149); the disjointness is family-specific (Gemini `candidatesTokenCount` excludes thoughts — no subtraction, unlike the OpenAI-compatible family) | Accepted |
| [0045](0045-list-role-headers-and-rendered-body.md) | The `-l` history listing presents each message as a **role header line** (`[USER]`/`[MODEL]`) + a body (**model** body rendered by the shared glamour renderer, **operator** body verbatim) + **one blank line** after every message; the header is accented blue/magenta **only when `stdout` is a terminal and `-r` is off** (honours `-r`, clarify Q1 → A); tool activity stays omitted (Q2 → A); a dedicated `stdout` probe + `TELL_ME_FORCE_STDOUT_TTY` seam; count/selection semantics + the frozen `history.jsonl` unchanged | Accepted (**amended by [0054](0054-list-backward-turn-indices.md)** — the role headers carry a backward turn index) |
| [0046](0046-cli-help-and-version-shorthands.md) | The `-h`/`--help` and `-v`/`--version` CLI shorthands: `-h`/`--help` prints the pflag flag list (`Usage of tellme:` + `fs.FlagUsages()`) to **`stdout`** and exits **0** (a successful, offline, prompt-less action, precedence before `--version`); `-v` is the byte-identical shorthand for `--version`; help emits no `tellme: …` phrase and the unrecognized-flag refusal (phrase on `stderr`, exit 2) is unchanged | Accepted |
| [0047](0047-skill-frontmatter-block-scalars.md) | Skill frontmatter block scalars: the loader resolves a YAML **block scalar** `description` (`>`/`|`, with chomping) to its text — so a skill authored with `description: >` lists by its **real text**, not the bare indicator; an inline value containing `>` stays literal, an indicator with no body skips the file (best-effort, unchanged); **stdlib-only** (no YAML dependency); a **deliberate divergence *beyond* the reference** (tell-me-go's line-based `parseSkill` shares the limitation) | Accepted |
| [0048](0048-recoverable-unknown-tool-name.md) | An **unknown tool name** becomes a **recoverable fold-back**: the loop appends a `tool`-role result naming the unknown tool + a bounded list of the available wire names and continues (never the terminal request-level failure), mirroring the reason-less refusal; the recoverable path is **bounded per turn** (`maxUnknownToolFolds = 3`) because tellme has no repetition/runaway detection and the only other bound is `MAX_TOOL_LOOP` (1000, paid) — after the cap, the frozen phrase + exit 7 (round-008 FR-010 is narrowed, not removed); closes [#154](https://github.com/gosharplite/tellme/issues/154) | Accepted |
| [0049](0049-mcp-tool-name-discoverability.md) | Make the callable MCP wire name **discoverable** in the offered declaration: every offered MCP tool's **description** is prefixed with a tellme-authored call-name note naming the callable wire name (`Call this tool as "mcp_github_get_me". <server text, verbatim>`), and the empty-description **fallback names the callable name** (not the bare one) — description-only, schema unchanged, server text never rewritten (ADR 0025 D1), no prompt change (D4); a recorded divergence *beyond* the reference (which neither advertises nor falls back); closes [#155](https://github.com/gosharplite/tellme/issues/155) | Accepted |
| [0050](0050-bounded-provider-transport-retry.md) | A **bounded provider transport retry**: a retryable provider failure (transport: dial/EOF/reset/broken-pipe/GOAWAY/TLS/timeout; or HTTP **429/5xx**) is retried automatically **at most twice** (fixed delays **1 s → 3 s**) before the run fails with the frozen phrase `the provider request failed` + exit **6** (unchanged); a non-retryable failure (4xx/auth/decode/truncation) fails at once; retryability is **typed** (`llm.ProviderError` gains `Status`/`Transport`; the predicate is domain-owned, never string-matched); **one** call/usage/frame per retried inference (round 034 "an internal retry does not count"); a `TELL_ME_FORCE_RETRY_DELAY_MS` hermetic seam; a recorded divergence (no failover / auth-refresh / jitter); **reconciles round 030's "no retry layer"** | Accepted |
| [0051](0051-interrupted-turn-partial-persistence.md) | Preserve completed tool steps on an **operator-interrupted** turn: when `SIGINT`/`SIGTERM` cancels a tool turn with **≥1 completed step**, tellme appends **one** `history.Entry` closed with a **synthetic assistant answer** (`[Turn interrupted by operator via Ctrl+C]`) so the partial turn replays to a valid `user … assistant` sequence (both families) and is resumable; **zero steps ⇒ nothing written** (today's clean abort); detection is **typed** (`errors.Is(err, context.Canceled)`); exit **0** + a `stderr` informational line (no class prefix, not in `turns.log`); no usage persisted; the entry schema unchanged; a recorded divergence from the reference (which discards the partial turn) — closes [#159](https://github.com/gosharplite/tellme/issues/159) | Accepted |
| [0052](0052-failed-turn-partial-persistence.md) | Persist a **failed** turn's completed tool steps: when a turn fails (provider failure — retry exhausted / non-retryable — or the tool-loop failure, exit 7) with **≥ 1** completed step, tellme appends **one** `history.Entry` closed with a distinct synthetic answer (`history.ProviderFailedTurnAnswer` / `history.ToolFailedTurnAnswer`), so the work survives and the stored history replays as a valid `user … assistant` sequence — while the **failure surface is unchanged** (frozen phrase + exit 6/7) plus one informational `stderr` line; the operator-interruption predicate is broadened to `ctx.Err() != nil || errors.Is(err, context.Canceled)`, folding issue #161 hole #2 (a `Ctrl+C` during a round-078 retry wait); a **zero-step** failure writes nothing; the failed-turn append is best-effort (never masks the failure); closes [#161](https://github.com/gosharplite/tellme/issues/161) | Accepted |
| [0053](0053-back-rollback-turns.md) | `-b`/`--back [N]`: roll back the last N turns of the session history — `tellme -b`/`-b N` (offline; the default-1 form + a plain `stdout` confirmation; joins the offline-path set) and `tellme -b [N] "p"` (roll back **then** run); a **durable** `history.Store.Rollback(n)` (temp-file + `fsync` + atomic rename; whole `history.Entry` lines including their `steps[]`; **clamp** when N exceeds the available turns; `n ≤ 0` is a usage error; **the archive is never touched**); the round-054 `-l` optional-int pre-pass is **generalised** to `-l`/`--list` + `-b`/`--back` — **resolves ADR 0023 §Forward RF-54-4**; `-b` × `--new` refuses; a recorded divergence (turn lines vs the reference's message pairs); closes [#163](https://github.com/gosharplite/tellme/issues/163) | Accepted (**resolves [0023](0023-list-default-and-chrome-colour.md)'s RF-54-4**) |
| [0054](0054-list-backward-turn-indices.md) | Backward-counting **turn indices** in the `-l`/`--list` role headers (`[USER] - N` / `[MODEL] - N`): each listed message's header gains a trailing ` - N` where `N = 1` is the **most recent** turn, aligning the listing 1:1 with `-b [N]`; both messages of one turn share the same index; the index is the **true** distance from the end of the loaded history (a lone leading `[MODEL]` of an odd `-l` slice keeps its true distance, never renumbered); the index rides a new `render.ListingMessage.TurnIndex` field (computed by the CLI) so the adapter stays slice-agnostic; the colour unit is the **whole** label; presentation-only (no `history.jsonl`/`-b`/chrome change) — **amends [0045](0045-list-role-headers-and-rendered-body.md)** | Accepted (**amends [0045](0045-list-role-headers-and-rendered-body.md)**) |
| [0055](0055-round-scoped-media-placement.md) | **Round-scoped media placement**: a tool round's media is folded **once, after** the per-call loop, so a round's `tool` results are **contiguous** on the wire (an assistant `tool_calls` block must be answered by its `tool` messages with no interleaved message) and the round's N images ride **one** `user` message in call order — fixing the OpenAI-compatible-family 400 on **≥ 2** `read_image` calls in one round; single-call output is **byte-identical**; the Gemini adapter is unchanged (one media turn of N `inlineData` parts for N ≥ 2); adds the missing tripwire (a loop-tier 3-media order pin + the shared E2E contiguity witness carried by the multi-image fixture) — **clarifies [0032](0032-agent-image-vision.md) D7** (its body read round-scoped; the pre-083 implementation read it per-call) | Accepted (**clarifies [0032](0032-agent-image-vision.md) D7**) |
| [0056](0056-rollback-durability-witness.md) | **Witness the rollback durability guarantee**: round 081's "temp file + `fsync` + atomic rename" clause had **no falsifier** (removing the file `fsync` left the suite green) — the round adds an **unexported `durableFS` seam** (`Sync`/`Rename`/`SyncDir`, `os`-defaulted, production unchanged) so a **mechanism-seam unit pin** (`TestFileStore_Rollback_SyncsTempFileBeforeRename`) reddens when the file `fsync` is removed; **calibrates the asserted effect** (temp file + `fsync`-before-rename + atomic rename; no power-loss claim); records the **best-effort directory `fsync`** as **accepted-unwitnessed** (§Forward); removes the misfiled behaviour guarantee from `techstack.md` (Rule 6) and the `dsl.md` prologue (truth-prose boundary); closes [#169](https://github.com/gosharplite/tellme/issues/169) — **witnesses [0053](0053-back-rollback-turns.md)** | Accepted (**witnesses [0053](0053-back-rollback-turns.md)**) |
