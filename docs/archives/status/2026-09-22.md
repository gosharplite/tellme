# tellme — Status Archive · 2026-09-22

> **Back-link**: live state is [`STATUS.md`](../../../STATUS.md) — this file holds relocated historical detail only (never a live-state source).

Contents relocated at the **round-076 closeout** (2026-09-22): the **round-075 delivered-round detail** (round 076 is now the current delivered round) and its **environment note**.

---

## Last delivered round - 075 `075-skill-frontmatter-block-scalars` (DELIVERED / FROZEN - PR [#153](https://github.com/gosharplite/tellme/pull/153) merged into `dev` `250221d`, **merge commit**; **ADR 0047**)

- **Theme (operator request; no anchor issue)**: the skills loader read a YAML **block scalar** (`description: >`) as the literal indicator, so `list_skills` advertised `- <name>: > (<path>)`. The round makes the frontmatter reader resolve block scalars. Clarify **not escalated (0 questions)** — the residual choices (stdlib vs a YAML library; folding/chomping semantics; the E2E authoring seam) are technical → `/axb-technical-research`.
- **Before → after**: a `SKILL.md` with `description: >` + indented lines listed as `- golang-patterns: > (/…/SKILL.md)`; now it lists as `- golang-patterns: Idiomatic Go patterns for robust code (…)`. Inline quoted/unquoted values are byte-identical; an inline value containing `>` (e.g. `a > b`) stays literal; an indicator with no body resolves empty ⇒ the file is not a skill (best-effort skip, unchanged).
- **The change**: `internal/infrastructure/skills/loader.go` — `parseFrontmatter` routes `name`/`description` through a new `resolveFrontmatterValue`; `isBlockIndicator` (`[>|][+-]?`) + `collectBlock` + `stripBlockIndent`/`joinBlockScalar`/`applyChomping` resolve the block to text, then `TrimSpace`d. **stdlib-only** (no YAML dependency); one function + helpers; the `Skill` type + the `list_skills` surface/order/bounds are unchanged.
- **Artifacts**: `specs/plans/075-skill-frontmatter-block-scalars/**` (spec · checklist · acceptance `listing-a-block-scalar-skill.feature` · research D1–D6 · plan · tasks T001–T010 · truth-delta) · **ADR 0047** (`docs/decisions/0047-skill-frontmatter-block-scalars.md` + index) · `specs/truth/techstack.md` §Skills *Skills catalog (load)* MODIFY · `specs/truth/features/cli/chat/listing-the-available-skills.feature` (+1 Rule/Example) · `chat/dsl.md` (+2 rows + a round-075 note) · `internal/infrastructure/skills/loader.go` · `internal/infrastructure/skills/loader_test.go` (+6 tests) · `tests/e2e/steps/step_r075_skills_given_folded.go` · `tests/e2e/steps/step_r075_skills_then_described.go`.
- **Verification**: `gofmt`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **51 features**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (51 features · 17 root + 407 module rows · 2100 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): force `isBlockIndicator` false ⇒ the E2E `A description written as a folded block scalar` reddens at `the listing describes the skill … as …` (result `- golang-patterns: > (…)`) **and** the unit pins redden (`folded description = ">"; want "…"`).
- **Divergence (recorded)**: the reference `tell-me-go`'s line-based `parseSkill` **shares the limitation** — this round is a **deliberate divergence *beyond* the reference** (a robustness improvement), not parity (ADR 0047 §Decision 6).
- **Review chain (PR [#153](https://github.com/gosharplite/tellme/pull/153), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-1** `research.md` D6 overclaimed the witness set + FR-005 (multi-paragraph folding) had no carrier · **F-2** chomping is unobservable (`TrimSpace` neutralizes it) · **F-3** an ADR context claim unverifiable · **N-1** the DSL `不該發生` clause had no carrier; RF-075-4…7 proposed) → fold `7fb0fa1` (F-1 added the pins + corrected D6 · F-2 recorded the chomping inertness · F-3 reworded · N-1 carried; RF-075-4…7 homed) → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the new pins proven to redden under a reverted reader) → residual homing `8126470` (R-1…R-4).
- **Delivered**: PR [#153](https://github.com/gosharplite/tellme/pull/153) **human-merged** into `dev` (`250221d`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged `round-075`. **No anchor issue** (an operator request).

## Round-075 — environment note (relocated verbatim at the round-076 closeout)

- **Round-075 (session 61)** — branch `075-skill-frontmatter-block-scalars` off `dev` `3dfc3a4`: specify `a61ec45` -> acceptance + research + **ADR 0047** + truth + implementation `b2df835` -> PR [#153](https://github.com/gosharplite/tellme/pull/153) -> fold F-1/F-2/F-3 + N-1, RF-075-4…7 homed `7fb0fa1` -> residual homing `8126470` -> merge `250221d`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the new pins re-run under a reverted reader). **Human-merged** into `dev` (`250221d`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-075`. Host: **linux/amd64** (Go 1.26.6). Gates at delivery: `gofmt`/`go vet`/`go build` clean - `go test -count=1 ./...` green (E2E 51 features) - `make verify` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (51 features - 407 module rows - 2100 steps) - `go.mod`/`go.sum` unchanged.

---

Contents relocated at the **round-077 closeout** (2026-09-22, session 63): the **round-076 delivered-round detail** (round 077 is now the current delivered round) and its **environment note**.

## Last delivered round - 076 `076-recoverable-unknown-tool` (DELIVERED / FROZEN - PR [#156](https://github.com/gosharplite/tellme/pull/156) merged into `dev` `851466e`, **merge commit**; **ADR 0048**)

- **Theme (anchor issue [#154](https://github.com/gosharplite/tellme/issues/154); DoD = close it)**: an **unknown tool name** was **terminal** — `Registry.Lookup` miss ⇒ `ErrIncomplete` ⇒ the frozen `the tool request failed` phrase + **exit 7** — so a single naming slip killed the whole turn (observed live: butler/`deepseek-flash` on the `misc` repo asked for the GitHub MCP tool by its bare upstream name `get_me`, the callable name being `mcp_github_get_me`). The round makes it a **recoverable fold-back**, bounded per turn. Clarify **not escalated (0 questions)** — the design was locked by the issue (recoverable + cap); the residuals (message, counter home, ordering, ADR shape) are technical → `/axb-technical-research`.
- **Before → after**: `tellme: the tool request failed: tool "get_me" is not available` (**exit 7**) → the loop feeds back `error: no tool named "get_me"; available tools: <…>, mcp_github_get_me, …` and **continues** (the model self-corrects). The genuine-incomplete contract is preserved: the tool-loop **bound reached**, the per-turn **cap exhausted**, and **`no tools are registered`** still yield the phrase + exit 7 (round-008 FR-010 **narrowed**, not removed).
- **The change**: `internal/agent/agentloop.go` — on a registry miss the loop emits the call's `[Tool Action]` block (`logAction`, chrome parity with the reason-less refusal), folds back a `tool`-role result carrying the call's `ToolCallID` (`unknownToolResult` names the tool + the available wire names) and `continue`s; a **loop-local per-turn counter** (`unknownFolds`) capped by `maxUnknownToolFolds = 3` bounds the recoverable path (after the cap, the incomplete error → phrase + exit 7). Family-local: the provider adapters, the registry, the reason gate and the loop clamp are untouched (**zero wire diff**).
- **Artifacts**: `specs/plans/076-recoverable-unknown-tool/**` (spec · checklist · acceptance `recovering-from-an-unknown-tool-name.feature` · research D1–D8 · plan · tasks T001–T010 + fold/residual ledgers · truth-delta) · **ADR 0048** (`docs/decisions/0048-recoverable-unknown-tool-name.md` + index) · `specs/truth/techstack.md` *Agent tool loop* MODIFY (+ a *Tool-usage accounting* parenthetical correction) · `specs/truth/features/cli/chat/using-an-unknown-tool-name.feature` (**NEW**, 2 Rules) · `failing-the-tool-loop.feature` (the unavailable-tool Example **relocated** out) · `offering-the-agent-tools.feature` (the sibling Example synced) · `chat/dsl.md` (+2 rows) · `internal/agent/agentloop.go` · `internal/agent/agentloop_test.go` (+ the pin set) · `tests/e2e/steps/step_r076_unknown_tool.go`. `docs/domain-model/**` **unchanged** (not modelled — ADR 0041 escape hatch; `plan.md` §5).
- **Verification**: `gofmt`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (24 pkgs incl. the godog E2E, **52 features**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (52 features · 17 root + 409 module rows · 2110 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): restore the terminal `return` ⇒ the unit pin **and** the E2E `A one-off unknown tool name is recovered…` redden; drop the fold-back's `ToolCallID` ⇒ the pairing pin reddens; `maxUnknownToolFolds = 10` ⇒ the value pin reddens; `= 0` ⇒ the three unknown-tool E2E Examples redden.
- **Review chain (PR [#156](https://github.com/gosharplite/tellme/pull/156), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the loop change was right — the folds were **falsifiability** · **F-1** the pairing + no-usage halves unwitnessed · **F-2** the cap Example non-discriminating + the value unpinned · **F-3** the reason×unknown ordering unwitnessed · **F-4** a false `clampBytes` claim · **F-5** the fold-back chrome unpinned · **F-6** a stale sibling feature; + TD-076-1…4, N-1…N-5) → fold `19a73bd` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; R1/R2/R3) → residual fold `4fb14a1` → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; no residual).
- **Delivered**: PR [#156](https://github.com/gosharplite/tellme/pull/156) **human-merged** into `dev` (`851466e`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged `round-076`. **Closes [#154](https://github.com/gosharplite/tellme/issues/154)**.

### Environment note (round-076)

- **Round-076 (session 62)** — branch `076-recoverable-unknown-tool` off `dev` `86d0deb`: specify `5454b8a` -> acceptance + research + **ADR 0048** + truth + implementation `c56bfd0` -> PR [#156](https://github.com/gosharplite/tellme/pull/156) -> fold F-1…F-6 + TDs/nits `19a73bd` -> residual fold R1/R2/R3 `4fb14a1` -> merge `851466e`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**) -> residual fold -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`851466e`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-076`. Host: **darwin/arm64** (Go 1.26.6). Gates at delivery: `gofmt`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E 52 features) - `make verify` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (52 features - 409 module rows - 2110 steps) - `go.mod`/`go.sum` unchanged.

---

Contents relocated at the **round-078 closeout** (2026-09-22, session 64): the **round-077 delivered-round detail** (round 078 is now the current delivered round) and its **environment note**.

## Last delivered round - 077 `077-mcp-tool-name-presentation` (DELIVERED / FROZEN - PR [#157](https://github.com/gosharplite/tellme/pull/157) merged into `dev` `273b5a9`, **merge commit**; **ADR 0049**)

- **Theme (anchor issue [#155](https://github.com/gosharplite/tellme/issues/155); DoD = close it; research-gated, a NOOP was acceptable)**: an MCP tool is offered under the namespaced wire name `mcp_<server>_<tool>`, but nothing in the text the model *reads* advertised it — the synthesized fallback named the **bare** upstream name (`MCP tool get_me from server github`), so a weaker model could call the bare name (the round-076 recoverable fold-back is the loop-side backstop). The round makes the callable name **positively discoverable** in the offered declaration. Clarify **not escalated (0 questions)** — the issue delegated the decision to research.
- **Verify-first (the issue's decisive step)**: a **live** `initialize` + `tools/list` against `api.githubcopilot.com/mcp/` (2026-09-22, bearer `$GITHUB_TOKEN`) showed **45 tools, 0 with an empty description**; `get_me`'s description names no tool. So tellme's fallback **did NOT fire** in the observed run — fixing it alone would not have changed the failure; the **note** is the mechanism that surfaces the name. **A fix IS warranted → the NOOP path was not taken.** Reference check: `tell-me-go` namespaces identically but neither advertises the convention nor synthesizes a fallback.
- **Before → after**: the offered declaration's description `"Get details of the authenticated GitHub user. …"` → `Call this tool as "mcp_github_get_me". Get details of the authenticated GitHub user. …` (the server's own text relayed **unchanged** — added-to, never rewritten); the empty-description fallback names the **callable** name, never the bare one.
- **The change**: `internal/infrastructure/mcp/tool.go` — a named `callNameNoteFormat` constant + a computed `description` in `NewTool` (`Call this tool as "<t.name>". <server text>`); the empty-description fallback names `t.name` (post-truncation, so the note never lies). **Description-only**: the offered envelope/schema is unchanged; the server definition is never mutated (ADR 0025 D1); **no** prompt change (D4). `mcptest.Options{EmptyDescription: true}` makes the fallback E2E-reachable.
- **Artifacts**: `specs/plans/077-mcp-tool-name-presentation/**` (spec · checklist · acceptance `discovering-the-callable-mcp-tool-name.feature` (2 Rules) · research D1–D8 · plan · tasks T001–T012 + the fold ledger · truth-delta) · **ADR 0049** (`docs/decisions/0049-mcp-tool-name-discoverability.md` + index) · `specs/truth/techstack.md` *MCP tool-name presentation* **ADD** row · `specs/truth/features/cli/chat/using-tools-from-a-remote-mcp-server.feature` (a new Rule with 2 Examples) · `chat/dsl.md` (+3 rows) · `internal/infrastructure/mcp/tool.go` · `internal/infrastructure/mcp/mcptest/server.go` · `internal/infrastructure/mcp/tool_r077_test.go` · `tests/e2e/steps/step_r077_mcp_name.go`. `docs/domain-model/**` **unchanged** (not modelled — ADR 0041 escape hatch; `plan.md` §5).
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (24 pkgs incl. the godog E2E, **289 scenarios**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (52 features · 17 root + 412 module rows · 2124 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): neutralize the note ⇒ the E2E `The offered declaration names the callable wire name` reddens; note the bare name ⇒ 3 unit pins + the E2E redden; the bare fallback ⇒ the fallback pins **and** the E2E `A server that ships no description …` redden; `TrimSpace`/interposed text ⇒ the byte-exact pin reddens; a stray envelope property ⇒ the set pin reddens.
- **Review chain (PR [#157](https://github.com/gosharplite/tellme/pull/157), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the design was right, the folds were **witness quality**: **F-1** the "server text unchanged" claim had no byte-exact witness · **F-2** the schema-unchanged pin could not redden for its claim · **F-3** the fallback's provenance assertion was vacuous · **F-4** a recorded (but absent) PM acceptance journey · **TD-1** US2/FR-004 unreachable in E2E; + N-1…N-5) → fold `24be271` → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; R-1/R-2) → residual fold `a37e838` (R-1 record hygiene; R-2 homed as RF-077-6).
- **Delivered**: PR [#157](https://github.com/gosharplite/tellme/pull/157) **human-merged** into `dev` (`273b5a9`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-077`**. **Closes [#155](https://github.com/gosharplite/tellme/issues/155)**.

### Environment note (round-077)

- **Round-077 (session 63)** — branch `077-mcp-tool-name-presentation` off `dev` `ab8fab7`: specify `2178efa` -> research + **ADR 0049** + implementation `999f51f` -> PR [#157](https://github.com/gosharplite/tellme/pull/157) -> fold F-1…F-4 + TD-1 + nits `24be271` -> residual fold R-1/R-2 `a37e838` -> merge `273b5a9`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`273b5a9`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-077`. Host: **darwin/arm64** (Go 1.26.6). Gates at delivery: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E **289 scenarios**) - `make verify` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (52 features - 412 module rows - 2124 steps) - `go.mod`/`go.sum` unchanged.

---

Contents relocated at the **round-079 closeout** (2026-09-22): the **round-078 delivered-round detail** (round 079 is now the current delivered round) and its **environment note**.

## Last delivered round - 078 `078-provider-transport-retry` (DELIVERED / FROZEN - PR [#158](https://github.com/gosharplite/tellme/pull/158) merged into `dev` `c291e1d`, **merge commit**; **ADR 0050**)

- **Theme (operator request, 2026-09-22; no anchor issue — rounds 073/074/075 precedent)**: a momentary network blip aborts a prompt turn (`tellme: the provider request failed` + **exit 6**), yet re-running usually works. tellme had **no retry layer** — one provider call, one failure, terminal. The round adds a **bounded transport retry**: a **retryable** failure is retried **at most twice** (fixed **1 s → 3 s**) before failing. Clarify **not escalated (0 questions)** — the theme/schedule are operator-given and the retryability predicate (transport + 429/5xx) was operator-locked in-session.
- **The change**: **typed facts at the adapter, the predicate in the domain** — `llm.ProviderError` gains `Status int` / `Transport bool`; the OpenAI-compatible and Gemini/Vertex adapters set them (`http.Do`/`io.ReadAll` → `Transport`; non-2xx → `Status`; decode / the round-030 truncation → neither). The **domain-owned** `llm.Retryable(err)` = `Transport || Status == 429 || 500 ≤ Status ≤ 599` (never string-matched — the status previously lived only inside a formatted message). A `llm.Gateway` **decorator** in `internal/cli` (`retryingGateway`) runs the fixed `retryDelays = {1s, 3s}` schedule (≤2 retries / 3 attempts), is `ctx`-aware (SIGINT aborts at once, and never announces a retry that will not run), and returns the last error unchanged on exhaustion. Wired **inner** to `withUnpairedDiagnostic` at the existing one seam (both families). A single `llm.Response` on eventual success ⇒ **one** `calls`/usage/frame; the retry line is a chrome-styled `[HH:MM:SS] retrying the provider request in <delay> (attempt <n> of <max>): <detail>` on `stderr` only (yields/restores the spinner; carries neither the `tellme: ` prefix nor the class phrase). The `TELL_ME_FORCE_RETRY_DELAY_MS` hermetic seam (scoped to the 078 E2E scenarios) collapses the delays.
- **Artifacts**: `specs/plans/078-provider-transport-retry/**` (spec · checklist · acceptance `recovering-from-a-momentary-provider-failure.feature` (3 Rules / 4 Examples) · research D1–D9 · plan · tasks T001–T012 + the fold ledger · truth-delta) · **ADR 0050** (`docs/decisions/0050-bounded-provider-transport-retry.md` + index) · `specs/truth/techstack.md` *Provider gateway port* MODIFY + *Provider output-cap truncation guard* MODIFY (reconciles the now-false "no retry layer" claim) + *Provider request retry (transient transport)* **ADD** · `specs/truth/features/cli/chat/reporting-a-failed-provider-request.feature` (a new Rule, 4 Examples) · `chat/dsl.md` (+4 Given / +4 Then rows + the round-030 and round-004 note reconciliations) · `internal/domain/llm/gateway.go` · `internal/infrastructure/llm/{openai,gemini}/client.go` · `internal/cli/retry_gateway.go` + `internal/cli/cli.go` · `internal/domain/llm/retryable_test.go` · `internal/cli/retry_gateway_test.go` · `tests/e2e/fakeprovider/fakeprovider.go` · `tests/e2e/steps/step_r078_retry_{givens,thens}.go`. `docs/domain-model/**` **unchanged** (not modelled — ADR 0041 escape hatch; `plan.md` §5).
- **Verification**: `gofmt`/`goimports`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **293 scenarios**) · `make check` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean · cross-compile 4/4) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (52 features · 6 feature modules · 17 root + 424 module rows · 2154 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): change the retry-line text ⇒ the E2E `tellme announces …` reddens; persist `calls = len+1` ⇒ the E2E `the turn was recorded as a single provider call` reddens (`persisted calls = 2, want 1`); drop the `YieldIndicator()` call ⇒ the unit `…YieldsAndRestoresAroundTheLine` reddens; remove the pre-notify `ctx.Err()` check ⇒ the unit `…NoAnnounceWhenCancelledMidCall` reddens.
- **Review chain (PR [#158](https://github.com/gosharplite/tellme/pull/158), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; the design was right, the folds were **witness / record quality**: **F-1** the `tellme: ` prefix was quoted on three record surfaces though the shipped line must not carry it · **F-2** the accounting MUST (one `calls`/usage/frame) had no witness · **F-3** the notifier's yield/restore branch was dead (unit passed `nil`; no 078 E2E terminal) · **F-4** the retry line had no E2E carrier; + TD-078-1…3, RF-078-1…4, N-078-1…4) → fold `9ac7e6a` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; R-1…R-3) → residual fold `657ae2a` → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; no residual).
- **Delivered**: PR [#158](https://github.com/gosharplite/tellme/pull/158) **human-merged** into `dev` (`c291e1d`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-078`**. **Operator request — no anchor issue; the tracker stays 0 open.**

### Environment note (round-078)

- **Round-078 (session 64)** — branch `078-provider-transport-retry` off `dev` `dd9b94b`: specify `489c4b7` -> acceptance `b098058` -> research + **ADR 0050** + truth `77b835b` -> implementation `d61935e` -> PR [#158](https://github.com/gosharplite/tellme/pull/158) -> fold F-1…F-4 + TD-1…3 + RF/N `9ac7e6a` -> residual fold R-1…R-3 `657ae2a` -> merge `c291e1d`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS**; R-1…R-3) -> residual fold -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`c291e1d`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-078`. Host: **darwin/arm64** (Go 1.26.6). Gates at closeout: `gofmt`/`goimports`/`go vet`/`go build` clean - `go test -count=1 ./...` green (24 pkgs; E2E **293 scenarios**) - `make check` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (52 features - 424 module rows - 2154 steps) - `go.mod`/`go.sum` unchanged.
