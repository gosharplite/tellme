# tellme — Status archive — 2026-09-25

> **Back-link**: [`STATUS.md`](../../../STATUS.md) (the single live-state source). This file holds the **2026-09-25 closeout's relocated history** (Rule 12 — never deleted).

---

## Relocated from `STATUS.md` (round-087 closeout)

*(moved **verbatim** — the round-086 delivered-round detail + its environment note.)*

## Last delivered round - 086 `086-tools-listing-line` (DELIVERED / FROZEN - PR [#179](https://github.com/gosharplite/tellme/pull/179) merged into `dev` `77c21c5`, **merge commit**; **ADR 0057**)

- **Theme (operator request, 2026-09-24)**: the `-l`/`--list` listing gains a per-turn **tool-activity line** `[TOOLS] - M (N calls)` — `M` the turn's backward turn index (round 082), `N = len(history.Entry.Steps)` (the **tool** calls, **not** `Entry.Calls`) — printed **between** the turn's `[USER]` block and its `[MODEL]` header as its own block (one blank line each side), **yellow** (whole label) on a terminal `stdout` with `-r` off, and printed for **every** turn incl. `(0 calls)`. It is a **rider** (`-l N` still selects the last N **messages**), surfaces **only the count** (never tool content), and rides the turn's `[MODEL]` message (a partial listing keeps it). Supersedes the round-073 *"tool activity must not be surfaced at all"* reading in substance (the content rule stands).
- **The change**: `internal/domain/render/ports.go` (`ListingMessage.ToolCount` — the `TurnIndex` precedent) · `internal/ui/listing.go` (emit the line before the `[MODEL]` header; `toolLine`) · `internal/ui/colour.go` (the yellow reuse note) · `internal/cli/cli.go` (`listingMessages` sets `ToolCount: len(e.Steps)`).
- **Truth**: `specs/truth/features/cli/history/dsl.md` (a `## Then (round 086)` block + the prologue note; the `tellme lists only the operator's messages` row reframed to `tellme lists the tools' activity but not their contents`; the `the listing carries no accents` row now covers yellow) · `inspecting-the-session-history.feature` (3 new Rules / 7 Examples; the tool-activity Rule reframed) · `specs/truth/techstack.md` (*Output rendering* + *Session lifecycle flags* reconciled in place — the two stale "tool activity omitted" clauses now read *count surfaced, content not*). **Records**: **ADR 0057** (+ index row; amends ADR 0045) · `docs/domain-model/tellme.modelith.{yaml,md}` (the listing colour invariant).
- **The witness**: `make check` **OK** · `go test -count=1 ./...` **green** (E2E **320 scenarios · 2394 steps**) · `make test-race` **no data races** · `gofmt`/`goimports` clean · `modelith-check` no drift · ADR index consistent · topology audit **PASSED** (53 features · 6 modules · 21 root + 459 module rows · 2368 steps) · `go.mod`/`go.sum` **unchanged**. Four mutation witnesses reproduced + reverted: reporting `Calls` ⇒ the CLI pin + E2E red; un-gating the yellow ⇒ the unit pins + the E2E no-accents Then red; emitting after the model body ⇒ the E2E tool-activity Then red; emitting the line for the `[USER]` message instead of the `[MODEL]` one (a literal guard swap) ⇒ **4** E2E scenarios red + the unit byte pins.
- **Review chain (PR [#179](https://github.com/gosharplite/tellme/pull/179), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-086-1** the `techstack.md` *Session lifecycle flags* cell left self-contradictory · **F-086-2** FR-005 (`-r`) had **no discriminating carrier** for the tool line · TD-086-1 · N-086-1 · N-086-2) → fold `88ed8cf` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — RES-086-1) → fold `bf440f7` → `FOLD-VERIFICATION` (**WITH RESIDUALS** — RES-086-2: the fold commit was **local-only**, the repo had drifted to a detached HEAD) → re-push `bf440f7` → `FOLD-VERIFICATION` (**WITH RESIDUALS** — RES-086-3: day-summary figure drift) → fold `0a4e7c7` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`**.
- **Delivered**: PR [#179](https://github.com/gosharplite/tellme/pull/179) **human-merged** into `dev` (**merge commit**, `77c21c5`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-086`**. No anchor issue (an operator request); the tracker is **0 open**.

- **Round-086 (session 74, 2026-09-24)** — branch `086-tools-listing-line` off `dev` `6f0004f`: specify + plan package + implementation (the `[TOOLS]` line + truth + records + witnesses) `e086690` -> PR [#179](https://github.com/gosharplite/tellme/pull/179) + name `f6755f6` -> fold F-086-1/2 + TD-086-1 + N-086-1/2 `88ed8cf` -> RES-086-1 `bf440f7` -> RES-086-2 (re-push) -> RES-086-3 `0a4e7c7` -> loop CLOSED `9df620a` -> merge `77c21c5`. **Presentation-only** on the offline `-l` path: `internal/domain/render/ports.go` (`ListingMessage.ToolCount`) + `internal/ui/listing.go` + `internal/ui/colour.go` + `internal/cli/cli.go`; truth `history/{dsl.md,inspecting-the-session-history.feature}` + `techstack.md` (2 rows reconciled) + **ADR 0057** (+ index; amends ADR 0045) + `docs/domain-model/tellme.modelith.{yaml,md}`; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> 4 fold-verification passes -> final **FOLDS VERIFIED — LOOP CLOSED (no residuals)**. Host: **linux/amd64** (Go 1.26.6). **PR [#179](https://github.com/gosharplite/tellme/pull/179) human-merged** into `dev` (**merge commit**, `77c21c5`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-086`**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **320 scenarios · 2394 steps**) · `make test-race` no data races.

---

## Relocated from `STATUS.md` (round-088 closeout)

*(moved **verbatim** — the round-087 delivered-round detail + its environment note.)*

## Last delivered round - 087 `087-mcp-tool-cache` (DELIVERED / FROZEN - PR [#181](https://github.com/gosharplite/tellme/pull/181) merged into `dev` `c6ccb14`, **merge commit**; **ADR 0058**)

- **Theme (anchor issue [#180](https://github.com/gosharplite/tellme/issues/180), 2026-09-24)**: the prompt prelude's remote-MCP tool discovery gains a **cross-invocation cache**, so the steady-state prelude makes **no MCP network call**. Round 032 dials **every enabled remote MCP server** (`initialize` + `tools/list`) before the first provider request — bounded (≤ 3 s) + warn+skip, so it never stalls, but **unconditional** on every prompt-bearing run. The remedy was the recorded forward item (`techstack.md` → *Not Introduced Yet → Cross-invocation MCP tool caching*, round-032 Q2 Option 2). **Option A** chosen; **B** (static config declarations), **C** (per-server proxy tool) and **D** (remove discovery) recorded as deliberate rejections.
- **The change**: `internal/domain/tools/toolcache.go` (`MCPToolCache` + `MCPToolCacheEntry`) · `internal/infrastructure/mcp/toolcache.go` (the file store; a same-directory temp file + `fsync` + atomic `rename` through the `cacheFS` seam) · `internal/infrastructure/mcp/lazyclient.go` (a cached tool binds a client that connects — and resolves its credential — on the first `CallTool`) · `internal/infrastructure/mcp/discovery.go` (`DiscoverCached`: fresh ⇒ serve **no dial**; stale ⇒ serve **no pre-request dial** + post-answer refresh; cold/mismatched/corrupt ⇒ the existing bounded concurrent discovery + a write) · `deps.Discovery.Refresh` + `MCPDiscoverer(ctx, home, servers)`; the CLI post-answer refresh hook; `cmd/tellme` (`mcpToolCacheTTL = 24h`). **Declaration-keyed** (`url` + effective `auth`); the **token is never stored** (round-032 FR-017); the cache lives at the `$TELL_ME_HOME` root (round 087) — **superseded by round 088 / ADR 0059 → `output/<mode>/`**; not cleared by `--new`.
- **Truth**: `specs/truth/techstack.md` (*MCP tool discovery* row + a new *MCP tool cache* row + the *MCP credential resolution* row; the round-032 *Cross-invocation MCP tool caching* forward item **retired**) · `specs/truth/data/data-model.dbml` (`mcp_tool_cache_entry`) · `specs/truth/features/cli/chat/using-tools-from-a-remote-mcp-server.feature` (+4 Rules / 8 Examples) + `chat/dsl.md` (+ `## Given/Then (round 087)` rows). **Records**: **ADR 0058** (+ index row) · `docs/domain-model/**` **NOT** modelled (an internal optimization; ADR 0041 same-PR rule does not engage — `plan.md` §5).
- **The witness**: `make check` **OK** · `go test -count=1 ./...` **green** (E2E **328 scenarios · 2471 steps**) · `make test-race` **no data races** · `gofmt`/`goimports` clean · `modelith-check` no drift · ADR index consistent · topology audit **PASSED** (53 features · 6 modules · 21 root + 464 module rows · 2445 steps) · `go.mod`/`go.sum` **unchanged**. Falsifiability witnesses reproduced + reverted: **W1** every key cold ⇒ **3** unit pins + **4** E2E Examples red; **W2** stale-as-cold ⇒ 2 unit pins + 1 Example; **W3** skip the cache write ⇒ 2 unit pins + 1 Example; **W5** return the lazy connect error ⇒ the unit pin red; **F-087-1** a non-delegating lazy client ⇒ the unit pin + the live-server Example red; **F-087-4** an in-place `Save` ⇒ the `cacheFS` seam pins red; **trust boundary** an un-validated cached name/schema ⇒ `TestDiscoverCached_CachedNameAndSchemaReValidated` red.
- **Review chain (PR [#181](https://github.com/gosharplite/tellme/pull/181), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-087-1** the lazy client's successful delegation had no carrier · **F-087-2** the recorded W1 figures did not reproduce · **F-087-3** FR-006 had no carrier and named the wrong mechanism · **F-087-4** the temp+`fsync`+`rename` write was undistinguished from an in-place write · **F-087-5** checklist carriers over-claimed · **F-087-6** the credential deferral un-reconciled + the cached path dropped `CredentialWarning` · TD-087-1 · N-087-1…6) → fold `d6def99` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — RES-087-FV-1…6) → residual fold `4d32d2c` → `FOLD-VERIFICATION` (**WITH RESIDUALS** — RES-087-FV-7) → fold `0844540` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`** (`be3199b`).
- **Delivered**: PR [#181](https://github.com/gosharplite/tellme/pull/181) **human-merged** into `dev` (**merge commit**, `c6ccb14`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-087`**. **Closes [#180](https://github.com/gosharplite/tellme/issues/180)**; the tracker is **0 open**.

- **Round-087 (session 75, 2026-09-25)** — branch `087-mcp-tool-cache` off `dev` `2162729`: full pipeline + plan package + implementation `a1154c0` -> docs `0986c8e` -> fold F-087-1…6 + TD-087-1 + N-087-1…6 `d6def99` -> docs `c92cb45` -> residual fold RES-087-FV-1…6 `4d32d2c` -> RES-087-FV-7 + nits `0844540` -> loop CLOSED `be3199b` -> merge `c6ccb14`. **Internal optimization of the MCP discovery prelude**: a cross-invocation cache at `$TELL_ME_HOME/mcp-toolcache.json`, declaration-keyed (never the token), TTL 24 h, served with **zero** MCP connections in the steady state (a lazy client defers the connect + credential resolution to the first call), refreshed post-answer (best-effort; a failed refresh keeps the prior entry), a cold key degrading to the existing bounded concurrent discovery + an atomic write. Offered set/order **identical** to live discovery; naming/envelope/schema/call-time-error contracts unchanged; offline paths stay network-free; stdlib-only. Code: `internal/domain/tools/toolcache.go` + `internal/infrastructure/mcp/{toolcache,lazyclient,discovery}.go` + `deps.Discovery.Refresh` + the CLI hook + `cmd/tellme`; truth `techstack.md` + `data-model.dbml` + the `chat` feature + `dsl.md`; **ADR 0058**; `docs/domain-model/**` **not** modelled; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold-verification (**WITH RESIDUALS**) -> residual fold + verification (**WITH RESIDUALS**, RES-087-FV-7) -> final **FOLDS VERIFIED — LOOP CLOSED (no residuals)**. Host: **linux/amd64** (Go 1.26.6). **PR [#181](https://github.com/gosharplite/tellme/pull/181) human-merged** into `dev` (**merge commit**, `c6ccb14`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-087`**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **328 scenarios · 2471 steps**) · `make test-race` no data races.


---

## Relocated from `STATUS.md` (round-089 closeout)

*(moved **verbatim** — the round-088 delivered-round detail + its environment note.)*

## Last delivered round - 088 `088-mcp-cache-header-routing-and-mode-location` (DELIVERED / FROZEN - PR [#183](https://github.com/gosharplite/tellme/pull/183) merged into `dev` `033f364`, **fast-forward**; **ADR 0059**)

- **Theme (anchor issue [#182](https://github.com/gosharplite/tellme/issues/182), 2026-09-25)**: repair the two defects round 087 shipped with the cross-invocation MCP tool cache. **(A, severe)** a **warm-cached MCP tool could not call any server that routes an argument as an HTTP header** — the MCP Go SDK resolves a tool's `x-mcp-header` (SEP-2243) annotations from its **`tools/list` cache**, and round 087's lazy client connected **without** issuing `tools/list`, so the SDK never emitted `Mcp-Param-*` and the whole **GitHub MCP server** 400'd (`missing Mcp-Param-owner header`); **(B)** the cache was written to the `$TELL_ME_HOME` **root** instead of the per-mode workspace. Both shipped because of a **witness gap** (the hermetic fake MCP server did not require a header-routed argument).
- **The change**: `internal/infrastructure/mcp/lazyclient.go` — `warmToolsList` issues **one `tools/list`** (memoized, best-effort, bounded by the call timeout) after `connect` and before delegating, so the SDK has the declarations and emits `Mcp-Param-*`; a cache hit that runs **no** tool still dials nothing. `internal/infrastructure/mcp/toolcache.go` + `internal/app/deps/deps.go` + `internal/cli/cli.go` + `cmd/tellme/deps.go` — the cache moves to **`$TELL_ME_HOME/output/<mode>/mcp-toolcache.json`** (the `MCPDiscoverer` seam takes `res.Workspace`, not `res.Home`); `--new` still preserves it. `internal/infrastructure/mcp/mcptest/server.go` — `Options.HeaderRouted` advertises a tool whose named argument carries `x-mcp-header`, so the SDK **server** requires the header (reproducing the real failure hermetically); `lazyclient_warm_test.go` — the warm-once / best-effort / no-warm-when-unused / workspace-path pins.
- **Truth**: `specs/truth/techstack.md` (*MCP tool discovery* + *MCP tool cache* rows) · `specs/truth/data/data-model.dbml` (the cache entry's per-mode location) · `specs/truth/features/cli/chat/using-tools-from-a-remote-mcp-server.feature` (+2 Rules / 2 Examples) + `chat/dsl.md` (+ `## Given/Then (round 088)` rows). **Records**: **ADR 0059** (+ index row; **supersedes ADR 0058 D2**, qualifies its D4) · `docs/domain-model/**` **NOT** modelled (an internal optimization; ADR 0041 same-PR rule does not engage — `plan.md` §5).
- **The witness**: `make verify` **OK** · `go test -count=1 ./...` **green** (E2E **330 scenarios · 2487 steps**) · `make test-race` **no data races** · `gofmt`/`goimports` clean · `modelith-check` no drift · ADR index consistent · topology audit **PASSED** (53 features · 6 modules · 21 root + 467 module rows · 2461 steps) · `go.mod`/`go.sum` **unchanged**. Falsifiability witnesses reproduced + reverted: remove the `tools/list` warm-up ⇒ the unit pin `lists=0 calls=1` **and** the header-routed E2E Example red (`did not record a call to "create_issue"`); root the cache one level up (`filepath.Dir(workspace)` = `<home>/output`) ⇒ **6** E2E scenarios red at `the cache must live in the session workspace … no such file`; an eager **assembly-time** warm ⇒ **2** zero-contact Examples red (the mutation that violates FR-002); a no-servers write ⇒ `TestDiscoverCached_NoServersIsInert` reports `saves != 0`.
- **Review chain (PR [#183](https://github.com/gosharplite/tellme/pull/183), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-088-1** `techstack.md` self-contradiction (*shared across modes*) + a stale path · **F-088-2** the round-087 `chat/dsl.md` rows documented the old path · **F-088-3** EC-003's carrier over-claimed (no write half) + a stale `EC-004` label · **F-088-4** the rejected alternative mis-stated (`connect` is itself lazy) · **F-088-5** superseded home-root claims still present-tense · TD-088-1 · N-088-1…4) → fold `81d83f6` → `FOLD-VERIFICATION` (**`FOLDS VERIFIED — LOOP CLOSED`**, nit residual RES-088-FV-1) → `b9d63f3`.
- **Delivered**: PR [#183](https://github.com/gosharplite/tellme/pull/183) **human-merged** into `dev` (**fast-forward**, `033f364`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-088`**. **Closes [#182](https://github.com/gosharplite/tellme/issues/182)** (at that closeout the tracker was **0 open**; round **089** later opened [#184](https://github.com/gosharplite/tellme/issues/184)).


- **Round-088 (session 75, 2026-09-25)** — branch `088-mcp-cache-header-routing-and-mode-location` off `dev` `13d5e08`: full pipeline + plan package + implementation `e452b33` -> docs `10a7950` -> fold F-088-1…5 + TD-088-1 + N-088-1…4 `81d83f6` -> RES-088-FV-1 `b9d63f3` -> day-log `033f364` -> merge (fast-forward) `033f364`. **Repairs round 087's cache**: (A) the lazy client now warms `tools/list` on the first call so a warm-cached tool again routes `x-mcp-header` args as `Mcp-Param-*` (the GitHub MCP server works again); (B) the cache moves from the `$TELL_ME_HOME` root to the per-mode workspace `output/<mode>/mcp-toolcache.json`; the fake MCP server gains a **header-routed** tool so the regression reddens. Code: `internal/infrastructure/mcp/{lazyclient,toolcache}.go` + the `MCPDiscoverer` seam (`res.Workspace`) + `cmd/tellme` + `mcptest`; truth `techstack.md` + `data-model.dbml` + the `chat` feature + `dsl.md`; **ADR 0059** (+ index; supersedes ADR 0058 D2, qualifies its D4); `docs/domain-model/**` **not** modelled; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold -> **`FOLDS VERIFIED — LOOP CLOSED`** (nit residual). Host: **linux/amd64** (Go 1.26.6). **PR [#183](https://github.com/gosharplite/tellme/pull/183) human-merged** into `dev` (**fast-forward**, `033f364`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-088`**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **330 scenarios · 2487 steps**) · `make test-race` no data races.


---

## Relocated from `STATUS.md` (round-090 closeout)

*(moved **verbatim** — the round-089 delivered-round detail + its environment note.)*

## Last delivered round - 089 `089-context-control-position` (DELIVERED / FROZEN - PR [#185](https://github.com/gosharplite/tellme/pull/185) merged into `dev` `761733a`, **fast-forward**; **ADR 0060**)

- **Theme (anchor issue [#184](https://github.com/gosharplite/tellme/issues/184), 2026-09-25)**: **truth/record-only**. **(a)** reconcile a **superseded present-tense claim** in `specs/truth/techstack.md` — the *History summarisation* bullet still listed the undo flag **`-b`** as an out-of-scope exclusion though round 081 shipped it (the round-088 **F-088-1** self-contradiction class, on a *scope* clause); **(b)** record tellme's **context-control position** as a durable decision.
- **The change (docs/truth/record only)**: `specs/truth/techstack.md` (*History summarisation* bullet) — **`-b`/`--back`** reads as **delivered** (round 081 / ADR 0053; forward pointer to the *Session lifecycle flags* row), **`--retry`** stays a **genuine non-introduction** (roll back the last user message + resend; the reference ships it, tellme does not), and pinning / token-budget pruning / `SafePath`-consent stay settled exclusions; **ADR 0060** (`docs/decisions/0060-context-control-position.md`) — tellme does **not** port `summarize_history`; context control is the **operator-manual** `-l` (inspect) + `-b` (roll back) pair; the ADR records the **non-equivalence** (compression vs coarse whole-turn trim), **cites** the existing E2E carrier (the DSL row `the request offered exactly the agent tools`' negative clause), and states a **revisit trigger**. **Records**: ADR 0060 + its index row + a back-pointer on the ADR 0053 index row.
- **Truth/records**: `specs/truth/techstack.md` (the clause reconciliation) · **ADR 0060** (+ `docs/decisions/README.md`) · `docs/domain-model/**` **NOT** modelled (ADR 0041 escape hatch — the model already records `-b`/`--back` and has no summarisation/pruning entity; `plan.md` §5) · no product code, no `.feature`, no `go.mod`/`go.sum` change.
- **The witness**: `make check` **OK** · E2E **330 scenarios · 2487 steps** (unchanged) · `make test-race` **no data races** · `verify-adr-index` consistent (ADR 0060 indexed once / unique) · `modelith-check` no drift · `go.mod`/`go.sum` **unchanged**. **W1** is a **carried, manual inspection** of `techstack.md:173` (no mechanical carrier — a docs-prose status claim has no `make verify` member; `grep -- '-b'` is non-discriminating — recorded RF-089-6); **W2** the `verify-adr-index` gate; **W3 not used** (no unfalsifiable equivalence claim).
- **Review chain (PR [#185](https://github.com/gosharplite/tellme/pull/185), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-089-1** the recorded W1 carrier was not discriminating · **F-089-2** FR-005/FR-002 had no ledger row · **F-089-3** the "already carried" half cited as a `Rule` (it is a DSL-row negative clause) · N-089-1…4 · TD-089-1) → fold `02005ee` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — RES-089-FV-1/2) → residual fold `35faa95` → **final `FOLDS VERIFIED — LOOP CLOSED (no residuals)`**.
- **Delivered**: PR [#185](https://github.com/gosharplite/tellme/pull/185) **human-merged** into `dev` (**fast-forward**, `761733a`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-089`**. **Closes [#184](https://github.com/gosharplite/tellme/issues/184)**; the tracker is **0 open**.

- **Round-089 (session 76, 2026-09-25)** — branch `089-context-control-position` off `dev` `53753a0`: specify + plan package + ADR/truth `c1108c6` -> PR [#185](https://github.com/gosharplite/tellme/pull/185) `0177611` -> fold F-089-1/2/3 + N-089-1..4 + TD-089-1 `02005ee` -> residual fold RES-089-FV-1/2 `35faa95` -> loop CLOSED `761733a` -> merge (fast-forward) `761733a`. **Truth/record-only**: `specs/truth/techstack.md` (*History summarisation* bullet reconciled — `-b` **delivered**, `--retry` a **genuine non-introduction**) + **ADR 0060** (no `summarize_history` port; operator-manual `-l`/`-b`; the non-equivalence + a revisit trigger) + the ADR index row + a 0053 back-pointer; `docs/domain-model/**` **not** modelled; no product code; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold -> **`FOLDS VERIFIED — LOOP CLOSED`** (via WITH RESIDUALS). Host: **linux/amd64** (Go 1.26.6). **PR [#185](https://github.com/gosharplite/tellme/pull/185) human-merged** into `dev` (**fast-forward**, `761733a`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-089`**; **closes [#184](https://github.com/gosharplite/tellme/issues/184)**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **330 scenarios · 2487 steps**) · `make test-race` no data races.


---

## Relocated from `STATUS.md` (round-091 closeout)

*(moved **verbatim** — the round-090 delivered-round detail + its environment note.)*

## Last delivered round - 090 `090-record-hygiene-offered-set-and-direction` (DELIVERED / FROZEN - PR [#187](https://github.com/gosharplite/tellme/pull/187) merged into `dev` `4017ad7`, **fast-forward**; **ADR 0061**)

- **Theme (anchor issue [#186](https://github.com/gosharplite/tellme/issues/186), 2026-09-25)**: four coupled **record-hygiene** fixes — reconcile the stale **"seven agent tools"** truth row (omitting `search_files`) **and** give the offered-set claim a **single-source carrier** so it is checked, not hand-copied; fix the `README.md` tool-surface enumeration; **relocate the operator-declared direction into an ADR** (README demoted to a pointer); correct the `cobra` note that called `retry` a subcommand.
- **The change (truth/record + a test-only carrier)**: `specs/truth/features/cli/chat/dsl.md` (the offered-set row: seven → **eight**, adds `search_files`; the hand-count dropped; bound to the live registry) · **the carrier** `cmd/tellme/deps_offered_set_test.go` (parses the row's `集合` cell; asserts set-equality with `agentTools()`) · the step **comment** fix · `README.md` (surface enumeration + direction demoted to an ADR pointer) · **ADR 0061** (`docs/decisions/0061-operator-declared-direction.md` + index) · `STATUS.md` direction line · `specs/truth/techstack.md:155` (`cobra` note).
- **Truth/records**: `specs/truth/features/cli/chat/dsl.md` (owner `/axb-dsl-refine`) · `specs/truth/techstack.md` (owner `/axb-technical-research`) · **ADR 0061** (+ `docs/decisions/README.md`) · `docs/domain-model/**` **NOT** modelled (ADR 0041 escape hatch) · no product code, no `.feature` step text, no `go.mod`/`go.sum` change.
- **The witness**: `make check` **OK** · E2E **330 scenarios · 2487 steps** (unchanged) · `make test-race` **no data races** · `verify-adr-index` consistent (ADR 0061 once/unique) · `modelith-check` no drift · `go.mod`/`go.sum` **unchanged**. **W-A (real carrier)** reddens under **both** mutations — doc edit ⇒ `doc (7) vs live (8)`; remove `NewSearchTool()` from `agentTools()` ⇒ `doc (8) vs live (7)`; non-vacuous; parse target = the `集合` cell. **W-C** `verify-adr-index`; **W-B/W-D** manual (README enumeration; the `cobra` note).
- **Review chain (PR [#187](https://github.com/gosharplite/tellme/pull/187), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-090-1** ADR §Consequences over-claimed + contradicted its own §Forward RF-061-3 · **F-090-2** the row kept a second, unchecked eight-tool copy in its `必查` cell · N-090-1…6 · TD-090-1 · R-090-1) → fold `9c8b623` → `FOLD-VERIFICATION` (**FOLDS VERIFIED WITH RESIDUALS** — RES-090-FV-1/2) → residual fold `17f0eee` → **final `FOLDS VERIFIED — LOOP CLOSED`** → prose-only tidy `d6a8a72` (N-090-FV-3).
- **Delivered**: PR [#187](https://github.com/gosharplite/tellme/pull/187) **human-merged** into `dev` (**fast-forward**, `4017ad7`); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged **`round-090`**. **Closes [#186](https://github.com/gosharplite/tellme/issues/186)**; the tracker is **0 open**.

- **Round-090 (session 77, 2026-09-25)** — branch `090-record-hygiene-offered-set-and-direction` off `dev` `72bb592`: specify + plan package + truth/records/carrier `b76bc54` -> PR [#187](https://github.com/gosharplite/tellme/pull/187) `3366ac9` -> fold F-090-1/2 + N-090-1/2/4 `9c8b623` -> residual fold RES-090-FV-1/2 `17f0eee` -> loop CLOSED + prose tidy (N-090-FV-3) `d6a8a72` -> day-log `4017ad7` -> merge (fast-forward) `4017ad7`. **Record hygiene + a test-only carrier**: (A) `chat/dsl.md`'s offered-set row → **eight** (adds `search_files`), bound to the live registry via the carrier `cmd/tellme/deps_offered_set_test.go` (reddens under both mutations); (B) the README tool-surface enumeration; (C) **ADR 0061** (the operator-declared direction as its authoritative home; README/STATUS demoted to pointers); (D) the `cobra` note (`retry` is a flag). `docs/domain-model/**` **not** modelled; no product code; **no `go.mod`/`go.sum` change**. Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations): `review` (**APPROVE WITH REQUIRED FOLDS**) -> fold -> **`FOLDS VERIFIED — LOOP CLOSED`** (via WITH RESIDUALS). Host: **linux/amd64** (Go 1.26.6). **PR [#187](https://github.com/gosharplite/tellme/pull/187) human-merged** into `dev` (**fast-forward**, `4017ad7`); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged **`round-090`**; **closes [#186](https://github.com/gosharplite/tellme/issues/186)**. Gates at closeout: `make check` **OK** · `go test -count=1 ./...` green (E2E **330 scenarios · 2487 steps**) · `make test-race` no data races.
