# tellme — Status Archive (2026-09-17)

> Back-link: the live state is [`STATUS.md`](../../../STATUS.md). This archive holds the **round-035,
> round-036, round-037, round-038, round-039 and round-040** detail, relocated **verbatim** on 2026-09-17 (Rule 12: 035 when round 036
> became the most recent delivered round; 036 when round 037 opened; 037 when round 038 became current; 038 when
> round 039 became current; 039 when round 040 became current; 040 when round 041 opened).
> History is relocated, never deleted.
> Older archives: [`2026-09-11.md`](2026-09-11.md) (rounds 001–002 + grill/clarify history) · [`2026-09-13.md`](2026-09-13.md) (rounds 003–012) · [`2026-09-14.md`](2026-09-14.md) (rounds 013–019) · [`2026-09-15.md`](2026-09-15.md) (rounds 020–026) · [`2026-09-16.md`](2026-09-16.md) (rounds 027–034).

## Current round — 035 `035-spinner-tail-residue` (DELIVERED / FROZEN)

**035-spinner-tail-residue** — the per-call tail is written with the spinner **phase-boundary yielded** (a synchronous clear before the tail, no resume; the next waiting phase re-activates). Pure display fix; `Spinner.OnCallEnd` stays a no-op (ADR 0005 D1 untouched); `callRenderer` / `internal/ui` unchanged; the final deferred tail is byte-identical. Truth: `techstack.md` spinner row + a new `presenting-the-progress-spinner` Rule reusing existing rows (`dsl.md` note; **no new `DSLRow`**). Witness: E2E (gate + tool round + whole-stream residue row, previously unpaired) + a unit yield-ordering pin. Merged via PR [#73](https://github.com/gosharplite/tellme/pull/73) (`18cd947`); frozen head `11cc9a3`.

## Current round — 036 `036-tool-reason-sanitize` (DELIVERED / FROZEN)

**036-tool-reason-sanitize** — the `stderr` tool log's model-authored `[Tool Reason]` value is now **sanitized and capped like its siblings**: `FormatToolReason` = `capRunes(oneLine(strings.TrimSpace(reason)), reasonValueCap)` with `reasonValueCap = 200` (one U+2026 inside the cap, rune-boundary cut), and the **three** blank-reason suppression sites emit **no** reason line for an empty/whitespace-only reason (the loop's `logAction` + `reasonsOf` = the production filters; a defensive guard in the `callRenderer.OnCallEnd` `emit` closure). Single-site fold in the pure formatter; `oneLine` relocated out of the retired orphan `internal/ui/toollog.go`. Restores round-022 **B1**'s fold guarantee (dropped **unrecorded** by round 034); recorded in the new **ADR 0006** (ADR 0005 stays immutable). Truth: `techstack.md` Agent-tool-loop row + `chat/dsl.md` reason row (explicit single-line guarantee + the `reasonValueCap` constant) + a round-036 note; **no new `DSLRow`/Example** (`/axb-spec-by-example` NOOP). Witness: **hostile-fixture unit pins** (`\n`, `\r`, trim, combined, 201-rune cap, mid-rune boundary) + loop/tail blank-reason pins — a deliberate **permanent narrowing** (the E2E surface is structurally blind to this class). Merged via PR [#75](https://github.com/gosharplite/tellme/pull/75) (`ddd6f7d`); frozen head `11356db` (12 commits).

## Current round — 037 `037-interactive-prompt-no-selection` (DELIVERED / FROZEN)

**037-interactive-prompt-no-selection** — the `-i` prompt's suggestion list no longer pre-selects a suggestion: the selection starts at a **no-choice** sentinel (`cursor == noChoice == -1`) and **resets to no-choice on every refresh** (`set(items)` → `cursor = noChoice`, mirroring the reference's `Update(msg, -1)`); the first `Tab` still selects the **first** suggestion (the reference's own `(cursor+delta+len)%len` — `-1 → 0`, and `Shift+Tab` from no-choice → `len-2` for `len ≥ 2` / the sole item for `len == 1`). `selected()`/`cycle()`/`view()` are unchanged; the `> ` glyph, styling, ordering/sources, editor, placeholder, debounce, no-metrics-header and the round-023 teardown are unchanged. **Supersedes** round 016's at-rest "first item is the current choice" (which followed the reference's mockup; the reference's code never pre-selects). Witness = updated **unit pins** (`model_chrome_test.go` incl. the table-driven `TestCycleArithmetic` + `TestModelUnselectedQuoteTextIsNotACursorRow`; `refresh_test.go`) + the flipped **E2E** `Then` asserting zero cursor rows in the rendered **suggestion block** via the exact `"  > "` cursor-row predicate (pinned at two layers: `tests/e2e/steps/tui_chrome_test.go` + the product-rendering pin). Operator Q1–Q4 locked (strict cursor-only scope; exact reference arithmetic; rule restated; witness set). Truth: `techstack.md` + `presenting-the-interactive-prompt.feature` + `chat/dsl.md`; **no new `DSLRow`/Example** (the row is edited in place). Reference parity **verified against `tell-me-go`** by the review. Merged via PR [#77](https://github.com/gosharplite/tellme/pull/77) (`3aacf32`); frozen head `5754cf3` (13 commits).

## Current round — 038 `038-tool-output-sanitize-and-empty-submit` (DELIVERED / FROZEN)

**038-tool-output-sanitize-and-empty-submit** — two folded operator issues on the `stderr`-bound interactive surface. **(a) #78 — the live `[Tool Output]` block leaked terminal state:** the streamed **content** lines are now **sanitized** at the `internal/ui` seam (every **7-bit** ANSI escape sequence — CSI/SGR, OSC, other ESC-introduced — and every stray C0/DEL byte removed; TAB kept; interior CR dropped; bytes ≥ 0x80 untouched; the ESC consumption is ASCII-gated and the scan window bounded per kind — CSI 128 / OSC 1024 — so a long **terminated** sequence is still removed in full and the sanitizer never *introduces* invalid UTF-8), so a colouring/killing command can no longer tint or alter the terminal; the block **always closes neutral** (a default-state restore, `\x1b[0m`, before the closing separator on every close path — normal/start-failure/trim/timeout). Presentation-only (the tool **result** keeps its raw bytes); **recorded divergence** (the reference sanitizes nothing). **(b) #76 — an empty submit quit the `-i` prompt:** `Ctrl+S`/`Alt+Enter` on an empty (or whitespace-only) editor is now a **no-op** (the prompt stays open; reference parity); only a non-empty submit submits, and `Esc`/`Ctrl+C` still aborts. Truth: `techstack.md` (the `execute_command` row + the `-i` row) + `watching-the-tool-loop.feature` (new Rule) + `using-the-interactive-prompt.feature` (new Rule) + `chat/dsl.md` (2 Given + 1 When + 2 Then + round-038 note); `/axb-api-plan` + `/axb-data-plan` NOOP; `/axb-ui-plan` skipped. Witnesses: hostile-fixture unit pins (`tooloutput_sanitize_test.go`, `model_submit_test.go`) + 3 new E2E Examples. Governance: **ADR 0007** (the round's presentation invariant + recorded divergence). `make verify` **OK**; E2E **218/218** (was 215). Merged via PR [#79](https://github.com/gosharplite/tellme/pull/79) (`a0b0653`); frozen head `3da9ed0` (7 commits).


## Current round — 039 `039-terminal-safe-lines-and-turn-spacing` (DELIVERED / FROZEN)

**039-terminal-safe-lines-and-turn-spacing** — two folded workstreams on the `stderr`-bound diagnostic surface. **(a) #80 — generalize the terminal-safe-line policy:** `sanitizeControl` + its helpers (`isControlRune`, `escSequenceLen`, `csiLen`, `oscLen`, `genericEscLen`, the per-kind windows CSI 128 / OSC 1024) **moved verbatim** into a dedicated single-owned `internal/ui/sanitize.go` and are now applied by **every** `[Tool …]` formatter — `FormatToolReason`, `FormatToolResult`, and `FormatToolAction` (argument **keys and values**) are control-free like `[Tool Output]` (byte-identical, round-038 pins intact). Order = **fold + trim → sanitize → rune cap**, so the caps (`{189, 200, 200}`) bound the **visible** output; the block's neutral-close restore stays `[Tool Output]`-scoped; an escape-only reason emits **no** row (`ui.ToolReasonRenders` reads the sanitized value, derived from `toolReasonText`); the `[Tool Action]` key path now folds and is **sorted after sanitizing** (a hostile key cannot render out of ascending order). **(b) Blank-line grouping (operator request):** one blank before **each** call's begin block (the `[Tool Reason]` line, else the `[Tool Action]` line — per call); one blank before the grouped tail `[Tool Reason]` block (no blanks inside it); one blank before the post-status group (measured `Payload:` + metrics + `Ready`) — **on a turn that rendered a tool round only** (a tool-less turn is unchanged; gated by a `renderedToolRound` marker set on a non-final `OnCallEnd`). Both are **recorded divergences** from the reference. Truth: `techstack.md` (Agent tool loop row MODIFY + the `execute_command` sibling-scope note closed) + `watching-the-tool-loop.feature` (2 new Rules) + `presenting-the-post-turn-status.feature` (1 tool-less Example) + `chat/dsl.md` (+1 Given · +8 Then · note); `/axb-api-plan` + `/axb-data-plan` NOOP; `/axb-ui-plan` skipped. Witnesses: hostile-fixture unit pins (`toolcall_sanitize_test.go`, `agentloop_spacing_test.go`, `call_renderer_reason_test.go`) + 8 new E2E Examples. Governance: **ADR 0008** (supersedes **ADR 0007**). `make verify` **OK**; E2E **226/226** (was 218). Merged via PR [#81](https://github.com/gosharplite/tellme/pull/81) (`d48ebbc`); frozen head `ca14276` (10 commits).

## Current round — 040 `040-spinner-liveness-and-turn-timer` (DELIVERED / FROZEN — PR #86 merged into `dev` `87af8c8`; frozen head `436bd70`)

**040-spinner-liveness-and-turn-timer** — two folded spinner workstreams on the round-019/025 **progress spinner** (`internal/ui/spinner.go`, `stderr`-bound). **(a) #82 — liveness during a streaming `[Tool Output]` block:** today the spinner is **paused for the whole block** (round-034 FR-012/G8, ADR 0005 D7), so a quiet/slow command shows no animation; round 040 keeps the block's `stderr` ownership but **resumes the indicator after an idle gap** (no new output line for the threshold; the seam `TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS`, ms, `0` = admit immediately; a **real** idle gap in E2E) and **synchronously clears it before the next output line** (the invariant is **mutual exclusion + join**, not "single-writer") — the block literals, the bound/stop semantics, and the non-TTY/`-r` gates are unchanged. **(b) #83 — a dual elapsed timer:** the elapsed segment becomes `({total}s {call}s)` — the **total** since prompt capture (turn-scoped; **never reset**, round-019 D4 preserved) **plus** the **current model call's** elapsed, **reset at each AI-endpoint call** (stamped in `OnInferenceStart`, fired exactly once per call — round-027 AI-call semantics; an **amendment** to round-019 D4); it appears in the model-wait **and** tool-execution labels (the resource segment is unchanged). Both are **recorded divergences** from the reference (its single figure restarts **per phase**; it does not re-activate a live spinner over streamed output; its figure is **unlabelled** — tellme's two figures are unlabelled too, ADR 0009 (QB4)). Pipeline through `/axb-dsl-refine`: **specify ✅** (`spec.md` + checklist + truth-delta) · **spec-by-example ✅** (`keeping-the-progress-visible.feature` + `timing-the-current-model-call.feature`) · **technical-research ✅** (`research.md` D1–D9) · **system-analysis ✅** (`plan.md`; 1 CLI end → `/axb-dsl-refine`) · **dsl-refine ✅** (`presenting-the-progress-spinner.feature` + `chat/dsl.md`); **`/axb-tasks` DONE** (`tasks.md` T001–T016 + the PR #85 folds); **`/axb-implement` DONE** (T001–T016 green) on `040-implement-v2-spinner-liveness-and-turn-timer` (PR [#86](https://github.com/gosharplite/tellme/pull/86) **merged** → `dev` `87af8c8`). Truth: `techstack.md` (**Turn progress spinner** row MODIFY) + `presenting-the-progress-spinner.feature` (`Rule: The spinner is paused while a command's output streams` **replaced** by `Rule: A quiet command's output still shows the progress spinner`; **new** `Rule: The spinner shows the total time and the current model call's time`; the two tool-phase Examples gain the dual-timer Then) + `chat/dsl.md` (the streaming row modified **in place** → `the run shows the progress spinner again while the command stays quiet`; `## Given (round 040)` + `## Then (round 040)` + a note); `/axb-api-plan` + `/axb-data-plan` **NOOP**; `/axb-ui-plan` skipped. Witness plan: unit pins for the two-figure arithmetic (injected clock) + the idle-gap resume/clear + the row-aware clear for the longer line; E2E for the two-figure **shape** and the in-block liveness frame (the per-call reset and the no-interleave/no-residue facets are unit pins — a fast scripted turn cannot tick whole seconds; `time.Sleep` is forbidden). Governance: **ADR 0009** (**supersedes ADR 0005 D7 only** — D1–D6/D8 stand, so 0005's body is **not** edited and its overall `Status` stays `Accepted`; **amends** round-019 D4). Topology audit **PASSED** (44 features · 6 modules · 16 root + 327 module rows · 1674 steps). **Merged:** plan + truth half via PR [#84](https://github.com/gosharplite/tellme/pull/84) → `dev` (`146210d`, by `thptcnec`, 2026-09-17T09:02:31Z; head `092a89c`; **16 files, +745/−30, 13 commits, zero product code**). **`/axb-tasks` DONE**: `tasks.md` (T001–T016) written on the branch `040-implement-spinner-liveness-and-turn-timer` (off `dev`); Foundational T001–T002 · Phase 3 T003–T008 (T003 `[BDD-REMOVE]` the dead stepdef (**keeping** its `toolOutputBlockIndexes`/`hasSpinnerStatusBetween` helpers for T004); T004–T005 `[BDD-RED]` the 4 sentences; T006–T007 `[UNIT]` dual-timer arithmetic + the race/anti-vacuity/row-aware-clear stress; T008 the review gate + **`Strict: true`** in the same change) · Phase 4 T009–T013 (WS-A coordinator + `admitResume()`; WS-B `FormatSpinnerLine(… total, call, …)` + `callEpoch`; `[CODE-REMOVE]` the retired whole-block pause) · T014–T016 regression/falsifiability/close; Pre-Delivery Orphan Sweep **0**. **`/axb-tasks` review loop CLOSED — architect CERTIFICATION: implementation-ready** (five fold reviews: B1–B4/TD-1…TD-5/N-1…N-3 → R-1…R-11/N-4…N-13 → R-12/R-13/N-14). **`/axb-tasks` half MERGED** via PR [#85](https://github.com/gosharplite/tellme/pull/85) → `dev` (`07c88ea`, **fast-forward**, by `thptcnec`, 2026-09-17T10:50:30Z; head `07c88ea`; 12 commits; **5 docs files, +235/−9, zero product code**). **Implementation half DELIVERED**: `/axb-implement` (T001–T016 all `[x]`) on `040-implement-v2-spinner-liveness-and-turn-timer` (PR [#86](https://github.com/gosharplite/tellme/pull/86) **merged** → `dev` `87af8c8`; head `436bd70`) — the `internal/ui` coordinator + the three lock-scoped entry points; the two spinner Rules; `Strict: true` + the 4 stepdefs; the dead-stepdef `[BDD-REMOVE]`. The branch is `-v2-` because the frozen `/axb-tasks` branch already owned `040-implement-…` (a name collision avoided, not a rewritten record). `dev` is **29** commits ahead of `main` (`git rev-list --count origin/main..origin/dev`; `main` also carries its own `dev → main` merge history — measure, don't derive).


---

## Round-040 review-fold history (relocated verbatim from `STATUS.md`, session 11 closeout)

> Relocated 2026-09-17 when round 041 became the delivered/current round (Rule 12 — keep `STATUS.md` lean). Verbatim; never deleted.

- **PR #84 review folds (architect, APPROVE WITH REQUIRED FOLDS) + fold reviews (FOLDS ACCEPTED; TD-9/TD-10/TD-11; review loop CLOSED)** — folded in-round: **TD-1** `Strict: true` is pinned as an **implementation-half** task directive (**not** landed here) so `dev` never goes red (`make test` = `go test ./...`, which includes `tests/e2e`) · **TD-2** truth states **mutual exclusion + join** (not "single-writer") · **TD-3** the block critical section does not span a frame write · **TD-4** the watcher poll period is pinned (~200 ms) · **TD-5** the idle seam is named/pinned (`TELL_ME_FORCE_TOOLOUTPUT_IDLE_MS`, ms, `0` = admit immediately) and the Given is renamed to a world state · **TD-6** the second figure is renamed **"the current model call's elapsed"** (turn vs model-call terms) · **TD-7** the spinner feature header is refreshed · **TD-8** this recipe + the commit counts · **RF-1** the acceptance-rule→carrier mapping + the task-list directives (incl. the dead-stepdef `[BDD-REMOVE]`) · **RF-2** ADR 0005's **index** row annotated (no body edit) · **RF-3** the no-label resume is a defined no-op · **RF-4** the second-epoch field stays internal (no sixth ctor seam; field later named **`callEpoch`** by the N-1 nit) · **RF-5** #69's body updated · **QB3/QB4** the 3+-digit row-aware-clear re-witness + "unlabelled by design" recorded. **Consequence of TD-1 (corrected per the fold review):** the plan half leaves the harness unchanged, so `make verify` **and** `make test` are **green** on this branch. The `Strict: true` flag + the 4 stepdefs land together in `/axb-implement` (witnessed by the FAIL-then-PASS transition); `dev` is never red. **TD-9 (fold review)** — swept the pre-fold vocabulary from `plan.md`, `truth-delta.md`, `checklists/requirements.md`, `STATUS.md`, and the daily log (`single-writer` → mutual-exclusion+join; "current turn's" → "current model call's"; D1–D8 → D1–D9; `timing-the-current-turn.feature` → `timing-the-current-model-call.feature`; "two divergences" → three). **TD-10 (fold review)** — the resume-only admission path is named (`admitResume()`; `activate()` keeps its synchronous first frame) and the resumed frame renders **immediately on start**; the E2E timing budget is pinned (the child's quiet stretch exceeds `N + 2·P` with margin, e.g. a 2 s child `sleep` at `N=50 ms`) in SC-002 + the `dsl.md` Given row; the `techstack.md` round-019 sentence gains the round-040 carve-out.
- **/axb-tasks review fold (PR [#85](https://github.com/gosharplite/tellme/pull/85))** — plan-side folds in `tasks.md` (no truth/code); **one row per review, naming both the reviewed head and the fold head**:
- **Review #1 (reviewed `7f070d0` → fold `f37a175`) — APPROVE WITH REQUIRED FOLDS (B1–B4 + TD-1…TD-5 + N-1…N-3):** B1 the block-mutex seam named (`ToolOutputWriter` sole lock/state owner; never reach into `w.mu`) · B2 T003 retires only the dead stepdef (helpers kept) · B3 the idle seam in `scenario_context.go`'s `envUnset` · B4 the in-code citation sweep (T009/T010) · TD-1 T014 gate = `go test -count=1 ./...` + `make verify` · TD-2 the pre-existing pins adapted · TD-3 the no-label + gated-off no-ops · TD-4 T013 reworded · TD-5 SC-003 → shape-only + unit pin · N-1…N-3 · the stalled-writer risk as T007(f).
- **Review #2 (reviewed `f37a175` → fold `f9e1ac9`) — FOLDS ACCEPTED (R-1/R-1b/R-2/R-3 + N-4/N-5):** R-1 T004's bound → the closing separator (`closingSeparatorIndex`) · R-1b `End` = stop→clear→separator→resume · R-2 the `WriteWith` line path · R-3 the `\r`-only pin · N-4 the fold note §9→§15 · N-5 STATUS swept.
- **Review #3 (reviewed `f9e1ac9` → fold `771e057`) — FOLDS ACCEPTED (R-4…R-7 + N-6…N-9):** R-4 the watcher admit entry point (`withLock`; R-2 scoped to the line path) · R-5 `\r`-only required · R-6 the back-pressure residual sharpened + T007(f) self-limiting · R-7 the B2 disposition sweep · N-6…N-9.
- **Review #4 (reviewed `771e057` → fold `188d9e6`) — FOLDS ACCEPTED (R-8/R-9 + N-10/N-11):** R-8 `EndWith(beforeSeparator)` + stop-and-join the watcher (+ T007(h)) · R-9 the idle clock into the writer · N-10 T002 reworded · N-11 the B1 annotation.
- **Review #5 (reviewed `188d9e6` → fold `8b71463`) — FOLDS ACCEPTED (R-10/R-11 + N-12/N-13):** R-10 deleted the stale `lastLine`-stamp clause · R-11 the idle query path `withLock(fn func(idle))` + the `Begin`-seeded `lastLine` + T007(b′) · N-12 the annotation restored · N-13 why `Begin` needs no hook.
- **Review #6 (reviewed `8b71463` → fold `9e02442`) — CERTIFICATION: implementation-ready (R-12/R-13 + N-14):** R-12 T002 (ii) carries the `withLock(fn func(idle))` signature · R-13 T007's `(b′)`/`(g)`/`(h)` re-merged inside the checkbox · N-14 T009's parenthetical. Seam model complete — line path · watcher admit · `End` · idle query.
- **Review #7 (reviewed `9e02442` → fold `c9f6ba7`) — CERTIFICATION CONFIRMED (R-14 + B-1/B-2 + N-15):** R-14 the per-call-reset witness re-attributed to the unit layer (T015(b) + `spec.md` SC-005) · B-1 the daily-log fold notes reordered · B-2 the ledger entry · N-15 Core Inputs primed.
- **Review #8 (reviewed `c9f6ba7` → fold `af86f3a`) — CERTIFICATION FINAL / review loop CLOSED (N-16):** N-16 (the reviewer's own mis-phrasing) — T015(a)'s witness-set parenthetical corrected.
- **Review #9 (reviewed `33aca91` → fold `c79eaca` + `07c88ea`) — log hygiene (C-1/C-2/C-3 + the append-anchor process lesson):** C-1 the ledger split into rows #6/#7/#8 with the pointer → `af86f3a` · C-2 `tasks.md`'s second `#7` renumbered `#8` · C-3 the daily-log fold notes reordered + Fold 8 appended · the process lesson recorded (one append anchor per artifact; one row per review). **Review loop CLOSED at `af86f3a`** (certification FINAL); Review #9 folded after the close — log hygiene only.
- **Round-040 `/axb-implement` review fold (PR [#86](https://github.com/gosharplite/tellme/pull/86))** — one row per review, naming both heads (two-SHA where a fold spans a code commit + a ledger commit): **Review #1 (reviewed `1d563be` → fold `c888f0d` + `b1ecc6d`) — APPROVE WITH REQUIRED FOLDS (R-40-1…R-40-3 + N-40-1…N-40-5; all landed) ⇒ re-review `CERTIFIED — READY TO MERGE` (N-40-6/N-40-7 non-blocking):** **R-40-1** `AdmitResume` captures `stopCh`/`doneCh` as locals (no field read after unlock — closes the latent mismatched-channel-pair race) · **R-40-2** the two general spinner rows (`the run shows the progress spinner while it waits` + `the progress spinner shows how long it has waited`) updated **in place** to the dual `({total}s {call}s)` shape + the round-019 note pointer + the two stepdef comments + a `truth-delta.md` MODIFY row · **R-40-3** the `STATUS.md` pre-fold residue swept + the dev-ahead figure corrected to the measured **29** · **N-40-1** `closingSeparatorIndex` bounded to the **first** block (on the next block's **header tail**, since output lines carry the marker; the residual direction is **fail-loud**) · **N-40-2** `newTestCoordinator(w io.Writer, …)`; the duplicate deleted · **N-40-3** the `coordinator.go` `#69` claim corrected (block-scoped yield only) · **N-40-4** the `End`-while-write-stalled residual named in `coordinator.go` · **N-40-5** a mutex-guarded `spinnerRunning()` test accessor · **N-40-6** the two-SHA fold label adopted here · **N-40-7** the advisory `toolOutputHeaderMarker` content-keying recorded (fail-loud, no change).
