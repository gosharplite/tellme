# tellme — Status Archive · 2026-09-21

> **Back-link**: live state is [`STATUS.md`](../../../STATUS.md) — this file holds relocated historical detail only (never a live-state source).

Contents relocated at the **round-073 closeout** (2026-09-21): the **round-072 delivered-round detail** and its **environment note**.
Contents relocated at the **round-074 closeout** (2026-09-21): the **round-073 delivered-round detail** (round 074 is now the current delivered round) and its **environment note**.
Contents relocated at the **round-075 closeout** (2026-09-21): the **round-074 delivered-round detail** (round 075 is now the current delivered round) and its **environment note**.

---

## Last delivered round - 072 `072-gemini-cached-token-usage` (DELIVERED / FROZEN - PR [#150](https://github.com/gosharplite/tellme/pull/150) merged into `dev` `6c3e5fe`, **merge commit**; **ADR 0044**)

- **Theme**: fix a **cost/accounting bug** — the Gemini/Vertex adapter decoded only 3 of the response's `usageMetadata` fields, so `llm.Usage.CachedTokens` stayed 0 and, downstream (`miss = prompt − cached`), the **whole prompt** was billed at the **MISS** rate on every Gemini turn (`gemini-3.8-flash`: MISS is **10×** HIT) — a silent over-charge of up to ~10× the input rate, with `H: 0` always. Anchor bug [#149](https://github.com/gosharplite/tellme/issues/149) (**closes it**). The fix: decode **`cachedContentTokenCount` → `Usage.CachedTokens`** and **`thoughtsTokenCount` → `Usage.ThinkingTokens`**, each floored at 0 and the cached count **capped at the prompt** (so the miss is never negative / the hit-rate never exceeds 100%). The **disjointness is family-specific**: Gemini's `candidatesTokenCount` **excludes** thoughts (no subtraction), unlike the OpenAI-compatible adapter (whose `completion_tokens` includes reasoning and is stored exclusive). **Clarify not escalated (0 questions)**.
- **The change**: `internal/infrastructure/llm/gemini/client.go` — the `usageMetadata` decode struct gains the two fields and the `llm.Usage` mapping sets them. **Family-local**: the request body, the emitted turn, and the OpenAI-compatible wire are byte-identical; the cost formula (`ComputeCost`/`miss`) is reused unchanged.
- **Artifacts**: `specs/plans/072-gemini-cached-token-usage/**` (spec · checklist · acceptance · research D1–D7 · plan · tasks T001–T013 · truth-delta) · **ADR 0044** (`docs/decisions/0044-gemini-cached-token-usage.md` + index) · `specs/truth/techstack.md` MODIFY ×2 (*Vertex/Gemini adapter*, *Post-turn status lines*) · `specs/truth/features/cli/chat/presenting-the-post-turn-status.feature` (+ a priced Gemini Rule/Example) + `chat/dsl.md` (Given row) · `docs/domain-model/tellme.modelith.{yaml,md}` `UsageRecord` (family-decoded counts) re-rendered · `tests/e2e/fakeprovider` (the Vertex `usageMetadata` gains the two fields when `detailed`) · `tests/e2e/steps/step_r072_gemini_usage.go`.
- **Verification**: `gofmt`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **276/276**) · `make verify` **OK** (layer gate 0 · modelith-check ×3 · verify-fmt · verify-adr-index · lint 0 · govulncheck clean) · topology audit **5 pre-existing, none new** (50 features · 395 module rows · 2031 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (reproduced then reverted): zero the cached/thinking mappings ⇒ the unit pins **and** the E2E RED (`M:390564 H:0 C:100 Th:0`); remove the cap ⇒ the cap pin RED (`CachedTokens = 999, want 10`; `miss = -989`). **Quantified**: the witnessed turn `$0.293298 → $0.045720`.
- **Review chain (PR [#150](https://github.com/gosharplite/tellme/pull/150), loop CLOSED; the `architect` peer — continuations)**: `REVIEW-B1` (**APPROVE WITH REQUIRED FOLDS** — **TD-072-1** *required* [cached > prompt ⇒ negative miss] + **TD-072-2** [the cost carrier] + **TD-072-3** [ADR link] + 3 nits) → fold `8be6ad6` → `FOLD-VERIFICATION-B1` (**FOLDS VERIFIED WITH RESIDUALS**; RES-B1/B2/B3) → residuals `0ea433c` → `FOLD-VERIFICATION-B1-FINAL` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**) → editorial `c8a671f` → `FOLD-VERIFICATION-LOOP-CLOSED`.
- **Delivered**: PR [#150](https://github.com/gosharplite/tellme/pull/150) **human-merged** into `dev` (`6c3e5fe`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged `round-072`. **Closes [#149](https://github.com/gosharplite/tellme/issues/149)**.
- **Live check — PERFORMED AND PASSED** (2026-09-20, session 59 cont.; the `coder` peer on the Vertex `dev` provider `gemini-3.8-flash`, `dev`-built binary `c76ec06`): a two-call session sharing a ~31.7k-token prefix → turn 2 `M: 4160 H: 27661 C: 10 Th: 5131` = **$0.02447** (**non-zero `H`**, 43.5 % hit ratio) vs the cached-ignored counterfactual `$0.04314` (**~43 % lower**); `tokens.log` records `cached_tokens` 0→**27661**. Recorded in **ADR 0044 §Verification** (*Live*).

## Round-072 — environment note (relocated verbatim at the round-073 closeout)

- **Round-072 (session 58)** — branch `072-gemini-cached-token-usage` off `dev` `5b1bfa1`: specify `d884f38` -> acceptance + research + ADR 0044 `abbad9c` -> implementation `3a2aaed` -> PR [#150](https://github.com/gosharplite/tellme/pull/150) -> fold B1 `8be6ad6` -> residuals `0ea433c` -> editorial `c8a671f` -> merge `6c3e5fe`. **Review chain (the `architect` peer, continuations)**: `REVIEW-B1` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION-B1` (**FOLDS VERIFIED WITH RESIDUALS**) -> `FOLD-VERIFICATION-B1-FINAL` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**). **Human-merged** into `dev` (`6c3e5fe`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-072`. Host: **darwin/arm64** (Go 1.26.6). Gates at delivery: `gofmt`/`go vet`/`go build` clean - `go test -count=1 ./...` green (E2E 276/276) - `make verify` **OK** - topology audit **5 pre-existing, none new** (50 features - 395 module rows - 2031 steps) - `go.mod`/`go.sum` unchanged. **Closes [#149](https://github.com/gosharplite/tellme/issues/149)**; the **live check** (a real Vertex turn showing a non-zero `H` and a lower cost) — **PERFORMED AND PASSED** (2026-09-20, session 59 cont.; `dev` @ `c76ec06`): turn 2 of a two-call session reported `M: 4160 H: 27661 C: 10 Th: 5131` → **$0.0245** (43.5 % session hit ratio), the cached 27,661 prompt tokens billed at the HIT rate (0.075) instead of MISS (0.75) — **~43 % below** the cached-ignored counterfactual (`$0.0431`); recorded in **ADR 0044 §Verification** (*Live*).

---

## Last delivered round - 073 `073-list-role-headers-and-rendered-body` (DELIVERED / FROZEN - PR [#151](https://github.com/gosharplite/tellme/pull/151) merged into `dev` `d90a79e`, **merge commit**; **ADR 0045**)

- **Theme (operator request; no anchor issue)**: the offline `-l`/`--list` history listing now presents each message the way the reference does — a **role header line** (`[USER]` for the operator, `[MODEL]` for the model) plus a **body** and **one blank line** after every message, instead of the flat `role: content` line. Three high-impact gaps were settled by `/axb-clarify` (one at a time): **Q1 -> A** the listing **honours `-r`/`--raw`** (raw ⇒ the model body verbatim, no accent) · **Q2 -> A** the listing stays **operator-messages-only** (no `[Tool Call]`/`[Tool Response]` lines; the round-007 contract preserved) · **Q3 -> B** **only the model body is rendered**; the operator prompt body is printed **verbatim** (a recorded divergence — the reference renders both).
- **The change**: a new domain port **`render.Listing`** (`ListingRole{ListingOperator,ListingModel}` · `ListingMessage` · `ListingSpec{Colour,Raw,Width,Warn}`) with the bytes single-owned by a new **`internal/ui/listing.go`** adapter (`ui.NewListing()`), injected through `deps.Dependencies.NewListing`. The **model** body reuses the **one** glamour renderer the answer path uses (same style / LaTeX sanitizer / `WRAP_WIDTH` / degraded fallback); the **operator** body is verbatim; the header is accented `\033[1;34m` blue / `\033[1;35m` magenta (the reference's codes, added to `colour.go`) **only when `stdout` is a terminal and `-r` is off**. `internal/cli`'s `renderHistoryList` now calls the port (`listingMessages` replaces `toMessages`); the accent gate wires tellme's **first `stdout` terminal probe** (`stdoutTerminalDetector()` + the `TELL_ME_FORCE_STDOUT_TTY` seam), **scoped to the listing** — the answer path wires no stdout probe, so round-006 / PR #16 **Obs 1 stays OPEN**; the width is resolved **best-effort** (an unreadable/invalid `WRAP_WIDTH` degrades to the renderer default, not a failure); one config parse for the offline path (`offlineConfigAndMode`, fold RF-8).
- **Artifacts**: `specs/plans/073-list-role-headers-and-rendered-body/**` (spec · checklist · acceptance `listing-the-session-as-a-conversation.feature` · research D1–D9 · plan · tasks T001–T018 + the review fold ledger · truth-delta) · **ADR 0045** (`docs/decisions/0045-list-role-headers-and-rendered-body.md` + index) · `specs/truth/techstack.md` MODIFY ×4 (*CLI flag parsing* / *Terminal detection* / *Output rendering* / *Raw output flag* / *Session lifecycle flags*) · `specs/truth/features/cli/history/inspecting-the-session-history.feature` (+5 Rules) + `history/dsl.md` (+9 rows, 3 rewritten) · `docs/domain-model/tellme.modelith.{yaml,md}` (`Session` offline-reader wording; the `Chrome` colour invariant scoped to the turn chrome) re-rendered · code: `internal/domain/render/ports.go`, `internal/ui/{listing.go,colour.go}`, `internal/cli/cli.go`, `internal/app/deps/deps.go`, `cmd/tellme/deps.go` · tests: `internal/ui/listing_test.go`, `internal/cli/list_render_test.go`, `tests/e2e/steps/step_r073_listing.go` (+ the re-pointed `-l` Thens).
- **Verification**: `gofmt`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **282 scenarios**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (50 features · 16 root + 404 module rows · 2071 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (each reproduced then reverted): drop the separator ⇒ the unit pin AND the E2E `Listing the last two messages` RED; render the operator body ⇒ the unit pin AND `A listed prompt is echoed verbatim…` RED; un-gate the colour ⇒ the unit pin AND `… the listing carries no accents` RED; ignore `-r` ⇒ the unit pin AND `The raw listing shows the answer's source` RED.
- **Review chain (PR [#151](https://github.com/gosharplite/tellme/pull/151), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-1** the rendered-model-body E2E carrier was unfalsifiable · **F-2** a self-contradicting techstack row · **F-3** the ADR Consequences over-claim; plus TD-4…TD-7, RF-8/RF-9, N-1…N-3) → fold `8916807` (F-1/F-2/F-3 + every non-blocking item) → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the F-1 falsifiability reproduced by mutation, 281/282) → residual homing `31caa39` (RES-FV-1 → RF-073-9 · RES-FV-2 → RF-073-10).
- **Delivered**: PR [#151](https://github.com/gosharplite/tellme/pull/151) **human-merged** into `dev` (`d90a79e`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged `round-073`. **No anchor issue** (an operator request).

## Round-073 — environment note (relocated verbatim at the round-074 closeout)

- **Round-073 (session 60)** — branch `073-list-role-headers-and-rendered-body` off `dev` `30dd8ac`: specify `9e254a3` -> clarify (Q1->A, Q2->A, Q3->B) `69795c5` -> acceptance `5ec4486` -> research + ADR 0045 + truth `db9e683` -> plan + dsl-refine truth + tasks `a4b4070` -> implementation `c23f3af` -> PR [#151](https://github.com/gosharplite/tellme/pull/151) -> records `bd12598`/`2b5483b` -> fold F-1/F-2/F-3 + TD-4..TD-7, RF-8/RF-9, N-1/N-3 `8916807` -> residual homing `31caa39` -> merge `d90a79e`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the F-1 falsifiability reproduced by mutation, 281/282). **Human-merged** into `dev` (`d90a79e`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-073`. Host: **linux/amd64** (Go 1.26.6). Gates at delivery: `gofmt`/`go vet`/`go build` clean - `go test -count=1 ./...` green (E2E 282 scenarios) - `make verify` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (50 features - 404 module rows - 2071 steps) - `go.mod`/`go.sum` unchanged.

---

## Last delivered round - 074 `074-cli-help-and-version-shorthands` (DELIVERED / FROZEN - PR [#152](https://github.com/gosharplite/tellme/pull/152) merged into `dev` `fd204f4`, **merge commit**; **ADR 0046**)

- **Theme (operator request; no anchor issue)**: tellme gains a **`-h`/`--help`** flag and a **`-v`** version shorthand, matching the reference's flag surface. Two clarify questions were settled (one at a time): **Q1 → A** `-h` ships **with** the long form **`--help`** (reference parity) · **Q2 → A** help writes to **`stdout`** and exits **0** (a successful action, like `--version`).
- **Before → after**: `tellme -h`/`--help` previously took pflag's **implicit** path → the flag block on **`stderr`** + `tellme: the command-line usage is invalid`, **exit 2**; now an **explicit** `-h, --help` prints `Usage of tellme:` + `fs.FlagUsages()` on **`stdout`**, `stderr` empty, **exit 0**. `tellme -v` previously an unknown flag (exit 2); now `tellme {version}` on stdout, exit 0 — the same code path as `--version` (byte-identical). The **unrecognized-flag refusal is unchanged** (phrase on `stderr`, exit **2**); a **parse error pre-empts help** (`-h -z` still refuses).
- **The change**: `internal/cli/cli.go` — `parseFlags` registers `fs.BoolVarP(&o.help, "help", "h", …)` and upgrades `--version` to `fs.BoolVarP(…, "version", "v", …)`, so pflag's implicit help path is **superseded**; the rendered flag list is captured at parse time; `run` writes it to `stdout` and returns `Success`, checked **before** the `--version` path. The help text is pflag's own rendering (one source — lists every flag, cannot drift). **Local to `internal/cli`** (no new port — the bytes are pflag's rendering of a flag set that lives there); no new dependency.
- **Artifacts**: `specs/plans/074-cli-help-and-version-shorthands/**` (spec · checklist · acceptance `asking-for-help-and-version.feature` · research D1–D7 · plan · tasks T001–T010 + the review fold ledger · truth-delta) · **ADR 0046** (`docs/decisions/0046-cli-help-and-version-shorthands.md` + index) · `specs/truth/techstack.md` MODIFY ×3 (*CLI flag parsing* · *Prompt input* — the precedence owner · *Version injection*) · `specs/truth/features/cli/dsl.md` (+ the cross-module `runs tellme with "{flag}"` row, promoted from `diagnostics`) · `usage/dsl.md` (+2 Thens) · **NEW** `usage/requesting-help.feature` (+2 Rules) · `diagnostics/version-and-setup-diagnostic.feature` (+ a `-v` Example) · `internal/cli/cli.go` · `internal/cli/help_version_test.go` · `tests/e2e/steps/step_r074_help.go`.
- **Verification**: `gofmt`/`go vet`/`go build` clean · `go test -count=1 ./...` **green** (E2E **51 features**) · `make verify` **OK** (layer gate 0 · `modelith-check` ×3 no drift · `verify-fmt` · `verify-adr-index` · lint 0 · govulncheck clean) · `make test-race` green · topology audit **the same 5 pre-existing errors, none new** (51 features · 17 root + 405 module rows · 2093 steps) · `go.mod`/`go.sum` **unchanged**. **Falsifiability** (each reproduced then reverted): drop the explicit `-h` flag (pflag's implicit path) ⇒ the E2E `The operator asks for help with "-h"` reddens at `tellme prints its flag list` **and** the unit pins; drop the `-v` shorthand ⇒ `… with the short flag` reddens at `tellme prints the build version` **and** the unit pins.
- **Review chain (PR [#152](https://github.com/gosharplite/tellme/pull/152), loop CLOSED; the `architect` peer — initialized once with `SESSION-BOOTSTRAP.md`, continuations thereafter)**: `review` (**APPROVE WITH REQUIRED FOLDS** — no `[ARCHITECTURAL BLOCKER]`; **F-1** the truth row owning the dispatch precedence was stale · **F-2** the durable record mis-described the error path; plus TD-3/4/5, N-1…N-3, RF-1/RF-2) → fold `1185292` (F-1/F-2 + every recommended item) → `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the F-2 witness re-run on the folded head) → residual homing `75e794e` (R-1 → RF-074-6 · R-2 → RF-074-7).
- **Delivered**: PR [#152](https://github.com/gosharplite/tellme/pull/152) **human-merged** into `dev` (`fd204f4`, **merge commit**); round branch **deleted local + remote**; propagation `dev -> main` (**no-ff**), tagged `round-074`. **No anchor issue** (an operator request).

## Round-074 — environment note (relocated verbatim at the round-075 closeout)

- **Round-074 (session 60)** — branch `074-cli-help-and-version-shorthands` off `dev` `5206f9f`: specify `706e27c` -> clarify (Q1->A, Q2->A) `8643ff7` -> acceptance + research + ADR 0046 + truth + implementation `65eee7c` -> PR [#152](https://github.com/gosharplite/tellme/pull/152) -> STATUS `5a310b0` -> fold F-1/F-2 + TD-3/4/5, N-1..N-3, RF-2 `1185292` -> residual homing `75e794e` -> merge `fd204f4`. **Review chain (the `architect` peer, init once with `SESSION-BOOTSTRAP.md`, continuations)**: `review` (**APPROVE WITH REQUIRED FOLDS**) -> `FOLD-VERIFICATION` (**FOLDS VERIFIED — CLEARED FOR HUMAN MERGE**; the F-2 witness re-run on the folded head). **Human-merged** into `dev` (`fd204f4`, **merge commit**); branch deleted (local + remote); propagation `dev -> main` (**no-ff**), tagged `round-074`. Host: **linux/amd64** (Go 1.26.6). Gates at delivery: `gofmt`/`go vet`/`go build` clean - `go test -count=1 ./...` green (E2E 51 features) - `make verify` **OK** - `make test-race` green - topology audit **5 pre-existing, none new** (51 features - 405 module rows - 2093 steps) - `go.mod`/`go.sum` unchanged.
