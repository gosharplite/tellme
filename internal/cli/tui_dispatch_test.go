package cli

import "testing"

// TestTUIDispatch is the round-015 T030 landing skeleton. The -i / USE_TUI_PROMPT
// / non-TTY gating-matrix unit assertions (driven through the tuiPromptRunner DI
// seam) land with T030.
func TestTUIDispatch(t *testing.T) {
	// skeleton — assertions land with the test-alignment task (T030).
}
