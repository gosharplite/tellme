package suggestions

import "testing"

// TestService is the round-015 T027 landing skeleton. The suggestion-engine
// unit assertions (subsequence match / dedupe / cap / empty query / path-like
// gating / ignore-directory pruning) land with T027.
func TestService(t *testing.T) {
	// skeleton — assertions land with the test-alignment task (T027).
}
